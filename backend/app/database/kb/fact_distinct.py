"""**「这几条事实彼此有没有区别」那把尺**（P94）。仓库里在这之前一把都没有。

这把尺**没有接进产品**。它只被 `scripts/recall_ruler.py --cf-shape` 和
`tests/test_p94.py` 叫；`kite_memory.recall` 一个字节都没动。判「不接」的理由
在第 ⑤ 格，**造出来 ≠ 要接**（P75 那把泛尺也是这样）。

---

## ① 缺的是哪一格：前三批量的都是「一个串泛不泛」，不是「一屏的八格彼此有没有区别」

P75 造了话题面（一个**串**泛不泛）、P84 把它接了、P86 试了五个候选、P88 把主语面
找出来、P90 走了留出集、P92 把「`topics` 轴 + 英文低门槛」那条路走完。
**六批全在量同一件事：一个串值不值得当证据。**

P92 走到头，判「不接」，而卡住的地方前三批都没量到过：

> **「不泛」≠「值得当证据」。** 放行的只有两个串（`agent` .450 / `memory` .592，
> **人标都对、尺子判对了**），可它们命中的是这个库里**同一句产品定位话的六种说法**。

`i=645` 那一屏最清楚（`terrence-rewrite`，8 → 8、**七进七出、整屏换人**）：
出去的七条全是 `Ask Memory` 的具体事实（1.2 多轮对话 / 暂不开放互联网检索 /
接 MCP 要明确授权 / 额度绑录音时长…），进来的除两条外全是下面这一族：

```
1578F1   MemuKet is described as a wearable AI agent powered by the user's own memory.
1580F5   MemuKet is presented as a wearable AI agent powered by the user's own memory.
1579F8   MemoKet is presented as a wearable AI agent powered by the user's own memory
         and intended to store, organize, and use memories from conversations.
1581F2   Speaker A is developing Memocat as a memory AI agent that stores, organizes, …
1522F14  The team describes Memo Cat as a memory OS or AI assistant with the user's own memory…
1589F9   MemoCat is positioned as a memory-powered AI agent that connects memories across …
366F1    团队的长期愿景是为每个人构建一个个人记忆操作系统（memory OS）和一个基于长记忆的超级 agent。
```

**串指着一件具体的事 ≠ 命中它的那些事实彼此有区别。**
一个库里只要有一族近乎同义的样板句，**判串的那条轴判得越准，它们灌屏灌得越狠**。

## ② 字面去重够不着（P92 实测，这一批没再走一遍）

3-gram **Jaccard** ≥0.6 聚族：`agent` 的 34 条命中只有 5.9% 落在重复组里
（**量错了轴**——这族样板句长短差一倍）。改**包含度**（overlap / min）重量是 14.7%、
最大一组 **3 条**——**六条里只认出三条**，另外三条是同义改写。

⚠️ 这一批**没再走一遍**（任务书点名不许），只把那一版原样搬进候选表当**对照**跑了一次：
`字面 3-gram 包含度 ≥0.6` 在那一族七条上打出来的是 **团 3**——**跟 P92 那个「只认出三条」逐格相同**，
口径对得上。**这一批量到的新东西是：`obj` 交集≥2 和 `obj` Jaccard≥0.5 也都只打到团 3**
（`test_p94::第一条d` 钉着这一格，**不需要事实的正文**）。
⇒ **收窄到「说的是同一个东西的同一面」，就从另外两条路退化回字面去重**。

## ③ 库里所有够得着的轴，在同一批正反例上并排摆了一遍（P88 的教训）

`FactRecord` 一共八条轴。正例 = 上面那族 7 条（该判「同一句话」），
反例 = `i=645` 被挤出去的 7 条（该判「各说各的」）。**最大团**（两两都判「没区别」）：

| 轴 | 正例 7 条 | 反例 7 条 | 判 |
|---|---:|---:|---|
| **`obj` 相交非空** | **团 6** | **团 2** | **分得开，方向对** |
| `topics` 相交非空 | 团 4 | 团 4 | 分不开 |
| `entities` 相交非空 | 团 4 | 团 3 | 缝太窄 |
| `who` 相同 | 团 2 | **团 7** | ⚠️ **完全反着** |
| `kind` 相同 | 团 4 | **团 6** | ⚠️ **反着** |
| `unit` 相同（同一场录音）| 团 1 | 团 4 | ⚠️ **反着** |
| `event` / `place` 相交非空 | 团 1 | 团 1 | 两边基本全空，量不了 |

⚠️ **`who` / `kind` / `unit` 那三条是这一批最值钱的反例**：被挤出去的那七条是
**同一个说话人（`产品团队` 7/7）、`kind` 6/7 都是 `plan`、七条只出自两场录音**
（`terrence-1604` 四条 + `terrence-1458` 三条），
而那一族是**五种 who、三种 kind、七场各不相同的录音**。
**「同一句话的多种说法」正好长在「不同的人、不同的场合、不同的体裁」上**
——按说话人去重 / 按录音去重 / 按体裁去重，砍掉的会**正好是该留的那七条**。

⚠️ `obj` 这条轴 P90 ⑥ 记着「**只量过 A/B 分不开**」，**没被排除过**。这一批量的是它。

## ④ 判据：`obj` 相交非空 **且** `topics` 相交非空，然后取**最大团**

单看 `obj` 分得开那一屏，但全库读下来**一个泛 obj 就能把团撑起来**：
`app` 撑起 i=39 / i=41、`product` 撑起 i=686 / i=692、`kol` 撑起 i=298
——三条各说各的事，只因为都挂着同一个 `app`。`terrence-rewrite` 上读 10 屏，
**单轴版真 4 / 假 6**。

第二条轴把这些砍掉了（**两条轴都得沾**）：`app` 那三条 `topics` 各不相同，
`product` 那三条也是。同一批十组正反例上，**只有这一版十组全对**：

正例四组要 ≥ 半屏（POS8 / POS7 要 ≥4，POSK / POST 要 ≥3），反例六组要 < 那个数：

| 判据 | POS8 POS7 POSK POST | NEG7 NEGH NEGA NEGP NEGK NEGC | 错在哪 |
|---|---|---|---|
| `obj∩≠∅` | 7 6 5 4 ✅ | 2 2 **3 3 3 3** | **误 4 组**：泛 obj 撑团 |
| `topics∩≠∅` | 4 4 5 4 ✅ | **4 4** 2 1 2 2 | 误 2 组 |
| **`obj∩≠∅ ∧ topics∩≠∅`** | **4 4 5 4 ✅** | **2 2 2 1 2 2 ✅** | **十组全对** |
| `entities∩≠∅` | 4 4 5 4 ✅ | 3 3 1 1 2 1 | 误 2 组（缝只剩一格）|
| `who` 相同 | **2 2 2 1** ❌ | **7 8** 1 1 2 1 | **漏 4 组 + 误 2 组** |
| `kind` 相同 | 4 4 5 3 ✅ | **6 7** 1 2 2 1 | 误 2 组 |
| `unit` 相同 | **1 1 1 1** ❌ | **4 4** 1 1 2 1 | **漏 4 组 + 误 2 组** |
| `obj` 交集 ≥2 | **3 3 1** 3 ❌ | 1 2 1 1 1 1 | 漏 3 组（退化成字面去重）|
| `obj` Jaccard ≥0.5 | **3 3** 3 **2** ❌ | 1 2 1 1 1 1 | 漏 3 组（同上）|
| `K ∧ 不同 unit`（`M`）| 4 4 5 4 ✅ | 1 1 2 1 1 2 ✅ | **也全对，没要** |

最后那一版（`M`）**十组也全对、反例还更低**，**这一批仍然没要它**，理由两条：
**一，同一场录音里的重复说法也是灌屏**，把它豁免掉等于给这把尺开一个永远不会红的口子；
**二，一刀只动一处**——两条轴已经够分开十组，第三个条件是在没有缝的地方再加一把锁。
`M` 那一版的读数**照实记在 `test_p94::第一条c`**，哪天两条轴不够了直接拿来用。

**屏级判据**：最大团 **≥ 3** 且 **≥ 半屏**（`团 × 2 >= 量得了的格数`）。
`≥3` 是因为两条撞一块儿太常见（全库 `团 == 2` 的屏有 99 个，读过的里绝大多数是巧合）；
**半屏**是 P92 那句「八格的屏一进就是半屏」的原话。

## ⑤ 全库量完 + 18 屏逐条读完 + 判「不接」

全库 765 屏（`recall(limit=8, evidence=True)`，跟 `cf_gates` 同一个调用）：

| 库 | 屏 | 量得了 ≥3 格 | **判「是这形状」** | 单轴 `obj` 版（对照）|
|---|---:|---:|---:|---:|
| `fresh678` / `b` / `c` | 6 / 6 / 6 | 2 / 4 / 2 | **0 / 0 / 0** | 0 / 0 / 0 |
| `shot-demo` | 20 | 14 | **2**（14.3%）| 3（21.4%）|
| `terrence` | 641 | 97 | **12**（12.4%）| 26（26.8%）|
| `terrence-rewrite` | 86 | 56 | **4**（7.1%）| 10（17.9%）|
| **合计** | **765** | **175** | **18（10.3%）** | 39（22.3%）|

**那 18 屏逐条读完了**（标注 `p94-shape-18`）：**真 12 / 假 6**。
⚠️ **按库分**（比率要按对的那条轴分）：

| 库 | 判「是」 | 人读**真** | 人读**假** | 假阳性率 |
|---|---:|---:|---:|---:|
| `terrence-rewrite` | 4 | **4** | 0 | **0%** |
| `shot-demo` | 2 | **2** | 0 | **0%** |
| **`terrence`（大库）** | **12** | 6 | **6** | **50%** |

**判：不接**（产品逻辑一个字节没改）。四条理由，都是量出来的：

1. **大库上假阳性 50%**，而 **641/765 = 83.8% 的查询落在大库**。
   拿它去产品里去重，大库上**一半的时候会砍掉真正有区别的事实**。
2. **它在 441/765 屏上一个字都说不出来**（`量得了 = 0`）：`terrence` 有
   **43.3% 的事实根本没有 `obj`**（20417 条里 8836 条）。
   一把在 **57.6%** 的屏上说不了话的尺，不该当产品的闸。
3. **它本来的活不是当闸，是当第三格的量具**。P92 判 en060「不接」是眼看出来的；
   这一批把那句话量成了数：en060 变了的 11 屏里，**3 屏从「不是这形状」翻成「是」
   （i=640 / 642 / 645），反向 0 屏**，翻的那三屏的团**逐条就是上面那一族**。
   这个用途**不需要改产品一个字节**。
4. **召回那一头也不满**：`i=641` 人读判「是」（五进五出、掉的五条全是 `Ask Memory`），
   这把尺打的是**团 3 / 8 格**，差半屏一格，判「不是」。**漏的这一格照实记着**。

## ⑥ 它答不了什么（照 P72 的规矩，写在尺自己身上）

* **`obj` 是空的它就不说话**。这不是「判不了就放行」，是**整格不进分母**——
  `terrence` 上八格里常常只有三四格有 `obj`，那时候「半屏」这个门槛量的是**半个可见屏**。
* **它不认字面**。两条逐字相同、但 `obj` 抽歪了的事实，它判「有区别」。
  反过来也一样。**跟字面去重是两把尺，不是一把的两个版本**（第 ② 格）。
* **它只看一屏之内**。同一族散在两屏各两条，它一屏都不判「是」。
* **`topics` 这条轴同时是 `TopicFace` 的轴**。拿这把尺去验话题面那条路上的改动
  = 拿跟被测尺共用一条轴的东西当验收尺，**不许**。它只答「这一屏彼此有没有区别」。
"""

from __future__ import annotations

from itertools import combinations

# 一族至少几条才算一族。两条撞一块儿太常见（全库 `团 == 2` 的屏 99 个，
# 读过的里绝大多数是巧合），三条才开始像「同一句话的多种说法」。
FAMILY_MIN = 3


def measurable(fact) -> bool:
    """这把尺在这条事实上说不说得了话：**两条轴都得有东西**。

    ⚠️ 判不了 ≠ 判「有区别」。判不了的格**整格不进分母**（第 ⑥ 格）。
    """
    return bool(getattr(fact, "obj", ())) and bool(getattr(fact, "topics", ()))


def same_thing(a, b) -> bool:
    """这两条事实说的是不是同一件事：`obj` 相交非空 **∧** `topics` 相交非空。

    两条轴都要，理由在第 ④ 格：单看 `obj`，一个泛 `obj`（`app` / `product` / `kol`）
    就能把三条各说各的事撑成一个团；单看 `topics` 连正反例都分不开（两边都是团 4）。
    """
    return (bool(set(a.obj) & set(b.obj))
            and bool(set(a.topics) & set(b.topics)))


def largest_family(facts) -> list[int]:
    """一屏里**最大的一族**，回的是在 `facts` 里的下标（**量得了的格之间**算）。

    取**最大团**（两两都判「没区别」）而不是最大连通块：连通块会串——
    a 和 b 靠一条轴连上、b 和 c 靠另一条连上，而 a 和 c 其实不沾。
    **全库量过这一刀**（`<scratch>/p94/compvsclique.py`，同一份判据只换这一处）：
    **团版判出 18 屏、连通块版 23 屏**，多出来的 5 屏是 **i=24 / 82 / 88 / 412 / 418**——
    **逐条读完，5 屏全是假**（i=82/412 和 i=88/418 是同一批华为 950 超节点的事实：
    「超节点由哪些刀片构成」/「一个计算柜装几片」/「鲲鹏 CPU 和昇腾 NPU 两条芯片线」
    靠 `server`、`chip` 串成一条链，其实是三件不同的事；i=24 是寄 KOL 的时间安排、
    6–7 月出第一款产品、按 4 月 20 日倒排的三件事靠 `product`、`crowdfunding_page` 串起来）。
    ⇒ **换成连通块，`terrence` 上的假阳性 6 → 10、`terrence-rewrite` 上 0 → 1，一屏都没多抓对。**
    （`test_p94::第一条b` 钉着这一格。）

    ⚠️ **判据 K 上这一刀比单轴 `obj` 上小得多**：单轴那一版 `i=591` 那三条是**三角形**
    （每一对都沾一个不同的 obj），团和连通块都给 3——**「串」这件事在单轴上根本不是链，
    是真的两两相交**。这一条是这一批自己先说错、量完改过来的（见第 ④ 格的表）。
    """
    idx = [i for i, f in enumerate(facts) if measurable(f)]
    for size in range(len(idx), 0, -1):
        for combo in combinations(idx, size):
            if all(same_thing(facts[a], facts[b]) for a, b in combinations(combo, 2)):
                return list(combo)
    return []


def screen_shape(facts) -> dict:
    """一屏的形状。

    `{"slots", "measurable", "family", "family_idx", "flagged"}`。

    `flagged` = **这一屏有半屏是同一句话的多种说法**：
    最大团 `>= FAMILY_MIN` 且 `团 × 2 >= 量得了的格数`。
    """
    fam = largest_family(facts)
    n = sum(1 for f in facts if measurable(f))
    return {"slots": len(facts), "measurable": n,
            "family": len(fam), "family_idx": fam,
            "flagged": n >= FAMILY_MIN and len(fam) >= FAMILY_MIN and len(fam) * 2 >= n}
