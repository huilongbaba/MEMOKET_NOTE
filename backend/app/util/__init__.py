"""谁都可能用、又不属于任何一块业务的两样东西。

    config.py   配置：数据目录、模型地址
    llm.py      调模型（文本）。多模态那两条不在这儿——``editor/vision.py``
                服务编辑器功能，``harness/tools/imagegen.py`` 是一个工具背后
                干活的那段，各自跟着使用者走。

**这里只该有这种东西。** 一个模块如果能说出「它为哪块业务服务」，它就该
搬到那块去；说不出的才留在这儿。这个包越小越好。
"""
