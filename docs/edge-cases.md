# 临界条件表：每个用户可触发的动作 × 九种条件（P32 起 **十种**）

> 产品就绪计划 §2 B 线（`docs/product-readiness-plan.md`），P3 第一版（2026-09-18，HEAD 开工 `1276a5f`）。
> 台账在 `docs/TRACELOG-product.md` P3 节。**动作清单是从代码里数出来的**（文件名在每组标题上），
> 不是凭印象；**❌ 的格子全部是在真实 app 里复现出来的**（探针名 + 截图名在文末「复现记录」），
> 推理出来但没复现的一律标 **？**，不冒充 ❌ 也不冒充 ✅。

## 怎么读

| 记号 | 意思 |
|---|---|
| ✅ | 有处理（格子里写一句是什么） |
| ✅P3 | P3 修的：修之前是 ❌，前后截图在「复现记录」 |
| ✅P9 | P9 修的（台账 P9 节）：修之前是 ❌ 或 ？，先复现再修，前后截图在「复现记录」#19 起 |
| ❌ | 没处理，**复现到了**（格子里写复现到什么；探针 / 截图在文末） |
| ✅P23 | P23 修的（台账 P23 节）：修之前是 ？，先复现再修，前后对比在「复现记录」#31 起 |
| ✅P32 | P32 修的（台账 P32 节）：第十列「模型答得不合形状」，先复现再修，前后对比在「复现记录」#39 |
| ✅P35 | P35 修的（台账 P35 节）：第十列里右键那三条会落正文的动作，先复现再修，前后对比在「复现记录」#40 起 |
| ✅P37 | P37 修的（台账 P37 节）：P35 留下的**三个 ❌**（校验说假话 / 来龙去脉指错地方 / 智能续写脏内容落正文），先复现再修，前后对比在「复现记录」#46 起 |
| ？ | 从代码看像是没处理，但**本批没复现**——下一批先复现再定 |
| — | 不适用（写为什么） |

九种条件（计划 §1.3）：**空输入**（有输入框的动作，什么都不写就确认）· **空正文**（整篇一个字没有）·
**无选区**（作用在选区上的动作，没选任何字）· **无知识库**（这个用户 0 条事实）·
**离线**（后端起着，模型服务连不上——分「拒绝连接」和「黑洞」两种，黑洞 = 主机不回包，实拍 `192.168.77.8` 关机就是这样）·
**后端没起来**（浏览器连不上后端：进程没在跑 / 刚重启）· **模型报错**（模型服务回 4xx / 5xx）·
**超时**（模型收下请求不回）· **重复点击**（连点两下 / 跑的过程中再点）。
**P32 加了第十种**：**模型答得不合形状**（200 + 脏内容，不是 4xx/5xx 也不是不回）——
它在下面那几张九列表里**没有列**，单独写在文末「P32 补」和「P35 补」那两节，别在这儿找
（P32 立了这一列并填了 AI 组 + `custom`，**P35 把剩下那七个 `？` 摆完了**：右键五项 + harness 两条）。

怎么摆这九种条件（都不碰真库，全在 `KITE_DATA_DIR` 指向的 scratch 拷贝上，见文末）：
空输入 / 空正文 / 无选区 用探针摆；无知识库 换一个没有 KB 的用户；**离线 / 模型报错 / 超时 用一个假模型服务**
（`fakellm.py`：`/ok`、`/err500`、`/err401`、`/hang` 四种模式 + 假语音服务两种），把 scratch 库的供应商地址指过去；
黑洞用真关机的 `192.168.77.8`；拒绝连接用 `127.0.0.1:1`；**后端没起来** 用 `netdown` 探针把页面里的 `fetch`
换成一律 `Failed to fetch`（跟后端进程死掉时浏览器看到的一模一样）。

三条横跨全表的结论（本批修了前两条）：

1. **后端没起来 ≠ 模型连不上**。原来两种都翻成「模型连不上——去设置里检查 LLM 供应商」；后端进程没起来时这是指错了地方。
   现在 `Failed to fetch` 单独说「连不上应用后台（后端进程没在跑，或刚重启还没起来）」，不带「打开设置」。
2. **明知模型不可达还照发请求**。状态栏红着写「LLM 不可达」，每个要模型的按钮照样发请求、转到连接超时才报错
   （黑洞主机下是操作系统 SYN 重试的 75 秒+）。现在：前端先看健康检查，红着就拦（带「打开设置」）；
   后端把**连接**超时单独收到 10 秒（读超时不动，本地模型算得久是正常的）。
3. **非流式动作没有「停止」**（校验 / 重写 / 润色 / 扩展 / 来龙去脉 / 骨架 / 排版 / 幻灯片）：模型卡住要干等 300 秒。
   P3 给右键菜单五项和幻灯片加了停止；**骨架 / 智能排版 P9 补上**（`p9-skeleton-hang-after` / `p9-restructure-hang-after`）。
   P9 顺手把第四条也修了：**「停止」只撤块、请求照跑、跑完仍插进正文**（图片转表格 / 语音输入 / 插入音频 / 录音钮）——
   实拍是真的（停了之后表格 / 文字照样插进来），全部挂上 AbortController；「⋯ 存入知识库」的后台 job 接上了本来就有的 cancel。

---

## A. `/` 菜单（`frontend/src/editor/slashMenu.ts` `SLASH_ITEMS`，18 项）

| 动作 | 空输入 | 空正文 | 无选区 | 无知识库 | 离线 | 后端没起来 | 模型报错 | 超时 | 重复点击 |
|---|---|---|---|---|---|---|---|---|---|
| 用 AI 写 | ✅ P1：输入框红字，不发请求 | ✅ 允许——一条指令就够写 | — 在光标处插一块，不作用于选区 | ✅ 没材料就自由写；运行块「查」那几行会看见 0 条 | ✅P3 状态栏红着就拦（带「打开设置」）；黑洞也只等 10 秒 | ✅P3 占位块「连不上应用后台…」（原来原样写 `Failed to fetch`） | ✅P3 占位块「模型服务返回 500（地址）——多半是那边出错了」（原来整段 httpx 英文 + MDN 链接） | ✅ 占位块有「停止」；读超时 300 秒 | ✅ Enter 之后输入框当场收起；每个占位块独立 id |
| 智能插图 | — 没有输入框 | ✅ P1：不发请求，toast「笔记还是空的…」 | — | ✅ 不查库也能画 | ✅P3 同上 | ✅P3 同上 | ✅P3 同上 | ✅ 停止 | ✅ 同上 |
| 智能表格 | ✅ 允许留空（输入框写着「留空则自动判断」） | ✅ P1 拦 | — | ✅ | ✅P3 | ✅P3 | ✅P3 | ✅ 停止 | ✅ |
| 图片转表格 | ✅ 取消选文件 = 什么都不发生 | ✅ 不吃正文 | — | — 不查库 | ✅ 看图那台模型不可达 → 502 → 占位块「处理失败：…」 | ✅P3 占位块「连不上应用后台…」 | ✅ 502 → 占位块 | ✅P9 占位块「停止」撤块 + 撤请求（AbortController），跑完不再插进正文 + toast「已停止，什么都没插进正文」（原来复现到：停了之后表格照样插进来，558 → 594 字，`p9-imagepick-slow-before`） | ✅ 每次选文件一个块 |
| 数据可视化 | — | ✅ P1 拦 | — | ✅ 光标附近没数字没表 → 后端 precheck，占位块说明 | ✅P3 | ✅P3 | ✅P3 | ✅ 停止 | ✅ |
| 智能数据分析 | ✅ P1 拦 | ✅ P1 拦 | — | ✅ 同数据可视化 | ✅P3 | ✅P3 | ✅P3 | ✅ 停止 | ✅ |
| 语音输入 | ✅ 静音 → 占位块「没听清，什么都没转出来」 | ✅ 不吃正文 | — | — | ✅ 语音服务离线：录音前就 toast，不让人录完才失败 | ✅ 转写失败 → 占位块「转写失败：连不上应用后台…」（`friendlyError` 同一条路，未单独实拍） | ✅ 占位块「转写失败：语音服务返回 5xx」（`asr.describe_error`，有测试） | ✅P9 录音中「停止」= 停录音；转写中再点「停止」= 撤块 + 撤转写请求，转出来的字不再插进正文（原来复现到：停了之后文字照样插进来，521 → 536 字，`p9-voice-slow-before-2`） | ✅P23 **一次只录一段**：录着的时候再来一次 → toast「已经在录一段了——先把正文里那个『录音中』停掉」（原来实拍**两个运行块**：`p23-voice-twice-before-light` runs=2 → `-after-light` runs=1） |
| 插入音频 | ✅ 取消选文件 = 无动作 | ✅ | — | — | ✅P3 播放器照样插进正文，占位块说「语音服务不可达，这次没转写」，**不发转写请求**（原来传完就去转写、失败后音频没插进笔记、报错指去「LLM 供应商」） | ✅P3 上传失败 → 占位块「连不上应用后台…」 | ✅P3 播放器已插入 + 「转写没成：语音服务返回 500」（原来音频丢、报错指错地方） | ✅P9 同语音输入：转写这一步带 signal，「停止」撤转写、播放器留着（同一处修法，未单独实拍） | ✅ |
| 排版 10 项（h1 / h2 / h3 / ul / ol / todo / quote / code / grid / flow） | — | ✅ 空文档上照常插 | ✅ 在光标处插 | — | — 纯本地 CM 命令 | — | — | — | ✅ 幂等：再点一次是切换 / 再插一个 |

## B. 右键选区菜单（`components/SelectionMenu.tsx` `SelectionAction`，6 项）

| 动作 | 空输入 | 空正文 | 无选区 | 无知识库 | 离线 | 后端没起来 | 模型报错 | 超时 | 重复点击 |
|---|---|---|---|---|---|---|---|---|---|
| 校验 | — | — 有选区就有正文 | ✅ mousedown 时没选区 → 让给浏览器原生菜单，AI 菜单不出 | ✅ 面板「没有找到能支持或反驳这段内容的记录」（花一次模型调用核内部一致性；零库时值不值得省这一次，下一批看） | ✅P3 红着就拦；黑洞 10 秒报「模型连不上：10 秒内没连上（地址）」（原来转 75 秒+，状态栏红着照发） | ✅P3 toast「操作失败：连不上应用后台…」（原来说「模型连不上——去设置里检查 LLM 供应商」） | ✅ toast + 「打开设置」（拒绝连接 / 500 / 401 各说各的） | ✅P3 菜单里有「停止」→ toast「已停止」（原来只能干等 300 秒，Esc 也关不掉） | ✅ 忙的时候菜单项换成转圈，点不到第二次 |
| 重写 | — | — | ✅ 同上 | ✅ 不需要库 | ✅P3 | ✅P3 | ✅ toast（实拍 500 → 「后端处理出错」；现在 502 带人话） | ✅P3 停止 | ✅ |
| 润色 | — | — | ✅ | ✅ 不需要库 | ✅P3 | ✅P3 | ✅ | ✅P3 停止 | ✅ |
| 扩展上下文 | — | — | ✅ | ✅ 有 toast（「模型认为不需要补充上下文」）；**但零库时模型会自己编背景**（代码注释承认），下一批看要不要零库直接拦 | ✅P3 | ✅P3 | ✅ toast | ✅P3 停止 | ✅ |
| 来龙去脉 | — | — | ✅ | ✅ 零库一句话回（零模型调用） | ✅P3 | ✅P3 | ✅P9 **实拍复现到头（这是一条真 bug）**：模型 500 时 KITE 3 次调用 × 3 次重试（退避 2s+4s）转 20–30 秒，然后右栏「脉络」写「知识库里没有跟这段沾边的记录」——**那是假的**（provider 异常被 KITE 的 pipeline 吞成 No information）。现在给 provider 装观察器：第一次失败就不再重试 / 退避，5 秒内 502 + toast「来龙去脉没查成：模型服务返回 500（地址）——去设置里看一眼 LLM 供应商」（`p9-trace-err500-3` → `p9-trace-err500-after`） | ✅P3 停止 | ✅ |
| 自定义提示… | ✅ P1：红字，不关、不发、选区不清 | — | ✅ P1：toast「没有选中任何文字」 | ✅ 同「用 AI 写」 | ✅P3 | ✅P3 | ✅P3 | ✅ 停止 | ✅ |

## C. 浮动按钮（`App.tsx` `.floating-buttons`）

| 动作 | 空输入 | 空正文 | 无选区 | 无知识库 | 离线 | 后端没起来 | 模型报错 | 超时 | 重复点击 |
|---|---|---|---|---|---|---|---|---|---|
| 续写 | — | ✅ toast「先写点内容（或标题）再续写」（P3 起前后端同一句） | — 从光标处续 | ✅ P1：「知识库还是空的 / 范围筛掉了」+ 一键换档 | ✅P3 拦 + 打开设置（实拍 `p3-tap-blackhole-gate-after`） | ✅P3 toast「续写失败：连不上应用后台…」（原来指去 LLM 供应商 + 「打开设置」） | ✅ toast + 打开设置 | ✅ 主钮变「停止」（读超时 600 秒） | ✅P9 第二下 = 停止 + toast「已停止续写，写到哪算哪——不要的话用正文上的「撤回」」（原来停下来一个字不说，`p3-tap-double-hang-before` → `p9-tap-double-hang-after`） |
| 智能续写 | — | ✅ toast（同一句） | — | ✅ 第一轮状态行说「知识库是空的」 | ✅P3 拦 | ✅ toast「智能续写失败：连不上应用后台…」（`friendlyError` 同一条路，未单独实拍） | ✅P3 toast「智能续写出错停下：模型服务返回 500（地址）」+ 打开设置，状态行同句；骨架那一步失败单独一条 warning（原来轮次卡片挂一段 httpx 英文、状态停在「在写…」、没有 toast） | ✅ 主钮变「停止」 | ✅P9 第二下 = 停止 + toast「已停止智能续写，写到哪算哪——改动在右栏「改动」里可以整层撤回」（原来一个字不说） |
| ▾ 打磨（只修不写） | — | ✅ 菜单项禁用 + hint「正文是空的」 | — | ✅ | ✅P3 | ✅ | ✅P3 | ✅ 停止 | ✅ 跑着时菜单项禁用 |
| ▾ 逐轮我来定 | — | — 只是开关 | — | — | — 本地 | — | — | — | ✅ 开 / 关切换 |
| 录音 → 插入正文 | ✅ 静音 → toast「没有转写出内容…」 | ✅ | — | — | ✅ 离线态按钮：点了只说明，不录 | ✅ toast「语音处理失败：连不上应用后台…」 | ✅ toast「语音处理失败：语音服务返回 5xx」 | ✅P9 「转写中… 停止」可点 → toast「已停止，这段录音没有转写」（原来复现到：按钮禁用、没有出口，`p9-rec-slow-before`） | ✅ 录音中按钮 = 停止录音；转写中禁用 |
| 录音 → 存入知识库 | ✅ 转写为空 → toast detail | ✅ | — | — 建库 | ✅ 同上 | ✅ 同上 | ✅ 同上 | ✅P9 同上（`ingestAudio` 带 signal） | ✅ |
| ⋯ 存入知识库 | — | ✅ 禁用 + hint | — | — 这就是建库 | ✅ 抽取在后台 job 里失败 → 状态卡「入库失败」 | ✅ toast「存入知识库失败：连不上应用后台…」 | ✅ job 状态 error | ✅P9 状态栏「存入知识库中…」旁加「取消」（后端本来就有 cancel，原来没接）；取消中显示「取消中（等这一块的模型调用结束）」（原来复现到：模型卡住这一行转到 job 自己超时、没有出口，`p9-ingest-hang-before`） | ✅P3 跑着时菜单项禁用 + hint「正在存入知识库中」，函数里再拦一次（原来第二下又发一个 job，同一篇抽两遍） |
| ⋯ 无限续写…（写作计划弹层） | ✅ 目标为空「生成写作计划」禁用 | — 按目标写，不看正文 | — | ✅ 照样拆分段 | ✅ 502 → toast「生成写作计划失败：模型连不上…」 | ✅ toast | ✅ toast | ✅P23 第二下 = 停止（`AbortController`，跟 P3 / P9 给右键五项 / 骨架 / 排版接的是同一条）；停了说一句「已停止，没有生成计划——目标还留在框里」 | ✅P23 转起来**不禁用**（那一下是这次跑唯一的出口）；目标为空照旧点不动 |
| ⋯ 做成幻灯片… | — | ✅ 禁用 + hint | — | ✅ 引用覆盖率会是 0，toast 里报出来 | ✅P3 拦 | ✅ toast | ✅ toast | ✅P3 忙态胶囊里有「停止」→ toast「已停止做幻灯片」（原来只能等满 600 秒） | ✅ 跑着时禁用 |
| ⋯ 导出这份幻灯片 → PDF | — | — 只在幻灯片上出现 | — | — | — 主进程离屏打印，不走网络 | — | — | — | ✅ 每次一份文件 |
| ⋯ 分屏对照另一篇… | ✅ 选择器取消 = 无动作 | — | — | — | — 本地 | ✅ 分屏那篇拉不到 → 「这篇笔记已经不在了」 | — | — | ✅ |
| ⋯ 现在存一版 | — | ✅ 禁用 | — | — | — | ✅ toast「存版失败：…」 | — | — | ✅ 每点一次一版（历史面板里看得见） |
| ⋯ 导出为 .md | — | ✅ 导出一个空文件（可接受） | — | — | — 本地 Blob | — | — | — | ✅ |
| ⋯ 导回到 Obsidian / Notion / 飞书… | **P2-fix 在做**（另一个 agent，`ExportBack.tsx` / `useExportBack.ts` / `export.py`） | | | | | | | | |
| ⋯ 导出全部笔记… | **P2-fix 在做**（`ExportNotePanel.tsx` / `exporters.py`） | | | | | | | | |
| ⋯ 复制正文 | — | ✅ 复制空串 | — | — | — | — | — | — | ✅ |
| ⋯ 正文怎么写 / 专注模式 | — | — | — | — | — 本地 | — | — | — | ✅ 开 / 关 |
| ⋯ 保存（⌘S） | — | ✅ 空正文照存 | — | — | — | ✅ 标题旁「⚠ 没存上」+ 悬停原因；草稿留在本机 | — | — | ✅ 幂等 |

## D. Ribbon（`App.tsx` `<Ribbon tabs>`，六个页签）

| 页签 · 动作 | 空输入 | 空正文 | 无选区 | 无知识库 | 离线 | 后端没起来 | 模型报错 | 超时 | 重复点击 |
|---|---|---|---|---|---|---|---|---|---|
| 格式 · 格式化 | — | ✅ toast「已经是规范格式了」 | — 整篇 | — | — 纯规则 | — | — | — | ✅ 第二次「已经是规范格式了」 |
| 格式 · 智能排版 | — | ✅P3 toast「正文是空的——智能排版没有可排的内容」，不发请求（原来点了什么都不发生） | — | — 不查库 | ✅P3 拦 | ✅ toast「排版失败：连不上应用后台…」 | ✅ 502 → toast（实拍） | ✅P9 工具栏那个钮变「停止」→ toast「已停止智能排版，正文没动」（`p3-restructure-hang-before` → `p9-restructure-hang-after`；后端那次调用照跑到超时，界面不再被它拖着） | ✅ 跑着时禁用 |
| 格式 · B / I / H1–H3 / " / • / 1. / ☑ / link / <> / { } / ▦ / 流程图 | — | ✅ 空文档上插标记 | ✅ 没选区就在光标处插一对标记 / 一行 | — | — 本地 CM 命令 | — | — | — | ✅ 切换 / 再插一个 |
| 引用 · 存入知识库 / 同步 | — | ✅ 禁用 + hint | — | — 建库 | ✅ 同 ⋯ | ✅ toast | ✅ job error | ✅P9 同 ⋯（状态栏「取消」） | ✅ job 跑着时禁用（这一处本来就对，⋯ 那一处 P3 补齐） |
| 引用 · 补一条（加上 / 算了） | ✅P23 空的时候「加上」点不动 + title「先写一句：谁、什么时候、什么」（原来实拍：**一声不吭地关掉那个框**，看起来跟「加好了」一模一样，`p23-fact-add-empty-before-light` 加上 `d=false` → 点完框没了；修后 `d=true`、框还在） | — | — | — | — | ✅ toast「加不上：…」（`String(e)`，英文原样） | — | — | ✅ |
| 引用 · 改 / 删 | ✅P23 清空之后「保存」点不动 + title「空的存不了——要去掉这条就用右边那个『从知识库删掉这条』」（原来实拍：静默关掉、原话原样留着，用户以为改成空了，`p23-fact-edit-empty-before-light` 保存 `d=false`、点完框没了；修后 `d=true`、框还在） | — | — | — | — | ✅ toast「改不了 / 删不了」 | — | — | ✅ |
| 引用 · 清掉这些引用 | — | — | — | — | — 本地改正文 | — | — | — | ✅ 第二次 0 处 |
| 链接 · 改成纯文本 | — | — | — | — | — 本地 | — | — | — | ✅ |
| 历史 · 现在存一版 | — | ✅ 禁用 + hint | — | — | — | ✅ toast「存版失败」 | — | — | ✅ busy 禁用 |
| 历史 · 恢复到这一版 / 与当前对比 / 全文 | — | — | — | — | — | ✅ toast「恢复失败 / 读不到这一版」 | — | — | ✅ busy 禁用 |
| 路径 · 克隆到… | ✅ 取消 = 无动作 | — | — | — | — | ✅ toast「克隆失败」 | — | — | ✅ |
| 信息 | — 没有按钮 | | | | | | | | |

## E. 右栏三个页签（`App.tsx` `<RightPane tabs>`）

| 页签 · 动作 | 空输入 | 空正文 | 无选区 | 无知识库 | 离线 | 后端没起来 | 模型报错 | 超时 | 重复点击 |
|---|---|---|---|---|---|---|---|---|---|
| 记忆 · 范围下拉 | — | — | — | — | — 存 localStorage | — | — | — | ✅ |
| 记忆 · 记忆卡片（点插入 / 打开这条） | — | ✅ 正文太短：「再多写几个字就会开始自动检索」 | — | ✅ 「知识库还是空的。导入会议记录，或…」 | — 召回零模型 | ✅ 召回失败静默保留上一批（`catch(() => {})`，可接受：这是被动浮现） | — | — | ✅ 已引用的标「已引用」，不重插 |
| 记忆 · 关系卡（引用这条 / 新的取代旧的 / 补进来 / 合成一条 / 忽略） | — | — | — | — 没库就没关系卡 | ✅ 冲突确认那一步模型不在 → 用代码判的那句（try/except） | ✅P9 走 `friendlyError`（五个动作抽成 `util/relationActions`，右栏卡和 P9 新加的页边卡**同一份**）；复现靠单测（`p9AgentNative.test.ts`：fetch 拒绝 → 「标不上：连不上应用后台…」）——真库里摆不出「一对可合并的记录 + 断网」，实拍两次 `合成一条` 都没出现（`p9-relcard-netdown-before`） | — | — | ✅ 处理过的对不再出现 |
| 目录 · 跳转 | — | ✅ 「正文里的 # 标题会列在这里」 | — | — | — 本地 | — | — | — | ✅ |
| 计划 · 生成骨架 | — | ✅P3 toast「先写点内容（或标题）再生成骨架」，不发请求（原来静默：点了什么都没有） | — | — 骨架不查库 | ✅P3 拦 | ✅P3 toast「生成骨架失败：连不上应用后台…」（原来静默） | ✅P3 toast「生成骨架失败：模型服务返回 500（地址）」+ 打开设置（原来静默） | ✅P9 按钮变「停止」→ toast「已停止生成骨架」，已有骨架不动（`p3-skeleton-hang-before` → `p9-skeleton-hang-after`） | ✅ 禁用 |
| 计划 · Agent 运行卡片 | — 只读 | | | | | | | | |

三格静默的根因是同一个：`SkeletonPanel` 写的是 `onClick={onRun}`，鼠标事件被当成了 `runSkeleton(background)` 的
`background` 参数，用户亲手点的失败全进了后台日志。

## F. 侧栏 / 启动栏 / 树（`App.tsx` `.launcher-pane`、导入页、`treeMenuItems`）

| 动作 | 空输入 | 空正文 | 无选区 | 无知识库 | 离线 | 后端没起来 | 模型报错 | 超时 | 重复点击 |
|---|---|---|---|---|---|---|---|---|---|
| 新建（启动栏 + / ⌘N / 标签栏 +） | — | — | — | — | — | ✅P3 toast「新建笔记失败：连不上应用后台…」（原来 promise 静默拒绝，什么都不发生） | — | — | ✅ 建出两篇，切走时空的那篇被 `dropIfStillEmpty` 删掉 |
| 导入页 · Markdown 文件 | ✅ 没选 = 无动作；✅P3 选的全不是 .md → toast「只认 .md / .markdown / .txt」 | ✅ 空文件照建一篇 | — | — | ✅ 勾了「同时存入知识库」：抽取失败单篇不挡其余 | ✅P3 toast「导入失败：连不上应用后台…」（原来静默） | ✅ 抽取 job 自己报 | — | ✅ 每次选一批一棵子树 |
| 导入页 · Obsidian / Evernote | ✅ 没选文件 = 无动作 | — | — | — | ✅ 抽取 job 报错 | ✅P9 toast「导入失败：连不上应用后台…」（原来实拍：右下角一条光秃秃的 `Failed to fetch`，`p9-import-netdown-before` → `-after`） | ✅ job 状态 | ✅P23 **有超时，在 provider 那一层**：实测把模型指到一个收下就不回的端点上，`memoket_kite.providers.llm` 按 `timeout=300 / retries=2` 抛 `ProviderError`（3 秒 ×2 那一档 **8.0 秒**后抛，出厂那档一块最坏 ≈ 15 分）——不是永远挂着；期间进度卡一直写着第几块，「取消」一直在。**改了话术**：「正在停止…」→「正在停止…（等这一块的模型调用结束）」，因为取消是在两块之间生效的 | ✅ importing 禁用 |
| 导入页 · Notion / 飞书 / Apple | ✅ token / App ID / Secret 为空按钮禁用 + title 说要填什么；Apple 不可用禁用 + 原因 | — | — | — | ✅ | ✅P9 同上（四个 catch 同一句） | ✅ | ✅P23 同上（同一个 job、同一条 provider 超时） | ✅ 禁用 |
| 快速搜索 | ✅ 清空 = 全部 | — | — | — | — 本地过滤 | — | — | — | ✅ |
| 树右键 · 打开 / 新标签 / 快速查看 / 分屏 | — | — | — | — | — 本地 | — | — | — | ✅ |
| 树右键 · 插入子笔记 / 在后面插入 | — | — | — | — | — | ✅P3 toast「新建笔记失败」（同 `newNoteUnder`，跟「新建」一处修） | — | — | ✅ |
| 树右键 · 重命名 | ✅ 空标题存成空 → 树上显示「未命名」 | — | — | — | — | ✅ toast「改名失败」+ 树回滚 | — | — | ✅ |
| 树右键 · 换个图标… | — | — | — | — | — | ✅ 选了存不上时图标不变（`catch`） | — | — | ✅ |
| 树右键 · 导入 .md 到这里… | 同导入页 Markdown（同一个函数） | | | | | | | | |
| 树右键 · 智能续写这篇 / 打磨这篇 | 同 C 组「智能续写 / 打磨」（先切到那篇再跑，同一个函数） | | | | | | | | |
| 树右键 · 对这棵子树无限续写 | — | — | — | — | — | — | — | — | ✅ 没有子笔记时禁用 + hint |
| 树右键 · 克隆到… / 移动到… | ✅ 取消 = 无动作 | — | — | — | — | ✅ toast | — | — | ✅ 成环时拦「不能放进自己的子树里」 |
| 树右键 · 从这个位置移除 | — | — | — | — | — | ✅ toast「移除失败」；最后一处禁用 + hint | — | — | ✅ |
| 树右键 · 复制笔记路径 | — | — | — | — | — | ✅P9 toast「复制不了路径：连不上应用后台…」（原来实拍：点了什么都没有，只有日志里一条 `promise Failed to fetch`，`p9-copypath-netdown-before` → `-after`） | — | — | ✅ |
| 树右键 · 删除 | — | — | — | — | — | ✅P9 toast「没删成「x」——它还在库里，列表刷新后会长回来：连不上应用后台…」（原来实拍：静默，`p9-delete-netdown-before` → `-after`） | — | — | ✅ 5 秒撤销窗口 |

## H. 屏幕活动（`components/JourneyPage.tsx` + `JourneyDenyPanel.tsx` + `JourneyRetentionPanel.tsx` + 菜单栏 `desktop/src/main.ts`）

> P20 补的（第 778 轮，台账 `TRACELOG-product.md` P20 节）。这一组**九种条件里有三种天生不适用**
> （空正文 / 无选区 / 无知识库：这一页不编辑正文、没有选区，事实是它自己产出的不是它读的）。
> **✅P20 = 那批修的**，每条都先在真实数据或真实 app 里复现过，前后截图名在 P20 节。
>
> **✅P21 = 第 779 轮修的**（台账 P21 节）。这一批把「× 后端没起来」那一列**真跑了一遍**——
> P20 那一列多数是「读代码读出来的 ✅」，真跑之后**两格是错的**：
> ① 删一段 / 删掉这一天**压根没有 catch**（`toasts=[]`，用户刚点过「确定」，他会以为删掉了）；
> ② 这一页的 toast **从来没过 `friendlyError`**，后端没起来时是一行英文 `Failed to fetch`。
> 靠新探针 `confirmyes`（把 `window.confirm` 换成「点了确定」）才复现得了——
> Electron 的原生确认框会把渲染进程整个挡住，这正是 P20 只能读代码的原因。
> 另外三行是这一批**新加的动作**（留多久 / 全部删掉）。

| 动作 | 空输入 | 空正文 | 无选区 | 无知识库 | 离线 | 后端没起来 | 模型报错 | 超时 | 重复点击 |
|---|---|---|---|---|---|---|---|---|---|
| 打开这一页（启动栏 `bx-desktop` / **⌘K「今天的屏幕活动」** / 欢迎页卡片 / 菜单栏「打开看看」/「文件 › 屏幕活动」/ **日记那篇的 ribbon**） | — | — | — | — | — 采集在壳里，跟模型无关 | **✅P20** 留在这一天、一条「后端没应答，这一天的记录读不出来——采集照常在壳里跑」，**天列表不清空**（修前实拍：整页退回第一次那屏「要不要开启」，四天记录像没存在过 `p20-netdown-before-light` → `-after-light`） | — | — | ✅ 虚拟页同 id 复用，不会开两个页签 |
| 知情选择「开始记录」/「先不开」 | — | — | — | — | — | ✅ 走 IPC 不走后端，后端死着也开得起来 | — | — | ✅ `bridge.start()` 幂等（`if (timer) return`） |
| 页头「开始记录 / 继续记录 / 暂停 1 小时」 | — | — | — | — | — | ✅ 同上 | — | — | ✅ 同上；**✅P20** 暂停 1 小时到点自己醒（修前：`pause(until)` 把状态压成 paused，`tick` 第一行就 return，**再也不会自己醒**，跟「暂停到我再打开」完全一样） |
| 翻天 ‹ › | — | — | — | — | — | ✅P20 天列表保住（同第一行），翻得动、翻到头置灰 | — | — | ✅ 按 `days` 列表走，到头 `null` → 禁用 |
| 「描述这 N 段」 | — | — | — | — | ✅ 看图那台连不上 → toast「看图失败：…」（`llm.describe_error` 统一翻） | **✅P21** toast「连不上应用后台（后端进程没在跑，或刚重启还没起来）——等几秒再试…」（修前实拍 `toasts=["Failed to fetch"]`，一行英文；这一页的 toast 从来没过 `friendlyError`，502 那种连状态码都原样印：「502 模型连不上：…」）`p21-catchup-netdown-before-light` → `-after-light` | **✅P20** 502 前先把这一批**已经描完的存下来**（修前：`raise` 在 `_save` 之前，而大图在描完当下就删了 → 描述没落盘、图也没了，下一轮只能标「没有截图」）；页面上还会说一句「自动描述停了：<原因>」（修前只有一整页「还没描述」，真机日志里 502 刷了 60 条没人看得见） | ✅ 看图 300 秒超时 → 502 + 中文；`limit` 一次最多 10 段，不会无限等 | ✅ `disabled={busy}` |
| 「描述这 N 段」按钮**在不在** | — | — | — | — | — | — | — | — | **✅P20** 只数「没描述**且大图还在**」的（修前：09-16 那天 38 段全无截图，按钮写着「描述这 10 段」，点了只回一句「没有要描述的了」，点多少次都一样 `p20-0916-before-light` → `-after-light`） |
| 「写这一天的回顾」/「重写」 | — | — | — | — | ✅ toast「连不上模型…」 | ✅P21 toast 过 `friendlyError`（同上一行） | ✅ 全局 `httpx.HTTPError` → 502 + `llm.describe_error`（P3 那套） | **✅P21** 忙态钮**不是禁用而是变成「停止」**，停了说一句「已停止，这一天的回顾没写成——上一份（如果有）还在」（修前实拍：灰掉的「正在写…」，Esc 没用，一次调用能跑满 300 秒，**页面上一个出口都没有**）`p21-report-stop-before-light-1` → `-after-light-1` | ✅ 两个入口同一个 flag；**第二下 = 停止**（不是发第二个请求） |
| 「写这一天的回顾」在**一段描述都没有**时 | — | — | — | — | — | — | — | — | ✅ 按钮压根不出现（`described > 0` 才渲染）；接口侧也拦：400「这一天还没有任何描述，先点『描述这几段』」——**不花那次调用** |
| 「存为笔记」 | — | — | — | — | — | ✅P21 toast 过 `friendlyError`（修前一行英文） | — | — | ✅ `upsert_child` 同名覆盖，点十次树上也只有一篇 |
| 「最近 7 天 / 30 天」回顾 | — | — | — | — | ✅ toast | ✅P21 toast 过 `friendlyError` | ✅ 502 + 中文 | **✅P21** 300 秒 + **跑着的那个变「停止」**（「已停止，这份回顾没写成，树上也没多出笔记」）——两个都禁用的话这次跑起来就没出口了 | ✅ 跑着的那个是「停止」，另一个禁用（跑完才能换范围） |
| 「最近 7 / 30 天」在**一份日报都没有**时 | — | — | — | — | — | — | — | — | ✅ 400「…还没有任何日报。先在那几天各写一份。」——不花调用、也不产出一篇空笔记 |
| 删一段 / 「删掉这一天」 | — | — | — | — | — | **✅P21**（P20 记的 ✅ 是**错的**）toast「没删成 2026-09-19，这一天还在：连不上应用后台…」——修前**一个字都不说**：`toasts=[]`，日志里只有 `promise Failed to fetch`，而用户刚点过「确定」，他会以为删掉了（探针 `confirmyes` 才复现得了，Electron 的原生确认框会挡住整个渲染进程）`p21-delday-netdown-before-light` → `-after-light` | — | — | ✅ 墓碑占位，下标不挪；删两次第二次 404；**✅P20** 删完这一天壳不再把内存里那份写回去（修前：目录被 rmtree，壳每次落盘 ENOENT，后端一建回目录**那一天自己长回来**）；**✅P21** 删不干净不再静默：`rmtree(ignore_errors)` 换成逐个删 + 500 + 把删不掉的路径写进 `detail` |
| 黑名单「加上」/ 删一个 | ✅ 空 / 全空格时按钮禁用 + title「先填一个」 | — | — | — | — | ✅P21 toast 过 `friendlyError` | — | — | ✅ 100 条 / 80 字封顶；重复的去重 |
| **「留多久」改一档**（P21 新） | ✅P21 只认列表里的档，别的**退回默认而不是 clamp**（clamp 会把 `-1` 夹成「一直留着」——把坏数据解释成最宽松那档，方向正好反了） | — | — | — | — | ✅P21 toast「连不上应用后台…」，档位回到后端那份（单测 `p21Panel`） | — | — | ✅P21 两个 `<select>` 在写的时候 `disabled`；改完**当场清一遍**并把清掉了什么说出来（看不出有没有生效的隐私开关跟没有一样） |
| **「全部删掉」**（P21 新） | — | — | — | — | — | ✅P21 toast「连不上应用后台…」，确认框留着不关（单测 `p21Panel`「后端没起来：说人话，不是一行 Failed to fetch」） | — | — | ✅P21 **两段式**：第一下摊开「会删掉什么」六行（几天 / 多少段 / 多少张图 / 几份日报 / 知识库里的记忆 / 一共多大）+「先不删」；确认钮在删的时候 `disabled`。**删不掉的文件原样摆在这一块上**，不静默 `p21-wipe-confirm-{light,dark}` |
| **到期清理**（挂在 `GET /days` 上，用户点不到） | — | — | — | — | — | ✅P21 清理在后端里跑，后端没起来就没有这件事 | — | — | ✅P21 自己限流 10 分钟（这一页每 30 秒轮询一次，每次都 `rglob` 一遍整根目录是拿用户的磁盘换一个没人要的即时性）；今天那份**一个字不碰**（壳正在往里写）；删不掉的收进 `failures` 端到界面上 |
| 缩略图（悬停那张） | — | — | — | — | — | ✅ `<img>` 加载失败就是不显示，不挡行 | — | — | — |
| 菜单栏（托盘）开始 / 暂停 / 继续 / 停止 | — | — | — | — | — | ✅ 纯本地 | — | — | ✅ 跟页面是同一个状态的两个视图；**✅P20** 用户自己按的暂停，锁屏解锁**不替他打开**（修前：`unlock-screen` 一律 `resume()`，「暂停到我再打开」被一次锁屏解掉） |
| 自动描述（壳里每 3 分钟一小批，用户点不到） | — | — | — | — | ✅P20 失败原因记一次、不刷屏（修前每 3 分钟一条一模一样的 `HTTP 502`），并送到页面上 | ✅ `if (!backend) return` | ✅P20 同上 | ✅ | ✅ 暂停 / 没开就不跑；**✅P20** 今天没得描了就补昨天（修前：过了零点只问「今天」，09-17 那 27 段截图**永远轮不到**，三天后大图过期作废） |

## G. 录音 / 转写、导入导出

录音 / 转写在 A 组（语音输入 / 插入音频）和 C 组（录音两个去处）里；导入在 F 组导入页；
**导出（导出全部 / 单篇导回）标「P2-fix 在做」**，另一个 agent 同时在改那几个文件，本批一行没碰。

---

## 统计（**P23 收尾**；P9 收尾时 ？ 还剩 6；P3 收尾时是 ✅ 189 / ✅P3 55 / ❌ 2 / ？ 19 / — 286）

> **P23（第 780 轮）把 A–G 组最后 6 个 ？ 清完了**：每一个都先在真 app 上复现，再定 ✅/❌，
> 能修的当批修。**其中两个复现出来的不是「没处理」，是「处理了但一声不吭」**——
> 引用的「补一条」/「改」遇到空文本时**静默关掉那个输入框**，看起来跟「存好了」一模一样；
> 还有一个（导入页 × 超时）复现出来是**本来就有超时**（provider 层 300 秒 × 3 次），
> 只是那句「正在停止…」没说清它要等这一块的模型调用结束。
> *「？」不是「大概没事」，是「还没去看」——去看了，六格里三格是真缺口、两格是话不老实、一格是记错了。*

> **H 组（屏幕活动）是 P20 新加的，单独计**。P20 收尾时：16 行 × 9 列 = 144 格 —— ✅ 31 / **✅P20 10** / ❌ 0 /
> ？ 0 半（「写这一天的回顾 × 超时」那格是 ✅ 带一个 ？：300 秒超时有，**转起来没有「停止」**）/ — 103。
>
> **P21 收尾（第 779 轮）**：加了三行新动作（留多久 / 全部删掉 / 到期清理）→ **19 行 × 9 列 = 171 格**
> —— ✅ 23 / **✅P20 10** / **✅P21 16**（其中 1 格两批都有）/ ❌ 0 / **？ 0**（那半个 ？ 修掉了：
> 忙态钮变「停止」）/ — 123。**这一批的 16 格里有 2 格是把 P20「读代码读出来的 ✅」翻成 ❌ 再修的**
> （删一段 / 删掉这一天在后端没起来时一个字都不说；这一页的 toast 从来没过 `friendlyError`）——
> 教训写在 H 组表头那段引文里：**没真跑过的格子不该记 ✅**。
> 下面那份 555 格的统计是 A–G 组的，没有变。

67 行 × 9 列，去掉整行合并的 4 格（导出两行「P2-fix 在做」、树右键两条「同 …」），**555 格**：

| 记号 | 格数 | 说明 |
|---|---|---|
| ✅（P1 之前 / P1 就有的） | 189 | |
| ✅P3（本批 ❌→✅） | 55 | 9 处根因修法铺到的格子；其中 **18 格有 before/after 截图**（下表），其余是同一处代码改动覆盖到的同款格子 |
| ✅P9（P9 ❌/？→✅，另 2 格 ✅ 补了一句话） | 15 | 2 个 ❌（骨架 / 排版 × 超时）+ 13 个 ？ 先复现再修（图片转表格 / 语音输入 / 插入音频 / 录音钮 ×2 / 存入知识库 ×2 的超时、导入页 ×2 / 关系卡 / 复制路径 / 删除 的后端没起来、来龙去脉 × 模型报错）；其中 **11 格有 before/after 截图**（#19 起），关系卡那格靠单测，插入音频 / 录音入库 / Notion 飞书 是同一处代码 |
| ✅P23（P23 ？→✅，另 1 格 ✅ 补了一句话） | 7 | **A–G 组最后 6 个 ？ 全部复现完、定完**：语音输入 × 重复点击（实拍两个 MediaRecorder → 拦住）、写作计划「生成写作计划」转圈无停止（接上 `AbortController`，转起来那一下改成「停止」，顺带把「重复点击」那格从「禁用」改成「不禁用 = 出口」）、引用「补一条」/「改」空文本（两处都是**静默关掉**，改成点不动 + 说清为什么）、导入页 ×2 × 超时（**量出来是有超时的**：provider 层 `timeout=300 / retries=2`，实测 8.0 秒抛 `ProviderError`；只把「正在停止…」那句话改老实）；6 格有 before/after（探针日志 + 截图，#31 起） |
| ❌（复现了、没修） | 0 | |
| ？（像没处理、没复现） | **0** | A–G 组一个不剩（H 组 P21 也清空了） |
| —（不适用） | 286 | 本地动作没有网络那几列、无输入框的动作没有空输入列、整篇动作没有无选区列 |

## 复现记录（每个 ❌ / ✅P3 一条）

全部在 scratch 拷贝上跑：`KITE_DATA_DIR=$S/p3data`（`notes.sqlite3` + `terrence/` 拷贝），供应商指向假服务
`fakellm.py`（`p3cfg.py <llm-mode> <asr-mode>` 切模式）。截图 / 日志 `$S/p3-<名>-{before,after}.{png,log}`。
探针（`frontend/src/probes.ts`，P3 新加）：`netdown[:ms]`、`click:<ms>:<选择器|text=文字>`、`toasts:<ms>`（把 toast / 红字 / 运行块 / 忙态写进日志）、
`type:<ms>:<文字>`、`selact:<id|new>:<动作>`、`audiopick:<id>[:bad]`、`mdpick[:netdown]`；用 `;;` 串起来。
「后端 0 个请求」看 `[backend] POST /api/...` 行；「什么都没发生」看 `toasts=[] … runs=[]`。

| # | 格子 | 复现到什么（before） | 修完（after） | 探针 / 截图 |
|---|---|---|---|---|
| 1 | 计划 · 生成骨架 × 空正文 | 点了什么都没有：`POST /api/skeleton 400` 发出去了，回来的话只进 `client:warn`，toasts=[] | toast「先写点内容（或标题）再生成骨架」，0 个请求 | `blank:ribbon:x;;click:3000:text=计划;;click:3600:text=生成骨架;;toasts:5500` → `p3-skeleton-blank-{before,after}.png` |
| 2 | 计划 · 生成骨架 × 模型报错 | `POST /api/skeleton 500`，toasts=[] | 502 + toast「生成骨架失败：模型服务返回 500（http://…/err500/v1）——多半是那边出错了」+ 打开设置 | `note:…;;click:2500:text=计划;;click:3000:text=生成骨架;;toasts:5500`（err500）→ `p3-skeleton-err500-{before,after}.png` |
| 3 | 计划 · 生成骨架 × 后端没起来 | `client:warn 后台骨架生成失败：模型连不上…`，toasts=[] | toast「生成骨架失败：连不上应用后台…」 | `note:…;;netdown:2000;;click…`→ `p3-skeleton-netdown-before.png`（after 走同一处修法，见 #1/#2） |
| 4 | 格式 · 智能排版 × 空正文 | 点了什么都没有（`if (!before.trim()) return`），0 请求、0 toast | toast「正文是空的——智能排版没有可排的内容」 | `blank:ribbon:x;;click:3000:text=格式;;click:3500:text=智能排版;;toasts:5000` → `p3-restructure-blank-{before,after}.png` |
| 5 | 新建 × 后端没起来 | `[client:error] promise Failed to fetch`，toasts=[] | toast「新建笔记失败：连不上应用后台（后端进程没在跑，或刚重启还没起来）——等几秒再试…」 | `netdown:1500;;click:2500:.launcher-btn[title^=新建];;toasts:4500` → `p3-newnote-netdown-{before,after}.png` |
| 6 | 导入 .md × 后端没起来 | `importMarkdown` 抛 `TypeError: Failed to fetch`，没人接，toasts=[] | toast「导入失败：连不上应用后台…」 | `open:app:import;;mdpick:netdown;;toasts:4000` → `p3-mdpick-netdown-{before,after}.png` |
| 7 | ⋯ 存入知识库 × 重复点击 | 两个 `POST /api/ingest/text 200`：第一个 job 还在跑，菜单项没禁用，第二下又发一个 | 第二下 `disabled=true`，只有一个 POST | `note:…;;click:2500:[title=更多];;click:3000:text=存入知识库;;click:3400:[title=更多];;click:3800:text=存入知识库;;toasts:6500` → `p3-ingest-double-{before,after}.png` |
| 8 | 用 AI 写 × 后端没起来 | 占位块原样写 `用 AI 写 Failed to fetch` | 占位块「连不上应用后台（后端进程没在跑…）」 | `netdown:2200;;slashpick:prompt:…:总结一下;;toasts:7000` → `p3-slashprompt-netdown-{before,after}.png` |
| 9 | 用 AI 写 × 模型报错 | 占位块整段 `HTTPStatusError: Server error '500 Internal Server Error' for url 'http://…/chat/completions' For more information check: https://developer.mozilla.org/…` | 占位块「模型服务返回 500（http://…/err500/v1）——多半是那边出错了，稍后再试」 | 同上（err500）→ `p3-slashprompt-err500-{before,after}.png` |
| 10 | 智能续写 × 模型报错 | 轮次卡片挂同一段 httpx 英文，卡片状态停在「续写整篇：在写…」，toasts=[] | 两条 toast：「「骨架」这一步出错了，本轮少了这个能力：…」（warning）+「智能续写出错停下：模型服务返回 500（…）」+ 打开设置；状态行同句 | `note:…;;click:2500:text=智能续写;;toasts:8000`（err500）→ `p3-harness-err500-{before,after}.png` |
| 11 | 续写 / 校验 × 后端没起来 | toast「续写失败：模型连不上——去设置里检查 LLM 供应商的地址和 key」+「打开设置」——**指错了地方** | toast「续写失败：连不上应用后台…」，没有「打开设置」 | `netdown:1200;;tap:…;;toasts:5000` → `p3-tap-netdown-{before,after}.png`；`netdown:2000;;selact:…:verify;;toasts:4500` → `p3-selact-verify-netdown-{before,after}.png` |
| 12 | 校验 × 离线（黑洞） | 状态栏红着「LLM 不可达 (http://192.168.77.8:8080/v1) · 去设置」，菜单里「校验中…」转到 11 秒还在转（要等 OS 的 SYN 重试 75 秒+） | 后端 10 秒报「模型连不上：10 秒内没连上模型服务（http://192.168.77.8:8080/v1）」；健康检查回来之后再点「续写」直接拦：「LLM 不可达 (…)——先在设置里把模型地址 / key 修好，再点这个」+ 打开设置 | `selact:…:verify;;toasts:11000`（blackhole）→ `p3-selact-verify-blackhole-before.png` / `…-after.png`（toasts:14500）；`note:…;;click:8000:text=续写;;toasts:10000` → `p3-tap-blackhole-gate-after.png` |
| 13 | 重写 × 超时 | 「重写中…」转圈，菜单没有任何按钮，Esc 关不掉，要等满 300 秒 | 菜单里「停止」→ toast「已停止」 | `selact:…:rewrite;;toasts:11000`（hang）→ `p3-selact-rewrite-hang-before.png`；after：`…;;click:4500:text=停止;;toasts:6000` → `p3-selact-rewrite-hang-after.png` |
| 14 | 做成幻灯片 × 超时 | 忙态胶囊「做幻灯片…」，没有停止，要等满 600 秒 | 胶囊里「停止」→ toast「已停止做幻灯片」 | `note:…;;click:[title=更多];;click:text=做成幻灯片…;;toasts:8000`（hang）→ `p3-slides-hang-{before,after}.png` |
| 15 | 插入音频 × 离线（语音服务） | 音频传上去了（`POST /api/assets 200`）但**没插进笔记**，占位块「处理失败：模型连不上——去设置里检查 LLM 供应商的地址和 key」，状态栏却写着「语音离线」 | 播放器插进正文（字数 2764→2838），占位块「音频已插入；语音服务不可达（http://127.0.0.1:1），这次没转写——在设置里检查语音服务地址」，0 个转写请求 | `audiopick:…;;toasts:6000`（asr refused）→ `p3-audiopick-asrdown-{before,after}.png` |
| 16 | 插入音频 × 模型报错（语音服务 500） | 同 #15：音频没插进笔记，报错说「后端处理出错——看一眼设置里的 LLM 供应商」 | 播放器已插入 + 「音频已插入，转写没成：语音服务返回 500（…/asr-err）」 | `audiopick:…;;toasts:6000`（asr-err）→ `p3-audiopick-asrerr-{before,after}.png` |
| 17 | 格式 · 智能排版 × 超时 | 「智能排版…」转圈，按钮禁用，没有停止（**本批没修**） | — | `note:…:ribbon:format;;click:3000:text=智能排版;;toasts:8000`（hang）→ `p3-restructure-hang-before.png` |
| 18 | 计划 · 生成骨架 × 超时 | 「生成骨架…」转圈，没有停止（**本批没修**） | — | `note:…;;click:2500:text=计划;;click:3000:text=生成骨架;;toasts:8000`（hang）→ `p3-skeleton-hang-before.png` |

| 19 | 图片转表格 × 超时（看图模型慢 12s） | 5s 点占位块「停止」：块没了，但 `imagepick returned chars=594`——表格在停止之后照样插进正文（558 → 594） | 停止后 `chars=558` 到 20s 不变 + toast「已停止，什么都没插进正文」 | `imagepick:…;;mdown:5000:.cm-run-stop;;toasts:8500;;toasts:19500`（vision=vslow）→ `p9-imagepick-slow-{before,after}.png`（`-2` 是 20s 那张） |
| 20 | 语音输入 × 超时（语音服务慢 12s） | 3.5s 停录音 → 转写中；5s 再点「停止」：块没了，20s 时文末多出「这是假语音服务转出来的一句话。」（521 → 536 字） | 20s 字数不变 + toast「已停止，这段录音没有转写」 | `mic;;slashpick:voice:…;;mdown:3500:.cm-run-stop;;mdown:5000:.cm-run-stop;;toasts:7500;;toasts:19500`（asr-slow；`mic` 探针把 getUserMedia 换成合成音轨）→ `p9-voice-slow-{before,after}-2.png` |
| 21 | 录音钮 → 插入正文 × 超时 | 停录音后按钮「转写中…」禁用，`text=转写中…停止 found=false`——没有出口 | 「转写中… 停止」可点 → toast「已停止，这段录音没有转写」 | `mic;;note:…;;click:[title^=录音];;click:text=录音→插入正文;;click:text=停止录音;;click:text=转写中…停止;;toasts`（asr-slow）→ `p9-rec-slow-{before,after}.png` |
| 22 | ⋯ 存入知识库 × 超时（模型卡住） | 状态栏「存入知识库中…」转着，`text=取消 found=false` | 「取消」在状态栏那一行；点了变「取消中（等这一块的模型调用结束）」 | `note:…;;click:[title=更多];;click:text=存入知识库;;click:6000:text=取消;;toasts:8500`（hang）→ `p9-ingest-hang-{before,after}.png` |
| 23 | 导入页 Evernote × 后端没起来 | 右下角 toast 一条光秃秃的 `Failed to fetch` | toast「导入失败：连不上应用后台（后端进程没在跑…）」 | `open:app:import;;netdown:2500;;filefill:3500:input[accept=".enex"]:probe.enex;;toasts:6000` → `p9-import-netdown-{before,after}.png` |
| 24 | 树右键「复制笔记路径」× 后端没起来 | 点了什么都没有；日志 `client:error promise Failed to fetch` | toast「复制不了路径：连不上应用后台…」 | `tree-menu;;netdown:1500;;click:2500:text=复制笔记路径;;toasts:5000` → `p9-copypath-netdown-{before,after}.png` |
| 25 | 树右键「删除」（叶子）× 后端没起来 | 乐观删除 5 秒后真删失败：静默，只有 `promise Failed to fetch` | toast「没删成「plaud的优势分析」——它还在库里，列表刷新后会长回来：连不上应用后台…」 | `tree-menu-leaf;;click:2000:text=删除;;netdown:3000;;toasts:10000` → `p9-delete-netdown-{before,after}.png` |
| 26 | 来龙去脉 × 模型报错（500） | 转 20–30 秒（假服务收到 **9** 个 POST：3 次调用 × 3 次重试），然后右栏「脉络」：「知识库里没有跟这段沾边的记录。」——假的 | 5 秒内 502（假服务只收到 1 个 POST）+ toast「操作失败：来龙去脉没查成：模型服务返回 500（http://…/err500/v1）——去设置里看一眼 LLM 供应商」 | `selact:…:trace;;toasts:7500;;toasts:19500;;toasts:39500`（err500）→ `p9-trace-err500-3.png`（40s）；after：`p9-trace-err500-after.png` |
| 27 | 计划 · 生成骨架 × 超时 | `p3-skeleton-hang-before`（P3 #18） | 「停止」→ toast「已停止生成骨架」 | `note:06647b9c2031;;click:2500:text=计划;;click:3000:text=生成骨架;;click:6000:text=停止;;toasts:8000`（hang）→ `p9-skeleton-hang-after.png` |
| 28 | 格式 · 智能排版 × 超时 | `p3-restructure-hang-before`（P3 #17） | 「停止」→ toast「已停止智能排版，正文没动」 | `note:…;;click:2500:text=格式;;click:3000:text=智能排版;;click:6000:text=停止;;toasts:8000`（hang）→ `p9-restructure-hang-after.png` |
| 29 | 续写第二下 = 停止 | `p3-tap-double-hang-before`：停了一个字不说 | toast「已停止续写，写到哪算哪——不要的话用正文上的「撤回」」；智能续写同款（「改动在右栏「改动」里可以整层撤回」） | `note:…;;click:3000:text=续写;;click:6000:text=停止;;toasts:8000`（hang）→ `p9-tap-double-hang-after.png` |
| 30 | 记忆 · 关系卡 × 后端没起来 | 实拍两次都摆不出卡（`p9-relcard-netdown-before`：那篇的数字全在引用编号里，剥掉之后没有量，右栏不查关系）；代码是 `String(e)` | 单测：fetch 拒绝 → 「标不上：连不上应用后台…」 | `p9AgentNative.test.ts`「后端没起来：「标不上」说人话」 |

| 31 | 语音输入 × 重复点击（录音中再 `/` → 语音输入） | `voicetwice 第二次之后 runs=2`——**第二个 MediaRecorder 起来了**：两个占位块、两条麦克风轨，而「停止」只停得了它自己那一个 | `runs=1` + toast「已经在录一段了——先把正文里那个『录音中』停掉，再录下一段」 | `mic;;voicetwice:0eecee3d7b94;;toasts:8200;;dom:8400:.cm-run-head` → `p23-voice-twice-{before,after}-light.{png,log}` |
| 32 | 引用 · 补一条 × 空输入 | 什么都没写点「加上」：`加上 d=false`（点得动）→ 点完 **`.ribbon-body textarea n=0`**（框一声不吭地关掉了，看起来跟「加好了」一样） | `加上 d=true` + title「先写一句：谁、什么时候、什么」；点了没反应、框还在（`textarea n=1`） | `note:0eecee3d7b94:ribbon:cites;;click:6500:text=补一条;;dom:9000:.ribbon-body button;;click:9500:text=加上;;dom:11000:.ribbon-body textarea` → `p23-fact-add-empty-{before,after}-light` |
| 33 | 引用 · 改 × 空文本 | 清空之后点「保存」：`保存 d=false` → 点完框没了、原话原样留着（用户以为改成空了） | `保存 d=true` + title「空的存不了——要去掉这条就用右边那个『从知识库删掉这条』」；框还在 | 同上 + `.icon-btn[title=改]` / `fill:…:textarea[aria-label=改这条事实]:` → `p23-fact-edit-empty-{before,after}-light` |
| 34 | 写作计划 · 「生成写作计划」× 超时 | 钮一转就禁用，Esc 只关面板，请求照跑到 300 秒——页面上一个出口都没有 | 第二下 = 停止（`AbortController`）+ toast「已停止，没有生成计划——目标还留在框里」 | 单测 `p23Forms.test.tsx`（假 `startWritingPlan` 挂着不回，点「停止」看 `signal.aborted`）；突变验 3 条 |
| 35 | 导入页 Obsidian / Notion × 超时 | **不是缺口**：真把模型指到收下就不回的端点上，`memoket_kite.providers.llm` 按 `timeout=300 / retries=2` 抛 `ProviderError`（3 秒 ×2 那一档 8.0 秒后抛）——有上限，进度卡和「取消」一直在 | 只改话术：「正在停止…」→「正在停止…（等这一块的模型调用结束）」（取消在两块之间才生效，卡住那一块最坏 ≈ 15 分） | `$S/p23/import_timeout.py` + `p23stub.py 18431`（18433 = 收下不回那个口） |
| 36 | 屏幕活动 · 删掉这一天 / 删一段 × 系统弹窗 | P21 立的那条：`window.confirm` **把整个渲染进程挡住**——实拍 before 树点完「删掉这一天」之后 `toasts:11000` 那一步**一行日志都没出来**（页面不再画） | 页面内红框（跟「全部删掉」一致），`toasts` 照常跑完；摊开的那几行写清「连它抽进知识库的记忆一起删」 | `open:app:journey;;click:8000:text=删掉这一天;;toasts:11000` → `p23-delday-{before,after}-light`；删一段 `p23-delseg-{before,after}-light` |

P23 新加的探针：`dom:<ms>:<选择器>`（把命中的元素有几个、写着什么、禁没禁用写进日志——`toasts` 只看 toast / 红字那几处）、
`voicetwice:<id>`（录着的时候再来一次语音输入）；`fill` 对 textarea 改用 `HTMLTextAreaElement` 的 setter
（老的那份拿 `HTMLInputElement` 的 setter 去 call textarea，Chrome 直接 TypeError、值一个字没改——「改成空」那一格在 before 树上因此摆不出来）；
`open:` 改成只跑一次（探针 effect 在 notes 变时会重跑，「去这天的日记」建出那一篇之后它当场又把人拽回屏幕活动那一页，
看起来像「这个链接没反应」——**那是量具在动，不是产品在动**）。这三样都补进了 before 树再对比，
**两边用的是同一把尺子**（`$S/p23_patch_before_probes.py` / `p23_patch_before_voice.py`，产品代码一行没动）。

P9 新加的探针：`mdown:<ms>:<选择器>`（只听 mousedown 的东西）、`fill:` / `filefill:`（往输入框 / file 框里填）、`mic`（合成音轨）、`imagepick:<id>`、
`tree-menu-leaf`、`cursorline:<ms>:<行>`、`title:<ms>:<文字>`、`blank:margin:<文字>`、`margin:<id>:card[:<关系>]`；假服务 `fakellm9.py` 多了 `slow` / `vslow` / `asr-slow` / `asr-hang` 四种模式
（慢 12 秒再正常回——「停止之后请求照跑、跑完仍插进正文」只有慢模式才看得见，卡死模式永远回不来）。

顺手拍到但没算 ❌ 的：`p3-tap-double-before`（假模型秒回，第二下点到的已经是「续写」——重复点击要在 hang 模式下测，
`p3-tap-double-hang-before` 证实第二下 = 停止，且停下来没有一句话）；`p3-selact-trace-err500-before`（来龙去脉在模型 500 下 6 秒仍在转，
KITE `ask()` 内部重试，标 ？）；`p3-verify-emptykb-before` / `p3-trace-emptykb-before` / `p3-expand-emptykb-before` / `p3-tap-emptykb-before`
（零库四种都有处理，见 B / C 组）；`p3-voice-asrdown-before`（语音输入离线前置拦，✅）；`p3-tap-refused-before` / `p3-selact-verify-refused-before`
（拒绝连接：续写有「打开设置」，校验只有一句「后端处理出错」——P3 之后是 502 带人话）。

---

## P31 补：两格 + 一条「第十列」的提案（2026-09-20，台账 `TRACELOG-product.md` P31 节）

P31 是第二次打包版全流程走查（P17 之后又过了 P18–P29 十二批）。走查撞出来两格这张表里没有的、
和一条**九列覆盖不到的条件**。

### 新增的记号

| 记号 | 意思 |
|---|---|
| ✅P31 | P31 修的：先在打包版里复现，前后对比在「复现记录」#37 起 |

### 两格

| 组 | 动作 × 条件 | 修之前 | 现在 |
|---|---|---|---|
| H 屏幕活动 | **今天这一页 × 没在记录（且今天 0 段）** —— 九列里没有「功能开关关着」这一条，所以一直没人摆过 | 上面写「没在记录 —— 以前记的还在」，下面同时写「**今天刚开始记，攒够一段就会出现在这儿**」：两句话互相打脸，而且是空头支票（不在记录，等到明天也不会有东西出现） | **✅P31**：按 `state` 分三句——running 照旧；paused「今天还没有记录——现在是暂停着的，「继续记录」之后才会接着记」；off「今天还没有记录——现在没在记录，点右上角「开始记录」才会开始攒」 |
| C 编辑器 · 分层历史 | **「只撤这一轮」× 同一轮撤第二次** —— 九列里「重复点击」指的是「跑的过程中再点」，这一格是**跑完之后再点一次**（用户常见动作：「刚才那下生效了吗？再点一次」） | 撤得对（幂等，正文不变）、话也说得对，但**引的那一处是空的**：「**「」**在现在的正文里出现了不止一处，不知道撤哪一处」——纯空行的 hunk 被 `snippet()` 折成空串，看着像程序出错 | **✅P31**：`conflictLead()`——有原文引原文，没有就说「第 N 处（只有空行、没有可引的原文）」 |

### 提案：第十列「模型答得不合形状」

九列里有**模型报错**（4xx / 5xx）、**超时**（收下不回），**没有「模型收下了、也回了，但回的东西不是这个动作要的形状」**。
P31 实拍：`/` 菜单「智能插图」在假模型 `ok` 档下拿到 `{"text": "...", "reason": "..."}`，
**整串 JSON 原样落进正文**，还跟着进了「计划」的目录和写作骨架（`p31-4b-new-slashai-light`）。
P17 把这记成「假模型 canned 是 JSON」的量具 artifact——**但真模型一样会答不合形状**
（小模型尤其），而这条路上没有任何一层校形状。

要补的话：AI 组那八九个动作各自落地前校一次形状，不合就按「模型没按要求答，这一次没往正文里写」提示 +
**不落正文**（跟「停止」那几条同一个原则：宁可什么都不做，也别往正文里塞脏东西）。
**本批只记，不改**——它要动 AI 组每一个动作的落地口，值一整批。

### 复现记录（接 #36）

| # | 格 | 修之前（实拍） | 修之后 | 探针 / 截图 |
|---|---|---|---|---|
| 37 | 屏幕活动 · 今天这一页 × 没在记录 | 页面同时出现「没在记录 —— 以前记的还在，可以回看、可以删。」和「今天刚开始记，攒够一段就会出现在这儿。」 | 「今天还没有记录——现在没在记录，点右上角「开始记录」才会开始攒。」 | 打包版 + `$S/p31/steps/s8.mjs` / `after3.mjs` → `p31-8a-old-journey-light` / `p31-fix3-old-after-light` |
| 38 | 「只撤这一轮」× 撤第二次 | toast：「第 2 轮改的 3 处在现在的正文里都对不上，没有改动。**「」**在现在的正文里出现了不止一处，不知道撤哪一处」 | 没有可引的原文时改说「第 N 处（只有空行、没有可引的原文）」；有原文的照旧引原文 | `$S/p31/steps/s6d.mjs` → `p31-6d-old-undo-twice-light`；单测 `editor/__tests__/p31.test.ts`「P31 #4」四条 |

**这一批的教训（跟 P17 那条同源）**：九列是按「输入 / 依赖 / 网络」切的，
**切不到「这个功能本身是关着的」和「这个动作刚做完又做一次」**——
两格都是走查时顺手多点一下才撞出来的，照着表格摆条件永远摆不到。

---

## P32 补：**第十列「模型答得不合形状」**（2026-09-20，台账 `TRACELOG-product.md` P32 节）

P31 提的那条提案这一批落了。**九列到十列**：

> **模型答得不合形状** —— 模型收下了、也回了、状态码 200、也不超时，
> **但回的东西不是这个动作要的形状**。它跟「模型报错」是两件事：那一列是
> 4xx / 5xx，这一列是 **200 + 脏内容**，而脏内容会**落进用户的正文**——
> 比「报错」贵得多，因为报错至少什么都没写。

### 怎么摆这一列

跟「模型报错」共用同一个假模型（`$S/p31/fakellm.py`）：把 scratch 库的
`provider_config.gpt_base_url` 指到 `http://127.0.0.1:18132/ok/v1`。
它的 `ok` 档回的就是 `{"text": "…", "reason": "…"}` ——**跟 P31 实拍到的那一串逐字相同**。

### 判据在哪、宽窄怎么定

`frontend/src/editor/blockShape.ts`（纯函数，`App.runBlock` 在**落正文之前**调它）。
**只认明确不合形状的三类**，其余一律放行——误伤一次正常产出（用户眼睁睁看着跑完
100 秒然后被拦下）比漏掉一次贵得多：

| 认 | 判据 | 为什么窄 |
|---|---|---|
| 整块是 JSON | `{…}` / `[…]` 且**真能 `JSON.parse`** | 只看开头那个 `{` 会把「{产品名} 的定价还没定，先看 {留存}」这种正常句子拦下来 |
| 该是表格的没表格 | `table` 档：一行 `\|` 都没有 | — |
| 该是图的没有图 | `eda`（数据可视化）档：既没有围栏也没有 `![](…)` | 围栏**和**图片都算，只认围栏会误伤「先说明再给图」 |

**`chart`（智能插图）只过 JSON 那一档**：它的界面承诺就是「数据用图表、概念用文生图，自动判断」，
产出形状本来就不唯一，拿围栏卡它就是误伤。**`/` 菜单新加一项时默认落在 `markdown`**——
漏判比误判便宜，而且不会静默开出一道新闸。

### 表里怎么填（AI 组九项 + 右键六项）

| 动作 | 模型答得不合形状 |
|---|---|
| 用 AI 写 / 从托盘写 / 智能插图 / 智能表格 / 数据可视化 / 智能数据分析 | **✅P32**：整串 JSON（三类之一）→ **不落正文**，占位块上一句人话 +「重试」；产出留在占位块的预览里（想看还能看） |
| 图片转表格 | ✅（P3 之前就有）：后端 `table-from-image` 自己判 `detected`，识别不出表格明确回 `detected=false`，前端如实说「没有检测到表格」——**这一列唯一一个早就做了的动作** |
| 语音输入 / 插入音频 | — 转写出来的是纯文本，没有「形状」可言 |
| 排版 10 项 | — 纯本地 CM 命令，不过模型 |
| 右键六项（校验 / 重写 / 润色 / 扩展 / 来龙去脉 / 自定义提示） | **？**：`custom` 走的是同一条 `runBlock`，所以**已经过闸**；另外五项各走各的端点（`/verify`、`/rewrite`、`/expand`、`/trace`），这一批**没有摆过这一列**，不冒充 ✅ |
| 智能排版（`/restructure`） | ✅（结构性保证，不靠形状闸）：模型只输出「第几行改成什么结构」，**一个字的原文都不输出**，原文由后端按行搬运；再加 `editor/textshape.content_drift` 兜底比对 |
| 智能续写 / 无限续写（harness） | **？**：harness 有打分器（`checks.rubric`），但打的是「写得好不好」不是「形状对不对」；这一批没摆 |

### 复现记录（接 #38）

| # | 格 | 修之前（实拍） | 修之后 | 探针 / 截图 |
|---|---|---|---|---|
| 39 | A 组 · 智能插图 × **模型答得不合形状** | `{"text": "（假模型改写）这一段由假模型返回。", "reason": "假模型"}` **整串落进正文**（字数 1790 → 1833），占位块消失、右栏「命中」里跟着冒出 `reason` 这个词 | 正文一个字没动（1790 字），占位块留着：「智能插图 · 模型这次答的是一串 JSON，不是能写进正文的内容——这一块没有落进正文。点「重试」再来一次。」+ 一个「重试」按钮 | 假模型 `ok` 档 + `--probe=slash:插图` + `$S/p32/steps/slashshape.mjs` → `p32-2-slashshape-before-light` / `p32-2-slashshape-after-light` |

**这一列的教训**：P17 把这个现象记成「假模型 canned 是 JSON」的**量具 artifact**，
于是它在表里一格都没占。**归因错了一次，这条缺口就多活了 14 批**——
假模型只是让它每次都发生，真模型是偶尔发生，而偶尔发生的脏数据落进正文一样收不回来。

---

## P35 补：**第十列那七个 `？` 摆完了**（2026-09-20，台账 `TRACELOG-product.md` P35 节）

P32 加了第十列，右键五项 + harness 两条填的是 `？`（「没摆过就不冒充 ✅」）。这一批摆出来了。

### 新增的记号

| 记号 | 意思 |
|---|---|
| ✅P35 | P35 修的：先在打包版里复现，前后对比在「复现记录」#40 起 |

### 摆之前先换量具——P32 那份摆不了这一列

P32 用的是假模型 `ok` 档回的 `{"text": …, "reason": …}`。
**它正好就是 `/rewrite` 要的形状**：P17 把这一串记成「假模型 canned 是 JSON」的 artifact，
P32 纠正成「在块生成那条路上它是脏内容」，**而在右键那条路上它是正品**。
拿它去测右键五项等于什么都没测（P35 第一版真这么跑了一遍，三条动作「正常落进正文」，
差点记成「这一列没问题」）。

新量具 `$S/p35/fakellm35.py`，按「不合形状」的**几种真形状**分档，一次只换一个变量：

| 档 | 回什么 | 对应真模型的哪种毛病 |
|---|---|---|
| `/prose/v1` | 纯散文 + 聊天开场白（「当然可以！以下是改写后的内容：…」） | JSON 模式没关干净 |
| `/wrongkey/v1` | 是 JSON、键名不对（`{"result": …, "why": …}`） | 照着别的 schema 答 |
| `/jsontext/v1` | 键名对、**值本身是一串 JSON** | 把整个响应又塞进 `text` 里 |
| `/listnot/v1` | 该给数组的给了对象 | 写作计划要的是 titles 列表 |
| `/streamjson/v1` | **流式那一步**吐 JSON | 智能续写「写」这一步 |

**摆的时候栽过四次，四次都是量具的毛病**（照实记，免得下一批重踩）：

1. 改了库、请求还是打到上一档的端口上——设置页选的是「本地模型」，`provider='local'`，
   生效的是 `local_base_url` 不是 `gpt_base_url`。**两列都写、再断言一次。**
2. 后端进程**缓存 `provider_config`**——不重起 app 不生效。改成每换一档重起一次。
3. 假模型把一个 **SSE 响应答给了一次非流式请求**——右键的 `REWRITE / POLISH / EXPAND_SYSTEM`
   开头也是「你是写作编辑」（`backend/app/harness/prompts/selection.py:35/46/56`），
   跟 harness 修订那一步撞了；那一支没看 `stream` → httpx `.json()` 炸 → 500。
   **差点把量具的毛病记成产品缺陷。**
4. `cm-content.innerText` 会把**占位块 widget 的字**算进正文——形状闸明明拦住了，探针却报
   「正文里有那串 JSON」。改成逐 `.cm-line` 读文档 + 对状态栏那个字数。
   **「看到 ≠ 真在正文里」，跟「选不到 ≠ 没有」是一对。**

### 右键五项：判据跟块生成**不是同一条**

右键那三条会落正文的动作（重写 / 润色 / 扩展上下文），产出是**正文里的一句话**，
不是「在光标处插一整块」。所以 `blockShape.shapeOf` 那张表**一条都不能搬过来**：
`table` 那一档会把「帮我把这段改成一张表」这种正当产出判死。
**只搬 JSON 那一档**（`revisionIsJson`，跟 `looksLikeJson` 逐字同一条判据）。

另一半是**结构性的、本来就有**：`anchor` 是**后端**填的 `body.selection`，不是模型给的
（`backend/app/routers/compose.py:376 / 428 / 433`）——所以一条建议永远锚在真实存在的原文上，
模型只能决定「换成什么字」。这也是为什么这一列在右键这条路上只剩「换上去的那段字本身脏不脏」一个口子。

### 表里怎么填（七个 `？` 的落点）

| 动作 | 端点 | 模型答得不合形状 |
|---|---|---|
| 右键 · 校验 | `/verify` | **✅P37**（修之前是 ❌）：**不落正文**（结果进右栏）这一半是对的；但抽不出 `findings` 就回空列表，前端 `VerifyPanel.tsx:37` 只认「空 = 没记录」，于是**满库下说假话**——实拍 terrence 20417 条事实、词法召回真的回了 1 条，面板却写「没有找到能支持或反驳这段内容的记录——…只是知识库里没有相关信息。」（复现记录 #42）。**P37 修法**：`VerifyOut` 多两格——`checked`（这一次真翻过几条记录）和 `unparsed`（模型答的抽不出判断），`VerifyPanel.emptyVerifyLine(checked, unparsed)` 按它们**分三句**说：没查到 / 查到 N 条但模型没按格式答 / 查到 N 条模型逐条看完没话说。判据窄：**空列表 `[]` 不算「没按格式答」**，那是模型按格式答的「翻过了，没话说」（复现记录 #46） |
| 右键 · 重写 / 润色 | `/rewrite` | **✅P35**：`text` 值本身是一串 JSON 时**不落正文** + toast「重写：模型这次答的是一串 JSON…这 N 处没有落进正文，正文一个字没动。」（修之前：原样换进正文，117 → 163 字，一句话不说，#40）。抽不出 `text` 那一档本来就不落正文（「模型没有给出修改建议。」） |
| 右键 · 扩展上下文 | `/expand` | **✅P35**：同上，`before` / `after` 两条各自过闸（修之前 117 → **210** 字，#41）。**遗留半格**：抽不出时那句「模型认为不需要补充上下文。」是**替模型说的**——模型从没说过，见台账 P35 问题 #5 |
| 右键 · 来龙去脉 | `/trace` | **✅P37**（修之前是 ❌）：不落正文（结果进右栏「脉络」）；但 KITE 的 `providers/llm.llm_json` 抛 `ProviderError: no JSON in llm output` → **500 原样透出来** → toast「操作失败：后端处理出错（**多半是模型没应答**）——看一眼设置里的 LLM 供应商」+「打开设置」。**模型明明答了，指去改地址 / key 是指错地方**——跟 P3 那条「后端没起来 ≠ 模型连不上」同源，升了一层：**「答得不合形状 ≠ 没应答」**（复现记录 #43）。**P37 修法**：`routers/memory.trace` 单独接 `ProviderError`，**只认 `no JSON` 这一句**（连不上 / 超时 / 401 照旧透出去，「打开设置」对它们才是对的出口），翻成 502 +「模型答的不是这个动作要的格式（不是没应答，供应商是通的）——换个模型，或者再试一次」。这句话不以「模型连不上 / 后端处理出错 / 模型服务」开头，所以 `isLlmUnreachable` 是 false——**界面上不再挂「打开设置」**（复现记录 #47） |
| 右键 · 自定义提示 | 走 `runBlock` | ✅P32（本批复核过，确实走同一条闸） |
| 智能续写 / 无限续写 | harness SSE | **✅P37**（修之前是 ❌）：**流式那一步吐 JSON → 两段整串落进正文，还跟着进了「目录」，一句话都不说**（117 → 211 字）。P32 的闸装在 `App.runBlock` 上，harness 的增量走 `App.tsx:2172 onDelta → streamJoin.insertStreamed`，**逐 token 落地，没有「这一轮写完了再看一眼形状」这一层**（复现记录 #44）。**P37 修法**：闸装在**判据层**——`backend/app/harness/checks/shape.output_not_json`（挂在 `modes.NOTE` / `modes.SECTION` 的 checks 最前面），判据跟 P32 的 `blockShape.looksLikeJson` **逐字同一条**（整段 unfence 之后真能 `json.loads` 成对象 / 数组），量程是 `st.fresh`；命中 → `fix` 把这次跑新写的那几段 JSON 从正文里摘掉 + `fix_done` 让摘完那版留下 + 短路打分并把「这一轮重写」送进 `State.steer`。**前端一行没改**：`STEP_FINISHED` 本来就带服务端的权威正文回去，`App.onRoundEnd` 照它对齐（复现记录 #48） |
| 写作计划（`/writing-plan/start`） | — | **✅**（早就有，这一批头一次摆）：模型该给数组却给了对象 → 后端 `writing_plan.py:150` 回 **502「模型没能生成有效的分段列表，换个目标描述再试试」**；正文一个字没动、**一篇笔记都没建**（核过还是 1 篇） |

### 第十列补完之后的统计

**28 格**（A 组 AI 九项 + 排版十项 + 右键六项 + 智能排版 + harness 两条）：

| 记号 | 格数 | 说明 |
|---|---:|---|
| ✅（早就有的） | 3 | 图片转表格（后端自己判 `detected`）· 智能排版（结构性保证）· 写作计划（502 + 那句人话） |
| ✅P32 | 7 | AI 六项（用 AI 写 / 从托盘写 / 智能插图 / 智能表格 / 数据可视化 / 智能数据分析）+ 右键「自定义提示」 |
| **✅P35** | **3** | 右键 重写 / 润色 / 扩展上下文 |
| **✅P37** | **3** | 校验 · 来龙去脉 · 智能续写（**P35 那三个 ❌，这一批修完了**） |
| ❌（复现了、没修） | **0** | **一个都不剩了** |
| — | 12 | 排版 10 项（不过模型）+ 语音输入 / 插入音频（转写出来的是纯文本，没有「形状」可言） |
| **？** | **0** | **七个全摆完了** |

**P37 收尾：这一列 28 格里 0 个 ❌、0 个 `？`。** 三条修法全在后端（`harness/checks/shape.py` /
`routers/compose.py` + `schemas.py` / `routers/memory.py`），前端只跟着换措辞（`VerifyPanel` 分三句、
`blockShape.unparsedEditNote`）；智能续写那条**前端一行没改**。

**P35 那半格遗留也一起清了**（「扩展上下文抽不出时那句『模型认为不需要补充上下文。』是替模型说的」）：
`EditOut` 多一格 `unparsed`——判据**只看键在不在，不看值**，因为 `{"before": "", "after": ""}` 是
`EXPAND_SYSTEM` 里明写着的「两个方向都不用补」，把它算成「没答上来」等于换个方向替模型说谎。
抽不出时前端说的是「扩展上下文：模型这次没按要求的格式答，抽不出可以落进正文的建议——正文一个字没动。再点一次试试。」

### 复现记录（接 #39）

| # | 格 | 修之前（实拍） | 修之后 | 探针 / 截图 |
|---|---|---|---|---|
| 40 | 右键 · 重写 / 润色 × **模型答得不合形状** | `jsontext` 档：正文首行变成「这周把众筹页面的文案定稿`{"text": "（假模型改写）…", "reason": "假模型"}`了，3月12号上线」，**117 → 163 字，0 条 toast** | **117 → 117**（正文一个字没动）+ toast「重写：模型这次答的是一串 JSON，不是能写进正文的内容——这 1 处没有落进正文，正文一个字没动。再点一次试试。」 | `$S/p35/fakellm35.py` `/jsontext/v1` + `$S/p35/steps/{reset35,one}.mjs` → `p35-B-rewrite-jsontext-light` / `p35-B-rewrite-jsontext-after-light`（润色同款：`p35-B-polish-jsontext-*`） |
| 41 | 右键 · 扩展上下文 × 同上 | `before` / `after` 各塞一串，**117 → 210 字** | **117 → 117** + toast「…这 **2** 处没有落进正文…」 | 同上 → `p35-B-expand-jsontext-light` / `-after-light` |
| 41b | **误伤那一侧**（同一套代码） | — | `ok` 档（`text` 是一句正常人话）**照样落进正文 117 → 134、0 条 toast**——闸没把正常产出拦下来 | `p35-B-rewrite-ok-nofalsepositive-light` |
| 42 | 右键 · 校验 × 同上（**满库**） | terrence 20417 条事实，选中「- 5G切换2.4G带来约一至两周」，零模型的词法召回真的回 1 条（`terms: ["切换","带来约"]`）；模型答散文 → 面板「没有找到能支持或反驳这段内容的记录——…只是知识库里没有相关信息。」**假的** | **未修**（后端要先能分开这两件事） | 老用户 + 假模型 harness 档（对 `/verify` 回的是散文） + `$S/p35/steps/verifyold.mjs` → `p35-B-verify-fullkb-light` |
| 43 | 右键 · 来龙去脉 × 同上（满库） | 词法召回回 5 条（`terms: ["dvt","6月3","节点由"]`）；`llm_json` 抛 `ProviderError: no JSON in llm output` → 500 → toast「后端处理出错（多半是模型没应答）」+「打开设置」 | **未修** | 同上 → `p35-B-trace-fullkb-light`；后端 traceback 在 `$S/p35/old.app.log` |
| 44 | 智能续写（harness）× 同上 | `streamjson` 档：**两段整串 JSON 落进正文**（117 → 211 字），右栏「目录」跟着多出两行 `{"text": "（假模型改写）…`，**0 条 toast** | **未修**（要在判据层加，不在前端逐 token 拦） | `/streamjson/v1` + `$S/p35/steps/harnessshape.mjs` → `p35-B-harness-streamjson-light` |
| 45 | 写作计划 × 同上 | **不是缺口**：`listnot` 档下后端 502，toast「生成写作计划失败：模型没能生成有效的分段列表，换个目标描述再试试」，正文没动、笔记数还是 1 | — | `/listnot/v1` + `$S/p35/steps/planshape.mjs` → `p35-B-plan-listnot-light` |

**这一批的教训**（跟 P32 那条接着说）：P32 的教训是「归因错了一次，缺口多活 14 批」。
P35 这一批把同一件事反过来又踩了一遍——**同一个假回答，在这条路上是脏内容、在那条路上是正品**。
量具跟着被测的那条路走，不是跟着「上一批用的那份」走。

---

## P37 补：**P35 那三个 ❌ 修完了**（2026-09-20，台账 `TRACELOG-product.md` P37 节）

复现走的是**后端真跑**（`loop.run` + `NoteHooks` + `rails_off=("save",)` + 假模型端点
`127.0.0.1:18137`，`KITE_DATA_DIR` 指到真目录的整拷），不是打包壳——这三条的根因全在后端，
而后端真跑能把「判据有没有响、正文有没有被改回去、下一轮 steer 说的是什么」逐个读出来，
比隔着一层壳去数字数准。**真模型 0 次调用**（`llm_usage` 最大 id 开工 = 收工 = 5738）。

| # | 格 | 修之前（实拍） | 修之后 | 探针 |
|---|---|---|---|---|
| 46 | 右键 · 校验 × 模型答得不合形状 | 空 `findings` 一句话通吃三种来历，满库下说「知识库里没有相关信息」——**假的** | 三句分开：`emptyVerifyLine(1, true)` =「模型这次没按要求的格式答，这一段没能核成——知识库里有 1 条相关记录，再点一次「校验」试试。」；`emptyVerifyLine(0, false)` = 原来那句（这一档它才是对的）；`emptyVerifyLine(3, false)` =「翻过知识库里 3 条相关记录，没有一条能支持或反驳这段内容——不代表内容没问题。」 | `backend/tests/test_p37.py` + `frontend/src/editor/__tests__/p37.test.ts`（含「三档说的是三句不一样的话」+ 两条接线断言） |
| 47 | 右键 · 来龙去脉 × 同上 | `ProviderError: no JSON in llm output` → 500 → 「后端处理出错（多半是模型没应答）」+「打开设置」 | 502 +「模型答的不是这个动作要的格式（不是没应答，供应商是通的）——换个模型，或者再试一次」，**`isLlmUnreachable` 为 false → 不挂「打开设置」**；`Connection refused` 那一档照旧透出去（判据窄，没误伤隔壁） | `tests/test_p37.py::test_trace把no_JSON单独接成502` / `::test_trace判据窄_别的ProviderError照旧透出去` |
| 48 | 智能续写（harness）× 同上 | **后端真跑复现**：假模型流式回一整串 JSON，两轮下来正文 **53 → 169 字**，两段整串留在正文里；当轮唯一响过的判据是 `no_echoed_text`（「这两段重复了」——**指错了地方**），⚑ 里一个字都没提「这是 JSON」 | **53 → 53（一个字没进）**；⚑ ×2「这一轮整段答的是一串 JSON，不是能写进正文的内容——已经从正文里撤掉了。这一轮重写：…」；`auto_fixed` 事件 ×2「把这一轮那段整串 JSON 从正文里撤掉了」；`State.steer` = `mechanics: 这一轮整段答的是一串 JSON…` | `$S/p37/{fakellm37.py,run_note.py}` `/streamjson/v1`，日志 `$S/p37/{before,after-final}.log` |
| 48b | **误伤那一侧**（同一套代码） | — | `/ok/v1` 档（流式回一段正常中文）**照样落进正文 53 → 187、0 条 ⚑**——闸没把正常产出拦下来 | `$S/p37/ok.log` |

**这一批的教训**：**闸装在哪一层，决定它能不能装得上。** P35 已经量过前端那条路——
流式增量逐 token 落地，拿到第一个 `{` 时不知道整段是什么，落完再撤又跟 `undoRound` 打架。
换到判据层之后，「不落正文」「说清楚」「下一轮重写」三件事全是既有机制
（`Verdict.fix` / `fix_done` / `fix_note` + ⚑ + `State.steer`），**一条新路都不用开，前端一行没改**。
*没地方装的闸，多半是层选错了，不是这件事拦不住。*

## P40 补：**第四次走查扫出来的两格**（2026-09-20，台账 `TRACELOG-product.md` P40 节）

第十列（「模型答得不合形状」）P37 收尾时是 **0 个 ❌ 0 个 ？**。这一批第四次走查把那一列
**在打包壳上**又走了一遍——那一列本身没退，但顺着同一个形状扫出两格**新的**：
一格在隔壁面板（`/trace` 空手而归时说的那句话），一格在那条判据自己的 `fix` 上。

### 新增的记号

* **❌P40**：这一批复现了、没修（根因在后端，留给下一批）
* **✅P40**：这一批修了 + 闸 + 突变验

| # | 格 | 摆出来是什么 | 记号 | 探针 |
|---|---|---|---|---|
| 49 | 右键 · **来龙去脉** × 一条都没召回 | 面板写「**知识库里没有跟这段沾边的记录。**」——而**同一屏上**「校验」对**同一段话**写着「知识库里有 **6** 条相关记录」。两块面板、同一个库、互相打架，而且前一句是假的。跟 P9 #26 / P37 #2（校验那句）**是同一个形状，只是换了一块面板**：`routers/memory._no_info_to_chinese(text, has_facts)` 里的 `has_facts` 数的是 **KITE 自己那条检索**回了几条，而界面上那句话说的是「知识库里有没有」 | **✅P41** | 复现：`$S/p41/repro1.py`（真库整拷 + 假端点，扫到两段 `recall=3 / ask_facts=0`、`recall=9 / ask_facts=0`，两段说的都是那句假话）· 修完：`$S/p41/after1.py` 走真路由 → `recalled=3 / 6`、「按这段话没能串出一条时间线——知识库里有 6 条沾边的记录…」· 闸 `backend/tests/test_p41.py`（8 条）+ `frontend/src/editor/__tests__/p41.test.ts` |
| 50 | 智能续写 × 这一轮全被判据撤掉 | 判据拦住了、正文一个字没进（**对的**），而收工那行写着「… · 3 轮 · **-1 字**」。少的那一个字是**用户原本结尾的那个换行**：`checks/shape.drop_json_paragraphs` 原来是 `re.sub(r"\n{3,}", "\n\n", "\n\n".join(keep).strip())`——它把**整篇**重拼了一遍，顺手压平了用户自己敲的空行、`strip()` 掉了首尾空白。它的文档写着「开跑前正文里就有的段落一个字不动」，**那句话对段落成立，对段落之间不成立** | **✅P40** | `backend/tests/test_p40.py`（7 条，含「三个以上连着的空行不许被压平」「首尾空白不许被 strip」「摘光了就还原成开跑前那一版」）· 后端真跑 `$S/p40/run_note.py` → `delta 0 / identical true` |
| 50b | **误伤那一侧**（同一套代码） | 这一轮真写了东西、只有其中一段是 JSON 时，**不许**把留下来的那段一起还原掉 | ✅ | `test_这一轮还写了别的东西时不许把它一起还原掉` |

**这一批的教训**：**「一个字节都不动」这句话要连分隔一起算。**
`drop_json_paragraphs` 的段落逻辑一直是对的（该摘的摘、不该摘的一个字不动），
错在它**重新拼了一遍**——段落之间那几个换行是用户排的版，重拼就等于改了他的正文。
用户那一侧看到的是收工那行的「-1 字」：一个字都没落进正文，界面却说正文少了一个字。
*量到一个说不通的数，先别当它是四舍五入。*

## P41 补：**P40 那两格的后一格收了，外加三格新的**（2026-09-20，台账 `TRACELOG-product.md` P41 节）

* **✅P41**：这一批修了 + 闸 + 突变验（#49 从 ❌P40 变成 ✅P41，就在上面那张表里）

| # | 格 | 摆出来是什么 | 记号 | 探针 |
|---|---|---|---|---|
| 51 | 右键 · **校验** × 换页签 / 把光标移到别的段 | 「校验结果」那块卡挂在右栏**最顶上、`RightPane` 外面**，页签体系管不着它：换页签、光标移到别的段它都还在，读起来像在说当前这一段。**不是 P17 #5 的回归**——那条闸（换篇就清）今天还在、还绿，这是它从来没覆盖的另外两种 | **✅P41** | 改法取 P40 给的第二条：卡上写清「说的是你选中的这一段：「…」」，那一段已经不在正文里了再多一句（**代码判得准的才说**）。真渲染前后对比 `$S/p41/render-p41.txt`；闸 `p41.test.ts` 5 条 |
| 52 | 「改动」面板 × 重开时有层放不回来 | 正文在别处改过、按文字也定位不到时，原来**只弹一句 toast**：话说完用户没有任何可按的东西，而那一层的数据其实还在手上；下一次冲库还会把它**悄悄抹掉**（`serializeLayers` 看不见没进编辑器的层） | **✅P41** | 每层一张卡 +「重新算这一层 / 丢掉这一层」，落库时把它们原样带着走。真渲染前后对比 `$S/p41/render-p41.txt`；闸 `p41.test.ts` 6 条 |
| 53 | 「全部接受」之后立刻关掉 app | 烧进正文之后那一次防抖冲库（`CHANGE_LAYER_SAVE_MS`）还没发出去，库里那几层原样留着——下次打开按旧坐标把高亮标到**已经烧进正文**的字上。**这是 P39 修好「关掉 app 层还在」之后反过来那一面**（P17 那个「幻影删除标」的形状，来源换成了库） | **✅P41** | 烧改成当场落库：`DELETE /api/notes/{id}/change-layers` → `store.burn_change_layers`，清完再数一遍、没清干净就抛。闸 `test_p41.py` 8 条（含接线洞那一条） |

**这一批的教训**：**修好一个方向，要回头看反方向有没有被打开。**
P39 把「关掉 app 层就没了 = 静默按接受」修好了，`note_change_layers` 从此活得比进程久；
可「活得久」立刻长出反面——**烧完还活着**。同一张表，同一条防抖路径，两个方向的毛病
用户看到的是同一件事：*界面上摆着的东西和库里躺着的东西对不上。*
## P42 补：**第五次走查扫出来的两格**（2026-09-20，台账 `TRACELOG-product.md` P42 节）

第十列（「模型答得不合形状」）这一批在打包壳上又走了一遍，**一条都没退**（P37 #1 / #2 / #4、
P35 #1、P32 #2 全部逐字复现）。新扫出来的两格都不在那一列上：一格在**改动层**这条新线上
（P39 刚落地），一格在**意图预填**上——两格的共同形状是**界面对用户许了一个诺，而承接它的那一处看的是另一个东西**。

### 新增的记号

* **❌P42**：这一批复现了、**没修**（落在另一个 agent 正在改的文件里，只记不修）

| # | 格 | 摆出来是什么 | 记号 | 探针 |
|---|---|---|---|---|
| 51 | 改动层 · **一层里每一处都处置完了** × 关掉重开 | 打开这篇，toast 弹「**上次没处置完的 1 层改动还在右栏「改动」里**」——而右栏只有「记忆 / 计划」两个页签，**整页搜不到「改动」两个字**。库里那一行确实还在（`note_change_layers`，`hunks[0].state = accepted`）。根因八成是**弹 toast 那一处数的是库里回来的层数，面板只渲染还有 `pending` 的层**：一层全处置完之后，两边对不上。跟 #49 是同一个形状——**一句话说的是 A，喂给它的数是 B** | **❌P42** | `$S/p42/steps/settle.mjs`（悬停高亮 → 点「✓ 接受」）+ `panetabs.mjs`（列右栏页签 + 搜「改动」两个字）· `p42-10c-old-settle-light.png` / `p42-10e-old-notab-light.png` |
| 52 | 文档意图 · **标题只写在正文 H1 里** × 新建笔记 | 新建、正文第一行打 `# 周报 9-20`：左栏树、标签页、状态栏**全都叫「周报 9-20」**，而标题下那行「这篇要干什么」**一个字都没预填**、没有「预填」角标、「完成标准」是空的（右栏「计划」里那条写着「还是空的，去填」）。把标题输入框 `input.note-title` 填成同一串，**预填当场出现**（目标 / 读者 / 完成标准 + 角标 + `0/2`）。根因：预填走的是 `resolveIntent(i, title)` 里那个 `title` = **标题输入框那一格**，而界面上到处显示的名字是 `displayTitle({ title, content })` ——**正文 H1 派生出来的那个名字没有喂给预填** | **❌P42** | `$S/p42/steps/intent.mjs`（先读意图行 + 标题框，再用 React 认的 input 事件填标题框，再读一遍）· `p42-2b-new-intent-light.png` |

**这一批的教训**：**「界面上它叫什么」和「代码里 `title` 是什么」可以是两个东西。**
`displayTitle` 这个函数存在的理由正是「旧界面建笔记时把标题填成了『未命名』」——
它在**显示**这一层把正文首行顶上去了，而所有**判据**层（预填、完成标准）读的还是原始 `title`。
一个只在显示层生效的兜底，会让下游每一条「按标题来」的规则各自失灵一次。
*给一个字段做兜底的时候，先数一遍有几处在读它的原始值。*

## P43 补：**P42 那两格收了 + 走查量具一次性换掉**（2026-09-20，台账 `TRACELOG-product.md` P43 节）

* **✅P43**：这一批修了 + 闸 + 突变验（#51 / #52 从 ❌P42 变成 ✅P43，就在上面那张表里）

| # | 格 | 摆出来是什么 | 记号 | 探针 |
|---|---|---|---|---|
| 51 | 改动层 · 一层里每一处都处置完了 × 关掉重开 | **修之前在壳上逐字复现**：toast「上次没处置完的 1 层改动还在右栏「改动」里」，页签条只有 `["记忆","计划2"]`、整页搜不到「改动」。两头各错一半——toast 数的是 `hunks ∪ settled`（**一处活的都不剩**的层也算），页签看的是 `pendingDiff`（**不算只差空白的那几处**），**两把尺子** | **✅P43** | 收进两个纯函数 `reopenedLayersNotice` / `changesTabHasContent`，并钉住它们之间那条不变式：**弹了 toast ⇒ 页签一定在**。修之后同一条路：**一个字都不说**，页签条 `["记忆","改动","计划2"]`，面板上那行灰字「润色 · 12:00 · 已处置：3 处接受、0 处撤回」看得到。`$S/p43/steps/{toastwatch,tabcheck,changestab}.mjs` · `p43-1c-before-notab-light.png` → `p43-1d-after-tab-light.png` / `p43-1e-after-settled-panel-{light,dark}.png`；闸 `p43.test.ts` |
| 52 | 文档意图 · 标题只写在正文 H1 里 × 新建笔记 | 同 P42：树 / 标签页 / 状态栏全叫「周报 9-20」，`input.note-title` 是空的 → 意图行一个字没预填 | **✅P43** | 预填改认 `displayTitle({ title, content })`，**依赖挂的是算出来的名字而不是 `content`**（名字没变一次都不跑）。重推代价先量过：47k 字 / 2402 行最坏 **0.10 ms**、30k 字 **0.03 ms**，一帧 16.67ms → **不用防抖**（`$S/p43/bench2.ts`）。壳上：标题框仍是空的，意图行「目标：周报 9-20：这段时间做了什么、进展到哪、卡在哪 / 读者：老板 / 团队 / 完成标准：每条进展有日期、有依据；卡住的说清要什么」+「预填」角标 + `0/2`。`p43-2b-after-intent-{light,dark}.png` |

### 走查量具：**读正文一律用 `d.docText()`，不要自己去数 `.cm-line`**（这是第五次栽在这上面）

栽过的五次：P35 占位块 widget、P40 删除标 widget、P42（docText 145 / 库 111 / 状态栏 101，
34 个字全是删除标记，差点记成「关掉重开正文多了 34 个字」）。这一批一次性收掉：

* **量具在 `$S/p17/cdp.mjs`（走查每一批都从这份拷）**：`d.docRead()` / `d.docText()`，
  `$S/p43/steps/lib.mjs` 的 `docText(d)` 转调它；老那串按 `.cm-line` 拼的改名成
  `DOC_JS_DONT_USE`，注释里写清它错在哪。
* **首选问 CodeMirror 自己要**：`view.state.doc` 是文档的定义，widget 根本不在里面。
  内容 DOM 上挂着内部视图对象——**老版本叫 `.cmView`，@codemirror/view 6.43（这个仓在用的）
  叫 `.cmTile`，两个都要试**（第一版只认 `cmView`，在真壳上当场静静退到了 DOM 那条近似路）。
  拿不到就**抛**，不静静退到近似值——「用退路量出来的数」跟「量准了」不是一回事。
* **两个方向都会错，而且会同时错**（这一批在壳上实测，`$S/p43/steps/widgetdelta.mjs`，
  右键「重写」一段之后，`p43-6-widget-delta-light.png`）：

  | 读法 | 数 | 错在哪 |
  |---|---:|---|
  | `doc`（`view.state.doc`） | **606** | 正文，基准 |
  | `raw`（按 `.cm-line` 读，P40 / P42 那把尺子） | 437 | 多算了 widget（`widgets = [[".harness-del", 34]]`——**正是 P42 追的那 34 个字**），又**少**了视口外的行 |
  | `dom`（摘掉 widget 再按 `.cm-line` 读） | 403 | widget 不算了，但 **CM6 只渲染视口里那几行**，长文读出来是半篇 |

  `.cm-line` 那把尺子**同时往两个方向错**，所以「跟状态栏对一遍」也救不了它。
* **widget 名单**（`cdp.mjs` 顶上 `WIDGET_SELECTORS`，跟 `grep -rn "extends WidgetType" frontend/src` 对齐）：
  `.cm-widgetBuffer` `.cm-placeholder` `.harness-del` `.harness-actions` `.cm-run-block`
  `.cm-md-table` `.cm-image-embed` `.cm-note-link` `.cm-task-checkbox` `.cm-mermaid-widget`。
  **新加一个 `WidgetType` 就要往这张名单里加一条**——它只喂退路那条分支，首选那条不受影响。

### 还有一条量具教训：**`--light` / `--dark` 一旦上了命令行，`d.setTheme()` 就是空操作**

`desktop/src/main.ts:350` 的 `ipcMain.on('set-theme')` 第一行就是 `if (forcedTheme) return`。
这一批第一轮的五张「深色」截图**全是浅色**（肉眼看图才发现，不是脚本报的错——
又一次「渲染出来 ≠ 屏幕上看得见」的同族）。**深色截图只能靠起 app 时带 `--dark`**，
步骤脚本里不要再自己切主题。


---

## P46 补：**P43 / P44 留下的那五条**（2026-09-20，台账 `TRACELOG-product.md` P46 节）

| # | 格 | 摆出来是什么 | 记号 | 探针 |
|---|---|---|---|---|
| 52 | 文档意图 · 标题只写在正文 H1 里 × **一个字一个字敲** | P43 那一格补的是「认不认正文 H1」，**没量「敲的时候什么样」**——它自己也写了「视觉上会跳，下一批真机上看一眼」。真机上一个字一个字敲 32 个字（160ms 一个）：「目标」那一格**变了 19 次**，第 3 个字先跳到另一个模板（「围绕「周」写清楚一件事」），第 4 个字跳回来，第 11–23 个字一个字一个字长出半句话。**标签页跟着变 20 次没关系（那是标题），这三格是用户能改的输入框** | **✅P46** | `INTENT_PREFILL_IDLE_MS = 500`：名字**停下来**才重推，换篇不等。同一趟量：**19 → 1**，打完那一刻三格的值逐字相同。**P43 那句「不用防抖」说的是 CPU，那句话没错；错在只量了 CPU 没量帧数。** `$S/p46/steps/p46intent.mjs` · `p46-5-intent-typing-light.png` → `p46-5b-intent-debounced-light.png`；闸 `p46.test.ts` |
| 53 | 右栏「记忆」· **证据被显示过滤砍光** × 有召回 | P44 那两条过滤让 **9 / 765** 条查询一条证据都摆不出来，前端退回 `display_terms`，摆出来是「命中：矿山行业最重要、**希望通过智能化能**、矿区工作环境恶劣」——**比被砍掉的那个碎片还碎**。后端刚判完「这些串都不合格」，前端转手拿一份**没判过**的去顶 | **✅P46** | 把「判过了、空的」（`[]`）和「没判成」（`None`）分开：`RecallOut.evidence` 改成 `list \| None`，判据抛了才回 `None`；`evidenceLine` 据此走两句话，`[]` 就**如实说一句**「这一段没有可摆出来的证据」。**召回一条不少。** `$S/p46/steps/p46evidence.mjs` · `p46-1-evidence-empty-light.png` |
| 54 | 右栏「记忆」· **真词的末字碰巧在 `_EDGE_STOP` 里** | `华为`（`why=vocab`、两端在词边界上）被剥掉 `为` 成单字「华」，再被「合不出 ≥2 个字」砍掉。同一形状还有 `阿里`（→「阿」）、`不变`（→「变」）、`上线`（→「线」） | **✅P46** | `is_whole_token`：这一串在**查询自己的切词**里正好占一个 token 才不剥。**P44 写的那句「对齐了的串不该再剥」量下来是错的**——`的高频`（的\|高频）、`月前后`、`导出的` 两端也都在词边界上，那把尺子会让 62 条查询的行变、多摆 70 串次。全库代价：换成 `is_whole_token` 之后只有 **9 条**变了，多摆 6 种 0 碎。`$S/p46/{measure46b,read46b}.py` · 夹具 `p46-unstrip9` |
| 55 | 改动层 · **20 层全处置完** × 第 21 次 AI 动作 | toast「N 层改动没能留到下次打开：格式化——这一篇的待处置改动层超过 20 层」，**而那 20 层早就逐处处置完了**——这句话请用户去关心一个他已经做完的决定。`CHANGE_LAYER_KEEP` 数的是**所有**层 | **✅P46** | `_layer_is_live`（还有 `pending` / `off` 的才算活口，跟前端 `layersOf` 同一条口径）+ 两档分开数：活口 20、全处置完的 60（`CHANGE_LAYER_SETTLED_KEEP`），**两句话也分开**。壳上验（同一个探针、同一份前端，**只换 `store.py` 一个文件**）：HEAD 弹那句话、库里剩 20；改完**一个字不说**、21 层一层不少。`probesP39.ts` 的 `p39:evict:<id>:settled20` · `p46-3b-keep20-settled-HEAD-light.png` → `p46-3-keep20-settled-light.png` |
| 56 | 「校验结果」/「脉络」· **说的是哪一段** × 那一段在两屏之外 | P41 #3 / P43 #5 让两块卡都报了户口，**但那一行点不了**——用户看完下一步就是回正文找那一段 | **✅P46** | `passageLine`：**整段原话在正文里不多不少一处才给跳**（跟 `changeLayers.relocate` 的「唯一命中才算」同一条规矩），跳不回去就不给 handler——**一个点了没反应的链接比不能点更糟**。两块卡用同一个 `jumpToPassage`，跳本身复用 P12 的 `jumpToLine`。壳上：点校验那行 → 第 7 行，点脉络那行 → 第 3 行（**两块说的是不同的两段，各跳各的**）。`$S/p46/steps/p46jump.mjs` · `p46-4-scope-{before,after}-light.png` / `p46-4-scope-after-dark.png` |

### 走查量具：**「点了没反应」先分清是「没点上」还是「handler 不通」**

P46 #4 第一趟读出来的是「两行都可点（`role=button` / `tabIndex=0` / `title` 都在），
**点下去光标一动不动**」。差一点就记成「跳回去这一刀没生效」。

分开核之后当场清楚：那一行是 inline `<span>`，换行之后有**两个 client rect**，
整体 bbox 的中心落在第二行右边的**空白**里——`elementFromPoint(cx, cy)` 回的是父 `<p>`，
**真鼠标根本没点在它身上**；而 `el.click()` 走一遍立刻就跳。
改成点**第一个 client rect 的中心**。

* **「选不到 ≠ 没有」的第五个形状**（前四个：选择器变了、widget 盖住、视口外、iframe）。
* 也是「**「红了」和「红的是那条」要分开核**」在**驱动**这一侧的样子——
  这个仓已经在**断言**那一侧栽过五次。
* **量具里加了 `elementFromPoint` 自检**：点之前先问一句「这个点落在谁身上」，
  不是自己就换成第一个 client rect。

### 还有一条：**裸 `fetch` 拿不到身份**

P46 #1 第一版在 `d.eval` 里直接 `fetch('/api/memory/recall', …)`——
**`X-User-Id` 是前端 api 客户端加的，裸 fetch 没有**，于是走了 `default` 用户，
回包 `facts: 0`、`codebook: …/default/codebook.xml`。差点读成「这条查询根本没有召回」。
**走查脚本里凡是绕过 UI 直接打 API 的，都要显式带上身份头，并且核一眼 `memory/stats`
里的 codebook 路径是不是这个人的。**

## P47 补：**P45 那一类扫干净了（14 处 → 逐档摆过）+ 第七次走查**（2026-09-20，台账 `TRACELOG-product.md` P47 节）

这一批**没有新的 ❌**：走查表 11 步 × 两个身份全对，一格「没摆出来」（见下）。
补进来的是三件**把已有格子从「推理成立」变成「真跑摆过」**的事。

### 复现记录（接 #56）

| # | 动作 × 条件 | 复现到什么 | 记号 | 怎么处理 / 证据 |
|---|---|---|---|---|
| 57 | 智能续写 / 分段写作 × **任意一条 `fix` 判据改过正文** | P45 只真跑验过 3 档（`output_not_json` / `no_foreign_script` / `SHIP_BEST_ON`），剩下 5 条只是「同一条路径应该也好了」。这一批各造一个形状真跑：**修之前 5 档全部「编辑器干净、库里脏」**（`no_junk_tail` 109/115、`no_echoed_text` 108/182、`citations_exist` 117/135、`charts_from_tools` 111/241、`chart_restates_list` 140/239） | **✅P45（这一批只是验完）** | 修之后五档 `notes.content` **逐字等于交出去的那一份**，`note_revisions` 的 `harness` 行也跟着对。**读库不读编辑器**：`$S/p47/repro47.py`（`loop.run` + `NoteHooks` + `rails_off=()` + `SELECT content FROM notes`）。闸从「清单对清单」换成**结构性**的一条：`backend/tests/test_p47.py` 10 条 |
| 58 | 同机第二份实例 × **抢端口那一下真的落在 `freePort` 的 TOCTOU 窗口里** | P45 ② 修的重试那一圈当时**只有源码断言**。这一批在真 app 上赢了一次：抢跑者预热 `app.main` 之后死等新 uvicorn pid，在 `close` 和 `bind` 之间瞬间 `bind` 并把 socket 交给 uvicorn（**那就是另一份实例的真后端**） | **✅P45（这一批只是验完）** | A 的日志逐字：「端口 47232 上应答的是另一份实例的后端（pid 65888，我的是 65942）——换一个端口重来（第 1 次）」→ 47233 起来 → 窗口 `loadURL` 到 47233 → **这一屏的库是 `P47-A-的库`、自检横幅（没有）**。`$S/p47/{racer47.py,toctou47.sh}` · `p47-toctou1-A.png` |
| 59 | **正常开 app**（两个身份、6 次起）× P45 那条钉在窗口顶上、点不掉的自检横幅 | **一次都没出现**。打包版三样（`port` / `pid` / `dataDir`）都有且三处一致——P45 留的「开发模式 `dataDir` 是空串」那一格在打包版上不成立 | **✅P45** | **误报比漏报更糟**，所以这一格每起一次 app 就读一次。`$S/p47/whoami47.mjs` · `p47-1-old.png` / `p47-1-new.png` |
| 60 | 屏幕活动 · 翻页 / 「删掉这一天」× scratch 的 userData | **没摆出来**：`--user-data-dir` 下 journey 目录是空的，而真的 journey 目录在 `~/Library/Application Support` 里，**这一批一次都不碰**。知情屏那一半两个身份都验了（五条 + 「开始记录 / 先不开」+ 权限那句） | **？** | **不冒充 ✅ 也不冒充 ❌。** 要验 P23 #5 得先想清楚怎么在 scratch 里造出一天的段落——**别为了走查去读用户真的屏幕活动** |

### 走查量具：**「点了没反应」的第六个形状——编辑器滚过之后，按行号点会点到视口外**

P46 记的是 inline `<span>` 换行之后 bbox 中心落空白（第五个形状）。
这一批是第六个，而且它**伪装成一个产品缺陷**：

跑完几趟智能续写之后编辑器是滚动过的，第 2 行的 `getBoundingClientRect().y = **-26.9**`
——在视口**上面**。`elementFromPoint` 回 `null`，真鼠标点下去谁都没收到，
于是右键菜单读出来 **0 项**。读起来就是「这个右键菜单没了」。

* 修法：`selectLineAndRightClick` 先 `el.scrollIntoView({ block: 'center' })`，
  **滚完再读一次 bbox**，还在视口外就**抛**（不是静默返回 null）。
* 这又是那条老规矩：**「点了没反应」先分清是「没点上」还是「handler 不通」**。
  这一次差一点把「没点上」记成走查表里的一个 ❌。

### 还有两条量具，记下来（下一批收）

1. **同名笔记两篇时，按标题点永远点不准那一篇。** 两次 seed 各建了一篇「P47 走查（可删）」，
   ⌘K 和左栏树点的都是另一篇——差点把「改动页签没了」记成缺陷，
   实际上那一篇本来就没有层。**判据得是「打开之后的 note id」，不是「点了」**；
   这一批绕过去的办法是点顶上的**标签页**（`opentab.mjs`）。下一批给走查笔记加序号。
2. **步骤脚本里写死的截图名会互相覆盖。** `darknarrow.mjs` / `cmdk.mjs` 的截图名写死了 `-old-`，
   新用户那一趟把老用户那几张盖掉了。**截图名该由调用方给。**
