# 300 轮改造 · 进度台账

> 随时打开这个文件就知道跑到哪了。每完成一块就更新，不用问我。
> 详细的改动和踩坑记在 `docs/TRACELOG-trilium.md`。

**最后更新**：2026-09-14 巡检循环跑到第 470 轮（TRACELOG [495]），正式版是第 470 轮的 dmg；后端 950 / 前端 112 tests

## 判据

- 每块做完：后端 `pytest` + 前端 `npm test` 都绿
- 新断言必须反向验证（注入违规看它变不变红）
- 界面改动**开 Electron 截图核对**，不靠想象
- 结构改动同步 `docs/`

## 进度

| 轮次 | 阶段 | 状态 |
|---|---|---|
| — | Electron 外壳（拉后端/托管前端/收干净） | ✅ 完成 |
| 1–15 | 树的后端：branches、克隆、迁移、folders 删干净 | ✅ 完成 |
| 16–40 | 主题令牌 + 六块外壳骨架 | ✅ 完成 |
| 41–60 | 树控件：展开/右键菜单/harness 长在节点上 | ✅ 完成（拖拽待补） |
| 61–75 | 标签页 + 分屏 + 前进后退 | ✅ 标签完成（分屏/前进后退待补） |
| 76–95 | ribbon（骨架=计划）+ 右栏标签（相关记忆常驻） | ✅ 完成 |
| 96–115 | 知识库：行内出处 peek + 动作按钮化 + 双向链接（引用数/摄入标记/反向链接） | ✅ 完成（虚拟子树挪到 151–180） |
| 116–125 | 特殊笔记：设置 / 知识库 / Skill | ✅ 完成（第 211–235 轮：设置 / Skill / 导入 / 最近删除都是 `app:*` 标签页；知识库是虚拟子树） |
| 126–140 | 打包：PyInstaller + electron-builder | ✅ .dmg 可装可跑（图标/菜单待补） |
| 141–150 | 笔记×知识库骨干：note_citations、⇡/◆ 上树、ribbon 知识库标签 | ✅ 完成 |
| 151–160 | 知识库虚拟子树（主题/实体/时间线/最近摄入）+ 事实以只读笔记打开 | ✅ 完成 |
| 161–185 | Trilium 比对报告第一/二/三批（现在就坏的 → 结构性 → 手感） | ✅ 完成 |
| 186–210 | 比对第四批：知识库工具节点（总览/主题地图/回顾）、ribbon ⋯/路径/信息、面包屑、快速查看、保存状态 | ✅ 完成 |
| 211–235 | 第五批：设置/Skill 特殊笔记、导入子树、删子树确认、树工具条、图标 | ✅ 完成 |
| 236–255 | 分屏、--shot 自截图、续写材料带 id + 引用规则 + citations_exist 判据（真跑验证通过） | ✅ 完成 |
| 256–275 | 树右键「导入 .md 到这里」、引用正则收紧五处同步、文档同步 | ✅ 完成 |
| 276–300 | 收尾：全量测试（后端 730 / 前端 36 + 11 条 check）、--shot 全套走查（亮/暗/事实页/菜单）、重新打 dmg、记忆与文档 | ✅ 完成 |

## 全量巡检循环（2026-09-11 起，用户要求「循环 1000 轮」）

方法：每轮挑一组界面用探针实拍 → 看图或读代码找问题 → 改 → tsc / vitest / pytest →
TRACELOG 记一条 → commit / push；隔几轮 `npm run dist` 重开正式版。逐轮细节在
`docs/TRACELOG-trilium.md` [26]–[93]。

| 轮 | 抓到 / 做了什么 |
|---|---|
| 1–3 | 空笔记离开即删；⌘K 事件没人接收；magic tap 跟滚、来源折一行；时间线 sqrt 比例 |
| 4 | 外观设置（跟随/浅/深）+ preload；端口固定 47231 让 localStorage 跨启动；flushStorageData；dev 独立 userData；单实例锁 |
| 5–6 | `/` 菜单 / @ 补全样式；路径面板加克隆；ribbon / 计划弹层 / 暗色知识库 / 3 万字笔记 |
| 7–8 | 跑完结果行常驻；欢迎页 + ⌘/ 快捷键表 |
| 9–11 | 笔记内链 `[[` + `note://` 标记 + 反链 + 悬停摘要；历史版本（自动/手动/可逆恢复）；状态栏 LLM/语音分级 |
| 12 | **定向续写**（首行声明放到哪一节）；harness 跑在另一篇 / A 写进 B / 一轮删 4451 字三处硬伤 |
| 13 | 从光标处续写；编辑器外部更新最小 diff；启动回到上次的笔记 |
| 14–15 | 来龙去脉 500（KITE Answer.evidence）；分段写作跟随视图不显示（事件名漂移） |
| 16–20 | 暗色回归；删除流树/标签/分屏一起收；暂停状态条粘顶；暂停快照只留最新；412 篇大库量 |
| 21–25 | 摄入后记忆重召回；`/` 块生成；模型连不上的人话提示 + 打开设置；有骨架不再重算；**流式是假的**（aread 攒完再吐）；知识库页只在摄入时轮询 |
| 26–30 | 健康状态跟设置变；帮助菜单（快捷键/数据目录/导出日志/报告问题）；目录高亮当前节；「**标题：**」裸星号修法；⌘[ ⌘] 在编辑器里也走后退前进 |
| 31–35 | ⌘K 不再插链接、⌘/ 不再插注释；窄窗正文优先；整库导出 zip（层级/前言/克隆/资产）；带目录导入静默失败（create_folder 不存在）修 + 往返测试；导入到哪可选 |
| 36–40 | 窗口标题跟笔记；后端崩溃自动重启；标签行双击新建、记忆卡已引用；定期回顾存为笔记；没存上的草稿落本机 |
| 41–45 | AI 运行中编辑器只读；图片走资产库不再内嵌 base64、提示词出口剥 base64；图片块级插入、导出带资产；删笔记不留孤儿行 + 一次性清理；建笔记同步引用 |
| 46–50 | 格式化不拽视口、只差空白不标绿、大文档 diff 先按行再逐词；老库运行记录修剪 |
| 51–55 | 退出前保存握手、记住窗口位置；激活标签滚进视野；快速搜索回车；空笔记上续写先提示；危险确认框焦点在取消 |
| 56–60 | 历史版本「与当前对比」（长段折叠）；格式化去整篇缩进、库里没有的标签自动收；列表接口实测；**笔记库每日 + 知识库每周自动备份** |
| 61–65 | 错误体 detail 进 toast；探针搬出 App.tsx；整篇缩进提示条；标签装不下的滚动钮；树上拖拽探针 |
| 66–72 | 实体页藏说话人标签（噪声规则一处定义四处用）；⌘K 标题优先 + 日期；树悬停全名；状态条折行；快捷键按平台显示；时间线 / 事实页回归 |
| 73–80 | 时间显示转本地；后台跑完系统通知；麦克风失败三句人话 + 打包版麦克风用途说明；搜索卡 + ⌘K 命中高亮、链接跟主题；暗色 `/` 菜单白板修；⌘F 查找条汉化 + 令牌皮肤 |
| 81–90 | 字数口径统一；UTC 残留三处；健康检查并行 6.5s→2s；语音离线先拦；原生 select 暗色；语音服务地址进设置页；toast 抬到状态栏上；退出收空笔记；树底钮悬停现身 + ⌘K 命令；窄窗分屏收左栏 |
| 91–121 | tap 截断提示 → 自动补尾；重写撞上限不给半句；JSON 调用撞上限翻倍重试；浮动条盖住顶部 8px；图片不切标题行；ESLint 接入；撤回可逆性修回（soft hunk）；选区忙态带文案；欢迎页回归；⌘F 全局兜底 + 入表；身份落 identity.json；删除流程回归；文件拖进树成笔记；链接悬停卡口径；目录跟随当前节；长文 diff 锚点对齐；找不到的引用只列 8 个；选区提示词只给窗口；tap 长文压缩；⌘K 余量提示；摄入进度可见；Mode.verb；活动条带轮次；块结果滚进视野；实体页容错 + 看总览修；知识库缺页态；旧虚拟标签收掉；旧事实标签补名；第 120 轮重打 dmg；正式版后端 kill 后 8s 自动重启验证 |
| 122– | 优先队列 ①–⑨ 做完后重开巡检（TRACELOG [153]+）：导回区窄窗占位截断；取代 / 合并标注补到主题页 / 会议页 / 某一天 / 事实表（之前只有实体页有）；按月图改连续日历月（错抽的 2005 不再拉长横轴）；两个月时标签不折行；深色下 CM gutter 白带；写作计划标题大小写；回顾等待态；⌘K 导回；字数不算图片 alt；空库不判关系；事实表带查询串；合并卡措辞；选择器长标题省略；探针草稿不漏进真笔记；tap 摘掉编造引用；会议页标签真名；长材料段号；第 147 轮重打 dmg；生成表格没表先判掉 + regressed 停机；EDA precheck；校验先用已引用事实；来龙去脉中文拒答；跨站写请求 403；user id 校验；第 160 轮重打 dmg；最近删除；今天的日记；文件菜单；模型用量账本；记忆范围；实体懒加载；启动清孤儿计划 / payload / 备份；测试隔离；第 180 轮重打 dmg；阶段回顾 / 校验 / 扩写 / 写作计划也遵守记忆范围；运行记录孤儿按任意 key 前缀清；站在文件夹上开无限续写就是这棵；[[ 补全重名带首句；相关记忆门槛不算引用 id；第 190 轮重打 dmg；召回排序不分大小写 + 数字词；词表拉丁词整词匹配；相对数据目录锚定 backend/；关系卡已引用的不再给「引用这条」；JSX 换行留下的中文句中空格；保存不再重拉整个笔记列表；快速查看 / 分屏对空笔记给占位、展开右栏钮不压关闭分屏；第 200 轮重打 dmg；召回 / 页边圆点前剥掉图片与链接地址；树上「实体」懒加载时 child_count=0 点不开（修）；树不列说话人伪实体；树上会议行「月-日 k/n · 标题」；知识库首页五张统计卡放不下才换行（flex，宽窗仍一行五张）；首页 / 树的主题计数按不同事实数；懒加载节点的面包屑（app:* 页不冠「知识库」）；空的未命名笔记不进最近删除；会议页 / 事实卡 / 事实页的实体 chip 滤掉说话人标签；补 LICENSE（AGPL-3.0）/ 第三方声明 / package.json license 字段 / 设置页出处行；说话人 speaker a / speaker_a 归一；Agent 运行面板轮次归属 / 排序（用户报）；第 220 轮重打 dmg（随包 LICENSE）；轮末暂停 7 天不处置启动时清掉、提示带日期；召回排序 ASCII 整词 + 零分不返回 + 行级回退要同一行两词命中 + 回退结果也过排序；侧栏搜索摘要命中不再被省略号吃掉；第 230 轮重打 dmg；范围提示去重「只看」；实体类型节点有页面；⌘K 最近编辑不列空壳；第 240 轮重打 dmg；偏好一条 ≤400 字、重复不入库；技能接口校验名字 / 作用域 / 正文长度；笔记标题一行 ≤200 字、父节点要存在；克隆成环 400、移到不存在的父节点 404；事实取代 / 合并不成环、手工事实 ≤2000 字；导回 Obsidian 目录不可写 400、文件名去掉首尾点；第 250 轮重打 dmg；供应商设置校验地址 / 模型名 / 空白 key；toast 里 4xx 只留人话；笔记搜索转义 LIKE 通配符；⌘K / [[ 补全改走轻量列表接口（413 篇 580KB → 16KB）；⌘K 同名笔记带正文第一行；侧栏搜索本地过滤不再请求；第 260 轮重打 dmg；用量账本 / 跑完的任务 / 非活跃计划启动时按天数清；空正文续写 400、计划起在不存在的文件夹 404；同一天切段的会议按段号正序；最近摄入按材料列（n 段）+ 树里展开给各段 + 会议页分段导航；首页两栏真正对半、chip 不折行；知识库根的面包屑不重复；第 270 轮重打 dmg；事实表 / 单条事实带实体显示名；MemoryBrowser 只剩主题地图（删掉没人走的三个 tab + 弹层壳）；第 280 轮重打 dmg；引用表启动重建 + 源侧同步也重建引用；零事实的会议段给原话；第 290 轮重打 dmg；实体去重规则版（大小写 / 分隔符 / 别名，展示与查询层归组） |
| 292–300 | 实体去重规则版（大小写 / 分隔符 / 词表别名并成一组，展示与检索层）；类型节点标签名；最近删除恢复时父链一起回来；重打 dmg（第 300 轮） |
| 301–310 | 正文底部垫 30vh（只给主编辑器）；放弃计划走 App 确认框 + 确认框排到面板之后；Esc 只关最上面一层；**分屏第二栏可编辑**（SplitEditor + 防抖自动保存，切主栏前先 flush）；重打 dmg（第 310 轮） |
| 311–320 | 全新用户 / 空库走查：空库的知识库树不列空主题、最近摄入 / 主题地图给空状态、右栏「记忆」空库给指引、首行当标题截到句读、「LLM 不可达」可点去设置；文档同步；重打 dmg（第 320 轮） |
| 321–330 | 桌面日志落盘（~/Library/Logs/memoket-note-desktop/）+ 帮助菜单「打开日志文件夹」；空库的主题 / 实体索引页空状态；动作 chip disabled 发灰；列表接口计时；重打 dmg（第 330 轮） |
| 331–340 | 新一篇的 ribbon 空状态（空笔记上存入知识库 / 存一版灰掉）；**笔记图标（Trilium NoteIcon）**：icon 列 + 接口 + 标题行选择器 + 树 / ⌘K / 欢迎页 / 链接面板同一个图标 + 随导出导入走；探针 `note:<id>:ribbon:<tab>`；重打 dmg（第 340 轮） |
| 341–350 | 图标收尾：⌘K / 树菜单「换个图标」、标签页带图标（窄标签不画）、挑过图标的空笔记不自动收、日记 / 回顾 / 导入默认图标；分屏那篇被删时静默；崩溃对话框带日志路径；空库首页不摆搜索框；重打 dmg（第 350 轮） |
| 351–360 | 智能续写真跑复核；树上一个分类超过 300 条给「还有 N 条 · 去事实表看」尾巴（债勾掉）；带筛选的事实表在标签 / 分屏头 / 面包屑 / 右键统一叫「事实表 · work」；主题 / 实体页翻页器旁「去事实表筛」；探针 openend / splitv / kbexpand:…:end；重打 dmg（第 360 轮） |
| 361–370 | 侧栏搜索标题命中给第一行正文；整篇缩进的笔记显示名 / 预览行 / 导出文件名都先 trim 再剥 #；导出文件名跟显示名同一条截句读规则（冒号也算）；harness-sync 日志带差异位置；dev 库体检；重打 dmg（第 370 轮） |
| 371–380 | 无障碍：图标按钮 / 弹层 / 输入框 / 下拉框补 title、role、aria-label；智能续写真跑定位出两类同步差异并修：接缝空行本地也压（streamJoin）、onRevision 不走 updater、revision 事件给全量 anchor / text、本地重放后 tidyBlankLines；重打 dmg（第 380 轮） |
| 381–390 | 真跑三次复核轮内同步（只剩落盘清理一类，轮末对齐）；scrub 事件 + 客户端镜像删句；文档补同步规则；README 接口表补齐 + /docs；自召回复测 58/57；仓库 / scratch 体检；重打 dmg（第 390 轮） |
| 391–400 | **「这篇周围有什么」笔记级局部图**（`GET /api/notes/{id}/graph` + ribbon 引用面板末尾的 LocalGraph，gap 表 NoteMap 勾掉）；探针 `note:…:ribbon:<tab>:end`；正式版冒烟改带真身份；重打 dmg（第 400 轮） |
| 401–410 | 坏依据 / 坏链接可一键清理：找不到的引用「清掉这些引用」、链到已删笔记的「改成纯文本」（都只改正文记号，可撤销）；坏链接 / 链出列表本地按正文算，链入 / stale / 局部图跟落库的 updated_at 重查（不再拿保存前的旧版）；重打 dmg（第 410 轮） |
| 411–420 | 历史版本列表跟落库刷；面板新鲜度规矩进文档；数据目录体检：笔记库 81% 空页 → 启动 VACUUM（10.1MB → 1.9MB，正式版 2.3 → 0.8）、没人引用的图 7 天后启动清掉、旧 codebook 副本 25MB 等用户点头；重打 dmg（第 420 轮） |
| 421–430 | 知识库备份 / 依赖 / gitignore 体检；on_event → lifespan（少一条弃用告警，退出时收后台任务）；前端 npm update 到 semver 内版本（CodeMirror 6.43.11 / React 19.3）；重打 dmg（第 430 轮） |
| 431–440 | 文件夹 harness 真跑复核（三段、跟随、浮条）；Electron / 闲置回收 / 第一屏 / 计时复核；树行 preview 只给没起名的（413 篇树 208KB → 120KB）；重打 dmg（第 440 轮） |
| 441–450 | 键盘 / 读屏：图标选择器方向键、弹层关掉后焦点回原位（useRestoreFocus，确认框 / 选择器 / 快速查看 / 快捷键表 / 右键菜单 / 选中菜单 / 斜杠输入框；⌘K 单独处理）、图标钮 aria；重打 dmg（第 450 轮） |
| 451–460 | 前端观测日志加 info 档；client-log 测试；按 openapi 反查没被测试碰过的路径 35 → 14（知识库 / 记忆只读接口路由层冒烟、树展开 / 重排、技能重排）；kbNoise 测试；重打 dmg（第 460 轮） |
| 461–470 | 噪声正则测试覆盖核对 + 说话人标签正则收紧（speakers / speaker phone 不再算）；北极星三条判据 + 13 个痛点逐条核对（PPT 记为债、10 个禁用按钮补说明）；虚拟页纯规则搬到 util/virtual.ts；重打 dmg（第 470 轮） |
| 471–480 | App.tsx 继续瘦身（面包屑 `util/crumbs`、引用 / 链接正则合成一份 `util/wordCount`、三栏宽度裁决 `util/layoutPanes`、草稿 `util/draft`，3249 → ~3210 行，+14 条单测）；`check-regex-parity` 前后端正则行为对拍（第一次就抓到自己的口径回归）；⌘⇧T 重开标签回原位；标签溢出 ▾ 列出全部 + ContextMenu 超高钳顶内部滚；重打 dmg（第 480 轮） |
| 481–490 | 桌面后端输出按行打 `[backend]` 标（lineTagger）；ContextMenu / ⌘K / 「移动到…」键盘高亮跟着滚，「移动到…」列表原来根本滚不到（`.palette-list` 无定义）；`check-css-classes`（用了的类名 CSS 里得有 + 反向查死样式，清 5 个）；后端删 3 个没人调的函数；坏事实页「收掉这个标签」；重打 dmg（第 490 轮） |
| 491–500 | 真跑三次抓到轮内 scrub 差 1 字 → `applyScrub` 照服务端规则 + `check-scrub-parity`；`prepareInsert` + `check-stream-parity`（tidy / insert_into 对拍）；文件夹 harness 真跑 0 warn；eslint 开 `no-floating-promises`（27 处 void / 4 处补 catch）；桌面壳 loadURL / boot 失败兜底；无标题材料兜底名「会议记录 · 日期」；README 测试段更新；重打 dmg（第 500 轮） |
| 501–510 | ⌘K 打字先列「打开的标签」（同名带正文首行）、▾ 列表同名项也带首行；应用菜单「标签」（tab-action 事件链）；8 处可点 div/span 键盘可达（`util/clickable`）+ `check-a11y` 进 npm test；toast 动作变真按钮；README 更新；重打 dmg（第 510 轮） |
| 511–520 | 标签顶部「正在写」色条（单篇 + 文件夹 harness）；trilium-ui-gap 全表对账（60 行补已做 + 状态说明）+ 右键目标高亮真做；用量「今天」改「24 小时」；mermaid / KbNoteView / LocalGraph 按需加载，主包 1870 → 1103KB；重打 dmg（第 520 轮） |
| 521–530 | 拆包后老路径回归；TODO / console 扫；桌面壳端口退回提示进落盘日志；虚拟页右栏「计划」角标不用上一篇的；召回剔英文虚词 / 说话人标签（自召回 57→60/60，seed 11 n200 196/194）+ `scripts/recall_selfcheck.py`；命中词不再列 speaker b；重打 dmg（第 530 轮） |
| 531–540 | 召回：说话人标签不进符号通道、重复出现的查询词加分、伪相关反馈（`recall_selfcheck` 三口径：seed 7 60/60/28、seed 11 197/195/98）；下游口径巡检；文件拖进树回归；欠账「列表不带全文」出方案 `docs/notes-list-brief-plan.md`（服务端一步已有）；重打 dmg（第 540 轮） |
| 541–550 | 知识库首页搜索探针 + 耗时取整；§10 复核；前后端对拍再加三条：正文首行 / 显示名（`check-preview-parity`，围栏不当正文）、说话人标签正则、字数（`database/wordcount` + `check-wordcount-parity`，历史 / 回收站的「N 字」跟状态栏一致）；README 19 个检查脚本；重打 dmg（第 550 轮） |
| 551–560 | 说话人标签六处口径统一（stats / 导入页数字 / 树实体层 / 实体列表 / 图的边 / 召回）；真跑抓到续写收尾的 scrub 不发事件 → `_scrub_and_record` + loop 发 `scrub`；客户端 TEXT_MESSAGE_END 做 fixBoldPunct（复用 format.ts、补围栏跳过）；harness 客户端 scrub miss / revision no-op 诊断日志；重打 dmg（第 560 轮） |
| 561–573 | 分段 harness 也镜像（`hooks/mirror.py`）；对拍到 8 处（+ `section_end`、命中片段、查询清洗 `clean_query`）；`check-a11y` 加「禁用要说原因」；**用户报的 `eriwa`**：续尾两道护栏 `harness/tailing.py`；同一段里整句被插进句中：`reject_revision` 第四道防线；正文路径硬截全扫；重打 dmg（第 573 轮） |
| 574–580 | 「模型回什么就用什么」同类全扫：写作计划分段数上限 + 同名去重 + 追加全被丢时不再空转、骨架长度封顶（`clamp_skeleton`）、关系说明封顶；harness 跑动时右栏「计划」轮次卡置前；重打 dmg（第 580 轮） |
| 581–590 | 痛点 6 出方案 `docs/slides-plan.md`（幻灯片笔记而非 pptx 文件）；超长笔记（47K 字）编辑性能实测；后端查询耗时实测；文件夹 harness 写着的那篇改只读 + 解释条；智能排版结果过时就丢弃（顺带修第 511 轮落错行的色条清理）；API 路径覆盖复算 115/3 + 补 skills toggle 测试；**切回笔记 / 从知识库回来恢复光标与滚动**（痛点 12）；重打 dmg（第 590 轮） |
| 651–700 | **用户点名的一整段视觉返工 + 三块巡检**。① 空知识库下 harness 空转：四层各修一处，三次真跑对照确认。② 冲突收件箱误报：`CONFLICT_MIN_OVERLAP` + 一致性判断 + 按事实去重，2 万条事实的真库上候选从 14 次降到 4 次，同一份材料连导三遍的误报 2 → 1 → 0（顺带记一条**负结果**：`CONFLICT_MIN_TERMS` 试过又撤了）。③ YAML front-matter 被 CM6 当 setext 标题渲染 → `editor/frontmatter.ts` + 8 条单测。④ **主线：照 memoket-desktop 做全面视觉适配**，用户一路打回四次（「UI 不止是颜色」「好丑」「整个背景全用紫色？？？」「这个紫色底很不协调」）。产出 `docs/UI_SPEC.md`（以后加任何东西都按它）+ `frontend/src/design-tokens.css`（**全应用唯一允许写字面值的文件**），两层别名落到全应用；闸门加到 25 条（`check-ui-tokens` 管颜色 / 字号 / 字重 / 圆角 / 间距 / 层级 / 死令牌 / **幽灵令牌** / 面色色偏，`check-icons` 管 boxicons 名字存在 + 不许彩色 emoji），两条都做过变异验证。**这一段最贵的教训是方法**：对齐一个设计系统**不能只读它的 CSS**——值对了不等于像，要看实拍看构图、该量的地方量像素；它的展示稿里还有不出货的东西（假桌面底色、16px 圆角窗体），照搬会出洋相。过程中我自己造的回归全部是**实拍**抓出来的（`min-height` 压死工具栏、`input{height}` 夹住标题、`nowrap` 拆了欢迎卡、`font-weight` 把正文加粗、不透明底让 backdrop-filter 白耗、`var(--card,…)` 幽灵令牌两次、`.linklike` 的 `color:inherit` 契约被我写死）。重打 dmg（第 700 轮，实拍验过打包版一致） |
| 641–650 | **两块新东西落地 + 一次用户点名的实测**。① 屏幕活动收尾：范围「全部」真的排除了屏幕活动（注释写着默认排除，`filter_rows` 却在 `all` 时原样返回——这条规矩从没生效过）、回顾笔记挂进日记树、停掉记录之后还能回看和删、黑名单能加自己的、翻天在有记录的日子之间走。② **痛点 6 落地**（13 个痛点里唯一没落点的那个）：这篇 → 一份幻灯片笔记（不是一个文件），后端一次模型调用 + 五条确定性判据、右栏纯 CSS 预览、导出 PDF（Electron `printToPDF`，零新依赖）。真跑 24 页 20 秒**当场推翻了我自己写的两条判据**（枚举谓词判「是不是判断句」→ 5/6 误报，**枚举语言现象的表永远补不全**；按标题比「有没有整节漏掉」→ 误报 3 节，而那 3 节的引用 6/6、4/5、18/21 都进了幻灯片）；真打一份 PDF 出来看，第 7 页 mermaid 被压成乱码 → `parseMini` 认围栏 + 导出前先渲 SVG。③ **飞书导入导出拿真账号跑通**（用户点名）：完整往返做了三遍，最要紧的发现是**读得到却列不出来**（按文档加协作者的 wiki 文档 `get_node` 读得到，`/wiki/v2/spaces` 却给 0 个空间）→ 补「直接粘文档链接」这条路；往返里抓到代码块语言标注丢失、列表后紧跟的块被吃成续行，修完**逐行含空行 0 差异**。④ 真跑 harness 读产出：修复那一步会在同一个维度上反复空转（第 2 轮排修复、第 3 轮修完没动分、第 4 轮又排一次）→ 「试过一次没用就不再为它花轮次，分数一动重新给机会」。⑤ 三个藏着的 CSS/布局 bug，共同形态是**样式的行为依赖它碰巧待在哪**：`.seg` 选中态用左栏的纯白底、搬到正文区就看不见；`.pane-tab` 少 `flex:none`，注释写着「自己横向滚」而那行为从没成立过；全新用户首页常用键的键帽飘在三行字正中间。重打 dmg（第 650 轮） |
| 631–640 | **Daily Journey 从方案做到能用**（P0→P4，用户提的新功能）：P0 真跑一小时验三个问题（169 段 → 合并后 75 段；15–20 秒 / 2k token 一段，本机 GPU 约 25 分钟一天，不出内网；描述里有同事真名、文档章节名、`PRD.md#70-81`、`app-dev-debug.apk 262M`——**足够凭它重建一天**）；P1 壳里常驻采集 + 菜单栏开关 + 第 21 条门禁（壳的 TS 和 P0 的 Python 分段必须一致）；P2 段进知识库（`screen-` 一档记忆范围，写作取材默认排除）+「今天」页（四种状态各一屏、一天一条带、时间轴）；P3 日报（**时长走代码、模型只写推进 / 卡住 / 计划外**）；P4 跨时间回顾（**喂日报不喂原始记录**，落成一篇笔记）。第 639 轮回头对着自己写的隐私前提逐条查代码，查出**界面上的承诺和代码里的行为对不上**：「截图不保存」写在知情选择那一屏上，而大图一直没删（一天 300MB）→ 描述完就删、过期也删、只留 256px 缩略图当凭据；再加「删一段」（黑名单挡不住所有东西，能精确地删才敢一直开着）。一路上实拍逼出的真 bug：壳和后端各自拼 journey 目录（开发时差 `-dev` 后缀，壳一直在记而页面一直是空的）、一行坏时间戳把整天打成 500、时间轴差一个时区（同一个坑第二次踩）、问不到采集状态时页面退回「没开过」。配色跑验证器选的（自己挑的八个当场判 FAIL，绿↔橙色盲 ΔE 2.8）。重打 dmg（第 640 轮，顺带验了新 Python 子包进得了 PyInstaller） |
| 611–630 | **用户主导的一段**：他连问三件事（知识库该不该在树里、无限续写该不该在左栏、列表该不该露时间和来源），合起来是「两套层级挤在一棵树里、而这棵树只有一种排法」→ 方案 `docs/sidebar-ia-plan.md`，四条全部落地：来源图标 / 知识库出树只留一行入口 / 无限续写一次都不猜（他指出「三个门」后又砍掉左栏火箭）/「按最近」排序模式。编辑区四条（按钮标签阈值 600→380——默认三栏下那几个字从没露过面、提示语换成 `/`、Markdown 挪进「更多」、按钮分主次）。他报的两个真 bug：右栏「点了等于没点」（被动布局规则否决了主动动作）、单篇导回没入口。他指出 UI 不合格一次（把整页组件塞进弹层）→ 重做，教训是**复用逻辑不复用排版**。新方案两份：`daily-journey-plan.md`（截屏 → 在做什么 → 日报 / 跨时间报告，主张做成新的摄入来源）。巡检侧：月度图窗口无界、用量标签漏路由名、导回凭证不校验、出厂技能不守自己的排版规则、合并建议把「同一主语的不同陈述」当重复、实体栏先截断后过滤、搜不到笔记时给条去知识库的去处。重打 dmg（第 630 轮） |
| 601–610 | **读产出这条线走到底**：判据死锁（同一条原样卡满就放行）、平手归后来者（四轮修订本来全丢）、`anchor_end` 配不上就丢（我们自己切坏过正文）、丢弃的修订回喂给下一轮、旧图不算这轮手写的（差点删掉用户的流程图）、分段补覆盖维度（620/434/429 字 → 1718 字）、判据打回不算质量退步也不触发只修不写、判据点名的工具真发下去、手写最简流程图放行（9 块合法图曾全被拒）、第 14 条判据「两段引同一批事实」；界面：撞名的行标出刚好够分开的时间；重打 dmg（第 610 轮） |
| 591–600 | 位置记忆落盘 + 退出/刷新也记；判据认出「流程写成箭头链」；**读产出**抓到两类真问题——取材料跑题（分段标题直接召回的材料当下限）、零引用产出（新判据 `citations_present`）、同一组清单列两遍（新判据 `no_repeated_lists`）；分段标题剥引用标记；重打 dmg（第 600 轮） |

## 当前优先队列（2026-09-12 晚，用户点名：这些排在巡检之前）

| # | 方向 | 方案 | 状态 |
|---|---|---|---|
| 1 | 笔记 ↔ 知识库链接层：贡献列表增删改、过期标识、同步、自动同步开关、知识库反链笔记 | kb-fusion-design §3.4.1 | ✅ 2026-09-12 |
| 2 | 记忆的六种关系：冲突 / 延续 / 叠加 / 合并 / 印证 / 缺依据；事实取代、演进线、冲突收件箱 | agent-native-editor §3.3.1 | ✅ 2026-09-12（六种齐：叠加「补进来」、合并「合成一条」记住；摄入时检冲突进收件箱，首页三键处理） |
| 3 | 提案分层账本（hunk 带 layer，按层开关） | agent-native-editor §3.2 | ✅ 2026-09-12（层是开关：关掉还原、再打开写回、原文改过的那处报「没能重放」；接受 / 丢弃是终点） |
| 4 | 边缘记忆（段落级 recall + gutter 圆点） | agent-native-editor §3.3 | ✅ 2026-09-12 第一版（右缘圆点 = 关系，点开右栏卡） |
| 5 | 导入：块级进度 / 预估时间 / 用量 / 断点续传 | import-sync-plan §4 | ✅ 2026-09-12（进度条按块、开始前预估、token 估算、服务重启后「继续」） |
| 6 | 笔记侧增量（source / source_id / content_sha） | import-sync-plan §3 | ✅ 2026-09-12（同一份导十次一篇；源侧改了更新正文，本地改过不动；知识库按源侧重抽） |
| 7 | 飞书导入 | import-sync-plan §1 | ✅ 2026-09-12（自建应用凭证，知识库 / 云空间两种范围，块 → markdown；待真账号实测） |
| 8 | 导回 Obsidian / Notion / 飞书 | import-sync-plan §2 | ✅ 2026-09-12（导入页「导回」区：Obsidian 写目录按 memoket_id 覆盖、对方改过报冲突；Notion / 飞书按记住的 page / document id 覆盖；信息面板「副本」行；Notion / 飞书没真账号实测） |
| 10 | 知识库抽什么：抽取偏好可选 / 可自定义（用户 2026-09-12 晚提） | extraction-profiles.md | 方案已出（P1 预设 + 自定义关注 / 忽略 + 试抽；P2 自定义 kind + 按笔记 / 按导入覆盖；P3 上游 PR），待指令落地 |
| 9 | 内存占用太高（用户点名，2026-09-12 晚） | TRACELOG [146] | 第一版 ✅：量出后端索引 ≈ 225MB/2 万条事实（大头是 memoket-kite 的词元倒排表 92MB + 词元计数 35MB），闲置 5 分钟自动放掉（回收 ~130MB）；health 带 rss。上游 PR 已提（memoket-kite#8：206 → 75MB，排序逐字节一致，本仓测试在分支上全过），**到第 670 轮仍未合**（远端 main 还停在 `8745feda`，装的就是它，`pip install -U` 拿不到新东西）；正式版实测闲置 5 分钟后 325 → 186MB；Electron 侧 375MB 是 Chromium 常态。**第 670 轮又省一笔**：`kite/kite_tokens.py` 让词元共用同一批字符串对象（真库里 token 出现 42.1 万次、去重后只有 7.26 万个，而每次出现都是新建对象）——服务态同一套步骤实测 300 → 271MB（加载索引后）、346 → 288MB（跑完召回和实体候选），单进程口径 308 → 242MB（-21%），**检索结果逐字不变** |

## 已知欠账（做完会划掉）

- [ ] `GET /api/notes` 带全文，每次保存后 `reload()` 整表重拉（413 篇 579KB / 10ms 还行；库大到几十 MB 时该改成列表只带摘要、打开再取全文）。知识库树那边已改懒加载（第 175 轮：实体 >200 不随树下发，391KB → 57KB）
- [ ] 知识库不分库：一个用户的所有来源混在一个 codebook（第 165 轮实拍硬件线里混进华为 950 超节点）。第一版缓解（第 170 轮）：「记忆范围」按 session 前缀分 笔记 / 会议记录 / 导入 三档，右栏切、召回 / 关系 / 续写 / 智能续写 / 块生成 / 取材料工具都带（第 171 轮）；真正分库仍待做
- [x] 分屏第二栏是只读的 → 第 309 轮：第二栏自己持正文 + 防抖自动保存（SplitEditor），主栏正开着的那篇仍只读；harness / 续写等仍只在主栏
- [x] 侧栏信息架构三条（用户 2026-09-14 提）→ 第 611–630 轮四条全部落地（来源图标 / 无限续写一次都不猜 / 「按最近」模式 / 知识库出树）。**仍等用户拍板的只剩改名**（「无限续写」vs「智能续写」，名字分不出作用域）
- [x] Daily Journey（用户 2026-09-14 提）→ **第 631–646 轮 P0–P4 全部落地**：壳里常驻采集 + 菜单栏开关、段进知识库（`screen-` 一档，「全部」默认排除）、「今天」页（四种状态 / 一天一条带 / 时间轴 / 缩略图当凭据 / 删一段）、日报（时长走代码、模型只写推进/卡住/计划外）、跨时间回顾（喂日报不喂原始记录，落成一篇笔记）、用户自己的黑名单。下面是当初的方案要点，留作对照：定期截屏 → 描述「在做什么」→ 日报 + 跨时间深度报告。方案在 `docs/daily-journey-plan.md`（含落地记录表）。主张是**做成一个新的摄入来源而不是新子系统**——一段屏幕活动变成跟会议记录同形状的东西（`screen-` session），知识库 / 时间线 / `digest` / 写作 harness 就全部免费复用，真正要新写的只有「屏幕 → 一段文字」这一段。先做 P0：一小时采样脚本，**读产出**再定稿（段数 / 成本 / 描述质量三个数字现在都是猜的）。UI/UX 在 §8：难点不是时间轴是**信任**（任何时刻不用开设置页就能回答「在记吗 / 记了什么 / 能不能抹掉」）；「今天」页**报告在上、证据在下**，日报里每句话能点回对应那一段
- [ ] 实体没有类型（KITE 抽取没标 etype），树上「实体」是一层扁平的 1230 个。**顺带记一笔实测**（第 656 轮）：实体表里混着 `110` `210` `3085` `99` 这种纯数字，但 `950`（华为 950 超节点）和 `2012实验室` 又是真的——**纯数字不能一刀切**。这块连同去重规则一起等用户拍板，别自作主张升级成模型判断
- [x] 北极星痛点 6「想做成 PPT 又要上传给另一个 agent 工具」→ **第 648–651 轮落地**：`POST /api/slides` 一次模型调用 + 五条确定性判据 → 落成原笔记的**子笔记**（Marp markdown，不是一个文件），右栏预览，导出 PDF（`printToPDF`，零新依赖）。`.pptx` 仍走第三方 skill——主路径要解决的是「两个工具之间没有链接」，不是「我要一个 pptx 文件」。方案 + 落地差别在 `docs/slides-plan.md`
- [x] 一个分类下最多给 300 条事实，再多要走事实表分页 → 第 352 轮：主题 / 实体展开到 300 条之后尾巴给一行「还有 N 条 · 去事实表看」（`kb:facts?topic=…` / `?entity=…`），月份 / 会议自己的页面本来就分页

