# MEMOKET_NOTE 质量与体验专项 — Tracelog

**范围**：用户直接指令——"这是一个 AI note 工具，magic 续写、智能续写、无限续写、
校验、重写、润色、上下文扩展等功能，都要确保效果是好的，不能搞出来乱七八糟的东西
出来"；"写作骨架、智能编辑要在续写的模式下怎么配合，这是你要解决的问题"；
"再把 UI/UX 整个优化一下，你做的是一个工具，不是一个 demo"。

**时间预算**：用户给了 28 小时，要求前台一个个盯着做完，不是丢后台任务。这份文档
就是那个"盯着"的记录——每次真实验证（真 LLM 调用、真数据）都记一笔，包括没问题
的检查（不是只记 bug），确保是在"往好的方向迭代"而不是原地打转或者退步。

**方法论**：每项检查都是真实 LLM 调用（本地模型），不是读代码猜行为；每个改动
之后重新跑一遍原来触发问题的场景，确认真的改好了，不是"看起来应该好了"。

---

## 计划

### A. AI 功能质量审查（逐个真实验证，不是走查代码）

| # | 功能 | 状态 |
|---|------|------|
| A1 | magic tap 续写 | 已单独验证（[6]）——质量整体好，1 处轻微已知局限性重复 |
| A2 | 智能续写（单篇 harness：自动修订+自动续写） | 已验证并修复 5 个问题（[2][3][8][10][13]），核心的"完成判定脱离 beats"问题已解决，跨内容类型泛化测试通过 |
| A3 | 无限续写（文件夹 harness：分段计划） | 已专项验证（[9]）——more_sections 克制、格式统一、跟另一份真实数据一起佐证了 [8] 的粘连 bug；[13] 的 grounding 修复对它同样生效（section-write 共用 MAGIC_TAP_SYSTEM） |
| A4 | 校验（selection verify） | 待查 |
| A5 | 重写（selection rewrite） | 待查 |
| A6 | 润色（selection polish） | 待查 |
| A7 | 上下文扩展（selection expand） | 待查 |

每项检查清单：
- 语言一致性（知识库事实经常是英文/口语转写，写进正文要意译成正文语言）
- 重复（跟已写内容/跟其他轮次换个说法说同一件事）
- 格式一致性（要么全篇统一用标题/加粗/列表分节，要么都不用，不能一节用
  `##` 标题、另一节用 `**加粗**` 冒充标题）
- grounding（知识库事实真的用上了，不是自说自话；校验类功能给的证据要具体）
- AI 味（套话开头、不必要的排比、"总的来说"类收尾）
- mermaid（该出图的场景真的出图，语法不出错——已在更早阶段验证过，这里只做回归检查）

### B. 写作骨架（spine/beats）与续写/智能编辑的配合

现状盘点 + 需要回答的问题：
- ~~智能续写（note harness）完成判定不看 beats，只看续写模型自己主观判断~~
  ——[10] 已定位并修复：加了独立的 `_beats_status()` 核对，全覆盖直接判定
  完成，不再等续写模型自己觉得"够了"。真实数据验证轮数从 5 降到 1。
- magic-tap 续写：有 spine/beats 就该往下一个未满足的节拍推进——prompt 里有这条
  指令，但没有机制检测"这一轮续写完，某个 beat 是不是真的被满足了"，节拍完成度
  全靠模型自觉。跟 note harness 不一样的是 magic-tap 是用户手动点一次触发一轮，
  不是自动多轮循环，"提前停"不是这里的风险——用户自己控制要不要再点一次，
  所以 [10] 那种"根本停不下来"的失控场景在 magic-tap 上不成立，暂不需要同样
  的独立核对机制，优先级更低。
- EDIT_SYSTEM（智能编辑/自动修订）与续写"互相看不到对方打算做什么"——回头
  看 `note_harness.py` 主循环的真实结构，这个风险比最初设想的要小：每一轮
  是**修订先跑、续写后跑**，不是两次并行独立调用，续写读到的是这一轮修订
  之后的最新正文；风险只可能出现在"上一轮续写刚写的内容，这一轮修订还没
  来得及看一眼就被下一轮续写继续往下延伸"——但这也不成立，因为修订在每
  一轮都会先跑一次，等于"上一轮续写写完的内容"必然会先过一遍这一轮的修订
  检查，才轮到这一轮续写继续。也就是说续写永远不会连续写两轮都不经过修订
  review——结构上已经有这个隐含的检查点，不需要额外加一次显式的"读对方
  打算做什么"的协调机制。[10] 的真实多轮测试也没有观察到矛盾版本共存的
  情况。这条问题视为已通过现有轮次结构解决，不再是待办。

### C. UI/UX 整体优化

待列具体条目——先完成 A/B 的真实验证，很多 UX 问题只有跑起来看真实输出才会暴露
（比如已经发现的格式不统一，本质是内容质量问题，但呈现出来是排版问题）。

---

## Log

### [1] 智能续写 harness 首次真实验证（A2）

**做的事**：新建测试笔记（种子内容：CaptureVoicesAnywhere 续航方案一句话），跑
`/api/note-harness/run`，3 轮，观察自动生成的骨架 + 每轮修订 + 续写。

**结果**：2074 字，多轮修订+续写都成功执行，grounding 良好（用上了功耗数据、
三端协同等真实细节），没有明显的胡编。

**发现的问题（A2-1）**：正文里标题格式不统一——开头一段用 `**功耗基线与场景模型**`
这种加粗短语冒充小节标题，后面几段却用规范的 `## 功耗模型拆解：从场景到瓦时`
真标题。同一篇笔记里两种"标题"混用，读起来不像一个统一体例写出来的东西。

**根因待查**：怀疑是修订阶段（EDIT_SYSTEM）和续写阶段（MAGIC_TAP_SYSTEM）对"用
加粗还是用标题分节"这件事没有统一约束——续写阶段的格式指令里明确提到"关键结论用
**加粗**"和"二级标题标出结构转折"两条并存，模型可能把"这是个小节"误判成"这是个
结论"从而选错了格式。EDIT_SYSTEM 那边目前没有专门的格式一致性指令。

**下一步**：检查 EDIT_SYSTEM 全文，确认它是否会引入/保留这种不一致；设计一条
"标题 vs 加粗"的判断标准并加进两个 prompt，再跑一遍同样的场景验证是否修好。

（清理：测试笔记已删除，不留在 terrence 真实账号里。）

### [2] A2-1 根因确认 + 修复 + 三处统一

**根因确认**：`note_harness_continue_user()`/`magic_tap_user()` 都用 `content or ""`
判断"是不是第一轮"，但 MAGIC_TAP_SYSTEM 的格式指令是"延续已有格式风格"——种子
内容哪怕非空，只要还没出现过 `## ` 标题，就没有"已有风格"可延续，模型这一轮就会
在"分不分节"上现场决定，选中了加粗假装标题。这跟更早在 writing_plan.py 的
`section_write_user()` 上修过的问题（无限续写"为什么没有格式"）是同一个缺口，
只是当时的判断条件写的是 `not content`（只覆盖真空正文），没有覆盖到"非空但无
标题"这种更常见的情况——note_harness 的种子内容通常就是一句话，正好落在这个
判断条件没盖住的地方。

**修复**：把判断条件从 `not content` 统一改成 `"##" not in content`，并把三处重复
的提醒文案（magic_tap_user / section_write_user / note_harness_continue_user）
收敛成一个共享函数 `_heading_format_reminder()`，避免以后改一处漏两处。

**验证**：拿完全相同的种子内容重新建笔记、重新跑 2 轮智能续写——`grep "^##"` 命中
3 处规范标题，`grep "^\*\*.*\*\*$"`（独立成行、首尾加粗的伪标题模式）零命中。
问题确认修复。

**加测**：过程中读输出发现第二轮几乎重复了第一轮"实现边界与验证方式"整节的内容
（换了措辞），继续追查见 [3]。

（清理：测试笔记已删除。）

### [3] EDIT_SYSTEM 缺少"主动找已存在的重复"能力 —— 任务 B 的核心发现

**问题**：[2] 发现的跨轮次重复，在同一篇笔记上又跑了第 3 轮（自动修订+续写），
自动修订阶段完全没有发现/删除这处重复——只做了 3 条事实性修正（时间线对齐知识库），
一条都没碰到结构重复。

**根因**：EDIT_SYSTEM 原有的"不要重复"指令是"已经有的内容不要用换一种说法重复
提一遍——检查一下 text 里要写的结论是不是正文别处已经说过了"——这条管的是"不要
建议新的重复"，前提是模型自己正要写一条新修订、顺手查一下会不会跟已有内容撞车；
它从来没有被要求主动扫一遍正文"这两处是不是在说同一件事"。多轮续写产生的重复
是既成事实，不属于"我正要写的新内容"，所以完全在这条指令的盲区里。

这正是用户要求解决的问题："写作骨架、智能编辑要在续写的模式下怎么配合"——答案
是：智能编辑（EDIT_SYSTEM）应该承担"续写多轮之后主动查重、清理"这个职责，而
之前它完全没有这个职责。

**修复**：给 EDIT_SYSTEM 加一条独立的指令，专门要求主动扫描正文找已经存在的
重复内容（不依赖 spine/beats），对较晚出现、信息量较小的重复段提 delete。

**验证**：直接调 `/api/edit` 喂那篇已确认重复的笔记正文——返回 2 条 delete
修订，分别精确指向"硬件就绪节奏"小节里重复"机型清单与基线"已经说过的硬件时间线、
以及"从口号到可执行保证"段落里重复"众筹与交付节奏"已经说过的销售节奏说明，
reason 字段明确写"多轮续写导致的……重复表述"——修复确认生效，不是巧合命中。

**意义**：这不只是修一个 bug，是把"智能编辑"从"被动等用户/系统调用一次生成
修订建议"变成了"主动承担多轮续写场景下的查重清理职责"——这正是 harness 里
"自动修订 + 自动续写交替"这个设计本来该有、但之前没有的协作关系。

（清理：测试笔记已删除。）

### [4] A4-A7 批量真实验证 + 发现 expand 完全没查知识库

**做的事**：建一篇真实风格的测试笔记（项目进展、续航方案、触发交互决策、后台
常驻难点四句话），对着不同选区分别跑 rewrite / polish / expand / verify（含一条
故意编造的假陈述，验证 verify 是否会正确标"矛盾"）。

**结果**：
- **rewrite**：保留了原文关键信息（物理按键 vs 语音唤醒、倾向物理按键、更可靠），
  去掉口语化套话，并主动跟上下文的核心张力（续航卖点、后台常驻难点）挂钩，
  reason 具体。质量好，无问题。
- **polish**：改动克制——只去掉一个冗余的"是"字，没有过度编辑，符合"润色是把
  同样的意思说得更好，不是重写"的设计意图。无问题。
- **verify（真陈述）**：正确识别选中内容是笔记自身就有的表述，如实说明"知识库
  检索结果中未提供外部佐证或矛盾信息"，没有为了给结论硬凑证据。无问题。
- **verify（编造的假陈述）**："目前该功能已经通过所有测试，预计下周正式上线"
  —— 正确标"矛盾"，而且是靠**笔记内部逻辑**发现的矛盾（"还在讨论"+"是技术
  难点"跟"已通过所有测试即将上线"自相矛盾），不需要知识库命中就能抓出来，
  说明笔记内部一致性检查这条路径是真的在起作用，不是摆设。无问题。
- **expand**：语言流畅、衔接自然，但读起来"太具体了"——补充了"团队把电池容量、
  功耗优化和充电策略一起纳入关键路径评审"这种有名有姓的细节，查代码发现
  **`/api/expand` 根本没调用 `_retrieve()`**，压根没查过知识库，纯粹是模型自己
  编的听起来合理的背景。

**根因**：expand() 的 handler 从写的时候就没接检索——rewrite/polish 不需要（只是
改写已有文字，没有新增事实性内容），但 expand 的任务定义就是"补充缺失的上下文"，
这恰恰是最需要接真实事实的地方，反而是唯一一个没接的。

**修复**：`/api/expand` 加 `_retrieve(user, body.selection, "", [], limit=6)`（用
选中片段当检索线索，跟 verify 同一个思路）；`expand_user()` 加 facts 参数；
EXPAND_SYSTEM 加一条"优先从知识库事实来，不要在有真实细节的情况下自己编听起来
合理但查无实据的背景"。

**验证**：同一个选区重新跑一次——补充内容变成"录完之后用户还是要打开手机进去看、
传，上传完成才会有结果"和"用户会用笔记本做主要操作、手机只在通勤时做轻量处理"
——这两句是 terrence 知识库里真实事实的原话/近似改写（跟本轮更早在别的验证里
见过的原始事实几乎一致），确认修复生效：expand 现在真的在用用户自己的知识库，
不是在编。

（清理：测试笔记已删除。）

### 阶段小结（A2/A4-A7 已过一轮，A1/A3 待专项复查）

到这里为止，A 类审查已经真实验证了 6 个功能，发现并修复了 3 个真问题：
1. 续写类功能（无限续写分段/智能续写/magic tap）首轮无格式可延续时用加粗冒充
   标题 —— 已统一修复，三处共用一个判断逻辑。
2. 智能编辑（EDIT_SYSTEM）不会主动清理多轮续写产生的重复内容 —— 已加查重指令，
   真实数据上验证确实能抓出并删除重复段。
3. 上下文扩展完全不查知识库，凭空编造听起来合理的细节 —— 已接入检索，验证后
   补充内容变成了真实事实的原话。

rewrite/polish/verify 三个功能这一轮真实验证没有发现问题。

**下一步**：A1（magic tap 单独专项复查，虽然共享了标题修复但还没单独验证）、
A3（无限续写分段计划的专项复查，尤其是 more_sections 判断"还有没有更多"这一步
有没有类似的质量问题）、任务 B 的骨架配合还需要专门测"beats 是否真的被完整
覆盖"这个问题、C（UI/UX）还没开始。

### [5] 智能续写前端补全（原始需求的最后一块）

**问题**：A2 一直在验证 `/api/note-harness/run` 这个后端接口，但用户最早的原始
需求"单个文件里也加入智能续写"从来没有接前端——之前只顾着验证后端质量，
没留意这个功能对用户来说其实还点不到。

**做的事**：`api.ts` 加 `runNoteHarness()` SSE 客户端；App.tsx 加"🤖 智能续写"
按钮（跟 magic tap 并排，两者互斥不能同时跑）+ 一行实时状态提示（第几轮/修订
几处/续写中）。修订到达时直接调 `applyRevision()`（跟 RevisionPanel 手动接受
同一套锚点语义）应用到本地 content，同时弹 toast 告诉用户"自动改了什么、为什么"
——虽然是自动应用不用手动接受，但用户应该看得见发生了什么，不是黑箱。用
`currentRef` 判断该不该应用增量/修订，防止运行中途切换笔记时把结果错应用到
另一篔笔记上（复用了给文件夹 harness 做后台化时已经验证过的同一个 ref 模式）。

**验证**：tsc/build 干净，三套 smoke suite 全过，后端 156 项测试全过，两个
dev server 健康。前端这部分涉及真实点击/流式渲染，没有浏览器工具没法自己点一遍
——已经在 A2 用直接调接口的方式验证过后端行为是对的，UI 这层的实际点击体验
还需要用户自己试一遍确认。

### [6] A1 magic tap 单独专项验证

**做的事**：建一篇已有两个明确论点的笔记（硬件续航一段、触发交互一段），给
spine + 3 条 beats（第三条"App 后台常驻与开车模式"还没被满足），跑一次
magic-tap，检查续写是否正确推进到未满足的 beat、有没有重复前两段已经说过的
结论。

**结果**：整体质量好——正确识别并推进到"App 后台常驻与开车模式"这个未满足的
beat，grounding 扎实（用上了 Speaker B 开车模式的原话、用户"不用看也能用"的
原话），结尾有一段合理的三点synthesis（不是重复，是把三条线收束成一句"信任
链"总结，属于正当的写作手法）。

**发现**：有一处真实的轻微重复——"CaptureVoicesAnywhere 的核心卖点是用户不
需要担心手机没电，可以在跟客户演示时用产品录下好几个客户的音频"这句知识库
原话，跟"## 硬件续航"那一段已经表达过的结论（"用户不用担心录制中途没电"）
本质是同一件事，又被完整插入了一次。

**结论**：不算新 bug——跟这次任务开始之前、上一阶段已经记录过的"重复问题
降低但没完全消除，可能是本地模型能力上限"是同一类、同一严重程度的表现，
MAGIC_TAP_SYSTEM 已有的"通读正文记住已提结论"指令覆盖了这个场景（这句结论
确实已经在可见正文里），模型没完全遵守。评估后判断继续加更强硬的反重复指令
风险大于收益——太强硬会让模型不敢做合理的跨段落呼应/收束写法，那本身是好的
写作技巧。计入已知局限，不单独整改，A1 结论：质量合格，可以往下走。

（清理：测试笔记已删除。）

### [7] C 开始：侧栏信息架构整理

**问题**：读代码盘点侧栏发现——这个会话里陆续加了"🧩写作Skill""🚀无限续写"
"+新建文件夹"这些入口，全部堆在顶部，新用户一打开先看到 6 个纵向排列的按钮，
最高频的"+新建笔记"跟不常用的工具类入口视觉权重完全一样，笔记列表要往下滚
才看得到。另外每条笔记的元信息行（时间戳+文件夹选择+置顶+删除）用了三个
`float: right` 堆在一起，220px 侧栏宽度下，文件夹名字长一点或者时间戳格式
变化都容易把这一行挤出去、换行错位。

**修复**：
1. "🧩 Skill"/"🚀 无限续写" 挪到侧栏底部，用 `position: sticky; bottom: 0`
   钉住（跟之前 `.md-toolbar` 用的是同一个技巧）——滚动查笔记时仍然随时
   点得到，但不再跟"新建/找笔记"这个核心流程抢第一屏视觉权重。
2. "+新建笔记"/"导入.md"合并成一行，导入从一整行文字链接压缩成一个图标。
3. 笔记元信息行从 float 布局改成 flex：时间戳用 `min-width: 0` + 省略号
   当"该缩的先缩"那一个，文件夹选择/置顶/删除三个控件固定大小不被挤压
   ——原理跟更早修的 `.col` 文字溢出 bug（CSS Grid `min-width: auto` 陷阱）
   是同一个模式，这次是 flex 版本。

**验证**：tsc/build 干净，两套 smoke suite（编辑器/图谱）过；这部分是纯排版
调整，没有涉及 DOM 结构大改，运行时行为不受影响，UI 层视觉效果还需要用户
自己看一眼确认（没有浏览器工具没法自己截图）。

### [8] 轮次拼接边界粘连——正文可能被拼成不可解析的 markdown

**发现方式**：不是靠 `grep "^##"` 之类的正则查重复/格式问题——那类检查本身
就有盲区（正则要求标题前有换行符，而这个 bug 恰恰是把标题粘到了前一行末尾，
没有换行符，所以正则完全看不见它）。是在核对 [3]/[6] 的一次三轮 note-harness
测试输出时，选择整篇读原始文本（不经正则），逐字符过一遍所有 ` ``` ` 出现的
位置，才发现 `### Kickstarter 上线准备`、`## 推进节奏总览` 两处标题，字面上
是直接贴在前一轮 mermaid 代码块的收尾 ` ``` ` 后面、同一行、零字符分隔——
形如 `` E --> F[首日转化迭代]\n```### Kickstarter 上线准备 ``。这样一段
markdown 渲染出来，代码块收尾符被标题文字拖进了同一行，代码块解析直接坏掉，
后面的标题也不会被当成标题渲染。

**根因**：`note_harness.py` 的 `content += round_text` 和 `writing_plan.py`
的 `note["content"] + round_text`，都是裸拼接——没有保证两轮之间有分隔符。
如果上一轮的流式输出恰好在没有尾随换行符的位置结束（比如正好停在代码块收尾
` ``` ` 上，模型没多打一个换行就命中了 SECTION_DONE_MARKER/NOTE_HARNESS_DONE_MARKER），
而下一轮续写又恰好用标题开头，两段文字就会在同一行首尾相连，拼出一个
markdown 解析器认不出的畸形结构。这跟 [2] 修的"首轮无格式可延续"是完全不同
的两个 bug——那个是模型选错了格式（加粗冒充标题），这个是拼接逻辑本身没做
分隔保证，属于框架层面的 bug，不是模型输出质量问题，比 [2]/[3] 更底层、
更容易被忽略（因为大多数轮次边界不会精确落在这种敏感位置，复现概率不是
100%，属于那种"能放过回归测试、但迟早会在真实使用里炸一次"的 bug）。

**修复**：新增共享函数 `prompts.join_round_text(content, round_text)` ——
`content.rstrip("\n") + "\n\n" + round_text.lstrip("\n")`，保证两轮之间
恒定有一个空行分隔，不管上一轮结尾/下一轮开头各自长什么样。两处调用点
（`note_harness.py` 的续写拼接、`writing_plan.py` 的 section 续写拼接）
都换成这个共享函数，不再各自裸拼接。

前端同样要修：`App.tsx` 里 `runHarness()`（文件夹 harness）和
`runNoteHarness()`（单篇 harness）各自维护一份本地 `content` 状态，靠
`onDelta` 增量拼接跟随显示——如果只修后端，前端本地拼出来的文本会跟后端
存的版本对不上（前端看到的是没打补丁的旧拼法）。在 `onSectionStart`（文件夹
harness 每次进入新一轮时触发，判定"还在同一篇笔记里继续写"用
`currentRef.current?.id === d.note_id`）和 `onRoundStart`（单篇 harness，
每轮都会触发，不涉及笔记切换）里，各自在这一轮首个 delta 到达前，对本地
`content` 做一次等效的"去掉尾部换行 + 补两个换行"处理，跟后端逻辑对齐。

**验证**：
1. 后端单测（`test_prompts.py` 新增 `join_round_text` 相关用例）+ 全量
   `pytest -q`：156 项全过。
2. 前端 `tsc -b` + `vite build` 干净，三套 smoke suite 全过。
3. **真实 LLM 调用复现验证**：新建测试笔记，正文故意以 mermaid 代码块收尾
   ` ``` ` 结束且不带尾随换行符（精确复现最初发现问题时的边界条件），跑一轮
   `/api/note-harness/run`，读回持久化后的真实正文——旧代码块收尾符后面
   紧跟的是 `\n\n` 再接模型这一轮的续写文字，不再是零字符粘连。用正则扫了
   全文所有 ` ``` ` 出现位置逐个核对上下文，确认边界处处都有干净的换行分隔。
   （测试笔记已删除，未留在 terrence 账号里。）

**加测发现的次要问题（不是这次要修的 bug，先记录）**：这次为了精确复现
"结尾无换行"这个边界条件，特意构造了一个不太自然的种子内容（代码块收尾符
后没有换行就结束）。模型在这种畸形输入上续写时，自己也生成了一个不太规范
的裸 ` ``` ` 围栏（没跟紧跟着的 "mermaid" 语言标记连在一起），像是被这个
异常输入干扰了一下。这是模型对畸形输入的鲁棒性问题，不是 join_round_text
修复引入的新问题（现在正文里已经不会再出现这种畸形边界了，这个问题的触发
条件本身就被这次修复消除了大半），暂不单独处理。

### [9] A3 无限续写专项验证 + [8] 的野生佐证

**做的事**：早前为了测 more_sections"还有没有更多"判断的克制程度，建过一个
故意窄范围的目标——"只写清楚 CaptureVoicesAnywhere 的硬件续航方案是什么，
不要展开成整个项目规划"（`a3-audit` 文件夹）。当时起了一个后台任务想让它
单独复查，但那个任务在这次会话跨天（[8] 修复时会话已经从 08-28 到了
08-31）之后已经找不到了——`TaskOutput` 直接返回 "No task found"，印证了
之前就记过的教训（[[feedback_foreground_only]]：后台任务不可靠）。改为
直接读这个文件夹当时真实产出的笔记内容做验证，比等一个可能已经丢失的
后台任务汇总更可靠。

**A3 结论（more_sections 克制程度）**：跟踪笔记显示 5 个 section 写完后
计划状态直接是"已完成"，没有无限追加——目标里"不要展开成整个项目规划"
这个限定词被遵守了：5 个 section 全部紧扣"续航方案"本身（卖点定位/前台
依赖条件/Apple Watch 对照/车载模式容忍/Beta 验证），没有一个跑题到"整个
项目规划"的其他维度（没有出现定价、团队分工这类无关内容）。more_sections
判断在窄范围目标上是克制的，没有为了凑数硬造新 section。

**格式/grounding 抽查**：5 篇内容读下来风格统一（全部用 `##`/`###` 规范
标题，没有加粗冒充标题的情况——[2] 的修复在这个更早的样本上也生效了），
论点之间有真实的交叉引用而不是各说各话（"车载模式常驻运行的耗电容忍"和
"录音传输的前台 App 依赖条件"两篇互相呼应同一个"录音功耗与传输功耗分账"
的核心结论，没有互相矛盾）。

**[8] 的野生佐证（重要）**：读原始正文（不经正则，逐段读）时发现，这个
文件夹是在 [8] 的修复**之前**生成的，正文里确实存在同样的粘连 bug，且是
两处独立案例，不是我这次刻意构造出来的：
- 「录音传输的前台 App 依赖条件」：`...确定性由前台 App 的常驻条件决定。## 依赖清单`
  ——句号后直接接标题，零分隔。
- 「CaptureVoicesAnywhere 的续航卖点定位」：`...录音连续性由设备硬件续航保障。## 卖点沟通的边界`
  ——同样的模式。

这两处在当时都被 [1]/[2]/[6] 的检查漏掉了，因为那几次检查用的是
`grep "^##"` 之类的正则去查标题格式问题，而这个 bug 恰恰是标题没有独占一行
（前面粘着上一句话），正则天然看不见——跟 [8] 里发现这个 bug 时用的方法
是同一个盲区。这也说明：这个 bug 不是我构造 [8] 复现实验时才出现的边缘
情况，而是这次任务开始以来产出的真实内容里就已经存在、且此前的多轮质量
审查都没抓到的真问题，修复是必要的，不是过度工程。

（清理：`a3-audit` 文件夹连同其 6 篇笔记、以及早前遗留的两个孤立测试笔记
`cleanup-pass-verify`/`beats-coverage-audit` 一并删除，见下方清理记录。
另外清理时还发现一个跟这次任务无关的孤立 `test` 文件夹（回忆录写作计划，
明显是这次会话更早阶段留下的测试数据，同样删除）。）

### [10] 任务 B 的核心 bug：完成判定跟 beats 完全脱钩，真实数据验证 + 修复

**做的事**：为了正面验证任务 B 的开放问题（"beats 是否真的被完整覆盖"），
建了一篇带清晰 spine + 4 条 beats 的测试笔记（评估要不要做企业版，四条
beats 分别是需求信号/资源冲突/定价模式/最大风险），跑 6 轮上限的智能续写，
人工核对每条 beat 是否真的被覆盖、有没有多余重复。

**发现的真问题**：4 条 beats 第 1 轮就已经被正文实质覆盖了（四个小节，
grounding 扎实，格式统一，风险那条还精确点出了具体卡点，不是泛泛而谈）
——但 harness 完全没有停，一路跑到第 5 轮才主观判定"完整"，中间 3 轮全是
把同样几个结论换着法子重新展开一遍：定价模式被独立成小节讲了 4 次
（"定价模式设想""定价模式的现实约束""定价与竞争的现实约束""定价模式
设想的落地前提"），资源冲突讲了 3 次，需求验证讲了 3 次，最终正文膨胀成
15 个标题、循环绕着同样 4 个主题打转。EDIT_SYSTEM 的查重指令抓住了其中
一部分字面近似的重复（修订日志里能看到好几条 delete），但抓不住"整节
内容换个角度重新论证同一个结论"这种更大颗粒度的语义重复——这不是查重
指令不够强，是完成判定这件事本身的设计就有问题。

**根因**：`round_result.get("done", False)` 是唯一的完成信号，而这个信号
完全来自续写模型自己在这一轮输出末尾的主观判断——续写模型的任务就是
"接着写"，没有任何机制告诉它"你已经把该写的都写完了，不需要再展开"，
它自然倾向于继续找新角度扩写。这正是"骨架/beats 应该在续写模式下扮演什么
角色"这个任务 B 核心问题的答案：**beats 不应该只是喂给续写模型当参考的
输入，还必须是一个独立于续写模型主观判断的、外部的完成判据**——现有实现
只做到了前半句。

**修复**（`prompts.py` + `note_harness.py`）：
1. 新增 `BEATS_COVERAGE_SYSTEM` / `beats_coverage_user()`——一个独立的、
   小而专的判断调用，只做一件事：给定 beats 列表和当前正文，逐条核对
   "这条 beat 描述的功能是不是已经在正文里实质出现过"，返回还没覆盖的
   beats 原文列表。指令里特别写明"不要因为还能写得更详细/更充分就判定
   没覆盖"——这条是针对性地堵住这次实测发现的失败模式（模型不是不知道
   已经覆盖了，是"能不能覆盖得更好"跟"是否已经覆盖"这两件事混在一起判断）。
2. `note_harness.py` 主循环：每轮续写完之后，如果有 beats 且续写模型自己
   没给完成标记，就跑一次这个独立核对；核对结果显示全部覆盖，直接判定
   `done_now = True`，不再等续写模型自己主观觉得"够了"。
3. 顺带修了实测中发现的另一个小问题：EDIT_SYSTEM 的查重 delete 有时会把
   一整节内容删空、只留下一个标题孤零零对着空行——给 EDIT_SYSTEM 加了一条
   "删完看一眼有没有留下空壳标题，有就把标题本身也一起删掉"的指令。
4. 顺带补齐了 [8] 遗漏的一半：`join_round_text` 的前端等效修复之前只做了
   文件夹 harness（`onSectionStart`），单篇笔记 harness 的 `onRoundStart`
   一直没做——这次一并补上，不然前端本地累积的 content 会跟后端修好的
   持久化版本对不上。

**UI 联动**：完成判定既然已经有了"哪些 beats 还没覆盖"这个结构化信号，
顺手把它透传给前端——新增 `beats-status` SSE 事件，"写作骨架"面板
（`SkeletonPanel.tsx`）不再是一份生成完就不再变化的静态清单，运行智能
续写时每条 beat 旁边会实时标出✓已覆盖/未覆盖，划掉已完成的。这是 [7]
之后任务 C 的第二处真实改动，直接由这次任务 B 的发现驱动，不是单独凑的
UI 工作量。

**验证**：
1. 后端：新增 `_beats_status()` 的单测（3 种模型返回场景：全覆盖/部分
   覆盖/返回非法 JSON 兜底），加上 prompt 层的断言（`beats_coverage_user`
   格式、`BEATS_COVERAGE_SYSTEM` 不因"能写更好"误判未覆盖、EDIT_SYSTEM
   含空壳标题清理指令）——全量 `pytest -q`：162 项全过。
2. 前端：`tsc -b` + `vite build` 干净，编辑器 smoke suite 过。
3. **真实 LLM 复现对比**：用完全相同的种子内容、spine、4 条 beats 重新
   跑一次（修复前后是同一个测试场景，不是换了更简单的例子凑数据）——
   修复前：5 轮、正文膨胀到 15 个标题、大量语义重复；修复后：**1 轮**、
   正文 1153 字、4 条 beats 全部清晰覆盖、`reason: complete`、没有重复
   小节。轮数从 5 降到 1，直接印证完成判定不再受"续写模型主观想不想
   继续写"支配。

**验证中发现的次要瑕疵（记录但不单独处理）**：这一轮的输出里，前 4 条
beats 用的是 `**加粗**` 标签而不是 `##` 标题，但同一轮末尾新增的"竞争
格局与定位冲突"一节却用了规范的 `##` 标题——同一次模型输出内部风格不
统一，这跟 [2] 修的"跨轮次开头没有格式可延续"是不同的失败模式（这次是
单轮内部前后不一致），现有的格式提醒指令没有覆盖这种情况。优先级低于
这次修的两个结构性 bug，先记录，不在这次任务里展开修。正文末尾还留了
几行多余空行（模型的完成标记前有多余空行，被 marker 剥离逻辑正确去掉了
标记但没去掉空行），纯格式瑕疵，同样先记录不单独处理。

（清理：测试笔记已删除，未留在 terrence 账号里。）

### [11] C 继续：系统性排查"空状态直接消失"，找到并修了一个真实空白面板

**做的事**：[7] 已经过一轮侧栏排查，这次接着看跟 AI 功能直接相关的几个
面板——WritingPlanPanel（文件夹 harness 控制台）、RevisionPanel（手动
修订面板）、MemoryPanel（知识库面板）——整体质量都不错，有明确的空状态
文案、加载态、进度反馈，没发现新问题。

**发现的真问题**：`RelatedMemory.tsx`（"相关记忆" tab 的**全部**内容，
不是跟别的面板并排的其中一块）在 `facts.length === 0 && !loading` 时
直接 `return null`——连组件自己的标题一起消失。这个 tab 平时大多数时候
就是这个状态（笔记刚开始写、内容太短还没触发检索、或者检索了但知识库里
没有相关内容），用户点进"相关记忆"这个 tab，看到的是彻底空白，什么都
没有：没有标题、没有说明、不知道是"还没开始"还是"坏了"。这正是典型的
"能跑但像demo"的那种缺口——功能本身没问题，只是完全没有告诉用户当前
是什么状态。

**排查范围**：为确认不是孤立个案，搜了全部组件里的 `return null` 用法——
`CommandPalette`（面板关闭时，正确）、`DocumentOutline`（"写作" tab 里
跟 SkeletonPanel/RevisionPanel 并排的其中一块，自己消失时 tab 不会空白，
正确）、`KnowledgeGraph` 内部（渲染列表里跳过单个子元素，不是整个组件，
正确）、`Toaster`（没有 toast 时全局浮层本来就该不可见，正确）——只有
`RelatedMemory` 是"整个 tab 唯一内容 + 完全消失"这个真正有问题的组合。

**修复**：不再无条件 `return null`；保留标题和说明文案，在 `facts.length
=== 0 && !loading` 时额外显示一行空状态提示，并且区分两种不同原因——
"内容还太短，还没触发检索"（贴着输入引导用户）vs "检索了但知识库里没
找到相关内容"（如实告知，不是判定为异常）。

**验证**：`tsc -b` + `vite build` 干净，编辑器 smoke suite 过——这部分
是纯渲染分支调整，没有涉及数据流/状态管理改动，运行时行为不受影响，
三种状态（内容太短/检索无结果/有结果）的实际视觉效果还需要用户自己看
一眼确认（没有浏览器工具没法自己截图，这一点从 [5]/[7] 起一直如此）。

**同一类问题的第二个真实案例**：接着看更大的 `MemoryBrowser.tsx`（知识库
浏览器，独立于上面那个小面板）时，发现它的默认 tab "概览" 用的是
`tab === 'overview' && stats && (...)` ——`stats` 初始是 `null`，要等
`memoryStats()` 请求回来才有值。因为"概览"正是这个浏览器打开时的默认
tab，用户点开"浏览"的第一瞬间，内容区在请求返回之前是彻底空白——没有
加载中提示，跟上面 `RelatedMemory` 是同一个"默认/首屏状态被短路成完全
空白"模式，只是这次是首屏加载延迟而不是"没有数据"，触发条件不同、
病灶相同。已加一行 `!stats` 时的加载态。其余三个 tab（主题地图/时间线/
事实表）逐一检查过，都已经有恰当的空状态或加载态，没有同样的问题。

**验证**：`tsc -b` + `vite build` 干净，编辑器 + 图谱两套 smoke suite
都过。

**顺带查到的第三个小问题**：`DigestPanel.tsx`（阶段回顾，MemoryPanel
里的一个子模块）三个时间区间按钮（最近7/30/90天）共用同一个 `loading`
布尔值——点了"最近 7 天"之后，因为这是一次 LLM 调用不是瞬间完成，三个
按钮会一起变成转圈图标，用户分不清自己点的是哪一个。改成记录"哪个区间
正在跑"而不是单纯 true/false，只有被点的那个按钮转圈，其余两个正常置灰。
顺手查了一遍其余用 `.map()` 渲染按钮组的地方有没有同样的问题——
`SelectionMenu.tsx` 的校验/重写/润色/扩展四个菜单项看起来像是同一类
风险，但检查后发现不是：那边 `busy` 为真时整个菜单列表会被替换成单独
一个转圈图标（不是四个按钮各自转），点完之后已经不存在"分不清点的是
哪个"这个选项列表了，不需要同样的修复。

**验证**：`tsc -b` + `vite build` 干净，编辑器 smoke suite 过。

### [12] AudioRecorder：静音录音时用户完全看不到任何反应

**做的事**：继续 C 的排查，看剩下几个没读过的组件，读到 `AudioRecorder.tsx`
（录音→转写/入库）时发现一处真实的静默失败。

**发现**：两条路径都有问题——
1. "录音插入"（`transcribeOnly`）：后端转写为空（静音/没说话）时返回
   `{text: ""}`，前端 `if (text) onTranscript(text)` 在 text 为空时
   什么也不做，没有任何提示。用户录了一段音、点停止，界面上没有任何
   反应，不知道是没录上、权限问题，还是哪里坏了。
2. "录音入库"（`ingestAudio`）：后端在转写为空时返回 `job_id=""` +
   `detail="转写结果为空，可能是静音音频"`——这条 `detail` 本来是给用户
   看的错误提示，但前端原来的代码是 `if (detail) onTranscript('')`，
   注释写着"只提示，不插入"，实际上从来没有真的"提示"过：`detail` 的
   内容被读出来又直接丢掉，`onTranscript('')` 插入了一个空字符串（等于
   什么也没做）。**这条 `detail` 字段还有个额外的坑**：它在成功路径下
   装的是转写文字预览（`text[:2000]`），只有在失败路径（没有 job_id）下
   才是错误信息——原来的代码把这两种完全不同含义的内容用同一个
   `if (detail)` 分支处理，恰好因为都直接丢弃所以没暴露出这个语义混淆，
   但这本身就是设计层面的隐患。

**修复**：两条路径都在失败时改成 `toast(..., 'error')`，不再是静默的
空操作；`detail` 只在真正的失败路径（没有 `job_id`）下当错误文案展示，
成功路径不读它（成功路径的反馈由已有的 `onIngested(job_id)` → 轮询
job 状态这条链路负责，不需要 `detail` 里的文字预览）。

**验证**：`tsc -b` + `vite build` 干净，编辑器 smoke suite 过——这个
bug 本身需要真的对着麦克风录一段静音才能触发，没有浏览器/麦克风权限
没法在这个环境里真实复现点击流程，改动是从读代码+读后端两条端点的
真实返回值直接对照出来的（`ingest.py:117-124` 的两个 return 分支），
不是猜的。

### [13] A2 补测内容类型泛化，发现新的问题：不相关事实被硬编成牵强类比

**做的事**：[1] 一直留着"还需多测几种内容类型"这个未完成项——之前所有
智能续写测试用的都是同一个业务主题（CaptureVoicesAnywhere）。这次故意
换一个完全不搭边的内容类型：个人专注力习惯改善（"怎么提高长时间写作时
的专注力，减少中途分心刷手机的冲动"），跟 terrence 知识库里几乎全是
硬件创业会议记录这个事实分布完全不匹配，正好测试两件事：[10] 的完成
判定修复是不是只对之前那个反复测的场景生效（会不会是调参调出来的假
象），以及知识库里没有真正相关的内容时会怎么处理。

**好消息**：[10] 的修复确认泛化——同样 1 轮、`reason: complete`，3 条
beats（触发点/具体改变/自检信号）都被清晰覆盖，不是只在原来那个反复
验证的场景里生效。

**发现的新问题**：检索机制本身没问题，`_retrieve()` 照样按相似度返回了
6 条"最接近"的事实——但这些事实全部来自硬件创业语境，跟"专注力"这个
主题没有真正的实质关联，只是碰巧被检索算法判定为字面/语义上最接近的
6 条。MAGIC_TAP_SYSTEM 原有指令是"如果给了知识库事实，优先用这些事实的
内容来写"——没有"这条事实其实不适合，可以不用"这个选项，模型只能硬着
头皮把不相关的事实编成类比塞进去。真实产出里能看到好几处：
- "参考硬件配件的使用逻辑：硬件是一个配件，手机不被占着时也能录音"——
  拿硬件产品的设计逻辑类比"把手机放远一点"，牵强
- 直接引用知识库里一句"我就摁这个呀，我说哎，帮我查一下这个AppleWatch的
  续航是多少，然后他就直接帮我拨了"，用来类比"确认动作压缩成一次物理
  摁压"——原始语料讲的是语音助手误解指令直接拨了电话，用来印证"专注力"
  的论点完全对不上
- 最严重的一处：把 KOL 样品交付时间线和"硬件软件APP各自开发进度平衡"
  直接套进个人习惯养成的时间表设定里——"像平衡硬件软件 APP 各自开发
  进度一样，给这个习惯测试设定窗口期"，以及末尾"Pillar one 的产品思维
  在这里适用：specs、功能、差异化点，都要能被自己检验"，把创业黑话
  原封不动地套进一篇跟创业毫无关系的个人笔记里，读起来非常突兀

这是一个新的、跟之前发现的都不一样的失败模式——不是完全不 grounding
凭空编（[4] 修的 expand 问题），也不是重复（[2][3][10] 修的问题），是
"过度 grounding"：检索到的事实明明不贴合，却被指令逼着强行用上，编出
读者会觉得莫名其妙的类比。

**修复**：给 MAGIC_TAP_SYSTEM（同时是 magic-tap、note-harness 续写、
无限续写 section-write 三处共用的续写基础指令）加一条"判断相关性"的
许可——检索到的事实不是每条都要用，正文主题如果跟事实所属领域根本不是
一回事，不要为了显得"有依据"硬编牵强类比，跳过、按上下文自然写更好。

**验证**：`pytest -q` 163 项全过（新增一条断言指令文案存在的单测）；
用完全相同的种子内容、spine、beats 重新跑一遍——修复前后对比：
- 字数：1213 字 → 231 字（大幅收敛，不再靠东拉西扯的类比凑内容）
- 轮数：都是 1 轮、`reason: complete`——[10] 的完成判定修复在这个新
  内容类型上同样成立
- 牵强类比：修复前那几处（硬件配件类比、AppleWatch通话轶事套用、KOL
  时间线类比、"Pillar one"黑话）在修复后的产出里**一处都没有**——这轮
  检索到的是完全不同的 6 条事实（Android 测试机、iPhone 配色策略、
  拍照支架），同样跟"专注力"主题不相关，但这次模型正确地完全没有使用
  它们，正文从头到尾只按话题本身的逻辑展开，读起来不再有莫名其妙的
  "这段话哪来的"感觉
- 3 条 beats（触发点/具体改变/自检信号）依然清晰覆盖，没有因为"不用
  牵强类比"就退化成空对空的正确废话

（清理：测试笔记已删除，未留在 terrence 账号里。）

### [14] 检索从 push 改 pull 的完整尝试：调研、实现、实测证伪、回滚

**背景**：[13] 加的"事实不相关可以跳过"是 prompt 层面的兜底，用户建议往
更根上查——去研究了主流 agent 框架（OpenClaw、Hermes Agent）和 agentic
RAG 的做法，结论是检索应该从"push"（不管用不用得上、每轮死板塞 top-K）
改成"pull"（模型自己判断要不要查、查什么）。Hermes Agent 的完成判定
（独立评判模型每轮判断 done/continue）印证了 [10] 的设计方向是对的；
检索这块调研到 Self-RAG/Corrective RAG（CRAG）——用轻量评判过滤不相关
检索结果，不强行塞进生成。

**中间走了一段弯路**：一开始以为要接 KITE 原生的 `ask()`（`compile_plan()`
采样 3 个候选查询计划再打分选优 + 生成回答文字，实测最低 4 次 LLM 调用），
被指出这是"纯为了刷分的接口"——3 个候选采样是 `ask()` 为了在评测集上
最大化准确率特意加的开销，不是"用 LLM 理解语义"这件事本身必须付的代价。
纠正后的方案：直接调 `compile_plan(..., scorer=None)`，只编译一次
（`n_candidates` 采样分支不走），拿到的 plan dict 直接扔给免费的
`execute_plan()`，跳过 `ask()` 用不上的"生成回答文字"这一步——理论上
每轮只加 1 次 LLM 调用。

**实现**：`kite_memory.py` 加 `UserMemory.plan_recall()`（编译一次+免费
执行）；`EDIT_SYSTEM` 输出契约从裸数组改成 `{revisions, search_for}`
对象，让修订调用顺带说清楚"续写下一步最需要确认什么"（不额外加调用，
折进已经在跑的修订调用里）；`compose.py` 加 `_plan_retrieve()`，
`search_for` 非空就走新检索、否则退回老的 `_retrieve()`；`note_harness.py`
主循环接上这条链路。170 项单测全过（新增 `plan_recall`/`_plan_retrieve`/
`_run_edit_pass` 新契约/`EDIT_SYSTEM` 新字段的测试），真实跑了一轮完整
harness，功能层面没问题——[10]/[13] 的修复在新链路上依然生效，1 轮
收敛，4 条 beats 全覆盖。

**用户追问延迟数字后，真相浮出水面**：被问"50 多秒不正常，你自己测测
输出 token 速度"——测了才发现之前报的"50+ 秒"是训练自己进程冷启动的
假象：`_compile_prompt()` 实际拼出的提示词有 **5.8 万字符**（几乎是
codebook 全部主题/实体词表 + 一份看起来是全量 unit-date 索引表的转储），
第一次调用要 53.7 秒，但同一进程里接着测 4 次不同问题，稳定在 **9.7-12.5
秒**——说明是提示词前缀缓存在起作用（前缀对同一用户每次都一样，推理端
只需要预填充一次）。稳态成本没有一开始说的那么夸张，这是我自己汇报
不严谨，用户指出来才发现。

**但延迟不是最后的问题，精度才是——两次真实对比彻底否定了这个方向**：
用完全相同的问题分别跑 `recall()`（老办法，纯词表字面匹配）和
`plan_recall()`（新办法，LLM 编译一次查询计划）：

- 问"这个功能具体的上线时间是哪天"：`recall()` 512ms 拿到 6 条**全部
  精准命中**的排期事实（"2月10号"、"2月15号锁定"、"3月10号是众筹版本"
  等）；`plan_recall()` 16.8 秒拿到的是"项链那个还得加工一下"、"今天
  四号了"这类完全不相关的内容，且编译出来的 plan 结构上是对的（正确
  识别出 `kind: event`、时间窗口、topic closure），但实际查回来的东西
  对不上——怀疑是 "project" 这个主题分类下挂的内容太杂、closure=true
  圈太宽，`execute_plan()` 又没按相关性排序。
- 问"用户对众筹页面的具体反馈是什么"：`recall()` 529ms 再次 6/6 精准
  命中；`plan_recall()` 14.4 秒返回的内容**来自完全不同的领域**——港中
  择校（国际学校申请咨询），跟众筹页面毫无关系。

两次对比，`recall()` 全部精准命中，`plan_recall()` 全部文不对题，还慢
15-30 倍。这不是运气不好撞见一个边界情况，是同一个模式复现了两次。

**结论：回滚整条链路，不是留着"反正无害"**——[13] 的兜底确实能防止
不相关事实污染最终正文，但花 10+ 秒/轮换回来的检索结果比不花钱的老办法
还差，"无害"掩盖不了"没有收益、纯粹在烧时间"这个事实。已完整回滚：
`note_harness.py` 续写检索退回 `_retrieve()`；`_run_edit_pass`/
`EDIT_SYSTEM`/`compose.py` 的 `search_for`/`_plan_retrieve` 相关改动
全部撤销；`kite_memory.py` 的 `plan_recall()` 连同未使用的 import 一并
删除，不留只有一个调用点、且那个调用点已经不存在的死代码。测试也同步
删除/撤销（`test_kite_memory.py`、`test_compose.py` 整个删掉，
`test_note_harness.py`/`test_prompts.py` 里对应的新增测试撤销）——
回滚之后 `pytest -q` 是 163 项，跟这次实验开始前的基线数字完全一致，
确认没有留下孤儿代码或孤儿测试。

**过程中顺手查到、且值得保留的真问题**（不因为回滚就作废）：`plan_recall()`
最初的 `except Exception` 抓得太宽，把"模型判断没什么可查"和"这里其实
是代码 bug"完全混成一回事，完全没有痕迹——这是这个代码库"静默失败+
兜底"这个通用模式的一个真实反例，值得以后写类似兜底逻辑时留意：兜底
可以静默对调用方，但不能对开发者也静默，至少要留一条能看见的痕迹。

**这次的教训（写给自己，不只是写给这次改动）**：
1. 调研"主流框架怎么做"能定方向，但用不用得上必须拿自己的真实数据测——
   Hermes/CRAG 的思路是对的，但 KITE 编译式检索在 terrence 这个具体
   codebook 的主题结构下发挥不出理论上的精度优势，这个差距只有真实
   对比测试才测得出来，读论文/读别人的架构文档测不出来。
2. 汇报数字之前要自己先验证，不能引用一行代码注释就当结论——"50+ 秒"
   这个数字被用户一句"这不正常"就戳穿了，说明我在引用别人写的
   docstring 时没有交叉验证这行注释本身是不是准确/是不是稳态数字。
3. "无害"不是"有价值"的同义词——[13] 的兜底让这条链路不会让输出
   变差，但这不代表它值得保留；沉没成本（已经写了代码、测试都过了）
   不应该成为不回滚的理由，真实数据说明它没用就该退，不是"能用就先
   放着"。

### [15] 实体去重：从"库里有但从没被调用过的休眠函数"到真的执行在
terrence 真实数据上

**背景**：用户指出实体表无限增长是个问题。查证：KITE 库里确实有
`consolidate_entities()`/`consolidate_topics()`，但整个库和我们自己的
`app/` 目录里**没有任何调用方，也没有任何测试**——是写好但从没被真正
跑过的休眠能力。

**候选生成质量**：`entity_merge_candidates()` 默认 `min_len=2`，在真实
1996 个实体上抽出 1701 个候选，抽样发现大部分是"ac \| af"这种 2 字符
噪声碎片互相撞出来的（ASR 转写噪声，跟真实实体重复无关）。改用
`min_len=5` 收窄到 295 个，抽样质量明显更好（真实候选如
`ai_insight/ai_insights`）。这个参数库函数内部写死拿不到，所以没有
直接调库函数，自己复刻了一份投票逻辑（`_vote_entity_merges()`，跟库版
完全同构：3 轮独立投票、2/3 一致才合并，复用库自带的
`_consolidation_round_votes` 解析投票结果，不重新发明这部分）。

**投票质量的两轮验证**：
1. 先手选 15 对（3 对像真的、11 对像误判的容器关系对如
   `apple|apple_watch`）单次投票——3 条合并全对，11 条拒绝全部合理，
   零假阳性。
2. 真实全量跑：295 个候选、3 轮投票、2/3 一致过滤，剩 **38 条高置信度
   合并**——`ai_insight→ai_insights`（单复数）、`browder→bill_browder`
   （姓补全名）、`granona→granola`（ASR 错别字）、
   `terrance→terrence`（**用户自己的名字被转写错，抓出来了**）等，
   人工审查没发现明显错误合并，只有 2 条存疑（`facebook广告→facebook`、
   `air_force→united_states_air_force`）但不是"张冠李戴"级别的错误。

**持久化路径是这次真正的风险点**：`consolidate_entities()` 只改内存里
的 Python 对象，写回磁盘 XML 完全没有先例可循——库里唯一现成的写入
函数 `append_session()` 是为"追加新 session"设计的，假设不匹配，不能
直接复用。这条持久化路径是这次专门写的，全库零参考实现，属于"第一次
真正跑这段代码"的高风险操作。分两步验证降险：
1. 在 terrence 真实 codebook.xml 的**隔离副本**上（不是真实文件）先跑一遍
   完整的合并+写入+重新加载，确认 fact 属性重写正确（引用数从 3→0 旧、
   0→6 新，精确符合预期）、vocab 正确移除 loser/保留 winner、文件写完
   还能正常读回来
2. 确认副本测试无误后，才对真实文件执行——写入前先自动备份（原子写
   模式仿照库自带 `append_session()` 的"临时文件+`_verify_loadable()`
   校验+原子替换"，不是自己发明一套更弱的写法）

**真实执行结果**：38 条全部生效——实体数 1996→1958（精确减少 38，
一条没漏），facts 数 15062 保持不变（合并只改实体归属，不删事实，
符合预期），逐条核对 loser 全部消失、winner 全部还在，随机抽查
`terrance`/`granona`/`ai_insight`——旧编码在任何 fact 里都查不到了，
新编码的引用数正确增加。备份文件确认存在。重启后端，`/api/memory/stats`
真实接口读到的实体数也是 1958，跟直接读文件校验的结果一致。

**验证**：新增 `test_kite_memory.py`（4 项：2/3 投票门槛、同一轮内不
链式合并、`preview` 不碰真身只碰深拷贝）+ 独立的隔离副本集成测试（不算
进 pytest，是一次性验证写入路径正确性用的），全量 `pytest -q`：167 项
全过。

**这次没有回滚**——跟 `[14]` 的检索实验不一样，这次每一步都有真实数据
验证支撑（候选质量、投票质量、写入正确性都分别独立测过），最终结果
干净、可解释、可核对，值得保留。

### [16] 实体去重投票问法重新设计：语义上下文错了，读音/拼写才是对的信号

**背景**：`[15]` 那 38 条合并落地之后，用户追问"实体数量才少 38 条"——
查了才发现这个数字确实小：`min_len=5` 把 54.6%（1069/1958）的实体
（编码短于 5 个字符）排除在候选之外，这批正是最早就记录过的噪声重灾区。

**先测了"给上下文能不能救回短实体"**——结果发现这个方向本身就想错了。
挑了 8 对看起来像真候选的短实体（安克/安客、会龙/慧龙等），带着从真实
fact 里查到的原文例句去问模型"这两个是不是同一个实体"，**0/8 被判定
合并**，包括模型精确抓到"德国达"和"德惠达"在同一句话里被明确拿来
对比的证据。这个结果一开始像是验证了"短实体候选大多是误判、min_len=5
是对的"——但用户指出这个判断本身就错了：**这些是 ASR 把同一个读音
识别成了不同的字，根子是读音问题，不是语义问题**；用语义上下文去判断
读音混淆，问的问题从一开始就不对——而且这个判断方式还有循环论证的
毛病：转写出两个不同拼法之后，后续抽取事实是分别独立处理每个拼法的，
产生的"上下文"本身就是错误转写的下游产物，不能反过来当"这两个不是
同一个实体"的证据。

**验证读音假设**：用 `pypinyin` 查了这 8 对的真实拼音（不是凭记忆猜）——
3 对是完全同音字（安克/安客、会龙/慧龙连声调都一样）或近音字（尹凯/
银凯，只差声调），另外 5 对读音确实不同（德国达系列、美光/美国、张敏/
张龙）。这 3 对同音字之前被两版投票（不带上下文的裸编码版、带语义
上下文版）**都错误拒绝了**——因为两版问的都不是"读音像不像"。

**尝试规则库方案，被质疑"是不是有点蠢"**：查了英文这边（1155 个实体，
比中文的 762 个还多）能用的规则——Soundex/Metaphone/NYSIIS 三个标准
算法，实测互相不一致（比如已知真实重复 `granona/granola` 三个算法全部
漏判），用哪个都不完整，而且中英文各自要接一套独立的规则库/词典，
维护成本高。用户提出更好的问题："有没有一劳永逸、对多语种都有效的
方法"——既然本来就在调 LLM，直接换个问法问模型"这两个读音像不像"，
不用另外接任何语音规则库，模型对多语言发音本身就有隐式知识。

**混合测试验证"换问法"能不能成立**：把中文（3 对该合并/5 对不该合并）
和英文（5 对已知真实重复/3 对已知不该合并）放一起，只问"读音像不像"——
中文 8/8 全部判对，**英文该判重复的一个都没判出来**（0/5）。查了才发现
不是模型判断错，是这几个英文例子本身**不是真同音**（比如
`salomon_brothers`/`solomon_brothers` 的"sa"/"so"元音不同）——它们是
**打字/拼写变体**（漏字母、单复数、缩写），跟中文那批的产生机制根本不是
同一类问题。换成"是不是同一个词的拼写变体"这个问法单独测英文那 8 对，
7/8 判对（只有 1 条本来就没有很强证据支撑）。

**结论**：中英文的重复实体是两种不同机制造出来的——中文是 ASR 同音字
混淆，英文是打字/拼写变体——用同一个问法（不管是"语义上下文"还是
"读音像不像"）去问都会在其中一边失手。修复：`_vote_entity_merges()`
按候选是否含 CJK 字符分流，中文走新写的 `_PHONETIC_MERGE_PROMPT`
（只问读音），英文/其他走 `_SPELLING_VARIANT_MERGE_PROMPT`（只问是不是
拼写变体）——两条路径复用同一套 3 轮投票、2/3 一致才合并的安全机制，
只是问的问题不同，都不需要接任何规则库/词典，纯粹是换了问法。移除了
`[15]` 里用的 KITE 自带 `ENTITY_CONSOLIDATION_PROMPT`（语义上下文版），
连同它的 import 一起删掉。

**验证**：新增 6 项单测（`_is_cjk_pair` 的脚本检测、路由到两个不同
prompt 且候选不串批），全量 `pytest -q`：169 项全过。真实全量数据的
端到端复测（1614 个候选，跑完拿到实际合并结果）因为单批调用耗时过长
（后面 `[17]` 记录了原因和解决），改成先用配置页切到更快的供应商再补跑，
这次没跑完就转向了 `[17]`。

### [17] 加了 LLM 供应商设置页：本地模型太慢，加一个切 GPT 的选项

**背景**：`[16]` 的全量验证（1614 个候选、约 84 次调用）实测本地模型
单次调用稳定在 40-46 秒（跟 prompt 长度关系不大，就是这个模型生成一批
判断结果本身的速度），84 次调用累计接近一小时。用户直接提议：与其
死等本地模型，不如加一个配置页，可以选本地模型还是用 GPT 的 token——
GPT 单次调用通常快得多，重活可以切过去跑。

**关键设计问题**：`Settings`（`config.py`）是 `@lru_cache` 过的，从
`.env` 读一次就定死，运行时改不了，也不该为了这个改成运行时可变——那是
给"部署环境切换"用的，不是给"用户在界面上随时切"用的。这两件事要分开：
供应商选择做成 DB 存的全局配置（`store.py` 新增 `provider_config` 表，
单行，`id='default'` 固定存取），没设置过就退回 `.env` 里的本地模型
默认值——这样没打开过设置页的用户行为完全不变，不是破坏性改动。

**实现**：
- `store.py`：`get_provider_config()`/`set_provider_config()`（切
  provider 不传 key 会保留原值，不用来回切的时候重新填）/
  `get_active_llm_config()`（真正生效的端点，GPT 选了但没填 key 时
  安全退回本地模型，不会拿着空 key 去发请求）
- `llm.py`：`_headers()`/`_payload()`/`complete()`/`stream()` 全部
  从直接读 `Settings.llm_*` 字段改成读 `store.get_active_llm_config()`
- `kite_memory.py`：`_export_provider_env()` 和三处读
  `kite_extract_model` 的地方（`remember()`/`ask()`/
  `preview_entity_consolidation()`）同步切过去——用户选了 GPT，续写
  这条路径切了，KITE 那边（抽取/问答/实体去重）不能还锁死在本地模型
- 新增 `/api/settings/provider` 的 GET/POST，`main.py` 里 `/api/health`
  也同步改成报"当前生效"的供应商，不是 `.env` 里那个写死的默认值——
  不然切换之后这个健康检查在说谎
- 前端 `SettingsPanel.tsx`：单选本地模型/GPT，选 GPT 时展开 API key/
  模型名/base URL 三个字段，key 用 password 类型输入框，GET 回来的
  配置**不回传真实 key**（只给"已设置 ...末四位"这种确认信息，不把
  完整 key 摆在前端），侧栏底部加了个"⚙️ 设置"按钮

**验证**：`store.py` 新增 7 项单测（默认值、读写往返、切换保留 key、
非法 provider 拒绝、`get_active_llm_config()` 的两种退回场景）+
`main.py`/路由/import 的 sanity 检查，全量 `pytest -q`：176 项全过。
前端 `tsc -b` + `vite build` 干净，编辑器 smoke suite 过。真实调用
验证：POST 一个假 GPT key 切过去，`/api/health` 正确报 GPT 的
base_url/model 且因为 key 是假的正确显示 `llm.ok: false`（证明健康检查
真的在打生效的端点，不是读缓存），切回本地模型后 key 被保留，最后
清空测试 key、确认回到干净的默认状态。

### [18] 接真实 GPT key 时发现的兼容性问题：推理档模型参数要求不一样

**背景**：用户提供了真实 OpenAI key，模型选的是 `gpt-5.6-luna`——这个
名字超出了训练知识范围，没有直接采信，先用这把 key 直接查了一遍
`GET /v1/models`，确认这个模型真实存在于这个账号下（跟同一批的
`gpt-5.6-sol`/`gpt-5.6-terra` 一起），才继续。

**真实调用立刻报错**：`/api/health` 显示连接正常（key 有效），但通过
`llm.complete()` 真的发一次请求直接 400。没有猜，直接拿 key 手动
`curl` 打 OpenAI 官方端点，读报错原文定位：
1. `max_tokens` 这个字段不认，"Use 'max_completion_tokens' instead"
2. `temperature` 传自定义值也不认，"Only the default (1) value is
   supported"——`gpt-5.x` 这类推理档模型的通用限制，跟本地模型
   `muse-glimmer-30b` 的参数习惯不一样

**修复不是简单粗暴地把两个参数都去掉**：
- `max_tokens` → 统一改用 `max_completion_tokens`——先实测了本地模型
  接不接受这个新字段名（直接打本地 llama.cpp 端点验证），确认它也认
  （而且是真的按这个值截断，不是忽略），所以两边统一用一个字段名，
  不用按供应商分叉。
- `temperature` 更复杂：本地模型的 temperature 调参是这次任务里反复
  验证过、有真实效果差异的（EDIT_SYSTEM 用 0.1 追求确定性、续写用 0.7
  要有变化），不能为了兼容 GPT 推理档就整体去掉，那是拿真实调过的效果
  换兼容性。改成运行时兜底：正常发送 temperature，如果撞上这个特定的
  400（`param=temperature, code=unsupported_value`，不是随便什么 400
  都当这个信号，避免真实请求错误被悄悄吞掉重试一次同样失败的请求），
  剥掉这个字段重试一次——`_rejects_temperature()` 精确识别这一种错误。
  这样本地模型、以及未来任何支持自定义 temperature 的 GPT 模型完全不
  受影响，只有真的不支持的模型会走这条兜底，不用维护一份"哪些模型是
  推理档"的名单（那份名单肯定会过期）。
- `stream()` 那边更麻烦一点——流式响应的 body 不会自动缓冲，判断是不是
  这个错误得先 `aread()` 拿到内容，判断完再决定是照常消费 SSE 流还是
  剥参数重开一次新的流式请求。

**验证**：新增 `test_llm.py`（5 项：`_payload()` 用
`max_completion_tokens`、`_rejects_temperature()` 精确识别目标错误/
不误伤其他 400/不误伤非 400/JSON 解析失败时优雅返回 false），全量
`pytest -q`：181 项全过。真实调用 `complete()` 和 `stream()` 都对
`gpt-5.6-luna` 跑通，重试兜底透明生效（调用方看不到任何错误，直接拿到
正确结果）。顺带量出真实速度差距：GPT 单次调用 **3.4-3.6 秒**，本地
模型同类调用是 **40-46 秒**——快了一个数量级，`[16]` 里因为太慢没跑完
的全量实体去重验证，现在用 GPT 应该几分钟就能跑完。

### [19] 全量实体去重跑完 + 抽取时纠错的小规模试验：先出了个真 bug，修完才勉强算干净

**全量 1614 候选跑完（用 GPT，`[18]` 量出来的速度换来的）**：107 组
合并建议，人工过了一遍，发现真误伤——`美国→美光`（之前拼音单独验证过
不该合并的那对，批量条件下又判错了）、`四号→十号`（日期）、
`贵州→广州`（真实不同城市）、`大疆→大厂`（品牌 vs 泛义词），
`elisa→alisa` 也是之前单独测过应该拒绝的对。这批**没有应用到真实
账本**——如实报给用户，没有因为"抽样验证过没问题"就默认全量也没问题。
判断质量在大 batch（60 对/批）下明显打折扣，是这条后处理路径本身的
局限，不是这次改的东西。

**用户追问**：为什么不直接改 `EXTRACT_PROMPT`，从源头上不让 ASR
听岔的词被写成新实体？之前以为"KITE 的 prompt 不能碰"是错的——翻出
`kite_extract_profile.py`（这次会话之前就有的文件，这次才真正读完）
证实这条路径确实开放：`memoket_kite.remember.DEFAULT_MEMORY_PROFILE`
是按模块级名字读的，重新赋值即可换掉抽取 prompt，已经用过两次（强制
content 跟随原语言、强制 topic 具体化）。照着 `_ORIGINAL_X_RULE`/
`_STRONGER_X_RULE` 这个既有模式，加了第三条规则："entities" 字段前
先比对 KNOWN ENTITIES，读音/拼写接近且没有上下文理由证明是不同东西
时，用已知实体码而不是新造一个。

**先小规模试了两个没入库的真实会议**（`terrence_records/1833.json`
`1838.json`，59 个从没跑过抽取的会议里挑的），不写回真账本、只读
真实 vocab 做上下文——第一版就出问题：`1838` 里 `applewatch` `woop`
`kol` `ai` `安克` `levy` 这些**已经在 vocab 里存在的**实体，AFTER
版本直接从输出里消失，不是换成已知码，是整体没了；`1833` 事实条数从
14 降到 10。规则里写的是"接近已知就替换"，但模型实际执行成了
"不确定就不写"——过度抑制，不是纠错。

**排查还带出一个方法论坑**：拿 `gpt-5.6-luna` 重复跑同一个 BEFORE
（代码没改），两次输出的实体集合完全不一样（9 个 vs 23 个）。查
KITE 自带的 `providers/llm.py._http_llm()`：凡是匹配 `gpt-5|o\d` 的
模型，`temperature` 字段整个不发送（推理档模型不接受自定义值），这条
调用路径不像我们自己 `app/llm.py` 那样有 `[18]` 做的重试兜底，直接
落到 API 自己的默认采样——GPT 这条路径本身就不是确定性的，没法用来做
干净的 A/B 对比。改用本地模型（`muse-glimmer-30b`，这条路径上
temperature 真的是 0）重新跑同样的两个会议做对比。

**改紧规则措辞**（"这条规则只增加替换，从不删除你本该写的实体；没有
接近匹配不是不写的理由"）后用本地模型重测：`1838` 的实体集合两版
完全一致（没有再吞掉已知实体），`1833` 有条目差异但看着更像本地模型
本身的 chunk 级抽取波动（数字、日期这类），不是新版本重复出现"删除"
这个特定故障模式。**没有观察到规则清楚地把 ASR 听岔的词正确纠正成
已知实体码**的干净证据——小样本 + 本地模型这次抽取行为本身就有波动，
不足以说这条规则在起作用。

**结论**：过度抑制的 bug 已经堵住，但"小规模看效果"要看的正面效果本身
还没在真实数据里清楚复现，判断没到能推全量的程度。规则改动留在
`kite_extract_profile.py` 里（3 条 patch，全量 `pytest -q`：181 项
全过，没引入回归），但先不接入真实 ingestion 脚本跑更大样本——留给
用户决定是继续多测几个样本、还是这条抽取时纠错的路子先放一放，退回
已经验证有效的批量事后合并（带人工过一遍误伤）这条路径。

### [20] 抽取时纠错这条路子被用户叫停重定向：目标不是纠错，是降低影响

**用户纠正了方向**："我只是希望你在 extract_facts 这一步考虑可能存在的
asr 错误，并且有相应的 mechanism 去减少这种错误带来的影响，不是让你
修正 ASR 的错误，我们的目标不是为了降低 WER"——`[19]` 里那条"读音接近
已知实体就替换"的规则，本质上是在做纠错（改写成"正确"的词），跟目标
不是一回事，而且已经被受控实验证伪（0/6，两个模型都是，根因是
`entities[:80]` 按字母序截断把 memoket 自己都挤没了）。

**换了个不需要纠错的机制**：改的不是实体文本，是每条 fact 已经在产出
的 `"conf"` 字段（high/med/low）——这个字段本来就真实接入检索排序
（`Where.conf_min` 过滤 + `0.2 * conf` 排序加权，`[14]` 系列早就确认
过），只是 stock prompt 从没告诉模型"这是语音转写，文本本身可能是错
的"这件事。改成：事实依赖的某个具体名字/词/数字听着像是听岔的片段时，
`conf` 标 low，不去猜"正确"应该是什么、原文照抄。不改写、不丢事实，
只是让知识库自己的置信度信号更诚实。

**受控配对实验**（同一句话结构，只换供应商名字：安克=真实已知品牌 vs
格瑞诺拉=编的、听着像音译但查无此词——6 次/组，`gpt-5.6-luna`）：
- 不加规则：两边全部 6/6 high，完全不区分。
- 第一版规则：格瑞诺拉 6/6 low（信号真实存在），但安克也被拖累到
  3 high/2 low/1 med——过宽。
- 收紧措辞（明确"已在 KNOWN ENTITIES/TOPICS 里的词不算理由"）：安克
  稳定 6/6 high 不受影响，但格瑞诺拉也回到 6/6 high——信号直接消失，
  过窄。
- **两版之间来回调了一次就没有再继续抠字**：目标是"降低影响"不是"精确
  校准"，第一版虽然有噪声但方向是对的（怪词的 conf 显著更低），比不
  区分的基线好；`conf_min` 默认是 "low"（默认不过滤），排序加权只是
  个小幅调整，误伤正常事实的代价可控。留的是第一版措辞。

**结论**：这条机制留在 `kite_extract_profile.py` 里（`pytest`
181 项全过），符合用户澄清后的真实目标——不纠错、只降低 ASR 噪声对
知识库置信度信号的污染。想要更精确的版本需要比一句 prompt 规则更结构
化的东西（比如 `[19]` 提到的 RLLM-CF 那种专门的置信度校验子任务），
不是靠继续改措辞能磨出来的，先不做。还没接入真实 ingestion 跑更大样本。

### [21] 用户纠正判断：entity 归并不是纠错，两个真 bug 叠加修好后，纠错真的生效了

**用户推回来了**：`[20]` 里我把"读音接近就归并到已知实体码"这条规则
整个撤了，理由是"目标不是纠错、不是降 WER"。用户指出这个判断错了：
`entities` 字段本来就是 `register_entity()`（`core/vocab.py`）产出的
归一化编码，不是原文——判断"memokat"和"memoket"是不是同一个真实
实体，改的是编码指向哪个已有码，不改 `content` 里写的是什么，根本不
属于"修正转写内容"。

**同时用户指出了第二个问题**：KITE 自己的 `_extract_facts_with_cache()`
把 KNOWN ENTITIES 提示词做成 `sorted(vocabulary.entities)[:80]`——
terrence 的 1958 个实体里有 105 个是纯数字（"100美元""10号"……），
数字排序在字母前面，正好把 80 个位置全占满，真实命名实体一个都进不去。
仅"memoket"这一个产品名，就已经在 vocab 里裂成 **61 个不同实体码**
（memocat/memocad/memokit/memokai/memo_kid……），15062 条 fact 里只有
27 条打对了 memoket 这个码。

**两步修复**：
1. `kite_entity_candidates.py`（新文件）：把 KNOWN ENTITIES 从"字母序
   前 80"换成"跟这段对话粗匹配的候选"——每个已知实体的 code/原名/别名
   跟本次对话文本算字符 bigram 重合度，命中原文子串直接算满分，取前
   80。这个逻辑在 KITE 内部函数里内联，没有 profile 级别的覆盖点（不
   像 `EXTRACT_PROMPT` 那样是个模板字符串），只能整个函数替换模块绑定
   ——比 `kite_extract_profile.py` 那种"换个模板字符串"要重得多，加了
   版本号校验（`memoket_kite.__version__` 对不上就拒绝装，不静默生效
   过期补丁）。修完验证：同一个 memokat 场景下"memoket"从压根不在候选
   里，变成排在第 6 位（80 个里）。第一版实现里 1-2 字符的垃圾实体码
   （"a""m"这种早年抽取产生的脏数据）靠"精确子串命中"刷到最前面，加了
   长度下限堵掉。
2. 重新把 `[19]` 里因为方向判断错误而撤掉的"读音接近就归并"规则加回
   `kite_extract_profile.py`（措辞是当时已经验证过不会误伤的版本——
   "只增不减"，不会因为不确定就把已经该写的实体删掉）。

**受控实验，同一个 memokat 场景，6 次/组，`gpt-5.6-luna`**：
- 只加候选修复、不加归并规则：0/6，模型看得到"memoket"在候选里，但
  没有指令告诉它可以做这个判断，照样原样抄"memokat"（一次还写出了
  第三个变体"memoket_product"，反而更糟）。
- 候选修复 + 归并规则一起：**6/6**，全部正确归并到"memoket"。

**真实会议回归检查**（GPT，用 1838 这个第一版规则曾经导致实体整体消失
的会议，跑 3 次）：`applewatch`/`auraring`/`woop`/`kol`/`aaron`/
`terence` 全部稳定出现，事实条数稳定在 12-13（不是当年那次 14→10 的
异常掉档），没有复现过度抑制。

**结论**：两个真 bug（KNOWN ENTITIES 提示词被数字垃圾占满 + 归并规则
被错误撤掉）叠加在一起，才是"model 明明能看见已知实体、却完全不归并"
的真正原因。`pytest` 181 项全过。全量重跑 171 场会议的抽取（用户要求
换用 `gpt-5-nano`，不用 luna）还没做——是否现在启动，以及是否要顺带
清理 vocab 里已经存在的 105 个数字垃圾实体，等用户决定。

### [22] Entity 质量怎么评估：频率被推翻，改成"垃圾形状 + 近邻碎片化"，拿真实标注测了一遍

**用户推翻了频率指标**："频率不能说明问题，有些时候低频的重要性反而
更高"——一个真实、只发生过一次的重要事实（比如某次会议提到的一个关键
人名），跟 ASR 造出来只写过一次的碎片，在"引用次数"上长得一模一样。
频率区分不了这两种，撤掉。

**真正该看的信号是"有没有近邻"**：一个实体如果跟 vocab 里另一个已建立
的实体音近/形近，大概率是同一个东西被拆开了；如果找不到任何近邻，才
更可能是真实、独一无二的内容。KITE 自己就带了这个候选生成器——
`Vocab.entity_merge_candidates()`（containment 或 hamming 距离 1，数字
类实体直接排除），`[15]` 的 1614 候选就是用的这个，不是新写的东西。

**建了真实 ground truth，不是拍脑袋**：把 memo*-家族的 61 个候选实体
逐个翻出对应的真实 fact 原文核实（不是靠"包含 memo"这个字符串模式硬
猜）——确认 `apple_memo`/`iphone_memo` 是苹果自己的 Memos 应用、
`memory_os`/`supermemory`/`memory_power` 是对比提到的竞品，都是真实
存在的不同东西，不是 memoket；另外发现两个长达 46/53 字符的"实体"
（`world_first_memory_powered_wearable_ai_agent` 这种）其实是营销话术
整句被误当成实体码收进去了——不属于数字/过短，是之前的形状过滤器漏掉
的第三种垃圾（新增"过长"判据）。

**跑出来的真实结论，不是一次到位的**：
- 形状类垃圾（数字开头/过短≤2字符/过长>30字符）：1958 个实体里 546
  个，27.9%。
- 直接用 KITE 原始候选对（1614 对）做连通分量聚类：memo*-家族
  recall=100%（51个真实成员全聚进同一簇），但 precision 只有
  91.1%——`mem`/`memo` 这两个 3-4 字符的垃圾词根本身也是 vocab 里的
  实体，度数（候选连接数）68/61，是所有实体里的极端离群值，靠
  containment 把 memocat（真产品）和 memory_os（真的不同产品，只是字面
  上包含"memo"）错误桥接到一起。
- 只砍掉这两个极端离群 hub 后重跑：precision 升到 97.4%，但 recall 掉
  到 74.0%——13 个真实成员（memo_kit/memokai/rmemory……）只有通过
  mem/memo 这两个 hub 才连得上主簇，砍掉 hub 顺带把它们也切断了，这是
  真实存在的 precision/recall 取舍，不是能免费拿到的。
- 追了剩下唯一的一条泄漏（apple_memo）：不是 mem/memo 的锅，是 `app`
  （度数 18，在阈值之下）桥的——`app <-> apple_memo` 和
  `app <-> memocad_app`。度数 13-20 这个区间同时混着真垃圾词根
  （app/pro/gem）和真实人名（vocab 里有一批"X哥"昵称，安哥/李哥/赵哥
  ……都是不同真人，因为共享"哥"这个后缀字符，度数也落在 13-18），纯
  结构阈值分不开，只能留给 `[15]`/`[16]` 已经建好的 LLM 投票裁决——
  这不是没调好，是这个区间本来就需要语义判断，两阶段架构（便宜的确定性
  候选生成 + 贵的 LLM 裁决）设计对了，边界现在有实测数据支撑。

**产出**：`backend/scripts/eval_entity_quality.py`，可重复运行的健康
检查脚本，跑一次报告：形状垃圾占比、聚类前后的"有效去重实体数"估计
（清洗后 1410 个实体聚成 844 个连通分量，压缩比 40.1%）、以及在
memo*-family 这个真实标注样本上的 precision/recall。`pytest`
181 项全过（脚本是独立评估工具，不影响现有代码路径）。

### [23] 全量重跑 200 场会议：GPT 账号 credit 耗尽中途改本地模型，结果是真实但不完整的改善

**用户下达全量指令**："现在跑200个session就可以了"，随后"要并行啊"推动
从原本设想的"跑一次全量脚本"改成真正的并行架构。

**并行不是简单开线程**：`kite_writer.py` 的 `write_lock()` 文档原话——
KITE 的 `Memory.remember()` 全量重写 XML、不加内部锁，我们自己的
`UserMemory.remember()` 把整个"读+抽取+写"都包在一把锁里，慢的那部分
（LLM 网络调用）也在锁内——直接开多线程只会排队等锁，不会真的并行。
真正能安全拆开的地方是 `memoket_kite.storage.append_session()`：这个
函数落盘时会重新读一遍当前磁盘状态，`_refuse_stale_vocab()` 发现自己
抽取时用的 vocab 已经过期就直接拒绝写、不会覆盖——fail-closed，不会
静默丢数据。所以设计成：抽取（慢，网络）在锁外并行跑，落盘（快，本地
I/O）在锁内串行跑，撞上"过期"就重试。新写了
`ingest_terrence_corpus_parallel.py`。

**调参踩了两轮坑**：
1. 6 worker、4 次重试、无退避：20 个里失败 6 个（30%）——竞争太激烈，
   重试次数不够就放弃了。
2. 4 worker、8 次重试、指数退避+抖动：79 个里只失败 4 个（5%），可接受
   （失败是安全丢弃不是数据损坏，脚本本来就幂等，事后能补跑）。

**全量正式跑，用 `gpt-5-nano`（用户指定），跑到 60/2427 时账号
credit 耗尽**——查了真实 429 响应体，不是限流是
`"code": "credit_balance_exhausted"`，账户余额真的是 0，不是我们代码
的问题，也不是重试能解决的。当场停下，没有继续空转浪费时间。用户决定
换本地模型（`muse-glimmer-30b`）继续，不等充值。

**本地模型验证＋全量执行**：8 场会议、117 个 chunk 小样本先测，
0 失败，确认稳定后启动全量（3 worker，复用已完成的 117 个 session，
不用重跑）。**总耗时 920.3 分钟（约 15.3 小时）**，2427 个 chunk，
2300 个成功、10 个失败（0.4%），19359 条新 fact。跑完后用同一条命令
再扫一遍（脚本幂等，只会重试那 10 个失败的），10 个全部补上，
0 失败。

**最终结果，不是完全解决，是真实但有限的改善**：
- 总量：2427 个 session，20361 条 fact，1203 个实体（旧库是 1958 个
  实体对应 15062 条 fact——新库覆盖更完整，实体总数却更少，方向是对
  的）。
- memoket 产品名家族碎片：**29 个变体，旧库是 61 个**——少了一半多，
  但没有收敛到 1-2 个。`memocat`（81 条 fact）依然比 `memoket`
  （28 条）本身更常见，说明这次修复起到了真实效果但没有完全解决问题。
- 最可能的原因，不是新猜的、是这次会话早前已经实测过的：
  `[21]` 那次受控实验里，同一个 memokat→memoket 归并测试，
  `gpt-5.6-luna` 是 6/6 全对，本地模型 `muse-glimmer-30b` 是 0/6——
  这次全量因为 credit 耗尽被迫全程用本地模型，效果打折扣是预期内的，
  不是新发现的缺陷。

**遗留**：账号 credit 需要充值；如果之后想用 GPT 补一轮针对性的实体
归并（比如单独对 memo*-家族这 29 个变体跑一次 `[15]`/`[16]` 已经建好
的事后合并流程），预期效果会比这次本地模型全量抽取更干净——但这是下一步
的决定，不是这次任务范围内。`pytest` 181 项全过。

### [24] 写作 harness 重构：抽成可开源的 writer_harness 包 + 原则打分闭环

**起点是用户读完一篇论文后的一个判断**：openJiuwen（华为的长程 coding
agent harness）能靠执行/测试通过与否当"完不完整"的客观 oracle，写作
没有这种东西——用户明确要求：不是照抄论文的具体机制，是要有一套显式的
principle/concept，用它给自己的执行结果打分，形成真正的闭环，而不是现在
`note_harness.py`（自报标记，命中率约 2/3）+ `writing_plan.py`（同样
自报标记，连独立核对都没有）这种拼凑。

**中途一个转折**：第一版计划做完之后，用户打断指出还要考虑"这个东西要
能开源"——参照这个仓库里 `memoket_kite` 的先例（纯逻辑、不绑定供应商、
不感知这个 app 的 FastAPI/sqlite，靠适配层接入）重新设计了一版：新增
`backend/writer_harness/` 独立可安装 Python 包（自己的
`pyproject.toml`/`src/`布局/`tests/`/`README.md`），打分维度做成可插拔
配置（`Dimension(name, guidance)`，包本身不认识 spine/beats 这类
memoket-note 专属词汇），LLM 通过注入的 `LLMClient` Protocol 接入。

**包提供的三块能力**：
- `evaluate()`——一次 LLM 调用，给一组维度打分（0/1/2），返回
  continue/complete/blocked 三态（`blocked` 是全新的第三态，之前完全
  没有）；`complete` 是程序化判定（每个维度都到 2 分），不再信任模型
  自己主观说"我写完了"——这正是要解决的问题本身。
- `find_repeats()`——纯 `difflib` 机械查重，不用 LLM，段落两两比对，
  排序/去重/限量后作为 `non_repetition` 维度的辅助证据喂给 evaluate()
  （对应论文的 LSP 被动反馈：便宜的机械信号，不用等模型自己发现）。
- `compact_context()`——长笔记的渐进式压缩，只用在续写 prompt（edit/
  evaluate 两步继续吃全量正文，压缩了会漏掉要抓的偏题/重复）。开发时
  第一版有个真 bug：内容本身很短（每节就一行）时，摘要+固定前言的开销
  比原文还长，"压缩"反而把东西压大了——加了"压缩后没变小就返回原文"的
  兜底，连带把测试用例的段落长度改真实（原来的单行测试数据永远测不出
  真实压缩效果）。

**App 侧改动**：`app/harness_adapter.py`（新）——`AppLLMClient` 包一层
`app.llm.complete()`，`SqliteRunHistoryStore` 落新表 `harness_runs`
（自测时抓到一个真 bug：`_now()` 是秒级精度，同一秒内连续几轮跑完会撞
时间戳，`ORDER BY created_at DESC` 排序不稳定——加 `rowid` 当 tie-break
修掉）；`note_dimensions()`/`section_dimensions()` 是 memoket-note 具体
的评分维度（从现有 `EDIT_SYSTEM`/`BEATS_COVERAGE_SYSTEM` 里已经写好的
原则提炼，不是新想的标准）。`note_harness.py`/`writing_plan.py` 的
round 循环都改成"续写 → 机械查重 → evaluate() → 按三态决定继续/完成/
卡住"，删掉了两处自报标记（`NOTE_HARNESS_DONE_MARKER`/
`SECTION_DONE_MARKER`）和唯一调用方已清空后变成死代码的
`llm.stream_marker_free()`。

**验证**：`writer_harness/tests/` 17 项（脱离这个 app 独立跑
`pytest`，这是"确实能拆出去开源"的验证标准，不只是 import 成功）+
`backend/tests/` 201 项全过（含新增的 adapter 测试）。前端
`tsc -b`/`vite build` 干净——`SkeletonPanel` 的节拍展示从"逐条勾选"
改成"聚合分数+一句话说明"（旧的逐条核对信号被吸收进统一打分后不再
存在，这是设计换来的代价，不是遗漏）；`note-harness`/`writing-plan`
两条 SSE 流都加了 `blocked` 态的前端提示。

真实端到端跑了一次（本地模型，GPT 账号 credit 还没充值）：建笔记 →
触发 `/api/note-harness/run` → skeleton 生成 → 自动修订 3 处 → 自动
续写 → evaluate 打出 4 个维度全部 2 分（没给 profile，`style_fit`
正确跳过）→ `status: complete` → `done` 事件 → `harness_runs` 表
正确落了一行记录。整条链路第一次真实跑通就是干净的。

### [25] "真实测效果"：evaluate() 能诊断，但一开始不能真的修好——找到根因并部分修复

**用户连续追问三次"真实的效果呢"**，倒逼出一次真正扎实的验证，不是
之前那种"机制跑起来了、分数看着合理"就算数：

**第一步，新旧机制直接对照**：把重构前的 `_beats_status`/
`BEATS_COVERAGE_SYSTEM`（这次会话里删掉的那份，从对话历史里原样
重建，没有 git 记录可查——这批代码这个仓库之前就没提交过）跟新的
`evaluate()` 喂**同一份真实内容**（terrence 一篇真实笔记跑完 1 轮
续写后的正文，里面确实有真实的跨段落重复）。结果：旧机制返回
`[]`（"全部覆盖"，会宣布完成），新机制正确识别 `non_repetition=1`，
`status: continue`——同一份真实数据，旧机制会在这里错误收尾，新机制
不会。`[24]` 里说的"旧机制完成判定跟重复脱钩"不是猜的，这就是证据。

**但被追问"效果"逼着往下测才发现**：诊断准≠能修好。同一篇真实笔记
放开跑 6 轮（不设短轮数上限），`non_repetition` 从头到尾没有改善过，
反而从 1 掉到 0——续写每轮都在加新内容，而这个继续写的 prompt 完全
不知道"重复"是当前最该注意的问题（`focus` 之前只传给了修订那一步），
新内容持续在已经重复的问题上再加一层，修订清理的速度追不上续写产出
的速度。这是"机制正确诊断问题、但没有阻止问题变严重"的真实反例。

**修复**：上一轮 weakest 是 `non_repetition` 时，这一轮跳过续写，改跑
第二次聚焦修订（两次独立机会清理同一处，不叠加新内容）。同一份真实
笔记重新跑 6 轮验证：不再往 0 掉，稳定在 1——是真实、可测量的改善。
**但没有全部解决**：6 轮内 `non_repetition` 从没到过 2 分。这份笔记的
重复是过去很多轮真实编辑积累出来的，对本地 30B 模型做增量式修补、
在这个真实难例上可能本来就不够——跟之前小样本合成测试里 1-2 轮就
`complete` 的干净结果不是一回事，真实、长期编辑过的内容显然更难。

**结论**：如实记录，没有说"修好了"。诊断层面的改进（`[24]` 的核心
主张）有真实证据支撑；修复层面（这次新加的"跳过续写"）也有真实、
可测量的改善，但离"稳定收敛"还有距离——是否要为这类高重复度的真实
笔记设计更强的修复策略（比如整体重写而不是增量打补丁），还是接受
"多轮打不平就诚实报告 forced/max_rounds、交给用户手动清理"这个现状，
留给用户决定。terrence 的真实笔记全程只用于测试、每次跑完都验证字节
级还原，没有真实数据被永久改动。`pytest` 201 项全过。

### [26] 写作工具全套真实验证：润色/重写/扩展上下文/校验/回顾

用户要求扩大验证范围——不只是续写/无限续写，"检验、润色、重写、扩展
上下文"这几个右键选中操作也要拿真实数据测一遍，"写的质量是最重要的"。
逐个用 terrence 真实笔记跑：

- **polish**：真实语法断裂句子（转写遗留的"洞察的不是"）被正确识别、
  干净修复，没有改变原意。
- **verify**：对笔记自身内部支持的判断正确留空 sources（fact_index=-1
  的情况），没有为了填字段硬编。
- **digest**：在真实数据上主动发现了知识库里的一处矛盾（同一个人
  "in Guangzhou" 又"live in Hong Kong"）——DIGEST_SYSTEM 完全没要求它
  专门找矛盾，"值得注意的变化"这个固定小节自己判断出来的，真实、意外
  的加分项。
- **expand 有真 bug**：对一篇已经写完整的真实笔记测 expand，
  before/after 生成的内容几乎原样复制了选中片段前后本来就有的邻近
  句子——两次独立测试都复现（一次是我选区切得不好导致的假阳性，重新
  用完整句子选区测第二次，同样的问题）。根因：EXPAND_SYSTEM 只说"不要
  重复选中片段本身"，没说"选中片段前后已经写了什么"。修复：
  `expand_user()` 显式把前后 150 字已有内容摆出来给模型看，
  `EXPAND_SYSTEM` 加"先看这里是不是已经写过了"。重跑验证：同一个已经
  完整的真实场景，正确改成留空；换一个真正稀疏、缺上下文的场景（笔记
  末尾一句孤立的断言），正常补充，不是被矫枉过正成"永远不补"。

**用户建议加"溯源"当第二校验维度**——查下来发现这个信号本来就有部分
在，只是没接好：`EDIT_SYSTEM` 的 JSON 契约本来就要求模型自报
`sources`（依据的事实原文），`/api/edit` 那条路径一直读对了，但
`note_harness.py` 的 `_run_edit_pass()`（harness 专用的自动修订路径）
漏了这一步——模型给了、代码没读。`expand` 更彻底，之前压根没有
`sources` 字段。两处都补上：`note_harness.py` 补一行读出模型自报的
sources；`expand` 的 JSON 契约新增 `sources` 字段 + 对应读取逻辑。
真实验证：同一个稀疏 expand 场景重跑，`sources` 精确报出了实际用到的
两条知识库事实原文，逐字核对跟 `recall()` 直接查出来的事实一致，不是
编的。前端 `RevisionPanel` 本来就会渲染 `sources`（"依据：..."），这次
是把数据真正传到位，`onRevision` 的 toast 也带上了依据摘要。

**遗留，没做**：`sources` 是否要提升成 `writer_harness.evaluate()` 里
一个独立的、结构化（不是纯 LLM 判断）的校验维度——比如检查"正文里每个
需要依据的具体断言，有没有对应的可追溯来源"，而不是现在
`factual_grounding` 这种整体性的 LLM 自评。这个改动更大，留给用户决定
要不要做、什么时候做。全量 `pytest` 201 项、`tsc -b` 全过。

### [27] 压测暴露一次真实数据事故 + non_repetition 的机械查重修复有真实效果

**用户要求大量压测（目标 100 轮）**，逼出了两个真发现：一个是测试
基础设施本身的真实事故，一个是 harness 修复的真实效果数据。

**真实数据事故**：新写的压测脚本 `backend/scripts/harness_stress_test.py`
每轮循环"备份真实笔记 → 跑 harness → 还原"，靠 `try/finally` 保证还原。
第一批跑的时候，有一次进程被外层工具的超时机制直接杀掉（不是 Python
异常，是进程被杀），`finally` 根本没机会执行——终端笔记（27f255a67662）
的原文永久丢失，压缩前 2549 字，事后发现变成了 3950 字且无法还原（翻遍
临时文件、任务日志、Time Machine 本地快照都没找到原文）。问过用户，
决定保留现状、当作既成事实，不再处理这篇笔记本身。

**根因**：脚本自己的时间预算（550s）跟外层调用超时（560s）只留了 10
秒安全边际，一次卡在中途的本地模型调用就能把这点余量吃光；更根本的是
`try/finally` 只在进程活着的时候有效，进程被 SIGKILL 直接杀掉时没有
任何 Python 级别的清理机会。修复：备份改成先落盘（不再只存在内存里），
成功还原后才删这个文件；脚本一启动先检查这个文件在不在，在就说明上次
没善终，先把那次的备份恢复回去再开始新一轮——不依赖"这次调用一定正常
退出"这个假设。外层调用超时也从"只留 10 秒"改成至少留 2 分钟余量。
27f255a67662 暂时移出压测池子，不再碰它。

**non_repetition 收敛效果，这次真的验证了**：`[25]`/`[26]` 的问题是
"续写模型每轮加新内容、修订跟不上"，`_run_edit_pass()` 加了机械查重
（`find_repeats()`，不用 LLM）给具体候选重复段落对，不再让模型每轮
自己从头找。用固定的三篇真实笔记（换掉了丢失原文的那篇）+ 种子笔记 +
一个 writing_plan 分段，跑了 26 轮、12 个独立会话：

- 8/12 会话（67%）在时间预算内收敛到 complete
- 内容简单的笔记（c3464ab74c7d，2029 字）**3/3 次全部收敛**，2-3 轮
  内稳定到 complete——这是这个笔记本次会话第一次真的被观测到完整收敛
- 曾经"从来没收敛过"的那篇长笔记（92d07b760f1e，4430 字，`[25]` 里
  连续 6 轮卡死的那篇）这次 3 次独立会话里**收敛了 1 次**（1→1→2，
  3 轮 complete）——不是每次都能修好，但从"从来没成功过"变成"偶尔能
  成功"，是真实、可测量的改善
- non_repetition 依然是绝对主导的瓶颈——18 次"当前最弱维度"里 16 次
  是它（89%），且更长、更复杂的真实笔记（e78306202d78/92d07b760f1e）
  收敛率明显低于简单笔记（各 1/3 vs 3/3），符合直觉：篇幅越长、历史
  编辑轮次越多，累积的跨段落重复越难在几轮内彻底理清

**结论**：机械查重修复方向正确、有真实效果，但没有彻底解决——内容
简单的笔记基本稳定收敛，内容复杂、历史悠久的真实笔记依然经常需要靠
`STALL_ROUNDS_CAP`/`MAX_ROUNDS_CAP` 这两个安全网兜底而不是真正
complete。是否要为这类"篇幅长、历史编辑多"的笔记设计更强的修复策略
（比如篇幅超过某个阈值时改用整体重写而不是增量打补丁），留给用户决定。
没有跑满 100 轮——中途发现压测本身的数据安全问题，修复基础设施优先于
凑够数字；26 轮虽然没到目标，但样本跨了 4 个真实/合成来源、覆盖了
"简单笔记""复杂笔记""writing_plan 分段"三种场景，足以支撑上面的结论。
`pytest` 207 项全过。
