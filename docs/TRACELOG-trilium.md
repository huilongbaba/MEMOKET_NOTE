# Trilium 化改造 — Tracelog

**范围**：用户指令「抄 TriliumNext/Trilium 的前端，适配我的功能，做成
Electron」「全部按照 Trilium 做」「做一个 150 轮的计划，全部改造完，并且我做
的功能要适配这种风格和交互格式」。计划见 `docs/trilium-migration-plan.md`。

**这份文档的用处**：每一次改动 + 它的测试情况记一笔，**包括没问题的检查**，
避免来回返工，也避免「看起来应该好了」。格式跟 `TRACELOG.md` 一致。

**许可证**：Trilium 是 AGPL-3.0，用户已明确本项目也会开源，因此直接复用其
代码可行。代价写在 `docs/desktop-plan.md`。

---

## [0] 真实数据的外部快照（改数据模型之前必须做的事）

`TRACELOG.md` 里记着两次事故：拿真实笔记跑自动化测试，**弄丢过两篇原文**，
两次都找不回来。当时的结论是——

> 必须有一份**独立于脚本自己运行状态的、外部的、只写一次的真原文快照**，
> 不能依赖「每个周期自己 backup-then-restore」这种自洽但没有外部锚点的机制。

这次改的是数据模型（文件夹变笔记、加 branches 表），比压测更有可能把数据搞
坏，所以先照做：

- `~/memoket-note-backups/20260910-192545/notes.sqlite3` —— 整库
- 同目录 `notes-markdown/` —— 42 篇导出成 markdown，**格式无关**，就算 sqlite
  schema 以后彻底变了，正文也还在
- 整个目录 `chmod a-w`，只写一次

**在仓库之外**，我后面的任何脚本都不会碰到它。

改前状态：notes 42（其中 6 篇正文为空，含 4 个由文件夹变来的）· folders 4 ·
branches 42 · writing_plans 49 · 迁移登记 `folders-into-tree-v1`。

## [1] 数据模型换成 Trilium 的 branches（已提交 6fe6b60）

**改了什么**：`notes` 表不再表达父子关系，树的边全在新的 `branches`
（note_id + parent_note_id + position）。一个笔记多条 branch = 克隆。
「文件夹」不再是一种东西，有子节点的笔记就是文件夹。

**迁移**：每个旧文件夹变成一篇空正文的笔记，**沿用原 id**，原来夹里的笔记
成为它的子节点。沿用 id 是有意的——`writing_plans.folder_id` 等外部引用不用
跟着改。

**测试**：新增 `tests/test_note_tree.py` 14 条；全量 690 条绿。两条防线各有
断言并反向验证过：最后一条 branch 不给摘、不许把节点移进自己的子树。

**路上的坑（值得记，差点白查一晚上）**：我给「看门狗被自己的日志弄死」写的
回归测试**自己把 pytest 干掉了**——它把 `os._exit` 换成记录函数并启动了看门狗
线程，而循环里 `os._exit` 之后没有 `return`；真实情况下那行不返回，被替换掉时
会返回，于是线程继续转。测试结束、monkeypatch 还原之后它拿到真的
`os._exit(0)`，**676 条只跑了 425 条就退出，退出码还是 0**，看起来像跑完了。
教训跟 TRACELOG 里那条同源：**测试环境比真实环境宽容，是这类 bug 的藏身处**。

## [2] 树的后端端点 + folders 删干净（第 1–15 轮）

**改了什么**
- 新增 `/api/tree`（整棵一次拿全）、`POST/DELETE /api/tree/branches`（克隆/
  摘除）、`PATCH .../move`、`PATCH .../expanded`、`GET /api/tree/paths/{id}`
- `create_note` 现在**同时挂树**——照 Trilium：笔记总是创建在某个父节点下，
  没有「建完再决定放哪」这个中间态。不挂就是一条用户看不见的垃圾数据
- `delete_note` 删**整棵子树**，返回被删的 id 列表；别处还有 branch 的克隆
  只摘边不删笔记
- `folders` 表、`notes.folder_id`、`routers/folders.py`、`tests/test_folders.py`
  **全删**；`writing_plans.folder_id` 改名 `parent_note_id`（值一个没动——
  迁移时旧文件夹沿用了原 id）
- 前端 `api.ts` 的四个文件夹函数换成六个树函数；`Folder` 类型删掉

**测试**：后端 682 绿（树 19 条 + 迁移 2 条）；前端 30 条 vitest 绿。
`test_folders.py` 里三条判据移植到了树上（按用户隔离、child_notes 的排除与
限量、以及**一个有意改掉的行为**：删文件夹从前保留里面的笔记，现在删子树）。

**踩的坑（这个文件自己警告过，我照样撞了）**
`_SCHEMA` 里加了一条引用新列名的索引 → 老库上 `CREATE TABLE IF NOT EXISTS`
是空操作，列还是旧名，索引当场 `no such column`。这个文件为 `idx_notes_folder`
写过一模一样的注释。索引已挪到迁移之后建。

## [3] 树控件 + 右键菜单 + harness 融入（第 41–60 轮，提前做）

**为什么提前**：`test_前端没有导出了谁都不用的东西` 抓到我写了四个树 API 却
没人用——右键菜单是它们唯一的使用者。与其挂着不如现在做。

**harness 怎么融入的**：直接长在树的右键菜单里——
「🤖 智能续写这篇 / ✨ 打磨这篇」对**一篇**，「🚀 对这棵子树无限续写」对
**一棵子树**，而树上本来就有这两个层级。禁用时带原因（「它只在这一个位置」
「它下面还没有笔记」），不是灰着不吭声。这是北极星判据 1 的直接落地：
一个按钮 + 一个可预期的结果。

**顺手修的真问题**：真库 18 篇有 15 篇标题字面就是「未命名」（旧界面建笔记
时的默认值），一列二十个「未命名」的树没法用。改成标题为空/占位时退回正文
首行——**只在显示层做，不写回库**：当成真标题存回去等于替用户起了名，而他
可能只是还没想好。

## [4] 两个自测设施（后面每一页都要用）

- `--user=`：Electron 新 profile 会造随机用户、看到空库，很容易误判成
  「数据没了」
- `--probe=`：右键菜单/弹层/分屏只有交互之后才存在，而发合成点击要系统
  「辅助访问」权限——不该为了自测让仓库主人去改系统设置。改成页面内探针，
  `?probe=tree-menu` 直接把界面驱动到那个状态。正常使用不带参数，那段代码
  一次都不执行

## [5] 六块外壳 + Trilium 令牌（第 16–40 轮）

启动栏 53px / 左栏 / 40px 标签行 / 中栏 / 右栏 / 状态栏，尺寸取自
`desktop_layout.tsx`（它自己写死的数字，不是我猜的）。

**主题只搬值不搬选择器**：`theme-next` 的 CSS 跟它自己的 DOM（`#root-widget`、
`.fancytree-*`、`body.layout-*`）绑得很死，整段搬过来会变成一堆永远不生效
的规则——那比不搬更糟，因为下一个人会以为主题已经在了。所以提取它的
**设计令牌**（亮暗两套配色值）写进 `shell.css`，再把老变量名指向这些令牌，
让整个应用一步吃到新配色。

状态栏放的是**当前在发生什么**（笔记数、harness 跑到哪、等你处置、后端
状态）——判据 3 要求自动化的过程看得见，一个只会转圈的指示器等于什么都没说。

## [6] 知识库：删掉问答框，能力封装成动作（第 96–115 轮，第一批）

**删了什么**：`MemoryPanel` 里那个「搜索或提问…」输入框 + 「提问」按钮。

**为什么**：判据 1——「界面上出现聊天输入框，就是我们没把意图封装好」。
但这里要分清一件事：**搜索不是聊天**。搜索框的结果是可预期的（匹配的记录），
而问答框要求用户自己组织 prompt，那才是要消灭的摩擦。所以搜索留着，
placeholder 从「搜索或提问…」改成「搜索知识库…」。

**能力没扔**：KITE 的时序推理恰好是痛点 13（汇总零散笔记时 AI 捋不清时间
线）的解药。新入口是**选中一段正文 → 右键「🕘 来龙去脉」**：

- 问题由后端 `/api/memory/trace` 拼，用户一个字都不写
- 结果落在右栏的「来龙去脉」标签里，**不是弹层**——判据 2：看一条旧记录
  不该离开这一页，弹层要么盖住正文、要么关掉就没了
- 每条事实可以一键插入正文（带 `[fact-id]`，为后面的行内出处 peek 铺路）
- `/api/memory/ask` 保留但 `include_in_schema=False`：它是 trace 和以后别的
  封装动作的底座，不再有对外入口

**测试**：`test_README里写的端点后端都有` 当场抓到 README 还写着
`/api/memory/ask`——契约测试正常工作。README 已同步（顺带补上笔记树那组
端点）。两端全绿。

## [7] 行内出处 peek（判据 2 最直接的落地）

正文里的 `[terrence-1872-5F8]` 变成可悬停的标记，浮层显示这条事实的原文 +
它的原始对话行。对标 Trilium 的 note peek / `openNoteInPopup`。

**为什么是浮层不是跳转**：跳走再回来，代价是那条思路（痛点 12）；浮层的
代价是「移开鼠标」，接近零。也直接对着痛点 13——汇总零散笔记时「有些事实
好像也不对」，事实旁边就是它的原话，对不对当场看得见，不用相信模型。

新增 `GET /api/memory/facts/{id}` 和 `UserMemory.fact_by_id()`。
**找不到明确 404**，不回空壳：一条指向不存在事实的引用是个真问题（模型编
的、或者知识库重建过 id 变了），静默当成没事等于把它藏起来。浮层里会说
「找不到这条记录——可能是引用写错了，或者知识库重建过」。

**实拍抓到的真问题**：第一版浮层几乎占满半屏。后端写了「最多三条原话」但
没限制单条长度，而一条会议记录原文能有几千字——浮层把正文盖住，反而违背了
判据 2（不打断当前这一页）。加了 220 字截断 + 换行压成空格，三条测试盯着。
**这个问题只有截图才看得出来**，测试和类型检查都是绿的。

出处识别的正则也有测试：认少了用户看不到出处，认多了正文里每个方括号都变成
点不开的假链接、还各打一次网络。写死「用户-数字-十六进制」三段形状，
`[2026-06]` 这种日期不会被误认。

## [8] 标签页（第 61–75 轮）

对标 Trilium 的 tab_row。尺寸取自它：单标签 100–240px，最窄 48px（再窄只剩
一个图标位、点不准），新建按钮 36px。中键关闭、⌘T/⌘W/⌘1–9（⌘9 是最后一个，
跟浏览器一致）。标签**持久化到 localStorage 按用户分**——关掉应用再打开桌面
还在，这是桌面应用的基本预期。

**为什么这个产品需要多标签**：痛点 10（笔记散在飞书和 Notion，想把两边交起来
写），以及写作时最常见的动作——对照着另一篇写。单篇编辑器逼着用户在脑子里
存住另一篇，那正是判据 2 要省下的注意力。

**两个 bug，都是截图才发现的**：

1. 我把 `setActiveTabId` 写在了 `setTabs` 的 updater 里面——而**这个文件在
   别处已经为同一件事写过警告**（liveContentRef 上面那段：「updater 必须是
   纯函数，在里面触发另一个 setState 是无效用法，React 19 StrictMode 还会
   双调用」）。实拍结果：连开三篇只出现一个标签。改成「哪个是当前」由一个
   effect 从 `current` 推导——一个真相，不用两处同步。

2. 探针的 effect 只依赖 `[tree]`，体内用的 `notes` 是旧闭包（那时还是空的），
   `notes.length >= 3` 静默跳过。修完才拍到三个标签。

两次都是 tsc 绿、测试绿，只有截图看得见。

## [9] 打包：装到没有 python 的机器上（第 126–140 轮）

**这是整件事里最后一个有真实失败风险的点**，所以做完就验。

- `backend/entry.py` + `backend.spec`：PyInstaller 把后端打成一个不依赖
  系统 python 的可执行程序（12MB 主程序 / 101MB 目录）
- `desktop/package.json` 的 electron-builder 配置把**前端和后端一起装进
  .app**（`extraResources`：web / backend）
- 打出 `MEMOKET NOTE-0.1.0-arm64.dmg`（181MB）

**验证方式不是「打包成功」，是真的跑打包产物**：

1. 先在 `env -i`（没有任何环境变量、没有 python）里跑冻结的后端——健康检查、
   首页、树接口全通
2. 再跑打包好的 .app——它自己拉起了**包内的**后端（
   `Contents/Resources/backend/memoket-note-backend`），接口全通

**实拍抓到的真 bug**：第一版打出来的 .app 是空库。查下去发现后端的
`kite_data_dir` 默认是相对路径 `./data`，打包后解析到了**.app 包内部**
（`Contents/Resources/backend/data/notes.sqlite3`）。这是个会丢数据的错：
macOS 的应用包在真实安装场景下只读，而且**更新时整个包会被替换，用户的
笔记跟着没**。

修法：Electron 用 `app.getPath('userData')` 把系统的用户数据目录经
`KITE_DATA_DIR` 传给后端，且只在 `app.isPackaged` 时传——开发时保持相对
`./data`，跟手工起后端用的是同一个库。重打包验证：数据落在
`~/Library/Application Support/memoket-note-desktop/data/`，**包内干净**。

七条回归测试盯着这些约定（`tests/test_packaging.py`）——它们不是「代码对不
对」，是「装到别人机器上会不会出事」，而那件事只有真打一次包才看得见。

## [10] 笔记 × 知识库：双向链接落地（第 141–150 轮）

设计见 `docs/kb-fusion-design.md`。这一批做的是骨干——**引用关系变成数据**，
而不只是正文里的一串字：

- `note_citations` 表随每次保存重建（`store.sync_citations`），树接口带
  `cite_count` / `ingested_at` / `preview`，左栏节点上直接看 ◆N 和 ⇡
- 摄入是后台任务：`_ingest_job` 结束时 `mark_ingested`，前端 `watchJob`
  结束刷树——不刷的话 ⇡ 要下次开应用才出现，用户会以为没存上、再存一遍
- ribbon 新增「知识库」标签（`NoteKbPanel`）：引用了哪几条、摄入状态、
  **找不到的引用**用红色报出来（模型编的、或者知识库重建过 id 变了——
  藏起来等于让一篇笔记建立在不存在的依据上）、每条依据「也引用于」哪几篇
- 反查端点 `GET /api/memory/facts/{id}` 与 `/citing`

**实拍抓到的问题**：`--probe=kb-tab` 往正文塞的两条假引用被 1.5s 自动保存
落进了真实笔记，连拍三次就多了三行「据 […] 所述」。修法：probe 模式下不
自动保存；顺手把已经污染的那篇清干净（sqlite 直接改，改前查过只有一篇）。

另一个小教训：假引用写成 `[terrence-9999-ZZZ]`，`ZZZ` 不是十六进制，正则
根本不认——所以「找不到」那条红提示这次没拍到。下一批换成
`[terrence-9999-FFF]` 再拍。

测试：后端 703 通过（+11 `test_kb_fusion.py`），前端 tsc + vitest + 11 条
check 脚本全绿。

## [11] Trilium 比对报告第一批：现在就是坏的六条（第 151–160 轮）

比对报告在 `docs/trilium-ui-gap.md`（11 严重 / 39 明显 / 58 细节，每条带
Trilium 的 `文件:行号`）。第一批按「现在会不会坏」排，不按好看程度：

1. **左栏没有滚动容器**——树塞在 `.left-pane-search` 里、外层 `overflow:hidden`，
   笔记一多底部被裁掉且滚不到。`.left-pane-body` 早就写好了但 0 处引用。接上。
2. **标签行溢出不可达**——一路缩到 48px 后再多就点不到。改成 strip 横向可滚
   （滚轮竖转横、不显滚动条）、下限 84px、间隙 5px；新建按钮挪出 strip；
   末尾 50px filler 做拖动区。
3. **`--bg` 语义倒置**——错指到 `--accented-bg`（Trilium 的「抬升面」，暗色
   `#555`），暗色下页面底比卡片还亮，遮罩是一块不透明中灰。改指 `--main-bg`，
   另立 `--surface-raised` / `--hover` / `--backdrop-color` / `--shadow-opacity` /
   `--accent-fg` / `--warn` / `--card-alt` / `--focus-ring`。后四个里有两个原来
   是**从未定义的幽灵变量**（`--card-alt` 被引 4 次），代码块底色一直不跟主题。
4. **`data-theme` 半坏**——shell.css 有 `:not([data-theme="light"])` 守卫，
   styles.css 是裸 `:root`，没有代码写过它。删掉守卫，两边一致只看系统偏好。
5. **红绿灯压住左栏**——`hiddenInset` 下只在 58px 宽的启动栏顶部留了 22px，
   红绿灯横向约 70px。照 Trilium 的处理：标签行提到最顶、整行宽，左端 72px
   spacer（仅 macOS，`html.is-mac`）。
6. **写作骨架 ribbon 永远默认收起**——直接违反判据 3。照 `Ribbon.tsx:46-51`
   加 `activate` 规则：换笔记时取第一个 activate 为真的 tab 展开。骨架有节拍、
   或正文有引用时自动展开。

顺手：尺寸抄**跑起来的** Trilium（theme-next 用 `!important` 盖掉了
desktop_layout.tsx 的 53/40）：启动栏 58、标签行 50、标签 36 全圆角 8 +
激活阴影、状态栏 28 走左栏色；树激活态换成「浮起来的卡片」（那四个
`--left-pane-item-*` 令牌抄来了从没接上）；标题行提出滚动区固定 50px；
标签名走共享的 `displayTitle`（三个「未命名」标签现在各叫各的名）；
`:focus-visible` 焦点环（此前全仓一条都没有）。

实拍：亮/暗各一张（`--dark` 标志强制 nativeTheme，不用去系统设置切）。
暗色层级终于是对的。

## [12] 比对报告第二、三批：结构性的和手感的（第 161–185 轮）

**第二批（越晚改越贵）**
- 左/右栏可拖宽（5px 把手，hover 才显形）+ 各自独立折叠（启动栏底部 « /
  右栏头部 »，⌘\ / ⌘⇧\），按用户持久化；focusMode 保留为「两个都收」
- 树的键盘导航：容器 `tabIndex=0` + roving focus，↑↓←→ / Enter / Delete /
  F2 / Home / End。**scope 问题的解法不是重构全局快捷键**：键盘事件挂在树
  自己的 DOM 上，天然只在树有焦点时生效——Trilium 的 note-tree scope 就是
  这个意思
- 树图标（叶子 ▢ / 文件夹 ▣ / 事实 ◆ / 分类 ▤）、hover 才出现的圆形「＋」、
  切笔记时激活行滚入视口、只在截断时给 tooltip、缩进 12+10/级

**第三批（补齐手感）**
- 树拖拽移动：行内 y 分三段 before/after/over，悬停 600ms 自动展开，
  drop 标记线 + 中空圆环照 tree.css:274-327。before/after 走新端点
  `PATCH /api/tree/branches/reorder`——整体重编号而不是相对插入：位置一旦
  有重复（克隆、老数据）相对插入不知道插到哪，重编号顺手把脏数据洗了
- `window.prompt` 全部替掉：`NotePicker`（带搜索 + 路径的选择器，对标
  move_to / clone_to）和 `TextPrompt`。Electron 里 prompt 是系统级模态，
  主题管不到；拍平成编号列表让人输序号的那个，上百篇后滚不动
- 标签右键菜单（关闭 / 关闭其他 / 关闭右侧 / 关闭全部 / 重新打开刚关的，
  禁用带原因）、拖拽排序、⌘⇧T、⌃Tab 轮换
- 前进后退：历史栈 + ⌘[ / ⌘] + 标签行左端两个按钮（TabHistoryNavigationButtons）
- ⌘F 页内查找：`@codemirror/search`
- 右键菜单：分组标题、`shortcut` 跟 `hint` 分槽、连续分隔线去重、上下键 +
  Enter、圆角 10 / 内边距 8 / 毛玻璃半透明底 / 破坏性项只染图标
- 滚动条照 forms.css:962-1044（细 thumb，hover 变粗）

**实拍抓到的**：选择器第一项的高亮不可见——`.palette-item.active` 用的
`--panel`，改完 `--bg` 之后它跟面板底色一样了。换 `--hover`。
`[terrence-9999-FFF]` 这回正则认得了，「1 条引用在知识库里找不到」红框拍到。

测试：后端 +3（reorder），前端 tsc + vitest 全绿。

## [13] 比对报告第四批：长出 Trilium 没有但我们需要的（第 186–210 轮）

- **知识库的可视化工具也是树上的节点**：`kb:overview` 总览、`kb:graph`
  主题地图、`kb:digest` 定期回顾。打开一张图跟打开一篇笔记是同一个动作
  ——开标签、进中栏、面包屑、前进后退全都白拿。`MemoryBrowser` 加了
  `embedded` 模式（不要弹层外壳），原来启动栏「浏览」那个弹层的内容原样
  搬进中栏。
- ribbon 右端 ⋯ 菜单（NoteActions）：导出 / 复制 / 存入知识库 / 专注模式 /
  保存 收进去，正文上方那行 `toolbar-secondary` 删掉——非 AI 的杂项不该跟
  magic tap 抢同一行的注意力（判据 1 的反面）。AI 动作行钉在滚动区顶部。
- ribbon 加「路径」（克隆时角标 = 几处，列出每条路径可点）和「信息」
  （创建/修改/字数/引用/摄入/位置/id）。
- 状态栏面包屑：当前笔记/虚拟节点在树上的位置，可点回上级。
- 快速查看（PopupEditor）：树菜单「快速查看」或 ⌥点击，浮层里只读看另一篇，
  正文和光标都还在原地——判据 2 的直接落地。
- 保存状态角标替掉「保存」按钮：存了淡出、出错变红不淡出。
- Electron 应用菜单：没有它 macOS 上 ⌘C/⌘V 这些 role 快捷键在 Electron
  里不生效；顺带 ⌘= / ⌘- / ⌘0 缩放。⌘J 做 ⌘K 的别名。

**实拍抓到的两个 bug**：
1. 虚拟节点的标签叫 `kb:graph`——探针在 kbRows 到之前就把标签开了。加了
   一个 effect：数据到了把还叫裸 id 的标签补成真名（恢复的标签同样受益）。
2. 探针污染又出现了：一篇笔记里 12514 字符全是「据 […] 所述」。上一批只拦
   了**自动保存**，而 `switchTo` / `openVirtual` 离开笔记前都会显式 `save()`。
   这回把拦截放进 `save()` 本身，再清一次库。教训：**同一条纪律要放在
   唯一的出口上**，放在某一个调用方那里迟早漏。

## [14] 第五批：特殊笔记、导入子树、收尾杂项（第 211–235 轮）

- **设置和 Skill 是特殊笔记**：`app:settings` / `app:skills`，启动栏点开是一个
  标签、进中栏，跟别的笔记一样对待——照 Trilium「选项是隐藏子树里的笔记」。
  两个面板加 `embedded` 模式，弹层外壳和 ✕ 按钮不再渲染。
- 导入 .md：一个文件一篇；**多个文件生成一棵子树**（「导入 日期」父节点 +
  每个文件一个子节点）。几十篇散在树根上没法收拾。
- 删子树先列出会删掉什么（`ConfirmDialog`）；单篇仍是乐观删除 + 撤销——确认框
  只给看不见后果的动作用。
- 树底部浮动工具条：定位到当前笔记 / 折叠全部。
- 主题地图内嵌中栏时随容器宽（ResizeObserver），不再写死 900。
- 右栏「浏览」改成打开「总览」标签（`open-virtual` 窗口事件），弹层版
  MemoryBrowser 不再有入口。
- 应用图标：PIL 画 1024 → iconset → `iconutil` → icns；electron-builder `mac.icon`。
  重新打了 dmg（181MB）。

**实拍抓到的**：
1. `kb:graph` 标签名还是裸 id——修名的 effect 只盯 `kbRows`，而标签在 kbRows
   到了之后才开，就永远等不到下一次。依赖加上 `tabs`，有 changed 守卫不循环。
2. 设置页单选框孤零零居中画在一行、文字掉到下一行：全局 `input { width: 100% }`
   把 radio 也吃了。`input[type=radio], [type=checkbox] { width: auto }`。

## [15] 分屏、自截图、以及一条真正的融合缺口：续写从来不带引用（第 236–255 轮）

- **分屏**：中栏右侧第二栏（Trilium SplitNoteContainer），树菜单 / 标签菜单 /
  ribbon ⋯ 三处入口，可拖宽、持久化。**第二栏是只读的**——「对照着另一篇写」
  要的是看得见，不是两个光标；编辑器状态是单实例的，做成可编辑要重构一半的
  App.tsx。虚拟节点也能进分屏（在分屏里看一条事实，「引用到笔记」直接插到
  左边正在写的那篇）。
- **`--shot=path` 自截图**：`webContents.capturePage` 存 PNG 后退出。这次
  `screencapture` 连拍三次都是桌面壁纸——窗口没在前台、`osascript` 没有辅助
  功能权限。自己截自己不依赖这些。
- **一条真缺口**：三条「找到并引用」的路径（@ 补全 / 相关记忆 / ⌘K）插的是
  `[事实：文字 · 日期]`，而后端 `store.cited_fact_ids` 只认 `[user-n-hex]`——
  于是用户手动引用的从来没算成引用（树上 ◆N 不涨、peek 不认、反向链接查不到）。
  更大的那半：**harness 续写的产出也从来不带引用**——喂给模型的材料是
  `[日期] 正文`，没有 id，模型想引也引不了。修法：
  1. `retrieval.format_fact` 输出 `[id] [日期] 正文`（跟 agent 工具输出同形）
  2. `facts_block` 在材料带 id 时讲引用规则（「句末原样照抄 [编号]」）
  3. 新判据 `citations_exist`：正文里的 `[id]` 要么在材料里、要么知识库里查
     得到，否则是编的——**可自动修**（摘掉那个 `[id]`，正文不动）。接进
     单篇和分段两条 harness
  4. 三条前端路径统一插 `原文 [id]`
- **收紧引用正则**：`[2026-01-01]` 也匹配旧正则（`01` 是合法十六进制）——
  判据会把正文里的日期当编造引用摘掉。用户名段必须以字母开头。**五处同步**
  （store / checks / fragments / factCite.ts / App.tsx），各自有测试。

测试：后端 730 通过（+9），前端 vitest +1。`test_harness_modes` 那段「踩满
所有毛病的正文」补了一个编造的 `[u-999-FF]`——否则新判据在那条「每条判据
至少触发一次」的断言下是空转的。

**真跑验证**（不靠测试靠产出）：起后端，对 terrence 的库调一次
`POST /api/magic-tap`（spine「众筹节奏要跟版本节点对齐」）。meta 里 6 条材料
都带 id；产出 298 字，**三处引用都是真的**：`[terrence-1872-5F8]`
「2.0 版本的时间先放 6 月 30」、`[terrence-1238-3F2]`「1.0 版本一个月补全」、
`[terrence-1462-1F8]`「众筹 plan 对齐」。存进笔记后树上会直接显示 ◆3、悬停
看出处、事实页反查得到这篇——整条「AI 写的每一句都有据可依」的链路第一次
端到端通了。

## [16] 收尾（第 276–300 轮）

- 全量：后端 730 通过，前端 tsc + vitest 36 + 11 条 check 脚本全绿。
- `--shot` 全套走查：标签行（亮/暗）、事实页（面包屑 `知识库 / 主题 / work / …`、
  原话折叠、相关事实）、树右键菜单。暗色层级正确，红绿灯让位正确。
- 重新打 dmg（后端的引用判据是这次之后改的），打包产物用 `--shot` 自截一张确认
  能起、能画。
- 记忆：`memoket_note_trilium_desktop.md`——截图手法、probe 拦截纪律、引用正则
  五处同步、台账位置。下次接手先读它和 `docs/PROGRESS.md`。

**没做完、明确写下的**：分屏第二栏只读；实体没类型；分类下 300 条上限。
都在 PROGRESS 的欠账里。

## [17] 用户反馈：「中间的画布这么粗糙吗」（2026-09-11）

装上 dmg 第一眼：编辑器是一个带边框的输入框、格式工具栏「流程图」掉到第二行、
AI 动作行「存入知识库」掉到第二行、正文一行拉满整个中栏。跟 Trilium 的
note-detail 差得远——它的正文就是画布，没有框。

改法：
- `.md-editor` 去框去底色；正文区限宽 880、跟标题行同一个左边距
  （`--content-margin-inline`），最小高度 60vh
- 格式工具栏：单行、图标钮 28px、`表格`/`流程图` 换成 ▦ / ⟁ 图标（tooltip 保留
  全名）、放不下横向滚不换行。**第一版没生效**：`.row { flex-wrap: wrap }` 在
  后面、特异性一样，盖掉了；用 `.row.md-toolbar` 提一级
- AI 动作行只留 harness 的三件 + 「逐轮我来定」；录音两个钮浮到正文右上角
  （Trilium FloatingButtons 的位置，半透明胶囊）；「存入知识库」已经在 ribbon
  「知识库」标签和 ⋯ 菜单里，不再占这一行
- 字数 / 阅读时长挪进状态栏右端

亮/暗各截一张核对。

## [18] 布局重梳理：「你这个布局根本就不是一个 note 工具」（2026-09-11）

计划在 `docs/layout-redesign-plan.md`（诊断 / 原则 / 总图 / 每个元素去哪 / 步骤）。
核心诊断：正文在第五层（ribbon → 骨架整块 → 两行 AI 按钮 → 格式工具栏 → 正文）；
右栏五种不相干的东西叠在 300px 里；图标是 emoji 和方块字符；一篇笔记两个名字。

做了什么（P1–P7）：
- **Boxicons**（Trilium 同款，MIT，Vite 打包离线可用）替掉全部 emoji / 字符画：
  launcher、树（笔记 / 文件夹 / 知识库 / 主题 / 实体 / 月份 / 会议 / 事实 / 工具各一个）、
  菜单、标签、按钮。launcher 顶部加 logo（SVG，跟 app 图标同一母版）、底部用户头像。
- **标题行**：图标 + 标题；标题是占位词时显示空、正文首行放在占位符里——跟树、标签
  用同一个 displayTitle。
- **ribbon** = 格式（默认展开，Trilium 的 classic toolbar 就这么放）· 引用（原「知识库」）·
  路径 · 信息。写作骨架从 ribbon 拆走。
- **浮动按钮**（正文右上角，Trilium FloatingButtons）：`续写` / `智能续写 ▾`（打磨、
  逐轮我来定在下拉里——它们是智能续写的参数，不是独立能力）/ `🎙 ▾`（插入正文 /
  存入知识库）/ `⋯`（存入知识库、分屏、导出、复制、专注、保存）。正文上方**不再有
  任何按钮行**。sticky 占一行：absolute 会跟正文滚走，height 0 会压住第一行——
  两个都实拍踩过。
- **右栏只有标签**：记忆（默认）· 目录 · 计划（骨架 + 运行合并；harness 跑起来自动
  切过来）· 修订 · 脉络。常驻区没了。
- **右栏「知识库」面板拆散**：导入 → 特殊笔记 `app:import`（launcher ⬆）；个人偏好 →
  设置；搜索 → ⌘K；阶段回顾 → 树上「定期回顾」；浏览 → 树上「知识库」。
- 打磨：按钮 hover 不再整体变蓝；文件选择框不用浏览器原生样子；右栏各面板的 h2
  降成 section 标题；`button.primary` 白字换 `--accent-fg`。
- app 图标按 Logo 同款重出，dmg 重打（182MB）。

亮 / 暗 / 事实页 / 设置 / 导入 / 空笔记各截一张核对。

## [19] 打包版白屏（2026-09-11 13:00）

用户报「挂了」。实拍：窗口一片白，后端还活着（对 OpenAI 的连接开着、库在
12:59 还在写）——渲染进程抛了未捕获异常，React 把整棵树卸掉。打包版没有
DevTools，日志里什么都没有。

- 加 `ErrorBoundary`（把错误摆在界面上 + 「重新加载」）和 `window.error` /
  `unhandledrejection` → `POST /api/client-log` → 打进 Electron 日志。**下次白屏
  会有原因。**
- 用 `--probe=harness` 跑了 100s 和 400s 两次智能续写（85 次落盘、两轮完成），
  没复现，没有 client 错误。根因未定。
- 顺手发现：无限续写的请求还在发 `folder_id`，后端迁移后只认 `parent_note_id`
  ——这个功能自迁移起就是 422。修了。
- **教训**：探针跑 harness 在**服务端**改笔记，probe 模式拦截 `save()` 挡不住——
  真实用户的「创业一年回顾」被写了两轮。用打包版 11:08 的库把改动过的笔记还原；
  探针改成只对 `shot-*` 用户跑，并加 ref 挡住 effect 的第二次触发。

## [20] 知识库体验：每个节点打开都是一页（2026-09-11）

计划 `docs/kb-experience-plan.md`。上一版把知识库拆成树上的节点，但节点打开之后
里面是列表——「可视化、查询都没了」。这次每个节点都是一页有设计的只读笔记：

- 后端 `kb/pages.py`（纯逻辑，手搭 Store/Vocab 测）：首页 / 主题页 / 实体页 /
  时间线（月→日）/ 会议页 / 某一天；路由 `/api/kb/dashboard|topic|entity|timeline|unit`。
  首页月度只给近 12 个月、主题/实体页近 24 个月——全史里几十个只有一两条的月份
  会把图压成一条线（实拍：主题页 2005→2026 一排扁条）。
- 首页：搜索框在第一屏（即搜即显，零 LLM）· 五个数字块 · 近 12 个月单色条形图 ·
  主题 Top（一级 + 闭包计数）· 实体 Top · 最近摄入 · 类型 / 说话人 chips（点进事实表
  带筛选）。
- 主题页 / 实体页 / 会议页：头 · 月度图 · 子主题 / 关系 / 共现 chips · 事实按月分组，
  每条 hover 出「打开 / 引用」（有正在写的笔记就插 `原文 [id]`，没有就复制）。
- 时间线：年跳转 · 月条形 · 展开到日 · 点一天看事实。日期只到月的一天标「日期不详」。
- 事实表：一行过滤器（类型 / 说话人 / 主题 / 实体 / 置信度）+ 分页；从首页 chips 进来
  自带筛选（id `kb:facts?kind=plan`）。
- 分类页：主题卡片网格、实体 chips（可搜、先给前 200）、会议列表。
- 事实页加主题 / 实体 chips（`FactPeekOut` 补 topics/entities）；右栏「记忆」卡片
  hover 出「打开 / 插入」；主题地图页的控件行对齐 `.filter-row`，新建主题收进开关；
  内嵌时不再显示那排 概览/主题地图/时间线/事实表 切换（都是树上的节点了）。
- 图按 dataviz 的规矩：单色序列（accent）、细 mark、圆角端、hover 有值、文字用文字色。

亮 / 暗各核对：首页、主题页、时间线、事实表、事实页。

## [21] 主题地图：默认只看到几个被裁掉的大空圆（2026-09-11）

实拍主题地图页：一百来个空心圆，没有名字，最大的几个占满画布还被裁掉一半。三个原因：
1. LOD 阈值（缩放 < 1.4 隐藏标签）是给两千个节点设计的；簇视图 111 个节点也被
   当成「先看形状」。改成节点 ≤ 150 时标签一直显示。
2. 半径 = sqrt(事实数) × 6，簇节点事实数上千 → 半径 465px。封顶 44px。
3. 「布局稳定后适应窗口」在 alpha < 0.25 时就做了，节点之后还继续外扩；再在
   simulation `end` 时适应一次，容器尺寸变化（内嵌时 ResizeObserver 晚一拍）也重新适应。

另：正文顶上那张「语音服务不可达」红卡片每一页都在，删了——状态栏右端已经有。

追记：修完标签和半径之后图还是一坨小点偏在左边。加了一条 client-log 才看清：
`fitTo W=320 H=520 box=3054x3051 scale=0.10`——上层量到的宽是 320（那个空 div
首帧没宽度），图按 320 布局、按 320 适应；而簇视图没有连线，斥力 -170 把 111 个
节点推成三千个单位宽。改法：图自己用 ResizeObserver 量 `<svg>`（width 100%），
小图改用「碰撞半径分开 + forceX/Y 向心」代替斥力。**教训**：截图看不出数值，
猜了三轮；一条日志一轮。内嵌时点节点 = 打开它的页面，不再切到藏起来的事实表 tab。

## [22] 细节走查（2026-09-11）

用探针把六个交互状态各截一张（树右键 / 引用标签 / 标签右键 / 删子树确认 /
选择器 / 分屏）逐个看：
- 分屏时中栏变窄，浮动按钮「续写」「智能续写」的字换行成两行。`.fb-btn`
  `white-space: nowrap`，正文区做 container，≤ 600px 只留图标。
- 「续写」钮一直没有图标：`bx-magic-wand` 在 Boxicons 2.1 里不存在，浏览器静默
  画空。换 `bx-edit-alt`，并写了一条循环把代码里用到的每个 `bx-*` 都去
  boxicons.min.css 里对一遍——其余全在。
- 其余五个状态没问题。

## [23] 自己跑智能续写：三个真问题（2026-09-11）

用户三条：主题地图不对、新建笔记「没存」、智能续写「好多没显示出来」。给探针加了
`--probe=harness:<noteId>` + `--shot-delay=a,b,c` 连拍，对一篇测试笔记跑了四次。

**agent 输出跟编辑器对不上（最严重）**：编辑器里 `[terrence-1848-5F6]` 变成
`-1848-5F6]`、`[terrence3-10F4]`。根因：客户端靠 `onRevision` 的 anchor 在**本地**重放
修订来追平服务端，anchor 一有歧义就改烂；而后端并不按轮落库，客户端自动保存的
正是这份烂的。修法：`STEP_FINISHED` / `RUN_FINISHED` 带上服务端权威正文，轮末直接
对齐；差异记 client-log（`harness-sync`）——对不对得上从此有数据，不靠感觉。

**修订「根本没实现、还截断了」**：修订 pass `max_tokens=1500`，一轮四五条修订每条
带一段正文，JSON 在第三条中间断掉，后面的全丢，面板里能看到 `"reason": "` 戛然而止。
预算加到 4000；截断时 `extract_json` 补尾、能解析的照用，并发一条「修订输出被截断」
让用户知道。

**白屏两个根因**（都在这次连拍里抓到）：
1. `roundDiffField.update` 用 `mapPos` 映射超出旧文档长度的 hunk 位置 → RangeError
   → React 卸整棵树。夹住位置。
2. `AgentActivity` 渲染 `r.toolCalls.length`，某些卡片字段没到齐。渲染一律兜底。

**显示**：删掉的整段大纲以划线原文堆在正文底部 → 超过 40 字折成「已删 N 字」小标签，
悬停看原文；浮动按钮滚动时压在标题上 → 改成带底色横带；一轮修订三四处弹四张
toast → 不弹，状态行说一句；判据文案全部中文；`Continue a whole note` 等 8 个 Mode
标签中文。单篇 / 分段模式加 `no_fake_charts` + `charts_from_tools`：模型自己写了一张
语法错的 mermaid，现在会被拦掉。

**新建笔记「没存」**：`newNote` 没刷树，新笔记不在树上；保存后树也不刷，名字一直
「未命名」。两处都 `reloadTree()`。

**主题地图**：簇视图没有边只是散点；簇之间按共享实体连虚线（每簇最强两个邻居），
节点填淡色，标签放宽到 18 字。

第四次连拍：10 轮跑完，无 client 错误，引用完整，`harness-sync` 无差异记录。

## [24] 知识库可视化加强（2026-09-11）

- 主题地图页工具栏重做：一行面包屑（全部簇 › 某簇 · N 个主题 · M 条事实），一行
  筛选（找节点 / 层级 / 实体 / 滤噪声，簇视图下层级筛选藏掉）；图内头部换图标钮，
  说明压成一行。
- **簇之间有边了**：两个簇共享的实体越多越相关，每簇取最强两个邻居画虚线；节点
  填淡色。之前的簇视图是一堆散点，看不出什么跟什么挨着。
- **主题页 / 实体页加「周围有什么」局部图**（Capacities 的做法）：这个节点 + 父 /
  子主题 + 共现实体，点节点进它的页面。一级主题有七八十个子主题，全画是一团
  小点（实拍 work）——只画最强的 12 个子主题 + 8 个实体，标题里说明。
- 亮 / 暗核对。

## [25] 主题地图「放大了缩回去、自己跳」（2026-09-11）

先按直觉修了两条：布局结束只自动适应一次（拖节点会 restart 模拟再触发 'end'）、
用户缩放过之后尺寸变化不再替他适应窗口、尺寸变化不重跑模拟只挪向心力——放大
后 30 秒截图仍然乱。加了两条 client-log 才看清：**每 1.5 秒 new simulation 一次，
三个依赖全变**——不是依赖变了，是组件**重挂**了。根因：MemoryBrowser 把外壳写成
render 里定义的 `const Shell = ({children}) => …`，每次 render 都是新的组件类型，
整棵子树（含力导向图）随 3 秒一次的轮询重挂一次。跟 [17] 的 SplitPane 是同一个
错误；这次全仓 grep 了一遍，没有第三处。

修法：外壳改成 `body` 变量 + 三元表达式。放大后 18s / 34s 两张截图完全一致。
`--probe=graph-zoom`：打开主题地图后模拟滚轮放大三档再连拍。

## [26] 全量巡检第 1–3 轮（2026-09-11）

用户要求「完整地、全量地检查整个交互、功能设计、效果 → 改进 → 测试 → 再提改进，
循环 1000 轮」。方法：每轮挑一组界面用 `--probe` + `--shot` 实拍，看图找问题，改，
跑 tsc / vitest / pytest，记这里。

第 1–2 轮（已在 afbac13）：新建后没写过一个字的笔记离开就删（不再堆一排「未命名」）；
知识库空库首页给「导入」引导；事实表筛选器最小宽度。

第 3 轮实拍抓到：
- **⌘K 面板打不开**：左栏放大镜按钮 dispatch 的 `open-command-palette` 事件从来没有
  接收方——只有发送方。补上监听，顺手把空态从「一个光秃秃的输入框」改成 Trilium
  jumpToNote 的样子：最近编辑 6 篇 + 前往（新建 / 知识库总览 / 主题地图 / 时间线 /
  导入 / 设置）；输入时命令也参与匹配；行内 emoji 换 boxicons，长标题单行省略。
- **magic tap 跑完了屏幕上什么都没变**：续写追加在文末，视口停在开头；六条来源
  摊在正文上方把标题挤出屏幕。现在每片到达就 scrollIntoView 文末；来源折成一行
  「引用知识库 · 检索到 N 条 · 查看来源 ▾」，带关闭钮；第一片落下前保证跟上一段
  空一行（之前黏在上一段末尾像同一段）。
- 时间线：sqrt 比例（1 月 10001 条把其它月压成一条线）；默认展开最近一个 ≥10 条
  的月；「日期不详」换「不详」不再折行。
- 选区菜单 emoji → boxicons；引用面板「也引用于」用 displayTitle（无标题笔记显示
  正文首行）；ribbon 面板限高 40vh 可滚。
- 跑测试时抓到两处**漂移**：smoke-graph 找「恢复」按钮而按钮早改成「恢复全部」；
  `test_directory_map` 对 harness-framework.md 第 3 节——middleware / checks / tools
  下 23 个文件没上图，frontend 那段被当成 python 目录树解析出三个幽灵文件。图补全，
  前端 / 桌面拆成第二个围栏。

## [27] 全量巡检第 4 轮：外观设置 + 桌面壳三处根因（2026-09-11）

实拍设置 / Skill / 导入 / 树菜单 / 暗色 / 空笔记。抓到：
- Skill 页标签写的是 `app:skills`——openVirtual 没人传标题时把 id 当标题。补一张
  `VIRTUAL_LABELS` 表（app:* 和 kb 各工具节点）。
- **没有外观设置**，暗色只能跟系统。加「外观：跟随系统 / 浅色 / 深色」（Trilium 的
  Appearance 打头）。界面暗色全靠 `prefers-color-scheme`，桌面壳里那个值由主进程
  nativeTheme 决定——加了第一个 preload（只暴露 setTheme 一件事），选择存
  localStorage，启动时再递一遍。
- 然后「选了深色、重开是浅色」，查了三层才到根因，每层都是真问题：
  1. **后端端口每次随机** → 页面 origin 每次不同 → localStorage 按 origin 隔离，
     标签页 / 分屏 / 右栏 / 知识库展开状态**从来没有跨启动保住过**。改成优先固定端口
     47231（等上一份 uvicorn 退干净，最多 5 秒），被占才退回随机。
  2. Chromium 的 DOM storage 是攒着落盘的，退出快就丢：before-quit 里
     `session.flushStorageData()`。
  3. client-log 证实同 origin 下存过 `dark` 重开却读到 `system`——leveldb 只允许
     一个进程持锁，**打包版还开着**，探针那份的 localStorage 静默变成内存版。
     开发 / 探针实例改用 `userData-dev`；正式版加 `requestSingleInstanceLock`
     （第二份把第一份窗口拉前台），两份同跑抢 sqlite 和 localStorage 的事从此没有。
  连拍验证：选深色 → 重开仍是深色，上一次开的「设置」标签也回来了。
- 设置页段与段之间加间距；选区菜单 / ⌘K 的 emoji 全换 boxicons。

## [28] 全量巡检第 5 轮：写作流（2026-09-12）

新探针：`slash`（文末打 `/`）、`mention`（打 `@样机`）、`pane:<id>`（右栏切标签）、
`--win=WxH`（窄窗）。实拍抓到：
- `/` 菜单和 `/` 提示框的 emoji 换 boxicons（bx-pen / bx-image-add / bx-table …），
  跟外壳一套。
- **@ 引用补全是 CM6 原生样子**：等宽字、列表宽到 900px、说明面板把整段原文拉到
  窗口外。加样式压过 CM6 主题（它的选择器带生成类名，高一级，要 `!important`）：
  系统字体、列表最宽 520、说明面板 340 宽 + 50vh 可滚。
- 窄窗（1000×700）树底部浮动工具条盖住最后一行：树列表补 44px 底边距。
- 探针启动不再恢复上次的分屏（localStorage 自 [27] 起真的能跨启动了，探针
  截图开始互相污染）。
- 正式版 47231 / 开发实例 47232 各用各的固定端口，同时开着也互不等待。

## [29] 全量巡检第 6 轮：ribbon 面板 / 写作计划 / 暗色知识库 / 大笔记（2026-09-12）

新探针 `ribbon:<tab>`、`plan-panel`、`big:<id>`（切到 3 万字笔记，client-log 报耗时）。
引用 / 路径 / 信息三面板、写作计划弹层、暗色下的知识库首页和主题地图都正常；
3 万字笔记切换无感。改动：路径面板底下加「克隆到另一个位置…」（Trilium 的
note paths 组件同款）；写作计划标题的 emoji 换 bx-rocket。

## [30] 全量巡检第 7 轮：搜索 / 多标签 / 智能续写全程（2026-09-12）

新探针 `search:<q>`、`many-tabs`（连开 10 个标签）。快速搜索结果卡、12 个标签的
收缩都正常。两次跑智能续写（60s / 150s / 再跑一次 150s）：轮次状态行、修订工具条、
表格渲染、节拍覆盖都对；harness_runs 记到 1 轮 complete、六维全 2。
抓到一处：**跑完之后只有一个 toast**，几秒就没了，用户回头看只剩「改了 1 处」，
不知道为什么停、跑了几轮。现在结果行留着：「内容已完整，自动停止 · N 轮 · +M 字」
带关闭钮，换笔记 / 再跑一次才清。

## [31] 全量巡检第 8 轮：新用户第一眼 + 快捷键表（2026-09-12）

用一个全新用户启动，中栏是两行灰字，第一印象就是一片白。现在没打开笔记时中栏是
一张「从哪开始」：Logo + 三张入口卡（新建 ⌘N / 导入 / 知识库——空库时引到导入）
+ 最近编辑 6 篇 + 四个最常用的键。快捷键一览 `⌘/`（Trilium Options → Shortcuts
的只读版），也在 ⌘K 的「前往」里；键表单独放 `shortcuts.ts`，改键要同步它。

## [32] 全量巡检第 9 轮：笔记之间的链接（2026-09-12）

Trilium 的核心之一——内部链接 + Referenced by——之前完全没有。现在：
- 语法用标准 markdown `[标题](note://<id>)`，导出到别处仍是合法链接；正文里打 `[[`
  搜标题补全（`editor/noteLinkCompletion.ts`）。
- 光标不在链接上时折成一枚带图标的标记，点一下打开那篇；光标进去展开成原文可改
  （`editor/noteLink.ts`）。⌘点击外链的老路保留，`note://` 走 `open-note` 事件。
- ribbon 新增「链接」标签：链出去的 / 链到这篇的两栏，角标是链出数。后端
  `GET /api/notes/{id}/links`，反链直接 LIKE 扫正文，不维护会漂的链接表；
  跨用户不可见（测试盯着）。
- 顺手抓到：打开一篇没骨架的笔记自动生成骨架时「智能排版」按钮跟着转圈——两个
  动作共用了 `loading === 'skeleton'`，拆开。

## [33] 全量巡检第 10 轮：历史版本（2026-09-12）

Trilium 的 Note Revisions 之前没有。现在：保存时正文变了、且离上一版超过 600s
（Trilium 默认同值）就把**旧**正文留一版，每篇最多 100 版；⋯ 菜单和 ribbon「历史」
里可「现在存一版」；点一版展开只读预览，「恢复到这一版」之前后端先把当前正文
强制存一版（reason=before_restore），恢复永远可逆。接口
`/api/notes/{id}/revisions[/{rid}[/restore]]`，测试盯着间隔、只改标题不留版、
恢复可逆、空正文 409、越权 404。踩到一处：同一秒存两版按 created_at 排序不稳，
加 rowid 兜底。

## [34] 全量巡检第 11 轮：链接悬停摘要 + 状态栏健康提示（2026-09-12）

- 笔记内链标记悬停 300ms 出摘要卡（Trilium 的 note_tooltip）：标题、日期、字数、
  正文前 240 字；同一篇只查一次；文末放不下就翻到上方（实拍被窗口底切掉）。
- 状态栏那行「⚠ 语音服务不可达 (http://…)」长期挂着像出了事故——语音是可选服务，
  没配 ASR 的用户是多数。现在 LLM 不通才红字常驻，语音只是灰色「语音离线」，
  悬停看地址。

## [35] 全量巡检第 12 轮：定向续写 + 三处 harness 硬伤（2026-09-12）

用户之前把「质量」压后：显示 / 同步问题解决了，现在回头看质量。拿草稿笔记对比
跑前跑后的标题：用户的标题一个没动（[revise 的 structure_intact] 在起作用），但
**所有新内容都堆在最后一节底下**——讲硬件延期的段落离「硬件」隔了两千字。
- **定向续写**（harness-framework §4.1）：模型第一行说「【放到：某标题】」，位置由
  代码算，前端按自己的正文重算落点、delta 直接插在那一节末尾并滚过去看。实测
  新内容分别落进「硬件」「上市日期…」「样机批次…」「5GG 切换…」各节。
- 探针连抓三处硬伤：
  1. **harness 跑在了另一篇上**：探针 setTimeout 里的 `runNoteHarness` 是旧 render
     的闭包，`current` 还是启动时自动打开的那篇。改走 `actionsRef` 永远取最新的。
  2. **A 的结果写进了 B 的编辑器**：onDone 的正文对齐没有 currentRef 守卫（一半
     handler 有、一半没有）。现在 `noteHarnessHandlers` 统一包一层：run 属于哪篇，
     用户切走之后它的每个事件都不碰当前这篇；onDone 只提示「已保存在那篇里」。
  3. **一轮删掉 4451 字**：骨架被另一篇的骨架顶掉后，修订按「不合骨架就是离题」
     把 4493 字删到 754。加 `too_destructive()`：单条最多删 35%、一轮累计 50%。
  另外修订从「另见 [链接](note://…)」中间切开留了个 `(note://…)。`，`breakage()`
  加「半截链接」判定。
- 起跑时 client-log 记「哪篇、多少字、骨架开头」；shot.sh 现在把每次探针的后端
  日志留在 `<name>.log`——没有日志只能猜，这一轮三处都是靠它定位的。

## [36] 全量巡检第 13 轮：从光标处续写 + 编辑器最小改动（2026-09-12）

- **「续写」现在在光标处写**（Notion / Craft 的「继续写」都是这样）：光标在文末就追加；
  在中间就把后文送给模型当衔接，写出来的段插在光标处（先退到这一段末尾）。
  后端 `MagicTapIn.following` + 提示词块。
- 探针第一次实拍插进了下一个标题的井号中间——原因是 `setContent` 的 updater 里维护
  游标：updater 的执行时机跟闭包里的游标对不上就漂。改成头尾定死、中间只增不改，
  每片直接算整篇；智能续写那边的 insert_at / delta 也一起改成从 `liveContentRef` 算。
- 第二次实拍位置对了，但第一段以「L样机」开头——client-log 与服务端对账证明是
  模型本身这么写的：后文块放在提示词最后、600 字处硬切到「…KO」，模型接着它最后
  看到的东西写。后文块挪到正文块前面、在段落边界截断。
- **编辑器外部更新改成最小 diff**（`editor/minimalChange.ts`）：原来是整篇替换 +
  光标挪到文末，流式往中间插一段时视口被拽走、装饰全量重算；现在只 dispatch 变
  了的那一段，选区由 CM 映射。
- 启动回到上次看的那篇（`memoket-note-active`），不再总是最近编辑的那篇。

## [37] 全量巡检第 14 轮：选区五个动作跑一遍（2026-09-12）

新探针 `sel:<action>`：选一段正文、触发校验 / 来龙去脉 / 润色 / 重写 / 扩展。
校验（右栏顶部「校验结果」卡：支持 + 依据 + 查看原文）、润色（行内红绿 diff）都对。
**来龙去脉直接 500**：`kite_memory.ask()` 取 `result.facts`，KITE 的 `Answer` 早已把依据
改叫 `evidence`——AttributeError 在生产里等于整个功能静默消失（用户只看到一个 toast）。
两个名字都认，`tests/test_kite_ask.py` 用假 Memory 盯住；kite-constraints.md §13 记下。
顺手：选区「自定义提示」的图标还是 emoji 字符串，`/` 提示框换成 boxicons 后它渲染成空。

## [38] 全量巡检第 15 轮：分段写作跟随视图（2026-09-12）

新探针 `plan-run:<parentId>`：没计划先生成一个，再对这棵子树跑。后端三段一段写完、
追踪笔记也建了，**但跟随视图里一个字都不显示**，跑完也是 0 字——
`api.runWritingPlan` 只认老的自定义事件名 `delta`，而 section 内的事件早就是
loop.run 直出的 AG-UI 名（`TEXT_MESSAGE_CONTENT` / `STEP_FINISHED`）。三条 router
切 AG-UI 时这一条的前端解析漏改了，又没有测试。现在认两套名字、`note_id` 从最近的
section-start 记；轮末 / 段末用服务端正文对齐（`onNoteContent`）；
`runWritingPlan.test.ts` 用假 fetch 回放一段 SSE 盯住映射。
来龙去脉出结果时右栏自动切到「脉络」（之前只有角标变了）。

## [39] 全量巡检第 16–17 轮：暗色回归 + 删除流（2026-09-12）

暗色下把这几轮新加的界面全拍一遍（快捷键表、历史、链接、欢迎页）：没有问题。
删除流按代码读出三处、探针 `delete:<id>`（先分屏打开再从树上删，4.5s / 12s 连拍）证实：
乐观删除只从 notes 里摘，**树上那篇还站着**，要等 5 秒真删之后下次刷树才消失；它的
标签留着（点了没反应）；分屏里还开着它。现在删除时树 / 标签 / 分屏一起收，撤销时
长回来；连子树一起删时提示「和它下面的 N 篇」。真删完成后刷一次树和列表。

## [40] 全量巡检第 18 轮：逐轮我来定 + 打磨模式（2026-09-12）

新探针 `review:<id>`（逐轮我来定）、`polish:<id>`。轮末暂停时用户多半已经滚到正文
底部看新写的内容，「接着写 / 到此为止」和那句话留在正文顶部等于没有（实拍只剩状态栏
一个「等你处置」）。现在暂停 / 运行 / 结果三种状态共用一条粘在浮动按钮下面的横条。
打磨模式跑完「+0 字」改成「正文没有改动」。

## [41] 全量巡检第 19 轮：暂停快照只留最新（2026-09-12）

两次「逐轮我来定」探针之后同一篇笔记攒了两份暂停快照。旧的那份恢复出来只会把
新内容盖掉——`save_snapshot` 现在同一篇只留最新一份（跨篇不影响），测试盯着。

## [42] 全量巡检第 20 轮：大库量（2026-09-12）

造一个 412 篇（12 个文件夹全展开）的用户实拍：树取数 59 ms、首帧 88 ms，快速搜索和
⌘K 都顺。搜索结果卡改了三处：第一行常常就是标题本身（「# 会议纪要 10」）不再重复
印；加上所在文件夹（几十条同名命中时唯一的线索）；置顶 / 删除的 emoji 换 boxicons。
`reloadTree` 在 ≥200 行时往 client-log 报取数 / 首帧耗时，以后大库量回归有数。

## [43] 全量巡检第 21 轮：存入知识库（2026-09-12）

新探针 `ingest:<id>`（用 shot-demo 用户，别污染真实库）：摄入任务 18 秒跑完，树上 ⇡
和知识库计数都跟上了。抓到一处：右栏「记忆」还写着「知识库里暂时没有相关内容」——
正文没变它不会自己再查，而刚入库的事实正是用户想看到的。摄入完成后让它重新召回。
README 功能表补上内链 / 历史版本 / 定向续写；PROGRESS 加「全量巡检循环」一节。

## [44] 全量巡检第 22 轮：小库量知识库页 + `/` 块生成（2026-09-12）

shot-demo 摄入一场会后的知识库首页 / 最近摄入页都对（15 条事实、7 主题、9 实体）。
新探针 `block:<key>:<id>`：文末跑「用 AI 写」，占位块（在核对… / 停止）→ 结果以绿色
新增落下来，没有问题。

## [45] 全量巡检第 23 轮：模型连不上时（2026-09-12）

把 LLM 地址改成打不通的再跑续写 / 智能续写：用户看到的是「续写失败：Error: All
connection attempts failed」「生成骨架失败：Error: 500 Internal Server Error」，都没说
该去哪修；智能续写还冒出「这一轮改了 1 处」——其实只是轮初预留的那个空行。
现在：`util/friendlyError.ts` 把连接 / 5xx / 401 / 429 翻成人话（19 处 toast 统一走它），
连不上时 toast 带「打开设置」；只差首尾空白不算改动。

## [46] 全量巡检第 24 轮：打开笔记不再重算骨架（2026-09-12）

读代码发现：后台自动骨架的基线从不按已存的骨架初始化，于是**每打开一篇有骨架的
笔记，8 秒后都再生成一遍**——一次模型调用白花，模型连不上时每开一篇弹一个错。
现在有骨架的笔记以打开时的正文为基线、改够 20 字才重算；后台跑失败只记
client-log，点「生成骨架」失败才提示。

## [47] 全量巡检第 25 轮：流式是假的（2026-09-12）

用带时间戳的脚本打 magic-tap：88 个 delta 全在最后 0.7 秒里到，第一个字要等 3.5 秒。
根因在 `util/llm.py`：为了识别「模型不支持 temperature」那种 400，`stream()` 无条件
`await probe.aread()`——把整个流式响应先攒完再交给 `_consume_sse`。现在只有 400 才
读 body；改完 181 个 delta 铺在 2.4 秒里，真的在流。`stream_events` 同一处一起改。
顺手：主题地图 / 时间线 / 事实表三个 3 秒定时器常开，改成只在摄入任务跑着时轮询
（`util/ingestActive.ts`，App 在任务开始 / 结束时设）。

## [48] 全量巡检第 26 轮：健康状态跟着设置变（2026-09-12）

状态栏的「LLM 不可达」只在启动时查一次：改完设置还挂着红字直到重启。现在保存
供应商设置立刻重查；有红字时每 30 秒再查一次，服务恢复红字自己消失。
重新打了 dmg（含流式修复）并重开正式版。

## [49] 全量巡检第 27 轮：帮助菜单（2026-09-12）

定期回顾页正常。应用菜单补上 Trilium 的 Help：快捷键一览（⌘/，走 preload 的
`onMenu` 递到界面）、打开数据文件夹、导出后端日志…（把内存里最近 400 行写到
`~/Library/Logs/…` 并在 Finder 里定位——打包版没有 DevTools，这是用户能给我们的
唯一日志）、报告问题（GitHub issues）。

## [50] 全量巡检第 28 轮：目录跟着滚动高亮（2026-09-12）

右栏「目录」之前只是一列可点的标题；现在跟着正文滚动区顶部那一行高亮当前所在的
那一节（Obsidian outline 的做法），没有标题时给一句提示而不是空白。探针 `outline:<id>`。

## [51] 全量巡检第 29 轮：「**依赖链：**」渲染成裸星号（2026-09-12）

实拍正文里「**依赖链：**容量确认 → …」整段是裸星号：模型把冒号写在粗体里面，
CommonMark 的右侧定界规则不认这种闭合。确定性修：`fix_bold_punct` 把标点挪到粗体
外面——后端放在 `checks/grounding_rules.py`（纯模块，四条落盘路径都经过的
`scrub_meta_sentences` 顺手做），前端「格式化」同一条规则（`editor/format.ts`）。
第一版放在 editor/textshape 被 test_layering 拦下：grounding_rules 不许依赖 app 内其它包。

## [52] 全量巡检第 30 轮：编辑器里的 ⌘[ / ⌘]（2026-09-12）

⌘[ / ⌘] 是外壳的后退 / 前进，但 CodeMirror 的 defaultKeymap 把它们绑成缩进——光标在
编辑器里时按了只会缩进一行。编辑器里加一层排在前面的 keymap，把这两个键转成
`nav-history` 事件交给外壳（探针 `keynav`：切两篇后在编辑器里按 ⌘[，回到上一篇、
前进箭头亮起）。续写流完之后也过一遍 `fixBoldPunct`；`/` 块生成落盘同样修。

## [53] 全量巡检第 31 轮：编辑器里的 ⌘K / ⌘/（2026-09-12）

把 CodeMirror 各 keymap 的 Mod- 绑定列了一遍跟外壳的对：`markdownCommands` 把 ⌘K 绑成
插链接——光标在编辑器里按 ⌘K 会**同时**插一个链接并打开搜索面板；defaultKeymap 的
⌘/ 是 toggleComment，markdown 有 HTML 注释符，会真的插 `<!-- -->`。插链接让位到 ⇧⌘K，
⌘/ 在编辑器里也转成快捷键表；快捷键表补一行「⌘B / ⌘I / ⇧⌘K」。探针 `keypalette`。

## [54] 全量巡检第 32 轮：窄窗口正文优先（2026-09-12）

900×600（最小窗口）实拍：两侧栏保持用户设的宽度，编辑器被挤到 300px，ribbon 折三行、
工具栏溢出、浮动按钮盖住正文。现在中栏不够 520px 就先收右栏（用户的开关不动，窗口
拉宽自动回来），左栏最多占窗口 30%。专注模式（⌘.）实拍正常。探针 `focus`。

## [55] 全量巡检第 33 轮：整库导出（2026-09-12）

数据可携带是笔记工具的底线（Trilium 有整库导出），我们只有单篇「导出为 .md」。
新增 `GET /api/export/markdown`：一个 zip，树的层级变成文件夹（有子笔记的笔记是文件夹
+ 同名 .md），每篇带 front-matter（id / 标题 / 时间），克隆只写一份、其余位置留
`.link.txt` 指过去，不在树上的进 `_未归类/`。入口：⌘K「导出全部笔记」、⋯ 菜单、
帮助菜单。导航到接口地址即下载，所以接口也认 `?user=`（桌面版单用户）。
测试盯层级 / 克隆 / 孤儿 / 跨用户；真库 21 个文件 110KB。

## [56] 全量巡检第 34 轮：导出再导入（2026-09-12）

拿整库导出的 zip 再导回来，树要长回原样。写这条往返测试时发现：**带目录的
Obsidian / Markdown 导入每条都静默失败**——`_land` 调的 `store.create_folder` 根本不
存在（文件夹早已变成笔记，那个函数删了、调用没删，而导入没有一条测试）。现在
目录 a/b/c 逐层建成嵌套笔记；「文件名 = 所在目录名」的文件（我们的导出、Obsidian
folder notes）当作那个文件夹的正文，不另建同名子笔记；front-matter 剥掉。

## [57] 全量巡检第 35 轮：导入到哪（2026-09-12）

Obsidian / Evernote / Notion / Apple 备忘录导入一律「笔记 + 知识库」，一个几百篇的
vault 会逐篇抽事实跑很久，用户没得选。导入页加「导入到」：笔记 + 知识库 / 只进笔记
（之后可对单篇「存入知识库」）/ 只进知识库。

## [58] 全量巡检第 36 轮：窗口标题（2026-09-12）

窗口标题一直是 `memoket-NOTE`：Mission Control / 窗口切换里认不出是哪篇。现在跟着
当前笔记（或虚拟页）走：「标题 · MEMOKET NOTE」。

## [59] 全量巡检第 37 轮：后端崩了自动拉起（2026-09-12）

后端子进程被杀 / 崩掉之后，界面只会每 30 秒红一行「后端不可达」，用户得重启整个
应用。现在 `startBackend` 区分「我们叫停的」和「自己没的」（起来过、又不是 stop()
叫的 → 崩了），主进程 0.8 秒后重新拉起并刷新窗口（端口固定，地址不变）；连崩两次
就不再硬撑，弹框把最后几行日志给用户。实测：杀掉 uvicorn 后 25 秒内界面恢复。

## [60] 全量巡检第 38 轮：小手势（2026-09-12）

标签行空白处双击开新标签（浏览器约定；中键关标签早就有）。右栏「记忆」里正文已经
引过的事实标「已引用」——不然同一条会被插两次。重新打了 dmg（含崩溃自动重启、
整库导出、导入目录修复）并重开正式版。

## [61] 全量巡检第 39 轮：定期回顾存为笔记（2026-09-12）

探针 `digest:<days>` / `treekeys`。回顾（49 条事实、三段）生成正常，但它是一次性的，
关掉页就没了。加「存为笔记」：标题「回顾 起 ~ 止」，正文带来源说明，存完直接打开
（`open-note` 事件遇到列表里还没有的新笔记会先拉一次再开）。

## [62] 全量巡检第 40 轮：没存上的草稿落本机（2026-09-12）

后端崩了 / 网断了的那几秒里用户还在写，自动保存失败只显示「⚠ 没存上」，这时关掉
应用正文就没了。现在保存失败把标题 + 正文 + 时间写进 localStorage；下次打开这篇，
草稿比库里的新就放回来并提示，下一次自动保存存回去；存成功就清掉。探针 `draft:<id>`
实拍：切回来正文末尾多出那段草稿、toast 提示。harness-framework §12 补上 onInsertAt /
handler 守卫 / 最小 diff 三条。

## [63] 全量巡检第 41 轮：AI 写的时候锁编辑器（2026-09-12）

智能续写 / 续写运行中用户手改的字会被轮末的服务端正文盖掉（同步是服务端权威）——
那几十秒里打的字等于白打。现在运行中编辑器只读（`readOnly` 放进 Compartment，能在
运行中切换），状态条提示「运行中正文由 AI 接管，停下来再改」；逐轮暂停、跑完、
停止都解锁。程序性 dispatch 不受只读影响，续写照常流进来。

## [64] 全量巡检第 42 轮：图片不再内嵌 base64（2026-09-12）

粘贴 / 拖入图片一直是内嵌 data: URI：一张截图让笔记胖几 MB，续写时整段 base64 跟着
正文进提示词把 token 烧在没有语义的字母上。后端早有 `/api/assets`（按内容哈希去重）
却没接：现在先上传、正文里只留短地址，传不上（后端不在）才退回内嵌。
`util/llm.py` 出口统一把文本消息里的内嵌图片换成占位（vision 的 image_url 分块不动），
`content_block` 也做一遍；两处都有测试。

## [65] 全量巡检第 43 轮：图片块级插入 + 导出带资产（2026-09-12）

探针 `imgdrop` 把图拖进了标题行，显示成一串裸 markdown：图片现在自己占一段（落在
一行中间就前后补空行，Notion 也是块级插入）。整库导出把正文引用的 `/api/assets/…`
一起打进 `_assets/`、链接改成相对路径——不然导出的 markdown 里全是指向本机 API 的
死链接。测试第一版因为 import 时绑定了 `_dir` 而 monkeypatch 不生效，改成运行时取。

## [66] 全量巡检第 44 轮：删笔记不留孤儿行（2026-09-12）

读 `delete_note`：只删 notes 和 branches，引用 / 历史版本 / 暂停快照 / 运行记录都留在
库里指向一篇不存在的笔记。现在一起删；`harness_runs` 每个 key 只留最近 50 条（策略
只看最近几次）。测试盯着。

## [67] 全量巡检第 45 轮：建笔记也进反查表 + 老库孤儿行清理（2026-09-12）

`create_note` 不同步引用：导入、回顾「存为笔记」这类带着 `[引用]` 建出来的笔记，
下次保存前在事实页的「也引用于」里查不到。补上。老库里 delete_note 以前留下的孤儿
行（引用 / 版本 / 快照 / 运行记录）加一次性迁移 `drop-orphans-v1` 清掉。

## [68] 全量巡检第 46–47 轮：暗色回归 + 格式化不拽视口（2026-09-12）

导入页「导入到」、历史面板暗色下正常。「格式化」（⇧⌘F）之前整篇替换，光标和视口被
拽到文末；改走 minimalChange，只换变了的那一段，完成后 toast 说改了多少字。
formatMarkdown 幂等测试。探针 `format:<id>`。

## [69] 全量巡检第 48 轮：只差空白不标绿（2026-09-12）

格式化一篇 8000 字的笔记满屏绿——补的全是中英文之间的空格和空行。`diffParts` 现在把
只差空白的增删归为不变，真正改了的地方才标出来。harness-framework §18 补上这几轮新加
的测试和探针工作方式。

## [70] 全量巡检第 49 轮：大文档 diff 先按行再逐词（2026-09-12）

[69] 的「只差空白不算改动」实拍还是满屏绿：8000 字的笔记逐词 LCS 是 8000×8000 的表，
早就走了「太大就整段删+增」的退路，空白规则根本没机会生效。现在超过阈值先按**行**
LCS，一删一增等量成对的行再逐词比——格式化只补空格时几乎没有绿，真改一个词只标
那个词（两条测试）。

## [71] 全量巡检第 50 轮：老库运行记录一次修剪（2026-09-12）

开发库 `harness_runs` 攒了 4000 条（bench 的 chart:n / t:n 各几千）。加一次性迁移
`prune-runs-v1`，每个 key 只留最近 50 条（窗口函数 ROW_NUMBER）。重新打 dmg 并重开
正式版（含 assets / 导出 / diff / 迁移这几轮的改动）。

## [72] 全量巡检第 51 轮：退出前存完 + 记住窗口位置（2026-09-12）

自动保存有 1.5 秒防抖，⌘Q 正好卡在这 1.5 秒里就丢最后几句。主进程 `before-quit`
先拦一次：通过 preload 问界面「存完了吗」，界面 `save()` 完（或失败——草稿已落本机）
回 `flushed`，800ms 没回音也放行。窗口位置和大小记到 `window.json`，下次开在原处
（显示器拔了就只恢复大小）；探针不用，截图尺寸要固定。

## [73] 全量巡检第 52 轮：激活的标签滚进视野（2026-09-12）

十几个标签时 ⌘9 / 后退切到的那个可能在标签行看不见的地方——切过去后把它滚进视野。
退出前保存握手实拍：界面 1ms 回应。

## [74] 全量巡检第 53 轮：首帧耗时 + 快速搜索回车（2026-09-12）

探针下报一次首帧：90–115 ms、JS 1.7 MB（本机后端，包体再瘦收益不大，先不拆 d3）。
快速搜索按回车直接开第一条命中（Trilium 同款），之前只能伸手去点。

## [75] 全量巡检第 54 轮：空笔记上点续写（2026-09-12）

空笔记（无标题无正文）上点「续写」/「智能续写」会让模型凭空编；现在先提示「先写个
标题或几句话」。只有标题时允许：续写接口新增 `title`，提示词开头带【笔记标题】
（智能续写早就走 ctx.note_title）。探针 `blank-tap`：没有发出 magic-tap 请求，只有提示。

## [76] 全量巡检第 55 轮：弹层键盘（2026-09-12）

把所有弹层的键盘行为读了一遍：选择器 / 输入框 / 右键菜单都有 ↑↓ 回车 Esc；快捷键表
没有 Esc（补上）；**删子树的确认框默认焦点在红色的「删除」上**——顺手一个回车就删掉
几十篇，危险操作的默认焦点改到「取消」（普通确认仍是确定键）。

## [77] 全量巡检第 56 轮：历史版本「与当前对比」（2026-09-12）

历史面板展开一版之前只能看旧版全文，看不出改了什么。现在默认「与当前对比」：红的是
那一版有、现在没了的；绿的是现在新加的（同一套 diffParts，大文档先按行再逐词）；
「这一版全文」仍可切。探针 `history-open`。删子树确认框实拍：焦点在「取消」上。

## [78] 全量巡检第 57 轮：回归一遍核心探针（2026-09-12）

tabs / kb-tab / split / quick-view / tree-menu / settings 六张全拍：
- 一篇从别处粘来的笔记整篇缩进了 4 格，markdown 把它整个当成代码块——标题不是标题，
  编辑器里一片等宽字。「格式化」现在会去掉整篇的公共缩进（有围栏或缩进不一致不动）。
- 标签行里还挂着早已删掉的笔记（分段写作测试的子树）：`reload()` 之后把库里没有的
  笔记的标签收掉（虚拟节点不动）。

## [79] 全量巡检第 58 轮：列表接口量一量（2026-09-12）

`reload()` 每次保存后都拉一遍 `/api/notes`（带全文）。实测：412 篇 454KB / 21ms，
terrence 20 篇（含两篇 3 万字）252KB / 3ms，整库导出 412 篇 31ms。本机回环下不值得
为它改成「列表只给摘要、打开再取全文」——那要动 open() / 搜索 / 欢迎页三处，先记着，
到几千篇再说。历史对比视图暗色实拍正常。

## [80] 全量巡检第 59 轮：笔记库自动备份（2026-09-12）

Trilium 有 daily / weekly / monthly 备份，我们一直没有。`database/backup.py`：后端每次
启动看一眼，今天没备份就用 sqlite 在线备份 API 拷一份一致快照到 `data/backups/
notes-YYYYMMDD.sqlite3`，留最近 7 日 + 每月第一份（3 个月）；备份失败不影响启动但会
打日志。帮助菜单加「打开备份文件夹」。知识库（KITE xml，几十 MB / 用户）下一步再进。

## [81] 全量巡检第 60 轮：知识库也备份（2026-09-12）

每个用户的 `codebook.xml`（KITE 的知识库本体，十几 MB）一周一份到
`backups/kb/<user>/codebook-YYYYWww.xml`，留两份；.lock / .bak-* 不拷。测试盯周界。

## [82] 全量巡检第 61 轮：错误体里的 detail（2026-09-12）

Skill 页 toggle 没接错误（后端不在就静默失败）——补 toast。`api.ts` 的 `json()` 之前把
整个错误体拼进错误信息，FastAPI 的 `{"detail": "…"}` 在 toast 里就是一坨 JSON；现在
把 detail（字符串或 422 的字段列表）拿出来。

## [83] 全量巡检第 62 轮：探针搬出 App.tsx（2026-09-12）

探针 effect 长到了 330 行，测试用的代码跟产品代码挤在一个 3200 行的文件里。搬到
`src/probes.ts`（`runProbe(probe, ctx)`，ctx 就是 App 里那些闭包），App.tsx 减到 2900 行；
tabs / kb-tab / palette 三个探针回归正常。

## [84] 全量巡检第 63 轮：整篇缩进的提示条（2026-09-12）

[78] 里「格式化」能去掉整篇公共缩进，但用户不知道该点它——看到的只是一片等宽字。
现在打开这种笔记时正文上方给一条提示「Markdown 会把它当成一整块代码」+「去掉缩进」
按钮（就是格式化）。

## [85] 全量巡检第 64 轮：标签装不下时的滚动钮（2026-09-12）

trilium-ui-gap §3「放不下时怎么办」还标着明显：十几个标签时最右那个一直藏着，只靠
滚轮横滚没人发现得了。现在 strip 装不下就显形左右两个 ◀ ▶（每次滚 210px），
ResizeObserver 盯着宽度变化。

## [86] 全量巡检第 65 轮：树上拖拽走一遍（2026-09-12）

新探针 `dnd:<dragId>:<targetId>`：合成 DragEvent 走真实的 onDragStart / onDragOver / onDrop
（dragover 之后要等一帧让 React 把落点状态渲染出来）。把一篇根笔记拖到「Notes」上：
`PATCH /api/tree/branches/move` 200，库里 parent 变了、树上进了文件夹。最近 25 次探针
的日志里没有一条 client:error。重新打 dmg 并重开正式版。

## [87] 全量巡检第 66 轮：主题 / 实体索引页（2026-09-12）

两页实拍都对，两处小的：主题卡片里「…还有 61 个」不能点（应该进那个一级主题页）；
实体页前二十个全是 ASR 的说话人标签（Speaker A / speaker_c…），默认把它们藏起来，
一个开关可以放出来。

## [88] 全量巡检第 67 轮：说话人标签一处定义四处用（2026-09-12）

[87] 只在实体索引页藏了 Speaker A 这类伪实体；首页 top 实体、主题页局部图和实体
chips、主题地图的「滤噪声」也都该认得它们。`util/kbNoise.ts` 一处定义（Speaker A /
speaker_c / 说话人 1…），四处用。

## [89] 全量巡检第 68 轮：⌘K 打字搜（2026-09-12）

新探针 `palette:<q>`。打「设」：命令「设置」对了，但笔记列出了几乎全部——正文命中也
算，「设」这种字每篇都有。现在标题命中排前、正文命中排后，最多 8 条；知识库那组
不动。首页 top 实体、主题地图滤噪声实拍已不含说话人标签。

## [90] 全量巡检第 69 轮：⌘K 同名笔记分不清（2026-09-12）

三篇都叫「创业一年回顾」（克隆 / 导入的副本），⌘K 里三行一模一样。行尾带上最后
修改日期（跟搜索结果卡一致）。

## [91] 全量巡检第 70 轮：树上悬停看全名（2026-09-12）

树的标题被截断（「当前团队的主要blocker…」）之后没有任何地方能看全；同名的几篇也
分不出。行上加 title：全名 + 修改日期。

## [92] 全量巡检第 71 轮：智能续写再跑一遍（2026-09-12）

8000 字的草稿上再跑 140 秒：只读锁、定向续写、mermaid 决策图渲染、轮末对齐（本地 vs
服务端只差修订产生的几十字）都正常。抓到一处样式：修订理由一长，状态条里的机器人
图标单独占一行——文案放进自己的 span 折行，图标不换行。

## [93] 全量巡检第 72 轮：时间线 / 事实页回归（2026-09-12）

时间线（40 个月、sqrt 比例、默认展开最近一个 ≥10 条的月）和事实页（原话折叠、被引用、
相关事实、面包屑）都正常，没有改动。
顺手：快捷键的写法按平台翻译（`util/keys.ts`：mac 用 ⌘⌥⇧，Windows / Linux 显示
Ctrl+ / Alt+ / Shift+），右键菜单、快捷键表、欢迎页、标签提示都走它——桌面版有 linux
构建目标，之前满屏 ⌘。

## [94] 全量巡检第 74 轮：时间显示的是 UTC（2026-09-12）

信息面板「修改 2026-09-12 05:38」——本地其实是 13:38。服务端给的是 UTC ISO，前端到处
`slice()` 字符串当显示。`util/time.ts` 统一转本地（信息面板 / 搜索卡 / ⌘K / 欢迎页 /
链接面板 / 快速查看 / 树悬停），历史面板本来就用 Date 算，没事。

## [95] 全量巡检第 75 轮：后台跑完给系统通知（2026-09-12）

智能续写 / 分段写作 / 摄入都是几分钟的活，用户多半切去了别的窗口，跑完只有一个 toast
在看不见的地方闪一下。`util/notify.ts`：窗口不在前台时发一条系统通知（静音），在前台
不发；拒绝了权限就静默。

## [96] 全量巡检第 76 轮：麦克风失败的提示（2026-09-12）

两处录音入口各写了一句：`/语音` 是「拿不到麦克风权限」，右上角录音钮是「浏览器要求
HTTPS 或 localhost 才允许录音」——后者是网页版的话，桌面版里不成立。macOS 拒绝过一次
之后再点永远静默失败，只有去系统设置打开才行。`util/micError.ts` 按错误名分三句
（系统未授权 → 指到系统设置；没设备；被占用），其它走 `friendlyError`；转写失败的
toast 也不再直接 `${err}`。打包配置补 `NSMicrophoneUsageDescription` 中文文案（系统
授权弹窗里显示的那句）。vitest 59。

## [97] 全量巡检第 77 轮：搜索卡看不出命中在哪 + 深色链接（2026-09-12）

实拍搜「创业」：六张卡的摘要全是各自第一行，没一张能看出命中在哪，标题不含关键词的
那张（harness 测试）尤其莫名其妙。`util/snippet.ts`：摘要改成命中处前后各一截、命中
`<mark>` 高亮（去掉井号/列表符号，Trilium 快速搜索同款）；搜索态把树底部「定位/折叠」
两个钮藏掉（搜索列表里没意义）。深色下导入页「看总览」是浏览器默认深蓝几乎看不见——
全仓没定义过 `a` 的颜色，补 `a { color: var(--accent) }`。vitest 63。

## [98] 全量巡检第 78 轮：⌘K 行布局（2026-09-12）

实拍 ⌘K 搜「创业」：知识库那几行日期拼在正文后面，长一点的先被省略号吃掉，一列里有的
有日期有的没有；标题里没命中、靠正文进来的「harness 测试」混在一排「创业一年回顾」里
看不出为什么在这。行改成 flex 三段（正文省略 / 命中片段 / 日期固定右侧），命中处
`<mark>`（`components/Highlight.tsx`，搜索卡同款）。空态「最近编辑」长标题溢出面板一并
收进省略。

## [99] 全量巡检第 79 轮：暗色下 `/` 菜单是白板（2026-09-12）

实拍 `--dark` 打 `/`：菜单白底灰字。查因（往 client-log 打 computedStyle）：showTooltip
返回的 dom 本身就被 CM 加上 `.cm-tooltip`，不再包一层，所以 `.cm-tooltip:has(.slash-menu)`
从来没匹配上；而 CM `&light .cm-tooltip` 的 #f5f5f5 带生成类名，比 `.slash-menu` 高一级。
三个自绘 tooltip（`/` 菜单、引用预览、接受/撤回条）改成 `.cm-tooltip.xxx` + `!important`
压回令牌。顺手修 fact-peek 探针：第一条引用常在折叠线下，坐标打过去 CM 什么都不弹，
改成滚到最后一条再打。

## [100] 全量巡检第 80 轮：⌘F 查找条是 CM 原生皮（2026-09-12）

新探针 `find`（打开 CM 查找面板、填「样机」、findNext）实拍：英文按钮（next / previous /
match case / by word…）、暗色下浅灰大板、输入框吃了全局 `input { width: 100% }` 撑满整行、
浏览器默认灰钮。`EditorState.phrases` 汉化全部文案；`.cm-panels / .cm-search` 压回令牌，
收成两行紧凑条，关闭钮钉右上；命中高亮暗色下改成 warn/accent 半透明。两种模式都拍过。

## [101] 全量巡检第 81 轮：同一篇两个字数（2026-09-12）

实拍信息面板「8449 字 · 约 21 分钟」、状态栏「8874 字 · 约 22 分钟」，同一篇笔记。一个数
`content.length`（空格换行都算），一个先去空白。`util/wordCount.ts` 一处定义：去空白、
markdown 记号、`[user-n-hex]` 引用标记后的字符数（同一篇现在两处都是 7760），阅读时间
400 字/分钟。信息面板「已摄入（日期）」还在 `slice(0,10)` UTC 字符串，第 74 轮漏的，改
`fmtDate`。vitest 66。

## [102] 全量巡检第 82 轮：引用 / 链接 / 路径三个 ribbon 页暗色回归（2026-09-12）

三页暗色都正常，没改。顺手把第 74 轮漏掉的三处 UTC 字符串收掉：树上「已摄入」悬停、
引用页「已摄入 · 日期」、批量导入建的父笔记标题「导入 YYYY-MM-DD」（之前用
`toISOString()`，晚上八点后建的会写成明天）。

## [103] 全量巡检第 83 轮：/api/health 要等 6.5s（2026-09-12）

重开正式版顺手 curl 健康检查：6.5s。LLM `/models` 1.6s + 语音服务（配置里是局域网机器
192.168.77.8，没开）等满 5s 超时，两个串着跑；桌面版每次启动、每次切供应商都要等这一下
才知道红不红。改 `asyncio.gather` 并行，语音探测超时 5s → 2s（本机/局域网要么秒回要么
没开）；实测 6.5s → 2.0s。`tests/test_health.py` 两个探测各睡 0.3s，串行 ≥0.6s 会挂。

## [104] 全量巡检第 84 轮：语音离线时录音钮照常可点（2026-09-12）

状态栏挂着「语音离线」，右上角麦克风钮和 `/语音输入` 却照常可用——用户录完一段才在
转写那一步失败。离线时录音钮变成灰的 mic-off，点了直接说明地址和影响；`/语音输入`
录音前先拦。顺手：`openVirtual` 传进普通笔记 id（探针 `open:<noteId>`、旧标签残留）会被
当实体名渲染成「别名：<正文前 100 字>」的知识库页，现在先查是不是笔记、是就走 switchTo。

## [105] 全量巡检第 85 轮：暗色下原生下拉是白块（2026-09-12）

事实表三个筛选下拉、导入页「导入到」在暗色下白底黑字一块——`<select>` 从没跟 input
一起定义过样式。补 `select` 令牌皮肤 + `:root { color-scheme }` 跟主题走（系统画的下拉
列表、滚动条、勾选框也跟着变暗）。主题页 / 时间线 / 事实表暗色回归正常。

## [106] 全量巡检第 86 轮：语音服务地址没处改（2026-09-12）

状态栏挂着「语音离线」、录音钮提示「在设置里检查语音服务地址」，设置里却没这一项——只能
改 .env 重启。`provider_config` 加 `asr_base_url` 列（老库 `_ADDED_COLUMNS` 补列），
`store.get_asr_base_url()` 填了用填的、空退回 .env；asr 转写 / 健康检查 / 设置接口都走
它。设置页加「语音服务」一栏（placeholder 显示默认地址，保存后 provider-changed 事件让
状态栏立刻重查）。确认框 / 标签菜单 / 设置页暗色回归正常。pytest 779。

## [107] 全量巡检第 87 轮：toast 盖住状态栏（2026-09-12）

写作 Skill / 写作计划弹窗 / 定期回顾暗色回归正常。toast 固定在 bottom:16px，正好盖在
状态栏的字数和「语音离线」上（实拍），抬到状态栏之上。定期回顾按钮加载时只剩一个
spinner、按钮缩窄跳一下，改成 spinner + 原文案。

## [108] 全量巡检第 88 轮：退出时空「未命名」留下来（2026-09-12）

demo 库攒了四篇空「未命名」。两个原因：`blank` 探针没有 harnessProbeDone 守着，runProbe
随 notes/tree 刷新再进来就又建一篇（一轮三篇）；退出时的 flush 只 save 不收空笔记（切走
时会收）。探针加守卫；flush 之后走 `dropIfStillEmpty(currentRef.current)`（经 actionsRef 拿
最新闭包），实测退出前多了一条 DELETE。

## [109] 全量巡检第 89 轮：树底工具钮压住数量角标（2026-09-12）

多轮实拍都撞到：树底部「定位 / 折叠」两个浮钮常驻，正好压在最后一行的数量角标上
（learning 2293、项目 10 27）。改成鼠标进左栏才现身（focus-within 也现身）；不用鼠标的
在 ⌘K 里加「定位到当前笔记」「折叠整棵树」两条命令（走 window 事件，collapseAll 经
actionsRef）。412 篇大库用户的标签栏 / 知识库树回归正常。

## [110] 全量巡检第 90 轮：窄窗开分屏把正文挤成 250px（2026-09-12）

1000×700 开分屏实拍：中栏 250px，ribbon 折两行、工具栏只剩 B。分屏宽度现在算进窄窗
逻辑：分屏最多占窗口 45%；右栏先收，中栏还不到 520px 就连左栏也收（关掉分屏自动回
来）。同尺寸复拍中栏 ~700px，ribbon 单行。树右键菜单 / 键盘导航暗色回归正常。

## [111] 全量巡检第 91 轮：magic tap 停在半句没人说（2026-09-12）

真跑一次 tap（实拍）：一段停在「…也不能把“有人跟进”写成事项已经完成」，没有句号。
`llm.stream` 早就能回 `finish_reason`，magic-tap 没接。现在 `done` 事件带
`truncated`（= length），前端 toast「撞到长度上限，停在半句，光标放末尾再点一次」；
已写内容一字不删。`tests/test_magic_tap_truncated.py` 假 stream 两种 finish_reason。
第 90 轮重打 dmg、重开正式版。

## [112] 全量巡检第 92 轮：重写 / 扩展撞上限会把半句当建议（2026-09-12）

顺着上一轮查 `complete()` 的调用方：重写 / 润色 / 扩展都是 JSON 输出，max_tokens 600 /
500，撞上限时 `extract_json` 很宽容——`{"text": "改到一半就` 被修成一条只有半句的
replace 修订（测试里实测），用户一点接受整段被换成半句。`complete()` 加 `stats`
（同 stream），撞上限就清掉建议、`EditOut.note` 带一句「选短一点再试」，前端红 toast。
`tests/test_rewrite_truncated.py` 三条。pytest 784。

## [113] 全量巡检第 93 轮：要 JSON 的调用撞上限会安静地少几项（2026-09-12）

接着查 `complete()` 的其它调用方：分段计划（600）、「还有没有更多」（400）、骨架
（800）、AI 生成 skill（500）、replan（600）全是 JSON 输出，撞上限时 `extract_json` 把
断尾修成合法但少了几项的列表——用户拿到的是六节变三节的计划，没有任何提示。
`llm.complete_json[_raw]()`：撞上限就把预算翻倍（封顶 4000）再要一次，六处调用改走它；
骨架那条路要原文做按行退化，用 `_raw` 版。test_llm 加两条。pytest 786。

## [114] 全量巡检第 94 轮：magic tap 撞上限自动补尾（2026-09-12）

[111] 只是提示「再点一次」；单篇 / 分段 harness 早就有「接着最后那半句写完」的补尾，
magic tap 补上同一套：撞上限就带着已写的半段再要一次（CONTINUE_TAIL_TOKENS），补尾还
撞上限才报 truncated。补尾指令 `FINISH_THE_SENTENCE` 搬进 prompts.writing，分段写作
共用。测试改成「补尾成功 / 补尾仍截断」两条。pytest 787。

## [115] 全量巡检第 95 轮：ribbon 下面一条蓝虚线（2026-09-12）

目录探针（正文滚到中段）实拍：工具栏底下露出一截引用的蓝色虚线。用 elementsFromPoint
查：滚动区顶部有 8px 内边距，浮动按钮条 sticky 在 top:0 时钉在内边距下面，那 8px 露出
滚过去的正文。按钮条改 top:-8px + 上补 8px 内边距，复查那几个像素全落在按钮条上。

## [116] 全量巡检第 96 轮：图片拖进标题行把标题切开（2026-09-12）

imgdrop 探针实拍：拖进「# 创业一年回顾」，光标在「# 」之后，图片在光标处块级插入，
标题被切成「#」+ 图 + 裸文字「创业一年回顾」。`imageInsertion()` 纯函数：非空行一律插到
这一行末尾之后自成一段，空行原地放；vitest 两条。顺手：格式化把表格分隔行对齐补宽后
字数从 7760 跳到 8241——`|---|` 整行是记号，wordCount 不再算它。vitest 69。

## [117] 全量巡检第 97 轮：前端补上 ESLint（2026-09-12）

欠账清单里挂了很久的一条。eslint 9 + typescript-eslint + react-hooks，只开抓 bug 的规则
（hooks 用法 / 依赖、未用变量、eqeqeq、恒真表达式），风格一条不开。首跑 7 条：format.ts
里一个逗号表达式（改成块）、KnowledgeGraph 清理函数里读 ref.current（先拷一份）、两个
故意不全的依赖数组加说明 + disable、两条失效的 disable 删掉。`npm test` 里加了 lint。
草稿恢复 / 窄窗标签栏 / 快速查看暗色回归正常。

## [118] 全量巡检第 98 轮：撤回可逆性坏了 15%（2026-09-12）

ESLint 接进 `npm test` 后第一次全量跑，check-hunks 随机 2000 例里 273 例还原失败，有的
还丢字。根因是第 69 轮「只差空白不标绿」：在 diffParts 里把空白的 del/ins 改成 keep，
一个 del " " 变 keep 之后后面所有位置都按「空格还在」算，整串 hunk 错位一格，撤回还原
出来的是错的。改法：diffParts 类型保持精确；toHunks 给只差空白的 hunk 标 `soft`，装饰层
不画、悬停不认、计数不算，但撤回全部照还原。vitest 加两条（含那条真实失败例），
check-hunks 2000/2000。教训：改 diff 的「显示」不能动「数据」。

## [119] 全量巡检第 99 轮：选区菜单忙态是个光秃秃的转圈（2026-09-12）

sel:trace 实拍 9 秒还在转圈，框里只有一个 spinner，不知道在等什么。`busy` 从布尔改成
「正在跑的动作」，框里写「校验中… / 重写中… / 润色中… / 找上下文… / 查来龙去脉…」。
⌘K 键盘打开、树键盘导航暗色回归正常。

## [120] 全量巡检第 100 轮：空库欢迎页回归（2026-09-12）

新用户欢迎页（三张卡 + 常用键）暗色 / 1000×700 都正常；新建第一篇的空笔记态正常。
一百轮小结：这 25 轮里真正的 bug 集中在三类——暗色下没走令牌的原生控件（select /
CM tooltip / 查找条）、时间与字数的口径不一（UTC、两套字数）、模型撞上限的静默截断
（tap / 重写 / JSON 列表）。截图探针抓前两类，第三类靠读产出 + 读代码。

## [121] 全量巡检第 101 轮：⌘F 没进快捷键表、焦点不在编辑器时不响应（2026-09-12）

对着快捷键表逐条核对全局 keydown：列出的都接了；第 80 轮加的 ⌘F 查找条没列进去，
且焦点在树 / 面板上时 CM 的 searchKeymap 收不到。表里补一行；全局 keydown 兜底：
编辑器没焦点时 ⌘F 打开查找条并把焦点给编辑器。

## [122] 全量巡检第 102 轮：身份只在 localStorage 里（2026-09-12）

翻正式版数据目录：多出一个 `user-4u6jzn`（今天 13:10 建的，零篇笔记）——就是第 51 轮
localStorage 丢失事故的残骸：界面拿不到身份就随手生成一个，用户看到空库，23 篇笔记都在
terrence 名下好好的。身份现在也落在主进程的 `identity.json`：渲染进程定下身份回报一次
（remember-user），主进程下次启动塞进 `?user=`；探针 / --user 强制时不写。实测：清掉
dev 实例的 Local Storage 再启动，URL 带 `?user=terrence`。知识库引用页 / 最近摄入 /
实体页暗色回归正常。

## [123] 全量巡检第 103 轮：删除流程回归（2026-09-12）

建一篇临时笔记跑 delete 探针连拍：4s 有「已删除 · 撤销」toast，9s 树 / 标签 / 分屏都
收干净、自动切到别的笔记，DB 里也没了。相关记忆检索实测 15ms（不需要再调 900ms 防抖）。
没改代码。

## [124] 全量巡检第 104 轮：系统文件拖进树直接成笔记（2026-09-12）

对着 trilium-ui-gap 再扫一遍：note_tree.ts:584-596「拖进来的文件直接导入成笔记」还没做，
之前只能右键「导入 .md 到这里…」走文件选择框。树上现在接系统文件拖放：落在某一行 =
导入到它下面（行高亮 drop-over），落在空白处 = 导入到根，走同一个 importMarkdown（多文件
成子树）；知识库虚拟行不收。新探针 `filedrop[:标题]` 用 DataTransfer 造一个 .md 丢上去，
实拍落在「链接测试」下面并打开。

## [125] 全量巡检第 105 轮：笔记链接悬停卡（2026-09-12）

对着 gap 清单的 note_tooltip 一项复核：链接悬停卡早就有（`.cm-note-peek`），只是 end 探针
之前 10s 截图赶在 300ms 悬停 + 拉笔记之前。两处小修：卡上的字数用 `content.length`
（8890）跟信息面板 / 状态栏（7760）不是一个口径，改走 wordCount；摘要里连续空行折成一行
——卡只有 160px 高，实拍前四行里两行是空的。

## [126] 全量巡检第 106 轮：超长笔记 + 目录跟不上（2026-09-12）

给 shot-perf 造一篇 47k 字、300 个标题的笔记（`a4cf66fca44f`，留着当性能探针）：打开、
树 413 行 fetch 67ms / paint 86ms，都不卡。目录探针滚到第 136 节时右栏目录还停在第 1～23
节——当前节高亮了但没滚进视野。activePos 变了就把 `.outline-item.active` scrollIntoView
（nearest）。

## [127] 全量巡检第 107 轮：长文格式化又是满屏绿（2026-09-12）

47k 字 / 1500 行的笔记格式化实拍：整篇绿。`diffByLines` 行数乘积超过 40 万格就直接
「整段删 + 整段增」（1500×1500 = 225 万）。改成 patience 思路：两边都只出现一次的行当
锚点（LIS 对齐，O(k log k)），锚点之间的小块再 LCS；块还是太大且行数相等就逐行配对。
实测同一篇：6002 个 part、1201 个 hunk 全是 soft、29ms。vitest 加两条（1500 行补空格
全 soft 且可精确撤回；1500 行改一句只标那一句）。

## [128] 全量巡检第 108 轮：300 条找不到的引用把面板撑满（2026-09-12）

长文的引用页实拍：「300 条引用在知识库里找不到」后面把 300 个 id 全列出来，整块红字
撑满一屏。只列前 8 个 + 「还有 N 个」。历史页空态正常。

## [129] 全量巡检第 109 轮：长文上校验一句话，整篇 3 万 token 进提示词（2026-09-12）

47k 字长文上右键校验实拍 8s 出结果，但 verify / rewrite / expand 三个提示词的「完整正文」
块都是整篇原样给——慢、贵，离选区两万字远的内容对「这句对不对」没帮助。
`prompts.selection.selection_context()`：正文 ≤8000 字原样；否则切选区前后各 3000 字，
切口对齐段落，两头标「前面 / 后面还有 N 字，略」。三个提示词都走它；测试四条。
长文分屏正常。

## [130] 全量巡检第 110 轮：magic tap 整篇进提示词（2026-09-12）

接着上一轮：magic tap 的「已写正文」块也是整篇原样给。改走智能续写同一个
`compact_context`（最近 6000 字原样，前面各节折成一行梗概）。发现它对几百个两行小节
的长文「压不小就原样」——梗概跟原文一样长；加 `max_summary_chars`（tap 用 2000）：超了
只留最近几条，前面折成「更早的 N 节略」。test_compaction / test_magic_tap 各加一条。
pytest 793。

## [131] 全量巡检第 111 轮：⌘K 只列 8 条不说后面还有（2026-09-12）

412 篇「会议纪要」的用户 ⌘K 搜「会议」：只列 001～008，没说后面还有 404 篇。加一行
「还有 N 篇没列出——多打几个字缩小范围」。主题地图空库态正常。

## [132] 全量巡检第 112 轮：存入知识库跑的时候界面上什么都没有（2026-09-12）

给 shot-demo 造一篇例会纪要真跑摄入连拍：点了之后 4 秒界面上没有任何变化（只有引用页
里一行），25 秒跑完也只有树上多个 ⇡、右栏记忆悄悄变了。现在状态栏挂「存入知识库中…
已抽出 N 条」（facts 数随 chunk 往上涨），结束 toast「已存入知识库：抽出 N 条记录 · 看看」
（点开最近摄入），失败 / 取消也各说一句；后台通知带条数。第二次实拍 16s 看到 toast。

## [133] 全量巡检第 113 轮：「智能插图：在写…」（2026-09-12）

`/智能插图` 真跑（文生图要几十秒）连拍：占位块两个阶段「在看要用哪些材料…」→「在写…」，
画图不是写。`Mode.verb`（默认「在写」，数据可视化 / 智能插图「在画」、数据分析「在算」、
生成表格「在拼表」），loop 的活动条用它。

## [134] 全量巡检第 114 轮：块生成第二轮又显示「在看要用哪些材料…」（2026-09-12）

`/数据可视化` 真跑连拍 30 / 70 / 110s：30s「在核对…」、70s 又回到「在看要用哪些材料…」
——其实是第 2 轮开始了，但看起来像卡在开头没动；110s 出图（标题 + 柱状图，标绿）。
活动条第二轮起带轮次：「数据可视化 · 第 2 轮：在看要用哪些材料…」。pytest 793。

## [135] 全量巡检第 115 轮：块生成跑完结果在视口外（2026-09-12）

上一轮实拍出图落在折叠线下。块落下来之后把落点滚到视口上部（yMargin 80）；`/智能表格`
真跑复核：95s 出表，落在视口上部、标绿，活动条「生成表格 · 第 3 轮」也对了。历史面板
「8890 字」跟状态栏「7723 字」不是一个口径——它数的是原始字符（服务端 length），改
标「字符」，版本间增减照旧。

## [136] 全量巡检第 116 轮：「看总览」跳到不存在的页 + 实体页按名字打不开（2026-09-12）

导入页「看总览」派发的是 `kb:overview`，没有这个虚拟节点，打开一页只有裸 id 的空页；
改成 `kb`。实体页 `kb:entity:Facebook` 报「没有这个实体」——代码是 `facebook`，显示名 /
大小写 / 空格写法都落不到；`resolve_entity_code()` 依次试原样、规范化、显示名、别名。
顺手：事实卡上的实体 chip 之前显示代码（speaker_a），页面接口带 `entity_names`，
chip 显示名。主题页 work 暗色回归正常。pytest 793。

## [137] 全量巡检第 117 轮：知识库缺页态（2026-09-12）

拿不存在的 id 开主题 / 实体 / 会议页：各一行灰字「没有这个主题：nope」，没有出口。
`MissingPage` 统一：标题 + 原因（旧标签 / 改过名 / 重建过）+ 回对应列表的按钮；事实页
早就有类似的红字版，保持。

## [138] 全量巡检第 118 轮：旧虚拟标签 + Skill 卡窄窗（2026-09-12）

时间线 / 定期回顾暗色回归正常。标签栏里留着 `kb:overview`、`kb:fact:terrenc…` 这类裸 id
标签：openVirtual 不认识的 `kb:*` 落到总览；reload 时不认识的虚拟标签一起收掉；事实页
标签写「事实 <id>」。Skill 卡在 1000px 下标题一长按钮组整个掉到下一行——标题省略、
按钮组不缩。

## [139] 全量巡检第 119 轮：导入 / 设置窄窗回归（2026-09-12）

1000×700 下导入页（三种来源 + 导入到）和设置页（外观 / LLM / 语音 / 偏好）都正常。
旧版本存下来的事实标签标题还是裸 id，reload 时补成「事实 <id>」。

## [140] 全量巡检第 120 轮：重打 dmg（2026-09-12）

第 120 轮：`npm run dist` 重打、重开正式版（identity.json 仍是 terrence，接口 200）；
后端 793、前端 tsc + eslint + vitest 72 + 11 个检查脚本全过。这 20 轮的主线：长文
（47k 字 / 300 标题）把 diff、目录、引用页、提示词长度都拉出了毛病；块生成和摄入的
「跑的过程看得见」；知识库缺页 / 旧标签 / 实体代码这类边角。

## [141] 笔记 ↔ 知识库链接层（2026-09-12，用户点名优先）

用户：「一个笔记 contribute 了什么到知识库应该能增删改；笔记更新了要能自动 / 手动同步，
要同步时要有标识；自动同步是设置项；知识库的显示也要链接回笔记。」查现状发现两个真
bug：① 摄入的 session 叫 `note-<id>-<n>`，KITE 拒重复 id，对摄入过的笔记点「重新摄入」
整个 job 报 StorageError（改过的内容永远进不来）；② 笔记摄入出来的事实 id
`note-f5e34e385aac-0F1` 不匹配五处引用正则，摄入进去的事实没法被引用。

做了：`UserMemory` 加 `facts_for_prefix / delete_facts / set_fact_text / add_manual_fact /
remove_sessions`（锁内改 XML、校验、原子替换）；`GET /api/notes/{id}/kb`（贡献 + stale）；
`PATCH/DELETE /api/kb/fact/{id}`、`POST /api/kb/fact`（手工事实进 `-manual` session，同步
时保留）；`POST /api/ingest/note/{id}/sync`（服务端读最新正文，删旧 session 重抽）；
`_ingest_job` 的「已存在」按 `_land` 的规矩当跳过；`provider_config.auto_sync_notes`
+ 设置页开关；前端「引用」页：过期黄条 + 同步钮 + 贡献列表（改 / 删 / 补一条）；树上
改过没同步的黄 ⇡；知识库事实卡「来自笔记」反链；自动同步 = 停止编辑 2 分钟或切走时。
真跑：shot-demo 一篇加一句后同步，12s 重抽出 7 条（新句进来了），手工补的那条保留。
pytest 798（新 5 条）、vitest 74（引用正则 2 条）。方案文档：agent-native-editor.md、
import-sync-plan.md；优先队列写进 PROGRESS。

## [142] 记忆的六种关系——第一版（2026-09-12，优先队列 ②）

用户：「记忆能不能体现更深层的 feature？检测到事实冲突、延续、叠加、合并……」方案写在
agent-native-editor.md §3.3.1（六种关系 + 事实生命周期）。这一版落地冲突 / 延续 / 印证 /
缺依据四种 + 事实取代 + 实体页演进线：

- `database/kb/relations.py`（纯代码）：抽正文里带单位的数字和日期，跟召回的事实按词面
  重合配对；同单位值不同 = 冲突，≥2 条记录值随时间变 = 延续线（把你写的接在最后），同值 =
  印证，有量却没沾边记录 = 缺依据。一条记录内部的「从 300 改到 380」不算线。
- `POST /api/memory/relations`：召回 → 判候选 → 只有冲突才让模型确认一句（`RELATIONS_SYSTEM`，
  模型不在就用代码那句）。真跑 shot-demo：「电池 420mAh」→ 冲突（那里是 380）+ 延续
  （0.4mm → 1.2mm → 1.5mm 你写的）；「DVT 9 月 1 日」→ 日期冲突（那里是 6-3、8-5）。
- 前端：编辑器回报光标所在段落（`paragraphAt`），右栏「记忆」顶上出关系卡（左色带：黄冲突 /
  蓝延续 / 绿印证 / 灰缺依据），动作：引用这条 / 新的取代旧的 / 忽略（记住）。
- 取代：`PATCH /api/kb/fact/{id} {superseded_by}` 写 XML 自定义属性；事实卡划线 + 「已被
  取代」；实体页「这些事怎么变的」按 obj 串线。
- 叠加 / 合并、冲突收件箱、摄入时检测：下一版。pytest 807、vitest 76。

## [143] 提案分层账本（2026-09-12，优先队列 ③）

痛点 8：AI 改了三轮只想保留第一轮——之前只有「这一轮」一层高亮，`setRoundDiff` 每次
整个替换。现在每次 AI 动作是一层（`addLayer`：润色 / 重写 / 扩展上下文 / 格式化 / 智能
排版 / 语音输入 / 图片转表格 / 插入音频 / 各种块生成 / 智能续写），hunk 带 `layer`，
旧层跟着文档改动映射；右栏新标签「改动」列层（时间 · N 处），整层接受（`acceptLayer`）
/ 整层撤回（`dropLayer`：逐处还原，之后叠在同一处的改动跟着退，面板上说清）。智能续写
是累积 diff，用 `replace` 清旧层。发现并修：智能排版整篇替换会把旧层位置全映射到一点、
账本清空——改走 minimalChange。vitest 79（分层 3 条）。「开关式」的层重放（关掉再打开）
留到下一版。

## [144] 边缘记忆第一版（2026-09-12，优先队列 ④）

记忆不再只在右栏等你转头：`editor/marginMemory.ts` 在正文右缘加一条 gutter
（CM `gutter({side:'after'})`），停止编辑 1.5s 后把含数字 / 日期的段落批量发
`/api/memory/relations/batch`（只要代码判的候选，29 段 1.3s、零 LLM），有关系的段落
段首行右边亮一个点：黄冲突 / 蓝延续 / 绿印证 / 灰缺依据；行号跟着文档改动映射；点一下
光标落到那段、右栏「记忆」的关系卡出来。实拍 demo 复盘三段：两黄一绿。
vitest 81、pytest 808。

## [145] 导入：进度 / 预估 / 用量 / 断点续跑（2026-09-12，优先队列 ⑤）

- 进度按块：item 加 `chunks_total / chunks_done`，job 累计每块耗时 / 字数 / 块数
  （`bump_job_chunk`），SSE 帧带 `job_progress()`：块数、当前在做哪个文件、预计还要多久
  （用本任务的每块中位耗时，没有就用历史平均 / 13s）、已用、token 估算（1600/块 + 字数/1.5）。
- 开始前预估：`_queue` 回 `estimate`，前端 toast「3 篇 · 3 块 · 预计 39 秒 · 约 4910 token」。
- 断点续跑：`_queue` 把清洗好的笔记落盘 `data/jobs/<job>.json`；启动时 `sweep_orphan_jobs`
  把有 payload 的任务标 `interrupted`（没跑完的 item 回 queued）而不是一律失败；
  `POST /api/import/jobs/{id}/resume` 只跑没到终态的 item；导入页顶上「上次没跑完的导入 ·
  继续」。连点两次「继续」409。发现并修：`create_batch_job` 位置式 INSERT 加列后就炸。
- 真跑：造一个跑到一半的任务 → 点继续 → 进度条 1/2 块 · 正在处理「导入探针 B2」· 预计 7 秒
  → 完成 3/3 块 · 16 秒 · 约 3288 token。pytest 810。
- 用户新点名：内存占用太高——进队列 ⑨，先量再砍。

## [146] 内存占用（2026-09-12，用户点名 ⑨，第一版）

先量：正式版 Electron 五个进程闲置 ≈ 375MB（Chromium 常态），后端闲置 40MB；一碰知识库
（树 / 总览 / 召回）后端 +225MB——tracemalloc 指向 memoket-kite `Store.load()` 的词元
倒排表（92MB，145k 个 set）+ 词元字符串（35MB）+ FactRecord（30MB），是 XML 体积的
20 倍。应用层能做的：索引闲置 5 分钟自动放掉（`UserMemory.evict_idle`，main.py 启动
一个每分钟的 janitor），实测 263 → 132MB；`fact_attrs` 派生缓存一起放；`/api/health`
带 `memory.rss_peak_mb` 和抱着几个索引。根治要改包（倒排表 id → int、postings 用数组），
写进 kite-constraints §14 作为下一步。pytest 811。重打 dmg、重开正式版。

## [147] 笔记侧增量（2026-09-12，优先队列 ⑥）

「同一份材料导十次，笔记里还是一篇、LLM 只花一次」之前只做到了知识库那一半——重导会再
建一篇同名笔记，源侧改过的内容永远进不来。`notes` 加 `source / source_id / source_sha /
imported_at`：导入时按 (source, source_id) 找旧篇，sha 没变整篇跳过（item detail「内容没
变」）；变了更新正文并刷新 sha；本地 `updated_at > imported_at`（改过）就不覆盖，只在
detail 说「本地也改过」；知识库侧内容变了先删 `<源>-<源侧 id>-*` session 再重抽（不删的话
稳定 id 会让改过的内容被当已导入跳过）。信息面板加「来源：Obsidian · 何时导入 · 本地改过」。
`tests/test_import_incremental.py` 三条。pytest 814。

## [148] 飞书导入（2026-09-12，优先队列 ⑦）

`database/ingest/feishu.py`：docx v1 的块结构 → markdown（标题 / 段落 / 加粗斜体链接 /
列表嵌套 / 有序 / 代码 / 引用 / 待办 / 表格 / 图片占位 / 分割线，页标题不进正文）是纯函数；
`FeishuClient` 包 tenant_access_token、知识库 space → nodes 递归、云空间递归子文件夹、取块，
`get/post` 可注入。`POST /api/import/feishu`（app_id / app_secret / scope / to），走同一条
item 流水线，`source_id = document_id`。导入页多一行「飞书」。凭证不落库。测试用手造的块
JSON 和假 get/post 跑通整条路（列两篇、跳过 sheet、payload 里 source=feishu）。
**没有真账号，块类型表按官方文档写，实测有出入再调。** pytest 816。

## [149] 导回 Obsidian / Notion / 飞书（2026-09-12，优先队列 ⑧）

原则照 import-sync-plan §2：导回是视图不是双向同步。`build_export` 拆成 `exporters.render_tree`
（路径 / 正文 / 资产 / note_id，zip 导出和目录写入共用，`only=` 可只出几篇），front-matter 加
`memoket_id`。Obsidian：写 vault 目录，`note_remotes` 记路径 + 写出内容的 sha；再导时内容相同跳过、
文件被对方改过（sha 对不上上次写的）报 `conflicts` 不覆盖、勾「覆盖对方改过的」才写——第一版用
mtime 判「对方改过」，自己刚写的文件 mtime 就比 exported_at 新，本地改一次就被当冲突，改成 sha。
Notion：`POST /pages` + 子块，再导按记住的 page id `PATCH` 标题、删旧块、写新块；飞书：建文档 +
`batch_delete` 旧块 + 50 个一批写 children。markdown → 块是粗粒度纯函数（标题 / 段落 / 列表 /
有序 / 代码 / 引用，2000 字切段）。凭证不落库。桌面壳加 `pick-directory` IPC；导入页底部多「导回」
区；信息面板「副本」行（Obsidian · 何时导回 · 之后改过）。terrence 真库跑过：20 篇写出、第二次
全部跳过、资产复制、克隆按 id 只写一次。`tests/test_export_back.py` 五条（假客户端）。pytest 821。

## [150] 叠加 / 合并 + 冲突收件箱（2026-09-12，优先队列 ② 收尾）

六种关系齐了。**叠加**：同一件事知识库里还有你没写的维度（别的单位的量、或你没写日期它有），
说「还有 N 个条件你没写：7-1」，动作「补进来」把那几条带引用插进正文。**合并**：召回的记录里
两条词面重合 ≥ 0.5（双向 ≥ 0.35，短句 min-归一化虚高）→「合成一条」：`POST /api/kb/fact/merge`
把早的标成 `superseded_by` 晚的 + `merged=1`；被取代 / 合并的不再参与关系判断（`_live()`），
「合并结果记住，下次不再问」靠这个，不靠 localStorage。合并候选不要求正文有数字（阈值 0.3）。
**冲突收件箱**：`kb/inbox.scan_session` 在摄入 / 导入每块完成后把新事实各召回一次判冲突，
同篇别的块、已被取代的不算旧记录；`kb_conflicts` 表按 (new, old) 唯一，重扫不堆重复；笔记同步
重抽前先把旧 session 事实指着的待办删掉。知识库首页最上一节「待处理冲突 · N」，三键：新的取代
旧的 / 旧的算数 / 两条都留。实拍三张（收件箱卡、叠加卡、合并卡）；种子事实污染过 shot-demo
的关系探针（420mAh 变成印证），清掉了，合并的两条「王工」留作演示。
`tests/test_kb_inbox.py` 五条。pytest 826，vitest 81。

## [151] 提案层可开关（2026-09-12，优先队列 ③ 收尾）

「撤回」是一次性的，agent-native-editor §3.2 要的是**开关**：关掉再打开。`Hunk` 加 `off / ins`：
关一层 = 每处还原成原文（`shelveHunk`，跟还原改动同一事务，位置直接给定不走映射），把摘下来的
新文字记在 `ins`；关着的处位置照样跟着文档映射、不画、不给悬停条；再打开 = 从后往前核对那处
现在还是不是 `del`，是就写回 `ins`（`unshelveHunk`），不是就丢掉计数，面板提示「N 处因为原文
改过没能重放」。「接受」定稿、「丢弃」忘掉才是终点；关着的层也算待处置（不算的话整层一关
「改动」标签就没了，用户找不到地方再打开）。右栏卡片：开关 + 接受 / 撤回（关着时是「丢弃」），
关着的卡片变淡。`scripts/check-layers.mts` 进 `npm test`：单层反复开关、两层交错、关着时改了原文、
关着时别处打字、200 例 fuzz、探针场景。实拍：格式化层关掉后开关灰、卡片淡、正文回到格式化前。
探针的第二层曾「消失」：追加在文末的那段紧贴格式化的最后一处，按「末尾续写算这处一部分」
被并进了格式化层，关掉格式化就一起收了——层之间重叠的语义就是「后叠在同一处的跟着退」，探针
改成插在第一行后面。

## [152] 上游 memoket-kite 倒排表瘦身 PR（2026-09-12，优先队列 ⑨ 收尾）

`Store.by_token` 一个词元一个 Python set 装 `("F", id)` 元组，20406 条事实的库是 145k 个 set
≈ 90MB，是加载后 206MB 的一半；旁边还有每篇自己切的 CJK 双字串副本 35MB、每条事实预建的
`_fact_terms` frozenset 28MB（只有 `fact_specificity` 读、只对进排序的几条）。改成 `array('I')`
装文档下标 + `_doc_keys[i]` 回解、词元 intern、term set 按需算；`by_token` 做成惰性视图保住
`get(stem, ())/len/迭代/in` 契约，`_lexical_scores_v2` 按下标打分只对返回的行解 key。同一份
codebook：206 → 75MB（峰值 223 → 92MB），加载 2.6 → 3.0s；20 个查询词法通道逐字节一致；包里
原有 9 条失败在 main 上就失败（harness_cli / speaker）。PR：memoket/memoket-kite#8。本仓后端
826 条在分支上全过；venv 装回 main 等合并（dist 不能带未合并的包）。

## [153] 巡检第 122 轮：导回窄窗、取代标注补全（2026-09-12）

优先队列做完，从第 122 轮接着巡检（第 121 轮之后重打过 dmg、正式版已重开，health 正常）。
实拍窄窗（1000×700）导入页：导回区 Notion / 飞书的占位文字被截断（「Integration token（ntn_… /
sec」「文件夹 token（可空」）——占位缩短，说明挪到 title。引用页 / 事实表（深色）没问题。顺手
查了一个口径漏洞：`superseded_by` 只在实体页带，主题页 / 会议页 / 某一天 / 事实表上一条已被取代
或已合并的记录看着跟现行的一样——`pages.annotate()` 统一给四种页面和 `/api/memory/facts` 带
`superseded_by` + `merged`，事实卡 chip 分「已被取代」/「已合并」。pytest 827。

## [154] 巡检第 123–124 轮：按月图、深色白带、月份标签（2026-09-12）

第 123 轮实拍实体页 DVT：只有两个月数据时「按月」的标签「2026-03」折成两行——MiniBars 的容器宽
按 31px × 月数算，两个月才 62px 放不下两个标签；改成至少 132px，只有一个月时只画一个标签，轴上
`white-space: nowrap`。第 124 轮实拍 work 主题页（6034 条）：「按月」横轴写着 2005-11 → 2026-12，
一根 2005 年错抽日期的条跟 2026 年并排画成等宽——`_months(last=24)` 之前取的是「有数据的最近 24
个月」，改成**连续的日历月**（空月补 0、开头一串空月砍掉），错抽的远古日期自然掉出窗口；前端 0
的月份不画（3% 最小高度会把 0 画成有东西）。同一轮深色实拍历史页：正文右侧一条白带——CM 的
gutter 默认 `#f5f5f5` + 右边框，边缘记忆的 gutter 一进来就把它带上了，`.md-editor .cm-gutters`
透明无边。pytest 828、vitest 81。

## [155] 巡检第 125–126 轮：写作计划标题大小写、时间线 / 分屏 / 快速预览复查（2026-09-12）

写作计划弹窗标题「NOTES · 写作计划」——全局 `h2` 是 uppercase（给「批量导入」这类固定段标题用
的），嵌进去的笔记名 Notes 跟着变成 NOTES，像另一篇。笔记名包 `.plain-case`。时间线页：年份
chip 里有 1996 / 2005 / 2007（错抽日期）但页面本身诚实、按月 → 按日的层级正常；会议页缺页态
文案正常；分屏（深色）、快速预览正常，没改。

## [156] 方案：知识库抽什么——抽取偏好可选 / 可自定义（2026-09-12）

用户提：KITE 抽的「要点」应该让用户选、让用户自定义。按「要方案先出文档」的约定写了
`docs/extraction-profiles.md`：现状是 prompt 一句「durable information」+ 固定 7 种 kind，用户碰不到；
方案把「抽取偏好」做成跟写作 Skill 同款的可叠加单元（要点类型 kinds + 关注 + 忽略），六份预设
（会议 / 日记 / 研究 / 访谈 / 项目 / 通用）+ 自定义 + AI 生成 + **试抽**（dry-run 走 `extract_facts`
纯函数、不落盘、左右对照），接入点是现有 `remember(profile=)` 的全局替换加换 `KINDS`，覆盖粒度全局 /
按笔记 / 按导入，上游要做 profile 正式参数化。分三期，判据写在表里。没改代码。

## [157] 巡检第 127–128 轮：回顾等待态、⌘K 找不到导回（2026-09-12）

定期回顾点「最近 30 天」之后页面只有按钮上一个转圈：这是一次几十秒的模型调用，得说在做什么、
等了多久——按钮下面一行「正在把最近 30 天的记录汇总成结论 / 决定 / 待跟进——一次模型调用，通常
20–40 秒 · 已等 N 秒」。⌘K 搜「导」只有导入 / 导出，导回住在导入页最底下没人找得到：加一条命令
「导回到 Obsidian / Notion / 飞书…」，打开导入页并滚到那一块。树右键菜单、搜索、空白笔记 tap
复查没问题（空笔记离开即丢的机制本来就在，探针留下的那篇没落库）。

## [158] 巡检第 129 轮：字数把图片 alt 和链接地址算进去了（2026-09-12）

窄窗多标签实拍「公司汇报」状态栏 27779 字 · 约 69 分钟——正文几百字，多出来的是 AI 配图的 alt
（生成提示词，几千字）和链接地址。`wordCount` 先去掉整条图片、链接只留显示文字。多标签窄窗
（右栏自动收起）、⌘K 面板复查没问题。

## [159] 巡检第 130–131 轮：空库不判关系（2026-09-12）

选中校验、[[ 链接、@ 提及、计划栏、链接页、目录复查没问题。性能探针用户（412 篇、零事实）打开
长文：每一段带数字的右缘都亮灰点「缺依据」——空库时「知识库里没有记录支持这句」对每一句都成立，
满页灰点全是噪音。`/api/memory/relations` 和 `/relations/batch` 先看 `stats().facts`，零就不判。

## [160] 巡检第 132 轮：首页类型 / 说话人 chip 点进去落回总览（2026-09-12）

知识库首页「类型 · 说话人」的 chip 打开的是 `kb:facts?kind=plan`，openVirtual 里「不认识的 kb:* 落到
总览」那条正则只认 `kb:facts` 后面跟 `:` 或结尾，带查询串的被当成不认识——点 chip 回到了总览
（第 132 轮实拍）。正则加 `?`。空库不亮灰点复查通过；跟随系统主题、专注模式复查没问题。

## [161] 巡检第 133 轮：事实表筛选、实体页、窄窗导入页、主题页复查（2026-09-12）

`kb:facts?kind=plan` 修好后事实表按类型筛出 3996 条、行悬停有打开 / 复制；实体页 1220 个（重复实体
memo cat / MemoCat / Memoket、Apple Watch / AppleWatch / 苹果手表 仍是 KITE 抽取侧的欠账，PROGRESS
已记）；窄窗导入页各行放得下；主题页六个一级主题卡片正常。本轮没改代码。

## [162] 巡检第 134 轮：合并卡的措辞（2026-09-12）

选区探针实拍右栏合并卡：「知识库里 2026-03-10 和 2026-03-10 这两条说的像是同一件事」——同一天的
两条被念成「X 和 X」。同日改说「2026-03-10 有两条说的像是同一件事」。斜杠菜单、快捷键表、选区右键
菜单、树键盘导航复查没问题。

## [163] 巡检第 135 轮：移动 / 克隆选目标的长标题（2026-09-12）

「移动到…」的目标选择器里首行当标题的一句话笔记（「我们产品当前遇到的挑战：四项核心挑战归纳为…」）
直接顶出右边框——行是 flex，容器上的省略号对文本节点不生效。标题包一层 span 省略，完整的放
title。删除确认框、格式化后的改动层、链接测试篇的引用页复查没问题。

## [164] 巡检第 136 轮：探针草稿漏进真实笔记（2026-09-12）

「文末」探针实拍 harness 测试笔记结尾多了一行「（这一段是上次没存上的草稿）」——那是 `draft:`
探针塞进 localStorage 的假草稿：探针模式 save() 被拦，草稿永远「存不回去」，键留在 dev 实例的
localStorage 里，下一次不带探针启动 dev 实例（第 120 轮验证自动重启那次）打开这篇就被当真草稿恢复并
自动保存。修：探针模式下恢复草稿后立刻删键；库里那行已清掉（8890 → 8875 字）。移动选择器长标题
省略生效；键盘导航复查没问题。

## [165] 巡检第 137 轮：图片拖入、页内查找、文件拖入树复查（2026-09-12）

三个都正常：图片走资产库（同一张探针图只存一份）、查找条替换齐全、系统文件拖到树上成一篇
真笔记。后者是真导入路径，探针每跑一次留一篇「拖进来的（可删）」——跑完手动删，记进 memory。

## [166] 巡检第 138 轮：标签菜单、窄窗深色设置、缺会议页复查（2026-09-12）

都正常，没改代码。下一轮跑两条真模型调用的路（magic tap、来龙去脉）确认这几天的改动没伤到主流程。

## [167] 巡检第 139 轮：magic tap 编造的引用（2026-09-12）

跑了两条真模型调用：magic tap 续写、来龙去脉。都通，但 tap 写出来的一段里有
`[terrence-23F3-4F3]`——段落顺序不对的假 id，它不匹配引用正则，于是既不被 grounding 判据抓、也不画成
引用，就那么留在正文里。单篇 harness 早就有「查不到的引用自动摘掉」的判据，tap 这条路没接。
`citations.py` 加 `malformed_citations`（长得像引用但格式不对的方括号：至少两段短横、不跟 `(`、
不在 `[[` 里）和 `fake_citations`（= dangling + malformed）；tap 写完在 done 事件里报
`fake_citations`，前端只从刚写的那段里摘掉并提示「摘掉了 N 个知识库里查不到的引用」。
pytest 830、vitest 82。

## [168] 巡检第 140 轮：选区重写 / 润色真跑（2026-09-12）

选区「重写」「润色」各跑一次真模型：改动按层进右栏「改动」标签（6 处 / 2 处），字级删改高亮、
长删折成「已删 53 字」胶囊，右栏关系卡（印证）同时在。没改代码。下一轮跑一遍完整的智能续写
harness。

## [169] 巡检第 141 轮：智能续写 harness 真跑 + 正式版进程（2026-09-13）

`harness:` 探针跑完整智能续写：第 2 轮在跑，自动修订一处（状态栏 + 顶部条都有）、绿色层、
骨架面板、引用 18 → 23，`harness-sync` 只报了 2 字的本地 / 服务端差异并按服务端对齐。没改代码。
顺手查正式版：21:59 拉起、22:15 自己退了（userData 的 window.json 是那时写的，说明是正常退出
路径而不是崩溃），dev 探针实例走的是另一份 userData 和端口，还没定位到是谁让它退的。重新拉起后
隔 20 秒再查仍在——继续观察下一轮探针跑完它还在不在。

## [170] 巡检第 142 轮：会议页标签裸 id、首页最近摄入换行（2026-09-13）

shot-demo 首页实拍：标签栏挂着「note-674aa9a1b4b7-0」——会议页（kb:unit:）的标签标题取的是裸
id（`/^kb:(topic|entity|unit):/` 那条分支排在「会议记录」前面，后者永远走不到）。改成先叫「会议记录」，
会议页加载到标题后发 `virtual-title` 事件把标签改成真名（「5 月 8 日硬件例会（摄入探针）」）；
旧标签启动时补名。首页「最近摄入」一行里标题长了日期折成两行、「4 条」竖排：标题省略、日期 / 条数
不换行。正式版重新拉起后跨了几轮探针仍在。vitest 82。

## [171] 巡检第 143 轮：长材料切成的段分不清（2026-09-13）

「最近摄入」十行「2026-03-12 · 围绕个人记忆库与产品化落地的战略讨论」同名同日期——一篇长材料导入
时按块切成 `<源>-<id>-0/1/2…` 好几个 session，标题日期一模一样。`kb/units.part_labels`：id 去掉
末尾块号相同、标题日期相同的归一组，多段的标题带「（k/n）」；树上「最近摄入」、首页、会议页
标题三处都用。主题页（深色）、实体页复查没问题。pytest 831。

## [172] 巡检第 144 轮：新用户首屏（2026-09-13）

用一个全新用户跑：欢迎页、空知识库、空白笔记点续写都对。空白笔记的占位文案还写着「点 magic tap」，
按钮早就叫「续写」了——改成「点右上角「续写」」；按钮 title 同步。探针留下的 `data/shot-fresh*`
空 codebook 目录清掉。

## [173] 巡检第 145 轮：摄入 → 冲突收件箱端到端真跑（2026-09-13）

shot-demo 的「5 月 29 日复盘（关系探针）」走真实抽取「存入知识库」：抽出 4 条（420mAh / DVT 9-1 /
订金 199 / 退款期 7-1），摄入完 `inbox.scan_session` 记下一条冲突（DVT 9-1 vs 旧记录 8-5），
首页「待处理冲突 · 1」卡片带旧 / 新记录、来自笔记链接、三个按钮。电池 420mAh 没进收件箱是对的：
知识库里 300 → 380 已经是一条线，420 判成「延续」不是冲突（正文右缘也是蓝点）。跑完把这篇的
session、冲突和摄入标记清掉，关系探针的基线不变。没改代码。

## [174] 巡检第 146 轮：shot-demo 事实表 / 搜索 / 时间线复查（2026-09-13）

小库（44 条）上事实表、搜索「DVT」（深色，命中高亮）、时间线（月 → 日）都正常，没改代码。

## [175] 巡检第 147 轮：出处浮层、主题地图、断点续传入口、图缩放（2026-09-13）

四个都正常。把今天所有探针日志里的 `[client:*]` 和后端 traceback 汇总扫了一遍：只有 first-paint /
perf 的常规上报，唯一一条 traceback 是 9 月 12 日凌晨第 14 轮那次（Answer.evidence，早修了）。
没改代码。第 121 轮之后又改了 26 轮，按「每约 10 轮重打 dmg」的纪律重打并重开正式版：新包的后端
带上了 exporters / inbox / units（`/api/kb/dashboard` 有 `conflicts_open`、`/api/export/remotes` 通）。

## [176] 巡检第 148–149 轮：边缘记忆 / 首页深色 / 标签 / 引用页复查，纯函数 fuzz（2026-09-13）

第 148 轮四张截图都正常（引用页那条「找不到 terrence-9999-FFF」是探针故意塞的）。第 149 轮换个
路子：把关系检测、值抽取、markdown → Notion / 飞书块、编造引用判定、段号、导出文件名这些纯函数
用随机中英数字标点混合串喂 3000 例，零异常；留 600 例做回归 `tests/test_fuzz_pure.py`。

## [177] 巡检第 150 轮：窄窗实体页 / 时间线 / 引用页（2026-09-13）

1000×700 下实体页图、时间线（深色）、引用页 + 编辑器上下分栏都放得下，没改代码。

## [178] 正式版闲置回收实测（2026-09-13，优先队列 ⑨ 收尾核对）

新包拉起后打了一次首页（索引加载，`rss_peak_mb` 325），闲置 5 分钟后 `/api/health`：`entries 0`、
`rss_mb 186`——索引放掉了，Python 进程 325 → 186MB；Electron 主进程 142MB。跟第 146 轮 dev 实测
一致，⑨ 的应用层部分核对完；根治等上游 #8 合并。

## [179] 巡检第 151 轮：`/` 智能表格真跑（2026-09-13）

文末 `/` 智能表格：占位块「生成表格 · 第 2 轮：在看要用哪些材料…」+ 停止按钮，35 秒时还在取材料
（这个块走工具循环，比续写慢是设计内的），没有报错。没改代码。

## [180] 巡检第 152 轮：`/` 块跑满三轮的浪费（2026-09-13）

数据可视化探针 45 秒时还在第 2 轮，于是把生成表格那条路整段计时看事件：第 1 轮 render_table 出了
一张好表（table_validity 2，只有 fits_context 没达标），第 2、3 轮模型写了句「[tool call needed]」
交卷——没表，每轮还各花一次打分调用，最后交的仍是第 1 轮（best_of 兜住了），总共 58 秒里后 35 秒
是白跑。两处改：① `checks/structure.table_present`——生成表格的产出里没有 markdown 表就先判掉
（`blockcheck.has_table`：表头 + 分隔行），不打分、下一轮提示直接说「必须真的调 render_table」；
② 内置停机条件 `regressed`：最好的一轮只差一个维度没达标、这一轮排名反而更低、还不是最后一轮
——停，交最好的那轮（单维度的 Mode 要求最好那轮全达标才算「接近」，不然一维度模式随便一轮差就停）。
前端智能续写的结束文案加「再改反而更差，留下了最好的那轮」。harness-framework §8 变 11 条 check、
停机条件加 regressed。再跑一次表格：第 1 轮直接 complete，24 秒。pytest 836。

## [181] 巡检第 153 轮：数据可视化在没数据的地方硬画三轮（2026-09-13）

把 `/` 用 AI 写（15 秒一轮 complete）和数据可视化整段计时：光标放在一段没有数字的复盘文字旁，
数据可视化跑了 89 秒三轮——`numbers_near_cursor` 只找到几条带引用编号的句子，模型硬画了「第 5 /
10 / 15 位用户」「节点状态 1/1/1/1/0」这种图，打分器每轮都给 numbers_from_tools=0，最后交的还是
一张没意义的图。规则就能判「这里没东西可画」：`Mode.precheck`（跑之前的确定性门槛，返回一句原因
就直接 blocked，一次模型调用不花），EDA 的门槛 = 光标前后 1200 字没有写数字的句子、旁边也没有
不隔标题的表。前端占位块把 blocked_reason 写出来（之前只会说「没有产出内容」）。顺带修了
「有数字」的口径：`numbers_near_cursor` 之前把引用 id（[terrence-1462-9F5]）、日期（8 月 5 日）、
序数（第 5、10、15 位）、型号（iOS16）都当数字——`tabular.has_quantity` 先剥掉这些再看还剩不剩
数字。那段复盘旁边 1200 字里还有一句真量（「EVD 标准样机 500 至 1000 台」），所以门槛放行、模型画了
一张诚实的「计划下限」图（85 秒）；门槛在纯文字段落上直接 blocked 由单测保证。pytest 840。

## [182] 巡检第 154 轮：定期回顾真跑（2026-09-13）

`POST /api/digest days=30`：10.7 秒，按事实日期取到 49 条（08-14 ～ 09-13），四节（核心结论 /
关键决定 / 待跟进 / 值得注意的变化）都有内容、都对得上知识库里的记录。没改代码。

## [183] 巡检第 155 轮：校验的证据来源（2026-09-13）

`POST /api/verify` 真跑：选中「4 月 16 日的 EVT 原计划是纯主机节点，准备 4 台主机和 15 套 PCBA」，
第一次答「支持，笔记正文明确写明…」（fact_id 空）——拿被选中的那句自己当证据，循环论证；提示词
改成「待核查内容本身出现在正文里不算依据，只有另一处独立记载才算」之后答「无法判断，知识库没有
记录」——其实用户就在这句后面引了 [terrence-2046-12F1…F9]，词法召回没捞回来。改 `verify`：先把
选区所在段落里引用的事实按 id 取出来放在材料最前面，再补召回。再跑：三条「支持」各带事实 id
（纯主机 / 4 台主机 15 套 PCBA / 手板），8.7 秒。`/api/expand` 选半句会回空修订，合理。
`tests/test_verify_cited.py`。pytest 842。

## [184] 巡检第 156 轮：来龙去脉的英文拒答、骨架真跑（2026-09-13）

`POST /api/memory/trace` 两段：DVT 那段 23.6 秒答出带引用的三条演进，好；电池那段（terrence 的库里
本来没有）17.7 秒回了一行英文「No information」，下面却还列着 10 条召回的记录——KITE 的拒答常量。
路由里把 `No / Not enough / Insufficient information` 换成中文「知识库里的记录串不出这件事的来龙
去脉——下面是最相关的几条，可能只是沾边」。骨架（`/api/skeleton`）10 秒，一句核心张力 + 六个节拍，
质量对得上正文。pytest 843。

## [185] 巡检第 157 轮：关系确认、智能排版真跑（2026-09-13）

`/api/memory/relations` 带模型确认：DVT 8-5 vs 库里 6-3 判成冲突并写出一句人话（5.2 秒）；
KOL 样机那段只给叠加（terrence 库里没有台数记录，对的）。`/api/compose/restructure` 7.2 秒，
标题层级和 mermaid 都保住了。没改代码。

## [186] 巡检第 158 轮：本地接口的跨站写请求（2026-09-13）

查了一遍本地 API 的越权面：后端只绑 127.0.0.1，CORS 白名单只有 Vite 开发页，`X-User-Id` 没有鉴权
（原型设计）。CORS 只管「跨站读不读得到」：跨站网页用 `<form>` / multipart 发的 POST 是简单请求，
不预检、照样执行——端口又是固定的 47231，任何网页都能往导入 / 上传接口塞东西（写进 default 用户）。
`main.py` 加一层：非 GET 请求带着非本机的 `Origin`、或 `Sec-Fetch-Site: cross-site`，403。Electron 页面
同源、Vite 页在 localhost、测试客户端不带 Origin，都不受影响（探针实测 POST 全 200）。README 记了
一段。`tests/test_cross_site.py`。pytest 844。

## [187] 巡检第 159 轮：user id 直接拼路径（2026-09-13）

`X-User-Id` 原样拼进 `data/<user>/codebook.xml`，`../x` 这种能走到文件系统别处。`deps.current_user`
只认 `[A-Za-z0-9_.-]{1,64}`（HTTP 头只能 ASCII，中文名本来就发不出来；跟桌面壳 identity.json 一条
正则），不合规 400；切换用户的输入框同一条正则先拦一道。pytest 845。

## [188] 第 160 轮：重打 dmg、正式版核对（2026-09-13）

第 147 轮之后又改了 13 轮（段号、表格判据、precheck、校验证据、跨站写请求、user id 校验），重打
dmg 重开正式版：health 200、同源 POST 200、跨站 POST 403、`X-User-Id: ../x` 400。

## [189] 巡检第 161 轮：图片上传被拒时的兜底（2026-09-13）

图片粘贴 / 拖入：上传到资产库失败一律退回内嵌 base64——这个兜底是给「后端没接上」的，但后端
明确拒掉的（超过 32MB、不支持的类型）也走了它：一张 40MB 的图会被转成 53MB 的 base64 塞进正文。
改成只有 fetch 本身失败（TypeError）才内嵌，4xx 按后端的话提示、不插。另量了列表接口：413 篇
579KB / 10ms，每次保存后 `reload()` 整表重拉暂时还行，记进欠账。

## [190] 巡检第 162 轮：删除是硬删，5 秒撤销窗口过了就没了（2026-09-13）

`DELETE /api/notes/{id}` 直接删行（连同子树、引用、历史版本），前端只有一个 5 秒的「撤销」toast——
Trilium 的删除是可找回的。加「最近删除」：删之前把笔记行 + branches + 最近 20 个历史版本存成快照进
`note_trash`，30 天内 `GET /api/notes/trash` 列出、`POST /trash/{id}/restore` 放回原位（id 不变，
[[链接]] 和知识库反链都还认得；父节点也删了就挂到树根；历史版本一起回来）、`DELETE /trash/{id}`
彻底删；读列表时顺手清超期的。前端 `app:trash` 页（⌘K「最近删除」），删除 toast 加一句「30 天内可
在最近删除找回」。顺手把树上那篇空「未命名」删进了回收站。`tests/test_trash.py` 三条。

## [191] 巡检第 163 轮：今天的日记（2026-09-13）

Trilium 的 day note 一直没有：`POST /api/notes/today` 按标题沿 `日记 / 2026 / 09 月 / 09-13 周日`
找或建（幂等，同一天永远同一篇；没有属性系统就按标题找，用户改了「日记」的名会另起一棵，有意为之），
正文预填「# 9 月 13 日 周日」。前端：launcher 一个日历键、⌘⇧D（Trilium 同键）、⌘K「今天的日记」、
快捷键表加一行。`tests/test_today.py`。pytest 849。

## [192] 巡检第 164 轮：同步真跑 + 笔记摄入的块进度（2026-09-13）

`POST /api/ingest/note/{id}/sync` 真跑（shot-demo 那篇摄入探针 2）：12.8 秒，旧 session 4 条换成
新抽的 5 条、没有重复、stale 回到 false。但状态栏只有「存入知识库中…」一个转圈——笔记摄入 / 同步
这条路没记块数和每块耗时（批量导入早就有）。`_ingest_job` 加 `set_job_chunks_total` +
`mark_job_started` + 每块 `bump_job_chunk`（`ingest_jobs` 加 `chunks_total` 列，没有 item 的任务
自己记），`job_progress` 没 item 时按 job 自己的数算；状态栏改成「存入知识库中… 第 2/5 块 · 还要约
40 秒 · 已抽出 3 条」。实拍（关系探针那篇点存入）：状态栏「存入知识库中… 第 1/1 块 · 还要约 7 秒」；跑完照旧把那篇的 session 清掉。

## [193] 巡检第 165 轮：无限续写计划真跑（2026-09-13）

`plan-run:` 探针对着 terrence 的「Notes」子树：start 建计划（三条线各一篇）、run 起第一篇，100 秒时
「硬件线：产品形态与一年迭代证据」已写 853 字、右下角跟随气泡「🚀 Notes · 正在写：…」、状态栏
🚀 Notes、子树下多了「📋 写作追踪」。一切按设计。内容里混进了知识库里华为汇报的 950 超节点 /
昇腾——库里两家公司的记录混在一起，检索按词面命中就都来了，这是知识库分库（按来源 / 项目）的
事，记进欠账。跑完把两篇和计划行清掉，基线不变。没改代码。

## [194] 巡检第 166 轮：树上拖拽（2026-09-13）

`dnd:` 探针把「5 月 22 日复盘」拖到「4 月 10 日产品周会」上：`PATCH /api/tree/branches/move` 200，
目标变成带 1 的文件夹、被拖的进去了。没改代码；跑完 `move_branch` 挪回根节点原位置。

## [195] 巡检第 167 轮：「文件」菜单（2026-09-13）

应用菜单的「文件」是 Electron 默认的 fileMenu（只有关闭窗口），Trilium 的 File 菜单有新建 / 日记 /
导入 / 导出。换成自己的：新建笔记 ⌘N、今天的日记 ⌘⇧D、导入…、导出全部笔记…、最近删除、关闭窗口。
快捷键用 `registerAccelerator: false` 只显示不注册——按键仍由渲染进程处理，免得 ⌘N 被菜单吃掉
再发一次事件建两篇。渲染进程的 onMenu 多认 new-note / today / import / trash 四个名字。

## [196] 巡检第 168 轮：空库上的三条路（2026-09-13）

新用户（零事实）上：定期回顾 0.02 秒直接答「这段时间没有记录」，好；来龙去脉却花了 12.4 秒——
KITE 的 ask() 在空库上照样跑一次规划调用再答「No information」，而且中文化后那句「下面是最相关的
几条」下面一条都没有。`trace` 先看 `stats().facts`，空库 0.02 秒直接答「知识库还是空的」；召回为
空时拒答改成「知识库里没有跟这段沾边的记录」。校验在空库上 2.2 秒一次调用是查笔记内部一致性，
留着。

## [197] 巡检第 168 轮（续）：空输入也在花模型调用（2026-09-13）

接着量空输入：骨架空正文 3.3 秒答「内容尚未提供」，重写 / 扩写 / 校验空选区各 2.7–4.1 秒回空。
四条路进门先看一眼：空正文且空标题、空选区直接 400（「先写点内容再生成骨架」「没有选中内容」），
一次模型调用不花；关系 / 召回在空库上本来就是 0 秒。`tests/test_input_guards.py` 用打桩的模型断言
一次都没调。

## [198] 巡检第 169 轮：模型用量账本（2026-09-13）

用 GPT 的用户不知道钱花在哪儿：`util/llm.py` 没有任何用量记录。加 `llm_usage` 表：非流式从响应体
的 usage 取，流式给 `stream_options.include_usage` 让最后一帧带 usage，`_consume_sse` 收完记一笔；
「谁、哪个功能」由请求中间件按 X-User-Id 和路径（去掉 /api/ 和 id 段：notes/abc/sync → notes/sync）
放进 contextvar，后台任务里是空。`GET /api/settings/usage`：今天 / 7 天 / 30 天 / 全部的次数和
token，按功能列前 8，用过的模型。设置页多一节「模型用量」，功能名映射成中文（续写 / 智能续写 /
块生成 / 校验…）。带思考的流（`stream_events`，修订轮用）同样记。KITE 抽取拿不到 usage：每块按
字数估一笔（1200 固定 + 字数 / 1.5，每条事实 60），功能名 `kb/extract~` 带波浪号、设置页标「估算」。
批量关系判定 80 段 0.93 秒、记账失败不影响调用。

## [199] 巡检第 170 轮：记忆范围（2026-09-13）

第 165 轮那个「硬件线里混进华为 950 超节点」的根子是一个库三种来源混着召回。第一版缓解：
`database/kb/scope.py` 按 session id 前缀分三档（`note-` 笔记摄入 / `obsidian- notion- feishu-
apple- evernote-` 导入 / 其余是会议记录），`UserMemory.recall(scope=)` 限范围时多取 4 倍再滤，
`recall_clustered` / `retrieval.retrieve` / 召回 / 关系 / 批量关系 / magic tap 全带 `scope`。前端
右栏「记忆」顶上一个下拉「全部记忆 / 只看笔记 / 只看会议记录 / 只看导入的」，存 localStorage，
api 层自己带上，换了范围召回、关系卡、页边圆点都重算。智能续写 harness 那条路和真正分库仍在欠账。
`tests/test_memory_scope.py`。pytest 857。

## [200] 巡检第 171 轮：记忆范围接进 harness（2026-09-13）

第 170 轮的范围只到召回 / 关系 / magic tap；智能续写、`/` 块生成、取材料的工具（search_memory /
filter_facts…）还是全库。`ToolContext.scope`（NoteHarnessRunIn / ComposeBlockIn 带过来，前端从
localStorage 取）→ hooks/note.py 两处 `_retrieve(scope=)`、memory_tools 的 recall / recall_clustered
都带。深色下的范围下拉正常。PROGRESS 的分库欠账只剩「真正分库」。pytest 858。

## [201] 巡检第 172 轮：孤儿写作计划（2026-09-13）

看了一眼 dev 库：`writing_plans` 里 15 个 active 计划，父节点全都已删——探针跑完删掉子树，计划
永远 active、指着不存在的节点。`delete_note` 把挂在被删子树上的 active 计划标 abandoned；启动时
`sweep_orphan_plans` 扫一遍老库（这次扫掉 15 个）。`tests/test_trash.py` 加一条。pytest 859。

## [202] 巡检第 173 轮：库里的孤儿行（2026-09-13）

对 dev 库跑了一遍孤儿查询：branches / 引用 / 历史版本都干净（删笔记时一起走），但作废计划下
指着已删笔记的 `writing_sections` 有 261 行、导回记录有 1 行指着既不在笔记也不在回收站的 id。
`sweep_orphan_plans` 顺手清这两类；启动扫一遍。

## [203] 巡检第 174 轮：测试往开发库写东西（2026-09-13）

`harness_runs` 里有 `t:n` / `chart:n` 各 50 行——是 `test_harness_loop` 带 BASE 中间件跑循环时
Save / RunHistory 写进了**开发库**：96 个测试文件里只有 47 个记得 monkeypatch `store._db_path`。
加 `tests/conftest.py`：autouse 夹具让每个测试默认用自己的临时 sqlite 和临时 codebook 目录，并在任何
app 模块 import 之前把 `KITE_DATA_DIR` 指到临时目录（skills / assets / backups / jobs 是按名字绑的
`get_settings`，事后 monkeypatch 够不着——实拍 test_endpoints 在 data/ 下长出 `u1/skills`），退出时
删掉。跑完整套：开发库 mtime 不变、data/ 目录列表不变。顺手清掉那 100 行运行记录和 13 个测试留下的
用户目录（cancel-test / smoke-user / u9…）。pytest 859。

## [204] 巡检第 175 轮：知识库树 391KB，每次保存都重拉（2026-09-13）

量了几个重端点：首页冷 761ms（索引加载）热 12ms，`/api/kb/tree` 10ms 但 391KB——1220 个实体节点占
358KB，而 `reloadTree` 在每次保存后都重拉一遍。实体超过 200 个就不随树下发（`ENTITY_EAGER_MAX`），
「实体」/「某类实体」节点只带数量，展开时走 `children?node=kb:entities` 取（13ms 333KB，只在展开
时一次）；实体索引页自己取一次；实体页加载到名字后把标签从代码改成真名（`virtual-title`）。树降到
57KB / 189 行。小库（≤200）照旧随树下发，前端展开时看树里有没有孩子、不重复取。pytest 860。

## [205] 巡检第 176 轮：保存时不重拉知识库树（2026-09-13）

`reloadTree` 在 22 处被调，每次都顺带重拉知识库树；其中 `save()` 那一处是每次自动保存都走的——
正文变了知识库不会变（引用计数在笔记树那边）。`reloadTree(kb=true)` 加个开关，`save()` 传 false。
配合第 175 轮的懒加载，自动保存的开销从「树 + 391KB 知识库树」变成只有 9KB 的笔记树。

## [206] 巡检第 177 轮：磁盘上的遗留（2026-09-13）

`data/jobs` 里 36 个导入 payload（跑完的也留着）、`data/backups/kb` 下 24 个用户目录里 20 个是测试 /
探针留下的（cancel-test、shot-fresh-*、u9…）。启动时 `prune_job_payloads` 删掉 done / error /
cancelled 和无主的 payload（interrupted 的留着断点续跑）；`maybe_backup_kb` 顺手把用户目录已经不在
的备份清掉。这次清掉 35 个 payload、20 个备份目录。pytest 862。

## [207] 巡检第 178 轮：懒加载之后的树 / 键盘 / 引用页复查（2026-09-13）

知识库首页、树的键盘导航（深色）、引用页都正常，client-log 无错。右栏「记忆」的说明文字加了范围
下拉之后折成两行，缩短成「跟着正文自动浮现，点一下插入引用。」。

## [208] 巡检第 179 轮：并发（2026-09-13）

两个用户四个请求同时打（召回 ×2、关系 ×1、批量关系 ×1）：全 200，墙钟 1.73 秒，terrence 那两个
等的是同一次冷索引加载（1.7 秒），shot-demo 的 0.5–0.6 秒。没改代码。第 180 轮重打 dmg。

## [209] 第 180 轮：重打 dmg（2026-09-13）

第 160 轮之后 20 轮：最近删除、今天的日记、文件菜单、模型用量、记忆范围、实体懒加载、启动清理、
空输入 400、编造引用摘除、conftest 隔离。重打 dmg 重开正式版：health / 最近删除 / 用量 / 知识库树
（59KB）/ 今天的日记 全通；顺手把核对时在正式版里建的日记删掉并从回收站清掉，正式版的库不留探针
痕迹。

## [210] 巡检第 181 轮：事实页（2026-09-13）

事实页本身没问题（原话可展开、被哪些笔记引用、相关事实）。两处小的：标签叫「事实 terrence-2046-
12F8」——拿到正文后改成前 18 个字（跟会议页 / 实体页同一条 `virtual-title` 路）；相关事实列表里
日期列「2026-\n01-21」会折行，日期 nowrap、正文另起 span。

## [211] 巡检第 182 轮：写作 Skill 增删改 + 深色（2026-09-13）

Skill 的建 / 开关 / 改 / 删走一遍 API 全 200、删后 404；深色页正常。作用域列表里还写着
「续写（magic tap）」——界面上早叫「续写」了，改掉。

## [212] 巡检第 183 轮：⌘K 搜知识库、深色回顾（2026-09-13）

- 实拍 `palette:DVT`（浅色）：笔记命中 + 知识库命中并列，关键词高亮正常；`digest:7 --dark`：45 条事实的回顾在深色下排版、对比度都对。
- 没改代码。

## [213] 巡检第 184 轮：阶段回顾漏掉了记忆范围（2026-09-13）

- **问题**：第 165 轮加的「记忆范围」（全部 / 只看笔记 / 只看会议记录 / 只看导入的）管住了召回、关系、续写，但「定期回顾」还是把三档混在一起总结——切了范围再回顾，别人公司的会议照样进来。
- **改**：`POST /api/digest` 加 `scope`（`kb/scope.filter_rows` 过滤后再喂模型）；前端 `digest()` 自动带 `memoryScope()`；回顾面板说明里显示当前范围（非全部时），并监听 `memory-scope-changed`。
- 顺手：范围内空结果的提示改成「这段时间『只看笔记』范围内没有记录，换成『全部记忆』再试」（之前只说「没有记录」，看不出是范围导致的）；0 条事实时「存为笔记」置灰。`SCOPE_LABEL` 挪进 `kb/scope.py` 供后端复用。
- 探针 `digest:30:notes` 支持第三段指定范围。实拍：说明行显示「只看『只看笔记』」，空结果提示带范围，存为笔记置灰。
- 测试：test_memory_scope +2（范围过滤条数 / 提示词只含该档事实；空结果提示带范围），后端 864 passed；vitest / check 脚本全过。

## [214] 巡检第 185 轮：还有四个取记忆的入口没带范围（2026-09-13）

- 顺着上一轮查：`/verify`（校验）、`/expand`（扩写）、`/writing-plan/start` 与 `/writing-plan/run`（每节取材料的 ToolContext）都不收 `scope`，切了「只看笔记」照样全库召回。
- **改**：四个入参各加 `scope="all"`，透传到 `retrieve()` / `recall()` / `ToolContext.scope`；前端四个调用自动带 `memoryScope()`。校验里正文显式引用的事实不受范围限制（用户自己引的，永远算证据），只有词法召回受限。
- 范围下拉的提示改成列全受影响的功能。
- 测试：test_memory_scope +1（假 recall / retrieve 记录收到的 scope），后端 865 passed；前端全绿。纯参数透传，没有可见 UI 变化，没截图。

## [215] 巡检第 186 轮：真库跑校验 / 扩写带范围（2026-09-13）

- terrence 的「harness 测试」那篇，选「矩阵不能只保留 4月16日EVT…」一句：校验 all 8.0s「支持」（判据来自笔记内部，fact_index=-1，fact_id 为空是对的）、notes 3.8s「无法判断」、meetings 5.7s「支持」——范围真的换了证据来源。扩写 all / notes 各 4 秒，模型判前后都已经写过、返回空 before/after（finish_reason=stop，不是撞上限），前端会提示「模型认为不需要补充上下文」。
- 没改代码。顺手发现 dev 库里还躺着隔离之前的测试用户（cancel-test*/cleanup-test/harness-test-2/search-test/test-skeleton/apple-test/import-test/quality-sample），下一轮清。

## [216] 巡检第 187 轮：运行记录的孤儿只清了 note: 一种（2026-09-13）

- **问题**：`harness_runs.key` 是 `<模式>:<笔记 id>`（note: / section: / prompt: / table:），更早的行是裸笔记 id。`drop-orphans-v1` 只认 `note:` 前缀，dev 库里 3899 条运行记录有 3858 条裸 id 指向早已删掉的测试笔记（9 月 3–8 日隔离之前的测试留下的）；`delete_note` 也只删 `note:<id>`，分段模式跑出来的 `section:<id>` 会留下来。
- **改**：新迁移 `drop-orphan-runs-v2`——冒号后那截（没冒号就整个 key）不在 notes 里就删；`delete_note` 改成 `key=<id> OR key LIKE '%:<id>'`。下次起后端自动跑。
- 测试：test_delete_cleanup +1，后端 866 passed。
- 没做的：dev 库里隔离前的测试用户（cancel-test*/cleanup-test/harness-test-2/search-test/test-skeleton/apple-test/import-test/quality-sample/skilltest/u/writing-bench/harness-smoke-test）批量 DELETE 被自动模式拦下了（不可逆的本地删除）。库已备份到 scratchpad `notes-before-r187.sqlite3`，等用户点头再清；对产品没影响（每个用户各看各的）。

## [217] 巡检第 188 轮：写作计划对话框、双链补全（2026-09-13）

- 起后端后 `drop-orphan-runs-v2` 跑了：harness_runs 3899 → 32 行。
- `plan-panel` 实拍：站在「日记」文件夹上点无限续写，对话框标题却是「09 月」——选父节点只看当前笔记的父节点，当前本身是文件夹时没优先它。改成「当前是文件夹就用它」。对话框说明里加「取材料只看『只看笔记』」提示（跟回顾面板一致）。
- `wikilink --dark` 实拍：`[[创业` 弹出三条同名「创业一年回顾」，只靠日期分不开。补全项重名时在日期后面加一截正文首句（优先正文行，小标题只兜底——「时间线与里程碑」几篇都一样）。vitest +2（noteLinkCompletion.test.ts）。
- 探针垃圾：记忆范围存 localStorage，上一次 `digest:30:notes` 切的「只看笔记」留给了后面每一次探针（右栏莫名只看笔记）。探针开始时没指定范围就复位到全部。
- 三张实拍都核对过。前端 84 tests，后端 866。

## [218] 巡检第 189 轮：历史版本页、出处浮层（2026-09-13）

- `history-open`（空文件夹「日记」）：「还没有历史版本」文案 + 「现在存一版」；`fact-peek --dark`：浮层日期 / 要点 / 原话在深色下都对。
- **问题**：右栏对着「据 [terrence-1872-5F8] 所述。」召回了五条不相干的事实——引用 id 把 4 个字撑过了 8 字门槛，召回查询里也带着 id。`stripCitations()` 挪进 `util/wordCount.ts` 一处定义，相关记忆的尾巴门槛、关系判断的段落门槛、`tooShort` 都先去掉引用标记；实拍改后右栏只显示「再多写几个字就会开始自动检索」。
- 顺手：0 字时状态栏 / 信息面板不再显示「约 1 分钟」。
- vitest +2（stripCitations.test.ts），前端 86；后端未动。

## [219] 第 190 轮：重打 dmg（2026-09-13）

- 含 184–189 轮：回顾 / 校验 / 扩写 / 写作计划遵守记忆范围、运行记录孤儿清理、文件夹上开无限续写、`[[` 补全重名带首句、相关记忆门槛去引用 id。
- `npm run dist` → `out/MEMOKET NOTE-0.1.0-arm64.dmg`（183MB）。杀掉第 180 轮那份，起新的：`/api/health` ok，`POST /api/digest {days:7, scope:"imports"}` 200，回顾只总结了导入那份年报（正式版库里 terrence 的导入）。

## [220] 巡检第 191 轮：召回排序英文词永远不得分（2026-09-13）

- 真库计时：关系判断（零 LLM）all 0.8s / notes 0.0s / meetings 0.0s；来龙去脉 all 20.4s、notes 19.0s（KITE ask() 那条路，不受范围约束，是已知的「分库是后话」）。
- **问题**：拿笔记里原封不动的一句「4月16日的EVT准备4台主机和15套PCBA…」去召回，前 8 条里没有「EVT 的大节点是 4 月 16 号」（terrence-2046-12F1），关系判断一条都判不出来。翻 `kb/search.py rank()`：英文候选词是小写的（evt / pcba），事实原文里是大写（EVT / PCBA），比对用大小写敏感的 `in`——英文词对模型抽出来的事实永远不得分；'evt' 出现两次还算两次。另外数字完全不参与：英文通道只认字母、中文通道只认汉字，「4月16」一分都没有。
- **改**：`_terms()` 去重 + 小写；两边比对前去掉空白（事实写成「4 月 16 号」）；新增数字通道 `_number_terms()`——只认「月+日」、带中文量词的（4台 / 15套）、三位以上的，光秃秃的一两位数不要（试过收「15」，把「5 月 15 号」「15 分」全拉进来），数字命中多给 2 分。
- 复测：那句话现在第一条就是 12F1，关系判出「印证」。60 条事实自召回抽样：全文 56→57/60，前 40% 43→45/60（原 docstring 的 90% / 88% 量法），平均 47ms。
- 测试：test_kb_search +2，后端 868 passed。

## [221] 巡检第 192 轮：词表半截词 + 相对数据目录（2026-09-13）

- 召回回给前端的「搜了什么」里有 ev / pc / pcb 三个半截词——词表反向扫描对拉丁表层词也用子串匹配，两三个字母的实体码（ev / pc / pcb）命中 EVT / PCBA，把电动车 / 电脑的事实拉进候选池。`_surface_in()`：ASCII 表层词按整词匹配（前后不是字母数字），中文照旧子串。60 条自召回抽样不变（57 / 45）。
- 顺手踩到一个坑：在仓库根目录起脚本跑 `UserMemory`，`kite_data_dir="./data"` 按 cwd 解析，在仓库根凭空建了个空的 `data/terrence/codebook.xml`，召回全空还以为索引坏了。`Settings.kite_data_dir` 加校验：相对路径一律按 backend/ 目录解析，绝对路径（桌面版的 KITE_DATA_DIR、测试的临时目录）不动。垃圾已删。
- 测试：test_kite_memory +2，后端 870 passed。

## [222] 巡检第 193 轮：召回改了之后实拍页边圆点 / 关系卡（2026-09-13）

- `margin:5f65df10cad6`：页边圆点还在（灰 = 缺依据、绿 = 印证），右栏第一条就是「EVT 的大节点是 4 月 16 号 · 已引用」。`relations:5f65df10cad6`：光标段（「例如，4月16日的EVT应先记为…」）现在判出「印证 · 日期跟知识库 2026-04-16 的记录一致」——第 191 轮之前这段一条关系都判不出来。
- 小改：关系卡对着正文里已经引了的事实还给「引用这条」按钮，下面的列表却标着「已引用」——卡片复用同一个判断，已引的显示「已引用」徽标。实拍（16 秒延时；12 秒那张还没渲染到）。
- 前端 86 tests。

## [223] 巡检第 194 轮：窄窗导入页 / 深色设置页（2026-09-13）

- `import --win=1000x700`：布局没问题；说明文字里有「已经导过的 内容」这种缝——JSX 里中文段落换行写，两行之间被当成一个空格。`settings --dark --win=1000x700`：正常。
- **改**：扫了一遍 `src/**/*.tsx`，用户看得到的中文文案凡是跨行写的都合成一行（MemoryPanel 导入说明、MarkdownToolbar 两个 title、SkillsPanel 导入说明、WritingPlanPanel 对话框说明）；代码注释里的换行不动。实拍导入页说明已连成一句。
- 前端全绿。

## [224] 巡检第 195 轮：续写真跑（召回改动的下游）（2026-09-13）

- terrence「harness 测试」前 6000 字续写：取材 6 条 810ms（第一条就是「EVT 的大节点是 4 月 16 号」），8.6 秒写完，引了 4 条真事实（1833-8F2 / 388-1F8 / 1872-4F5 / 1872-6F1），fake_citations 空，没撞上限。开头接着被截断的半句写——是我截的输入，不是问题。
- 没改代码。

## [225] 巡检第 196 轮：树右键菜单（深色）、标签右键菜单（2026-09-13）

- `tree-menu --dark`：分组（打开 / 新建 / AI / 移动 / 删除）、快捷键角标、禁用态（打磨这篇、从这个位置移除）在深色下都对；`tab-menu`：关闭 / 关闭其他 / 关闭右侧 / 全部、「重新打开刚关的」禁用态带说明。树底部的定位 / 折叠小圆钮只在鼠标进左栏时半透明现身（之前的轮次改的），截图里鼠标正好在左栏。
- 没改代码。

## [226] 巡检第 197 轮：每次保存都重拉整个笔记列表（2026-09-13）

- 量了列表接口：`GET /api/notes` 带全文，terrence 23 篇 250KB / 10ms，shot-perf 413 篇 580KB / 6ms；`?q=创业` 42KB。服务端不慢，但 `save()` 每次自动保存后都 `reload()` 整个列表——写一篇 400 篇库的笔记，每次落盘都拖 580KB JSON 回来解析一遍。
- **改**：保存只改这一篇，用 PUT 返回的那篇就地替换 `notes` 里的那一项；树照旧 `reloadTree(false)`（不含知识库那半）。增删 / 移动 / 导入那些路径照旧 reload()。
- ⌘K 每敲一个字、`[[` 补全每敲一个字也各拉一次带全文的列表（补全的重名摘要就靠它）——「列表接口带摘要」仍是欠账，等第 200 轮正式版里量过手感再定。
- 前端全绿；保存路径在探针模式下不落库，留到第 200 轮正式版冒烟核对。

## [227] 巡检第 198 轮：快速查看 / 分屏对着空笔记（2026-09-13）

- `quick-view`（空文件夹「日记」）：一整块 80vh 的白板，什么都不说；`split --dark`（空的「2026」）：右半边全空，而且右栏收起时那个绝对定位的「展开右栏」小钮正好压在分屏的「关闭分屏」上，两个图标叠在一起各露一半。
- **改**：空笔记在快速查看 / 分屏里显示「这篇还是空的。」（快速查看的弹层随之缩到一行高）；分屏开着时「展开右栏」挪进分屏标题栏那一行，绝对定位的那个不再渲染。实拍两处都对。
- 顺手：⌘K 与侧栏搜索的防抖回调加了过期守卫（快速连敲时上一个字的结果晚到不会盖掉这个字的）。
- 前端 86 tests。

## [228] 巡检第 199 轮：⌘F 查找条（深色）、快捷键面板（窄窗）（2026-09-13）

- `find --dark`：查找 / 替换两行、下一个 / 上一个 / 全选中、三个选项、命中高亮都对。`shortcuts --win=1000x700`：面板两栏在 1000 宽下排得开；第一张实拍面板上叠了个「切换用户」输入框——再跑两次（含空白探针）都没复现，当作探针窗口弹出时吃到了一次外部点击，不改代码。

## [229] 第 200 轮：重打 dmg（2026-09-13）

- 含 191–199 轮：召回排序不分大小写 + 数字词、词表拉丁词整词匹配、相对数据目录锚定、关系卡已引用、中文文案不跨行、保存不重拉列表、空笔记占位 / 分屏钮、⌘K 防抖过期守卫。
- `npm run dist` → 183MB dmg。杀第 190 轮那份起新的：`/api/health` ok；正式版上走了一遍「建一篇 → PUT 改正文 → GET 读回 → 删除 → 最近删除里有 → 彻底删除」，全 200，正文读回一致（第 197 轮改的保存路径在正式版上没问题）。冒烟笔记已彻底删掉，没留垃圾。

## [230] 巡检第 201 轮：拖图进空笔记，右栏冒出 Bill Browder（2026-09-13）

- `imgdrop --dark`：图落库、正文插了 `![probe](/api/assets/52dd….png)`（光标在这行所以显示源码，正常）。但右栏立刻列出五条「assets of Russia frozen」「Magnitsky Act」的英文事实——召回的查询词就是那行图片 markdown，「assets」撞上了。`outline:5f65df10cad6`：目录三级缩进、当前标题高亮都对。
- **改**：`stripForRecall()`（去引用 id + 整条图片 + 链接地址只留文字）一处定义在 `util/wordCount.ts`；相关记忆的尾巴 / 关系判断的段落 / 页边圆点批量判断三处都先过一遍（页边那条：图片地址里的十六进制数字之前也能让它过「有数字」的门槛）。实拍改后右栏安静。
- vitest +1，前端 87。

## [231] 巡检第 202 轮：@ 引用、/ 菜单、引用页（2026-09-13）

- `mention --dark`（`@样机`）：候选八条带日期 / 月份，深色对；`slash`：七项各带一句说明；`notekb:5f65df10cad6`：「还没摄入知识库 · 存入知识库」+ 23 条引用的事实，第一条带「也引用于：创业一年回顾」。
- 没改代码。

## [232] 巡检第 203 轮：知识库各页计时 → 树上「实体」点不开（2026-09-13）

- 计时（terrence，2 万条事实）：dashboard 首次 784ms（索引加载）之后 12ms；`tree` 58KB 9ms；`tree/children?node=kb:entities` 1220 行 334KB 5ms（每行 280 字节，都是字段名开销，只在展开时取一次，不动）；topic learning 首次 203ms 后 4ms；timeline 3ms；某一天 224ms；clusters 32ms；quality 75ms。
- **问题 1**：实体超过 200 走懒加载（第 175 轮）时，`build()` 先把 `entity_rows` 清空再算 `kb:entities` 的 child_count → 0 → 树上「实体」没有展开箭头，1220 个实体在树里永远点不开。已有的测试没抓到是因为它的夹具里实体带类型（分组时数的是类型节点）；真库 1220 个实体全没标类型。改成清空前先记数量；补一个不分组的测试。
- **问题 2**：实体里事实最多的前五个是 Speaker A / B / C / D / E（2858 / 2296 / 1318 / 483 / 259）——转写的说话人标签。索引页早就默认藏着（有开关），树上没藏。树这一侧同样过滤（`isSpeakerTag`）。
- 新探针 `kbexpand:<id>`：只展开树上那个分类并滚到它（要把 `kb` 根也放进展开集合）。实拍：「实体」展开后从 Facebook 211 开始，没有 Speaker。
- 测试：test_kb_virtual_tree +1，后端 871 passed；前端 87。

## [233] 巡检第 204 轮：树上另外三层懒加载（2026-09-13）

- `kbexpand:kb:timeline`：月份行「2026-12 · 3」——最新的月份是未来的 12 月（抽取时日期错的那几条，第 165 轮见过 2005，同类）；`kbexpand:kb:recent`：15 条会议行，「2026-09-07 · 公司汇…」两行一模一样，切段的段号「（1/2）」在标题末尾被省略号吃掉；`kbexpand:kb:entity:facebook`：探针没找到节点（大小写）。
- **改**：树上的会议行改成「09-07 2/2 · 公司汇报」——日期只留月-日、段号半角紧跟日期、再是标题（先试过把「（1/2）」挪到日期后面，标题被挤没了，实拍两次才定）；完整日期在会议页上。探针的父链展开 + 节点匹配不分大小写。实拍：两段公司汇报分得开，Facebook 展开后 211 条事实列在树里。
- 测试改 2 条（月-日格式）+1，后端 872；前端 87。

## [234] 巡检第 205 轮：时间线上的「未来月份」（2026-09-13）

- terrence 2 万条事实里 `when` 晚于今天的只有 4 条，全是一月的会里说的计划（「12 月 1 号会上广告费」「11 月 19 号前搞定功能样机」）——事实的日期是它指的那一天，不是说的那一天，KITE 就是这么设计的，时间线上 2026-12 那个月份是对的。早于 2015 的 14 条也是背景事实（2005 年上市、1996 年到某地）。事实日期跟所属会议日期一致的 19962 条，超前 30 天以上 51 条、滞后 9 条，没有超前一年以上的。
- `kbexpand:kb:month:2026-12`：展开后三条计划事实列出来，对。没改代码。

## [235] 巡检第 206 轮：820×600 极窄窗（2026-09-13）

- 笔记页：右栏自动收起、AI 按钮只剩图标、格式工具条横向可滚（藏滚动条，Trilium 同款）——都是设计内的；知识库首页：五张统计卡固定五列，每张只剩 130px，「1996-03 → 2026-12」折成三行把整排撑高。
- **改**：统计卡 `repeat(auto-fit, minmax(150px, 1fr))`，放不下就换行（820 宽下 3+2），跨度那格不折行。实拍对。
- 前端全绿。

## [236] 巡检第 207 轮：极窄窗主题页 / 事实表 / 实体页（2026-09-13）

- 三页在 820 宽下都排得开（筛选行自动换行、图谱卡片随宽度缩）。两个真问题：
  1. 首页主题 chip「work 6151」、主题页「6034 条事实」——首页闭包计数把每个子主题的直接计数相加，一条同时挂 work 和 work_marketing 的事实被加两次。改成按不同事实数（每个主题一个 id 集合，闭包取并集）。
  2. 实体页状态栏显示「23 篇笔记」而不是面包屑——实体那层是懒加载的，不在 allRows 里，父链找不到就退回默认。按 id 形状拼父链（kb / 实体 / Facebook，名字用标签页上的），主题事实、某月、某会议同理。
- 实拍：状态栏「知识库 / 实体 / Facebook」。测试：test_kb_pages +1；后端 873。

## [237] 巡检第 208 轮：树上的主题计数（2026-09-13）

- 第 207 轮把首页对上了主题页，树上的 `fact_count` 还是闭包直接计数相加（learning 2293 vs 首页 2266）。`virtual_tree.build()` 同样改成按不同事实数。三处（首页 / 主题页 / 树）现在一致。
- 测试 +1（树计数 = 主题页计数），后端 874。

## [238] 巡检第 209 轮：极窄窗日记 / 最近删除 / 写作 Skill（2026-09-13）

- `today --dark`、`open:app:skills`：排得开，面包屑「日记 / 2026 / 09 月 / 09-13 周日」对。两处不对：
  1. 最近删除里躺着两条「未命名 · 0 字」——新建后一个字没写就被自动扔掉的空笔记也进了回收站，没什么可恢复的，只会越攒越多。`delete_note` 对「正文为空 + 标题是占位」的不留快照。
  2. 第 207 轮补的面包屑把 app:* 页也冠了「知识库 /」（「知识库 / 最近删除」「知识库 / 写作 Skill」）——它们不在知识库下面。app:* 只显示自己。实拍「写作 Skill」。
- 测试：test_trash +1，后端 875；前端 87。

## [239] 巡检第 210 轮：极窄窗主题地图 / 时间线（深色）/ 会议页 404（2026-09-13）

- `open:kb:graph`：全部簇 111、搜节点、新建主题一行排得开，图谱随宽缩；`open:kb:timeline --dark`：年份 chip 换行、月份条 / 天数展开都对；`open:kb:unit:terrence-2046`（我给错了 id，会议 id 是 `terrence-2046-12`）：「没有这个会议」+ 回最近摄入，面包屑「知识库 / 最近摄入 / 会议记录」。
- 没改代码。

## [240] 巡检第 211 轮：会议页的实体 chip（2026-09-13）

- `open:kb:unit:terrence-2046-12 --win=820x600`：标题（13/24）、日期 / 条数 / 说话人、事实按月分组都对。但「这场会在说什么」里 speaker_c 12 / Speaker E 5 / Speaker A 2 / speaker_d 1 跟 PCBA / KRL 并排当实体，每张事实卡也挂着一个 speaker_c chip——说话人标签不是实体（主题页早就滤了，这三处没滤）。
- **改**：会议页 chips、事实卡 chips（KbBits）、事实页 chips（KbNoteView）都过 `isSpeakerTag`；事实页 chip 顺便用显示名而不是代码。实拍：会议页只剩 PCBA / KLR / 白老师 / KRL，事实卡只剩真实体。
- 前端 87 tests。日页首次 175ms 之后 1ms、会议页 3ms（顺手量的）。

## [241] 巡检第 212 轮：事实表（深色）+ 首页统计卡回归（2026-09-13）

- `open:kb:facts --dark`：筛选行、按月分组、卡片悬停两个动作在深色下都对。
- **回归**：第 206 轮把统计卡改成 grid auto-fit(150px)，正常宽度（内容栏约 690px）也挤成 4+1。改成 flex 换行：每张 `flex: 1 1 118px`，跨度那张 168px（「1996-03 → 2026-12」不折行要这么宽）；宽窗一行五张，820 窗 4+1、最后一张占整行。两种宽度各实拍一次（第一版跨度卡文字被裁掉半截，再调的 168）。
- 前端 87 tests。

## [242] 巡检第 213 轮：键盘导航 / 多标签（深色）/ 跳到文末（2026-09-13）

- `keynav`（落在空的「日记」上，没什么可看）、`many-tabs --dark`（标签行 12 个标签 + 溢出箭头，深色对）、`end:5f65df10cad6 --win=1000x700`：第一次实拍是一张「0 篇笔记 · 还没有笔记」的欢迎页、启动栏图标都没画出来——日志里 `/api/notes` 明明 200 了；14 秒延时再跑一次正常（文末段落 + 页边绿点）。连续第三次起探针实例时出现过两次这类一闪而过的怪状态（第 199 轮那次「切换用户」框也是），先记着，没改代码。

## [243] 巡检第 214 轮：导出 zip 真跑（2026-09-13）

- `GET /api/export/markdown`：terrence 23 篇 → 27 个条目 5.2MB / 136ms（三张生成的 PNG 占 5.1MB，正文 23 个 .md + 一个克隆说明 .txt）；shot-perf 413 篇 121KB / 18ms。文件名：占位标题的退回正文首行、截到 60 字，非法字符换空格，长标题最长 80 字——都在设计内。没改代码。

## [244] 巡检第 215 轮：文档引用核对 → 许可证缺口（2026-09-13）

- 扫了 17 份 docs 里反引号引用的仓库路径，两处指向不存在的文件：TRACELOG 里的 `scripts/check-layers.mts`（历史记录，实际在 frontend/scripts/，不改）；`docs/desktop-plan.md` 引用的 `docs/third-party-notices.md` 从来没写过——而那份计划已经明确「本仓库必须以 AGPL-3.0 发布、复用的文件要保留出处」，仓库里却没有 LICENSE、package.json 没有 license 字段、README 一个字没提。
- **补**：根目录 `LICENSE`（AGPL-3.0 全文，34.5KB，取自 gnu.org）；`docs/third-party-notices.md`（复用 Trilium 的是什么、在哪；后端 9 个 / 前端 33 个 / 桌面 4 个依赖的许可证逐个核过——PyMuPDF 是 AGPL 双许可，跟本项目一致）；frontend / desktop `package.json` 加 `"license": "AGPL-3.0-only"`；README 加「许可证」一节；dmg 的 extraResources 随附 LICENSE 与声明（下次 `npm run dist` 生效）；设置页底部一行出处 + 仓库地址（AGPL §13 的源码提供）。

## [245] 巡检第 216 轮：计划面板（深色）、信息页（2026-09-13）

- `pane:plan --dark`：骨架 + 六条节拍在深色下对；`ribbon:info`：创建 / 修改 / 字数 / 引用 / 摄入 / 副本 / 位置 / id 八行都对。
- 小改：右栏「记忆」头部那句「跟着正文自动浮现…」在转圈和范围下拉一起挤上来时折成两行把头部撑高——文字改成可截断不折行，转圈和下拉不收缩。
- 前端 87 tests。

## [246] 巡检第 217 轮：导回区（深色）、冲突收件箱、说话人写法（2026-09-13）

- `exportback --dark`：Obsidian / Notion / 飞书三行导回表单在深色下对；`conflicts`：terrence 没有未处理冲突，收件箱按设计不显示。
- **问题**：首页「类型 · 说话人」里 speaker a 6185 和 speaker_a 1106 并排、speaker b / speaker_b 也是——不同批次转写把同一个人写成两种（空格 / 下划线 / 大小写），事实表按说话人筛也只能筛到一种写法。
- **改**：`kb/who.py norm_who()`（空白和下划线归一、小写）；首页说话人按归一键分组计数、显示最常见的原写法；会议页说话人列表同样去重；事实表 `who` 筛选两边都归一化后比。
- 测试 +1（三种写法算一个人、筛选三条都在），后端 876。

## [247] 巡检第 218 轮：说话人筛选端到端（2026-09-13）

- 首页说话人 chip 点进事实表：`kb:facts?who=speaker a` → 下拉停在「speaker a · 7291」，列表 7291 条；接口直接拿 `speaker a` / `speaker_a` / `Speaker A` 三种写法查都是 7291 条，`user` 416 条。没改代码。

## [248] 第 219 轮：用户报的 bug——Agent 运行里第 0 轮 / 第 1 轮顺序不对（2026-09-13）

- 复现（`polish:5f65df10cad6` 实拍）：右栏「Agent 运行」先是「第 1 轮」卡片（只有「据此调整了下一轮的跑法…」），下面才是「第 0 轮」卡片，挂着「续写整篇：在写…」和事实依据的判定；跑完了那个「在写…」还在。
- 根因三个：① `consumeHarnessStream` 里 AG-UI 的 `ACTIVITY_SNAPSHOT` / `TOOL_CALL_RESULT` 自己不带轮次，前端写死 `round: 0`——于是凭空多出一张「第 0 轮」，所有阶段标签、工具结果全堆进去；② 卡片按事件到达顺序建（开跑前的初始策略先到、挂到第 1 轮；快照后到、建了第 0 轮），面板按数组顺序画，就成了 1 在 0 上面；③ RUN_FINISHED 时没摘阶段标签。
- **改**：解析器记住最近一次 `STEP_STARTED` / `round_summary` 的轮次，快照和工具结果跟着它走；面板按轮次升序画，轮 0（如果有，比如断点续跑）标成「开跑前」；跑完 / 暂停时把所有卡片的阶段标签摘掉。
- 单测 `harnessStreamRounds.test.ts`：快照跟着 STEP_STARTED 的轮次（0,1,1,2,2）、工具结果归第 1 轮。前端 88 tests。

## [249] 第 220 轮：重打 dmg（2026-09-13）

- 含 201–219 轮：召回前剥图片 / 链接、树上实体懒加载点得开、说话人伪实体不进树 / chip、会议行「月-日 k/n」、首页 / 树的主题计数一致、懒加载节点面包屑、空笔记不进回收站、统计卡换行、LICENSE + 第三方声明随包、说话人写法归一、用户报的 Agent 运行轮次顺序。
- `npm run dist` → 183MB。`Contents/Resources/` 里有 `LICENSE`（AGPL 全文）和 `third-party-notices.md`。杀第 200 轮那份起新的：`/api/health` ok，`facts?who=speaker_a` 7291 条（归一在正式版生效）。打磨那一轮的状态行「到达轮数上限 · 1 轮」跟面板一张卡对上了。

## [250] 巡检第 221 轮：轮末暂停真跑（2026-09-13）

- `review:5f65df10cad6`（120 秒）：第 1 轮写完暂停，状态行「这一轮写完了，逐条看过之后：接着写 / 到此为止」，右栏「改动 1」「计划 1」角标都亮，写作骨架被这一轮 replan 换成了新的核心张力。第 219 轮的修复在暂停路径上也生效（onDone 先摘阶段标签再进 awaiting_review 分支）。没改代码。

## [251] 巡检第 222 轮：轮末暂停的存放（2026-09-13）

- 第 221 轮留下的暂停快照：`GET /api/harness/paused` 列出来了；「到此为止」= `resume {stop:true}`（删快照 + 把留下的正文写回笔记），没有单独的丢弃接口，设计上够用。`prune_snapshots` 只按条数留最近 20 份，**不按时间**——一个月前的暂停照样在，每次打开那篇都提示「等你处置」，真去恢复等于把一个月前的材料盖到已经改过的正文上（docstring 自己都这么说）。
- **改**：`sweep_stale_snapshots(days=7)` 启动时清掉超过 7 天没处置的；打开笔记时那句提示带上日期（「09-13 打磨写到第 1 轮停下来等你处置」）。探针留下的那份快照已删。
- 测试 +1，后端 877；前端 88。

## [252] 巡检第 223 轮：深色下的主题地图 / 主题页 / 最近删除（2026-09-13）

- `open:kb:graph --dark`：111 个簇的力导向图、节点标签、虚线在深色底上都看得清；`open:kb:topic:work --dark`：按月柱、周围图谱、子主题 chip 都对；`trash --dark`：两条「未命名 · 0 字」是第 209 轮修复前探针留下的，已通过接口彻底删掉。没改代码。

## [253] 巡检第 224 轮：⌘K 命令项、系统文件拖进树 → 召回噪声（2026-09-13）

- `palette:今天 --dark`：命令「今天的日记」+ 笔记 + 知识库三段；`palette:导回`：「导回到 Obsidian / Notion / 飞书…」。`filedrop`：真导入一篇 .md 成功（跑完已删）。
- **问题**：那篇「这篇是从系统拖进树的 .md」右栏冒出「SMDowner」「B2ECMD」两条英文事实——英文词 `md` 在排序里子串命中 SMD / CMD（第 192 轮只把词表那层改成整词）；去掉这两条之后剩下的还是候选池里零分的噪声，再往下是行级回退用「这篇是」这种三字组把整场会的事实都拉出来。
- **改**：排序 / 命中词都按整词匹配 ASCII 词（保留空格比对，`ai` 不靠 said 得分）；一个词都没命中的候选不返回；行级回退要一个会议被至少两个不同词命中才算。复测：那句召回空；「Bill Browder 冻结俄罗斯资产」「电池容量 380mAh」照常；60 条自召回 57 / 45 不变。
- 测试 +1、改 1，后端 878。

## [254] 巡检第 225 轮：召回回退的噪声（2026-09-13）

- 第 224 轮改完再拍 `filedrop`：右栏还是有一条不相干的「我本周就要开始做这件事情…」——来自行级回退：「拖进来」「系统」各命中了某场会的某一行，整场会最新的事实就被端上来。
- **改**：回退要同一行原话里同时出现至少两个查询词才算那场会命中；回退拉出来的事实同样过一遍排序、零分的不要。复测：那句召回空；「Bill Browder」「苹果手表的表带」「Anker 的开发流程」照常；自召回 57 / 45 不变。`margin --dark` / `relations --dark`：页边圆点、印证卡在深色下都对。
- 后端 878。

## [255] 巡检第 226 轮：侧栏搜索、来龙去脉（深色）（2026-09-13）

- `search:EVT`：两张卡的摘要「…“调整方案”或“停止推进”。矩阵不能只保留…」「…E 要求可对外讲清…」——命中在哪看不见：摘要前后各留 28 字，侧栏卡片一行 nowrap 只装得下十几个字，命中被省略号吃掉。`sel:trace --dark`：「查来龙去脉…」浮层在深色下对。
- **改**：`matchSnippet` 多一个 `before` 参数，侧栏用（40, 8）——前面最多 8 字，命中总在可见区。实拍两张卡都能看到高亮的 EVT。vitest +2（snippet.test.ts），前端 88。

## [256] 巡检第 227 轮：最长的一篇（3 万字）（2026-09-13）

- `big:309f19202309`（公司汇报，30588 字符 / 27419 字）：切换到绘制 3ms，生成的配图内嵌显示，右栏跟着尾段召回；`outline --dark`：三十多个标题分级缩进、当前标题高亮。没改代码。

## [257] 巡检第 228 轮：历史 / 链接 / 路径三个 ribbon（2026-09-13）

- `ribbon:history --dark`：12 个版本、每版字符数和增减、「恢复到这一版」「现在存一版」；`ribbon:links`：链到的 / 链到这篇的（链接测试那篇）；`ribbon:paths --dark`：「树根 / harness 测试」+ 克隆到另一个位置。没改代码。

## [258] 巡检第 229 轮：今天的日记幂等（2026-09-13）

- 连叫两次 `POST /api/notes/today` 返回同一篇；树根只有一个「日记」文件夹。没改代码。

## [259] 第 230 轮：重打 dmg（2026-09-13）

- 含 221–229 轮：轮末暂停 7 天过期 + 提示带日期、召回噪声三连修（ASCII 整词 / 零分候选不返回 / 行级回退要同一行两词命中并过排序）、侧栏搜索摘要前留 8 字。
- `npm run dist` → 183MB。杀第 220 轮那份起新的：`/api/health` ok；正式版上「这篇是从系统拖进树的 .md」召回 0 条（之前一屏噪声），「4月16日的EVT…」第一条仍是 12F1；`harness/paused` 空。

## [260] 巡检第 231 轮：事实页（深色）、地图缩放、删除确认（深色）（2026-09-13）

- `kb-fact --dark`：日期 / 类型 / id、原话折叠（展开全文 1193 字）、被哪些笔记引用、相关事实六条；`graph-zoom`：地图缩放后节点标签仍可读；`confirm --dark`：「删除『09 月』和它下面的 1 篇?」列出子笔记，取消 / 删除两键。没改代码。

## [261] 巡检第 232 轮：静态检查（2026-09-13）

- `pyflakes app`：只剩两类——`main.py` 健康检查里一个没用到的 `s = get_settings()`（删了）；`harness/tools/__init__.py` 四个为副作用导入的模块（已标 noqa，pyflakes 不认行内 noqa，忽略）。`desktop` 的 `tsc --noEmit` 干净。后端 878。

## [262] 巡检第 233 轮：校验、magic tap 真跑（召回改动之后）（2026-09-13）

- `sel:verify`：选 mermaid 里的 DVT 调整关系 → 「支持：正文后文明确记载 DVT 原计划 6 月 3 日、后续调整至 8 月 5 日…」，证据来自笔记内部，合理。`tap:5f65df10cad6 --dark`：45 秒内续出「团队建设 / 研发」两节，7763 → 7978 字，右栏「计划 5」角标（节拍覆盖数变了）。没改代码。

## [263] 巡检第 234 轮：回顾带范围真跑（深色）（2026-09-13）

- `digest:30:meetings --dark`：30 天内会议记录 4 条事实 → 核心结论 / 关键决定 / 待跟进 / 值得注意的变化四节，深色对。说明行写成「只看『只看会议记录』」——范围标签本身带「只看」。回顾面板和写作计划对话框的提示都去掉标签开头的「只看」。

## [264] 巡检第 235 轮：窄窗深色首页、实体类型节点、实体索引（深色）（2026-09-13）

- `kb-overview --win=1000x700 --dark`：统计卡 4+1、图、chip 都对；`open:kb:entities --dark`：1220 个实体前 200、说话人开关。
- **问题**：`kb:etype:<type>`（实体按类型分组时树上的类型节点）没有页面——中栏只显示一行裸 id「kb:etype:other」。terrence 的库实体没分类所以树上没有这种节点，但分了类的库点进去就是这样。
- **改**：`EntitiesIndex` 多一个 `node` 参数，类型节点复用实体索引页（标题「实体 · 组织」，只列这一类，取 `tree/children?node=kb:etype:x`）；没分组的库进来给一句「这个库的实体没有按类型分组」。实拍对。前端 86。

## [265] 巡检第 236 轮：⌘K 空态（深色）、三标签窄窗、跟随系统（2026-09-13）

- `keypalette --dark`：空态列「最近编辑」六篇 + 「前往」命令；六篇里有四篇是今天建的日记脚手架（09-13 周日 / 09 月 / 2026 / 日记），都是空壳，把真正编辑过的挤掉了。改成只列写过字的。`tabs --win=1000x700`、`theme-system`：对。

## [266] 巡检第 237 轮：断点续导、智能表格（深色）（2026-09-13）

- `import-resume`：没有没跑完的导入，探针如实报「no 继续 button」；`block:table --dark`：块的进行中态「智能表格 · 生成表格：在看要用哪些材料… · 停止」深色对。没改代码。

## [267] 巡检第 238 轮：摄入 → 改动 → 「需同步」真跑（2026-09-13）

- 拿「链接测试（可删）」走 `POST /api/ingest/note/{id}/sync`：15 秒抽出 2 条事实，引用页「已摄入知识库 · 2026-09-13 · 重新同步」+ 两张事实卡；再 PUT 改一句 → 树上橙色 ⇡、引用页橙框「笔记在 2026-09-13 摄入之后又改过，知识库里还是旧版——点『同步到知识库』，或在设置里打开自动同步」。都对。
- 探针脚本自己踩的坑：摄入完立刻 PUT，updated_at 跟 ingested_at 同一秒，`>` 判不出「改过」——真用户不会在同一秒内做完这两件事，不改。
- 收尾：删掉抽出的 2 条事实、还原正文、清 ingested_at。没改代码。

## [268] 巡检第 239 轮：克隆真跑（2026-09-13）

- 把「链接测试」克隆到 Notes 下：树上两处都出现、克隆角标 ⧉、Notes 计数 1 → 2；再挂一次同一位置原样返回那条 branch（不重复，表上有 UNIQUE）；摘掉 Notes 下那条 → 恢复；摘最后一条 400（那是丢失不是删除）。没改代码，没留垃圾。

## [269] 第 240 轮：重打 dmg（2026-09-13）

- 含 231–239 轮：静态检查清理、范围提示去重、实体类型节点页面、⌘K 最近编辑不列空壳、克隆 / 摄入 / 校验 / tap 真跑核对。
- `npm run dist` → 183MB。杀第 230 轮那份起新的：`/api/health` ok，`tree/children?node=kb:etype:other` 空表（未分组库）、`harness/paused` 空。

## [270] 巡检第 241 轮：事实表带查询串（2026-09-13）

- `kb:facts?topic=finance`：主题框填上 finance、706 条按月分组、卡片悬停两个动作；`kb:facts?kind=decision`：这个库没有 decision 这种类型，0 条——下拉退回「全部类型」显示，跟 0 条对不上，但首页 chip 只会给出真实存在的类型，用户到不了这个组合，不改。

## [271] 巡检第 242 轮：个人偏好接口（2026-09-13）

- 空白 400、删不存在的 404 都对。两处不对：一条 5000 字照单全收（每条偏好都原样拼进写作提示词）；同一句话连加两次出现两条一模一样的。
- **改**：一条最多 400 字（超了 400 + 一句人话）；同文重复返回已有那条。测试 +1，后端 879。探针加的几条已删干净。

## [272] 巡检第 243 轮：写作 Skill 接口（2026-09-13）

- 空正文 400、改 / 切换不存在的 404、reorder 给未知 slug 422——对。三处不对：空名字 200（随机 slug）；作用域给 `bogus_scope` 照收；正文 6 万字照收（整段拼进提示词）。
- **改**：建 / 改共用一份校验——名字必填、作用域必须在 `SKILL_SCOPES` 里、正文最多 2 万字。测试 +1，后端 880。

## [273] 巡检第 244 轮：笔记接口（2026-09-13）

- 置顶两次翻转、恢复不存在的版本 404、手工存版 60 次留 60（上限 100）——对。两处不对：标题一万字、带换行 / 制表符照收（树、标签页、面包屑都按一行画）；`parent_note_id` 给个不存在的 id 也 200——挂到一个不存在的节点下，树上永远看不见。
- **改**：标题在入参层压成一行（空白折叠、前后去掉、最多 200 字），建 / 改都走；父节点不在 404。测试 +1，后端 881。2M 字符的正文 21ms 照收，不动。

## [274] 巡检第 245 轮：树的 branch 接口（2026-09-13）

- 移动成环 400、reorder 给外来 id 忽略、展开态给不存在的节点 422——对。两处不对：**克隆到自己的子树里 200**（A → B → C，把 A 再挂到 C 下：成环，任何深度遍历都会转死）；**移到不存在的父节点 200**（挂到「nope」下面，树上永远看不见）。
- **改**：`attach` 跟 `move_branch` 用同一个 `_would_cycle` 判环，成环抛 ValueError → 400；move 的目标父节点不在 404。测试 +2，后端 883。

## [275] 巡检第 246 轮：知识库事实的手工增改（2026-09-13）

- 空文本 400、不存在 404、同一条合并 400——对。三处不对：一条事实「被它自己取代」200；a 合并进 b 之后再把 b 合并进 a 也 200——两条互指、都标成 merged，页面上两条都不显示；两万字的一条手工事实照收。
- **改**：取代 / 合并共用一个判环（自指、互指、指向已被取代的那条都 400，给出该怎么做）；手工事实最多 2000 字。测试 +1，后端 884。

## [276] 巡检第 247 轮：冲突收件箱接口（2026-09-13）

- 同一对只记一次、非法 action 400、处理过再处理 404、new_wins 之后旧的被新的取代、首页 `conflicts_open` 归零、删掉一方之后列表自己清掉——全对。没改代码。

## [277] 巡检第 248 轮：导回 Obsidian 的文件名与目录（2026-09-13）

- 标题「../../evil」「sub/dir:name?」都写在 vault 里面（斜杠冒号问号全换成空格，没有逃出去）；目录不存在 400——对。两处不对：vault 选到 /etc 这种写不进去的目录直接 500（PermissionError 没接）；「../../evil」写出来的文件叫「 .. evil」（先去空格再去点，去完点又剩空格）。
- **改**：目录不可写 400；写文件的 OSError 接住给 400 带文件名；`safe_name` 一次去掉首尾的空格和点。测试 +1，后端 885。

## [278] 巡检第 249 轮：Obsidian 目录导入的文件名（2026-09-13）

- 读了 `/api/import/files`：文件名里的 `/` 只用来还原树的文件夹层级和算稳定 id，不落盘；`../x.md`、`.obsidian/…`、`.trash/…` 这类以点开头的段整段跳过；空文件、非 md、解不出 utf-8 的跳过；一次最多 MAX_FILES 个。没改代码。

## [279] 第 250 轮：重打 dmg（2026-09-13）

- 含 241–249 轮：一轮接口边界扫——偏好 ≤400 字 / 去重、技能校验名字 / 作用域 / 正文长度、笔记标题一行 ≤200、父节点不存在 404、克隆成环 400、移到不存在的父节点 404、事实取代 / 合并不成环、手工事实 ≤2000、导回目录不可写 400。
- `npm run dist` → 183MB。杀第 240 轮那份起新的：`/api/health` ok；正式版上「建到不存在的父节点」404、「401 字的偏好」400。

## [280] 巡检第 251 轮：供应商设置与附件接口（2026-09-13）

- 附件：`..%2F..%2Fetc%2Fpasswd` 404、空文件 400、类型白名单——对。供应商设置四处不对：`gpt_base_url` 给「not a url」照存；`asr_base_url` 给「192.168.1.1:8081」（没 scheme）照存——之后每次调模型 / 转写都是莫名其妙的连接错误；模型名「   」照存；API key 「  」被当成一个新 key 存下、把原来的覆盖掉。
- **改**：两个地址只认 http(s):// 且去掉尾部斜杠（语音地址允许空 = 退回默认）；模型名去空白后不能空；只有空白的 key 当没填、保留原值。测试 +1（新文件 test_settings_provider.py），后端 886。

## [281] 巡检第 252 轮：两个服务都不通时的健康检查（2026-09-13）

- 把模型地址和语音地址都指到一个不通的 IP：`/api/health` 5.0 秒返回 `llm:false / asr:false`（两个探测并发、各 5 秒超时），召回 8ms、空库回顾 3ms 不受影响。状态栏红字时每 30 秒查一次，5 秒一次在后台跑，不卡界面，不改。

## [282] 巡检第 253 轮：错误提示的措辞（2026-09-13）

- 读了 `friendlyError` / `api.json`：后端 4xx 的 `detail` 会被拿出来，但前面挂着状态码——toast 里是「保存失败：400 模型地址要以 http:// 开头」。状态码对用户没意义，去掉；连接类 / 5xx / 401 / 429 的翻译不动。vitest +1（friendlyError.test.ts），前端 87。

## [283] 巡检第 254 轮：改动分层（深色）（2026-09-13）

- `layers:5f65df10cad6 --dark`：两层（格式化 5 处已关、探针 1 处开着），关掉的那层只剩「丢弃」、开着的有「接受 / 撤回」，正文里探针那段绿色下划线，横幅「改了 6 处 · 按层处置」。深色对。没改代码。

## [284] 巡检第 255 轮：搜索里的特殊字符（2026-09-13）

- 括号、星号、引号、5000 字、空白都不报错。但 `%` 和 `_` 是 SQL LIKE 的通配符，搜「_」「%」全库都命中（实测三篇全出）。`list_notes` 的模式先转义再 `ESCAPE '\\'`。召回接口对这些字符也正常。测试 +1，后端 887。

## [285] 巡检第 256 轮：备份的保留策略（2026-09-13）

- 读了 `database/backup.py`：笔记库一天一份、留最近 7 天 + 每月第一份（最近 3 个月）；知识库一周一份、每用户留 2 份。dev 库 `backups/` 共 23MB（两份笔记库 + kb/）。没改代码。

## [286] 巡检第 257 轮：⌘K / `[[` 补全每敲一个字拉全文列表（2026-09-13）

- 第 197 轮记的欠账：`GET /api/notes` 带全文，⌘K 和 `[[` 补全每敲一个字拉一次——413 篇的库一次 580KB。
- **改**：新接口 `GET /api/notes/brief?q=`：只给 id / 标题 / 日期 / 前 80 字 / 有没有正文，正文命中的那几篇在服务端算好命中片段（跟前端 `matchSnippet` 同一条规则，前 18 后 40），最多 50 行 + 总数。⌘K（最近编辑、搜索、片段）、`[[` 补全（重名带首句）都改走它；⌘K 选中后由 App 按 id 找本地那篇、找不到再 GET 一次。量：terrence 250KB → 7KB，shot-perf 580KB → 16KB（搜「会议」576KB → 19KB）。
- 实拍：⌘K「EVT」笔记段带服务端片段 + 高亮；`[[创业` 三篇同名各带首句（第一版 preview 240 字一次还是 300KB，压到 80 字；被截半截的最后一行会剩个「#」，补全里整行丢掉）。
- 测试：后端 +1（brief 不带全文 / 片段 / 总数），888；前端 87。
- 追加：`[[` 补全的重名摘要改由服务端给 `first_body`（跳过跟显示名一样的第一行、优先正文行、整篇只有骨架标题就退回第一个小标题）——三篇「创业一年回顾」库里标题都是「未命名」、正文第一行就是这几个字，前端拿 80 字 preview 算出来三条一样。实拍三条各不同。后端 889。

## [287] 巡检第 258 轮：⌘K 搜「创业」（深色）、空态（2026-09-13）

- 轻量接口切换之后再拍：`palette:创业 --dark` 六篇命中、标题高亮、harness 那篇带服务端片段；`keypalette` 空态「最近编辑」六篇都是写过字的。三篇同名「创业一年回顾」在 ⌘K 里也分不开——同名的行后面补服务端给的 `first_body`（跟 `[[` 补全一致）。前端 87。

## [288] 巡检第 259 轮：侧栏搜索每敲一个字拉一次全文列表（2026-09-13）

- 侧栏「快速搜索」跟 ⌘K 一样每敲一个字 `GET /api/notes?q=`（带全文）。App 启动时全文列表已经在内存里，改成本地过滤（标题或正文包含、不分大小写，顺序沿用列表）；`search:EVT` 实拍两张卡、摘要、高亮跟之前一样，日志里一次 `notes?q=` 都没有。现在只剩启动 / 增删后的 `reload()` 拉全文列表。前端 87。

## [289] 第 260 轮：重打 dmg（2026-09-13）

- 含 251–259 轮：供应商设置校验、toast 只留人话、LIKE 通配符转义、`/api/notes/brief` 轻量列表（⌘K / `[[` 补全）、⌘K 同名带首句、侧栏搜索本地过滤。
- `npm run dist` → 183MB。杀第 250 轮那份起新的：`/api/health` ok；正式版 `notes/brief?q=EVT` 3 篇带片段、`notes?q=_` 0 篇（不再当通配符）。

## [290] 巡检第 261 轮：导入页（深色窄窗）、导回区（820 宽）（2026-09-13）

- `open:app:import --dark --win=1000x700`：Markdown / Obsidian / Evernote 三个文件框、说明、导入到下拉在深色下都对；`exportback --win=820x600`：Obsidian / Notion / 飞书三行各自换到一行、飞书三个输入框挤但都在。没改代码。

## [291] 巡检第 262 轮：Notion / 飞书导入的坏凭据（2026-09-13）

- `apple/available` 15ms 返回可用；Notion 给假 token → 400「Notion 拒绝了这个 token：401」（975ms）；飞书给假 app id / secret → 400「飞书拒绝了这个应用：10003 invalid param」（579ms）。都是同步先验证凭据再排队，不会建一个注定失败的任务。没改代码。

## [292] 巡检第 263 轮：无限续写真跑（召回 / 范围改动之后）（2026-09-13）

- `plan-run:2f73294746f0`（Notes 文件夹，110 秒）：起计划拆出三个分段，建了「📋 写作追踪」+ 三篇分段笔记，两篇 done、第三篇 in_progress 时截图——正文 929 字、两级小标题、右下角「🚀 Notes · 正在写：海外种子用户与市场定位验证」浮条、面包屑「Notes / 海外种子用户… · 🚀 Notes」。跑完没留暂停快照。
- 收尾：作废计划、删掉四篇生成的笔记并从最近删除彻底删掉。没改代码。

## [293] 巡检第 264 轮：只增不减的流水（2026-09-13）

- 查了几张表的过期机制：历史版本每篇 100 条、暂停快照 7 天、回收站 30 天、备份 7 天 + 3 月——都有；`llm_usage`（45 条）、`ingest_jobs` / `ingest_items`（27 / 201，done / error / cancelled 都留着）、`writing_plans` / `writing_sections`（18 个 abandoned / done 计划）没有。
- **改**：`store.sweep_old_rows()` 启动时清：用量账本留 90 天，跑完的任务（连 items）留 30 天，非活跃计划（连 sections）留 30 天；活跃 / 在跑的不动。测试 +1，后端 890。

## [294] 巡检第 265 轮：写作类接口的坏输入（2026-09-13）

- 把模型地址指到 127.0.0.1:9（连不上、立刻失败）扫 14 个写作 / harness 接口：骨架空正文 400、校验空选区 400、rewrite 非法 intent 422、harness 不存在的笔记 404 / 非法 mode 422、智能排版空正文 400、恢复不存在的暂停 404、来龙去脉空选区 400——对。两处不对：**续写空正文空标题照样开流**（白花一次模型调用）；**写作计划 start 先调模型拆分段、最后才发现文件夹不存在**。
- **改**：续写空正文空标题 400；start 先查父节点。测试 +1（断言父节点不在时一次模型调用都没有），后端 891。
- 第一版扫描脚本把模型地址指到一个不通的 IP，每个调模型的接口都要等好几十秒超时，200 秒都跑不完——换成本机关着的端口立刻拒绝，14 个接口 30ms 跑完。

## [295] 巡检第 266 轮：事实表（深色窄窗）、会议页（深色）、最近摄入（820 宽）（2026-09-13）

- `kb:facts?who=user --dark --win=1000x700`：说话人下拉停在「user · 416」、筛选行换成两行；`open:kb:unit:terrence-2046-12 --dark`：标题（13/24）、chips、按月分组都对。最近摄入索引页：「09-07 2/2」排在「1/2」前面——按 (日期, id) 倒序把段号也倒了。
- **改**：树 / 首页 / 索引页三处的会议排序改成日期倒序、同一份材料段号正序。测试 +1，后端 892。
- 追加：第一版只把段号反过来，不同材料的段在同一天里交错（1/7、1/6、2/7）；改成两遍稳定排序——先 (材料, 段号) 正序、再日期倒序，同一份材料的几段挨在一起。索引页的「N 条」不再折行。测试改 1。
- 再追加：段号 k/n 的 n 之前按窗口里的段数算（最近 15 场里截进 6 段就显示「1/6」，其实那份材料有 13 段），树 / 首页都改成按整个库算。测试 +1，后端 893。

## [296] 巡检第 267 轮：最近摄入被一份材料的段占满（2026-09-13）

- 第 266 轮按材料分组之后再拍：最近 15 场里 13 场是「Interview Prep」的 13 段——按段列表，一份长材料就把窗口占完，别的材料看不见。
- **改**：最近摄入（树 / 首页 / 索引页）按**材料**列，一份一行，多段的标「（n 段）」、条数是各段之和；树上展开材料行先给「第 k/n 段」各段、再展开才是事实（`kb/units.py` 新增 `materials()` / `parts_of()`）；会议页顶上加分段导航（第 k/n 段 + 各段按钮）。实拍：索引页 15 份不同材料各带段数和总条数；树同样；「产品计划会（13/24）」页顶一排 24 个段钮、当前高亮。
- 测试改 3 加 1（材料行 / 各段子节点 / 首页按材料 / unit_page 的 parts），后端 893；前端 87。
- 追加：第一版让第一段的 `kb:unit:<id>` 兼任材料行，树按 note_id 挂父子，各段同时挂在材料行和「第 1/24 段」下面（实拍套了两层）。多段材料改用 `kb:material:<第一段 id>`（打开 = 第一段的页 + 分段导航；面包屑「知识库 / 最近摄入 / 03-10 · 产品计划会（24 段）」），前端 needsFacts / knownVirtual / openVirtual / 面包屑 / 探针父链都认它。实拍：材料行展开是 24 个段行，段行再展开才是事实。

## [297] 巡检第 268 轮：时间线月份展开（深色）、首页右栏（2026-09-13）

- `kbexpand:kb:month:2026-09 --dark`：48 条事实列在月份下，不受材料分组影响。首页深色：「类型 · 说话人」那栏被左栏的长标题挤成一条，「speaker a」折成两行；grid 子项默认 min-width:auto。
- **改**：`.kb-two-col > * { min-width: 0 }` 两栏真正对半；chip 不折行、放不下就省略。实拍：说话人 chip 三行整齐排开，左栏标题省略号。前端 87。

## [298] 巡检第 269 轮：1000×700 首页 / 主题页（深色）（2026-09-13）

- 首页 1000 宽：统计卡 4+1、两栏对半（最近摄入按材料、说话人 chip 三行）；主题页深色：按月柱、周围图谱。状态栏面包屑写成「知识库知识库」——kbRows 还没到时走第 207 轮那条拼父链的路，id 本身就是 `kb`，链里出现两次。去重。前端 87。

## [299] 第 270 轮：重打 dmg（2026-09-13）

- 含 261–269 轮：Notion / 飞书坏凭据、无限续写真跑、过期流水清理、写作接口坏输入（空正文续写 400、计划先查父节点）、最近摄入按材料列 + `kb:material` + 会议页分段导航、首页两栏对半、面包屑去重。
- `npm run dist` → 183MB。杀第 260 轮那份起新的：`/api/health` ok；正式版 `kb/tree` 最近摄入 15 行、第一行 `kb:material:apple-…「09-07 · 公司汇报：（2 段）」`。

## [300] 巡检第 271 轮：材料页（深色）、最后一段（1000 宽）（2026-09-13）

- `open:kb:material:terrence-2046-0 --dark`：打开的是第 1/24 段，段钮一排在深色下对；`open:kb:unit:terrence-2046-23 --win=1000x700`：第 24/24 段高亮、段钮两行排开。没改代码。

## [301] 第 272 轮：架构文档同步（2026-09-13）

- `docs/harness-framework.md` 目录图和第 15 节补上这几十轮新加的：`kb/units.py`（materials / parts_of）、`kb/who.py`、`store.list_notes_brief` 与四个启动清理、`kb:material`、搜索排序规则、记忆范围贯穿的入口；README 的 API 表补 `GET /api/notes/brief`。变更记录一节把 TRACELOG 范围写到 [300]。

## [302] 巡检第 273 轮：最近删除（深色 820）、定期回顾（820）（2026-09-13）

- 最近删除空态「最近 30 天没有删过笔记」（探针垃圾都清干净了）；定期回顾三个按钮一行排开。树上「03-09 · Single-utteranc…」没有箭头没有计数——那场会零条事实，本来就没得展开。没改代码。

## [303] 巡检第 274 轮：智能续写真跑（150 秒截图）（2026-09-13）

- `harness:5f65df10cad6`：第 2 轮同步（本地 9612 vs 服务端 9385，用服务端的）；150 秒时状态行「已自动删除一处：这一段重复了前文硬件部分…」+「运行中正文由 AI 接管，停下来再改」，右栏「改动 4 · 计划 4」，写作骨架被 replan 成新的核心张力和五条节拍。召回 / 范围 / 材料分组这些改动没有影响主链路。没改代码。

## [304] 巡检第 275 轮：事实表筛选组合（2026-09-13）

- 接口扫了 11 种组合：实体给显示名 / 代码 / 中文都能落到同一个（Facebook 211、苹果手表 27）、主题不分大小写走闭包（Work 6034、work_marketing 1609）、置信度 high 2358 / 非法值忽略、三个筛选叠加 74、limit 5000 封顶 200、offset 负数当 0、limit 0 当 1；首次 182ms 之后 3ms。`kb:facts?entity=Facebook --dark` 实体框填上、211 条。没改代码。
- 追加：事实表卡片的实体 chip 显示的是代码（facebook），首页 / 筛选框显示的是 Facebook——`/api/memory/facts` 和 `/api/memory/facts/{id}` 之前不带 `entity_names`（知识库各页早就带）。两个接口补上，卡片 / 浮层 / 事实页都用显示名。实拍 chip 变成 Facebook。测试 +1，后端 894。README 的 API 表那行写成 `/brief?q=` 让契约测试对不上端点，改回 `/brief`。

## [305] 巡检第 276 轮：出处浮层、静态检查（2026-09-13）

- `fact-peek`：悬停 `[terrence-1590-2F3]` → 日期 / 要点 / 原话三段（原话截到 220 字），浮层不带实体 chip（设计如此，扫一眼用）。pyflakes / eslint 干净。没改代码。

## [306] 巡检第 277 轮：空笔记上点续写（2026-09-13）

- `blank-tap`：新建空笔记后点续写——前端就拦了，日志里一次 `/api/magic-tap` 都没发；空笔记切走后自动扔掉，笔记数仍是 23。第 265 轮加的后端 400 是第二道。没改代码。

## [307] 巡检第 278 轮：有没有没人用的前端模块（2026-09-13）

- 扫了 `src/components` / `src/editor` / `src/util` 所有模块的引用：没有一个是没人 import 的（MemoryBrowser 走 KbNoteView 的主题地图页，KnowledgeGraph 走 LocalGraph）。没改代码。

## [308] 巡检第 279 轮：主题地图页里没人走的代码（2026-09-13）

- `MemoryBrowser` 只在 `KbNoteView` 里以 embedded + topics 挂载，但文件里还留着概览 / 时间线 / 事实表三个 tab、弹层外壳、Esc 关闭、事实翻页——那些早就各自是知识库的一页了；而且它每次挂载还多发一次 `/api/memory/stats`（概览 tab 的）。
- **改**：删到只剩主题地图（568 → 330 行），点主题 / 实体一律走 `open-virtual`。tsc / eslint / vitest 全绿；`open:kb:graph` 浅色、`graph-zoom` 深色实拍不变。

## [309] 第 280 轮：重打 dmg（2026-09-13）

- 含 271–279 轮：架构文档同步、README API 表、事实表 / 单条事实带实体显示名、MemoryBrowser 瘦身到只剩主题地图、若干实拍核对。
- `npm run dist` → 183MB。杀第 270 轮那份起新的：`/api/health` ok；正式版 `memory/facts?entity=Facebook` 带 `entity_names: ['Facebook']`。

## [310] 巡检第 281 轮：主题页（820 宽）、实体页（深色）（2026-09-13）

- `open:kb:topic:finance --win=820x600`：别名、按月、周围图谱都排得开；`open:kb:entity:elisa --dark`：149 条、按月、图谱、相关主题、演进线。没改代码。

## [311] 巡检第 282 轮：快捷键表 vs 真接的键（2026-09-13）

- `shortcuts.ts` 列的 15 组键跟 App.tsx 里 keydown 分支逐个对：⌘S / ⌘⇧D / ⌘N / ⌘T / ⌘. / ⌘/ / ⌘\ / ⌘⇧\ / ⌘F / ⌘⇧F / ⌘⇧T / ⌘[ / ⌘] / ⌃Tab / ⌘W / ⌘1…9 都在；⌘K / ⌘J 在命令面板里接。没改代码。

## [312] 巡检第 283 轮：固定端口被占（2026-09-13）

- 先用一个假服务占住 47232 再起探针实例：桌面壳打出「固定端口 47232 一直被占，退回随机端口 59139（本次 localStorage 状态会丢）」，后端在 59139 起来、页面正常、跨站写请求守卫也认这个来源。设计内的退路，没改代码。

## [313] 巡检第 284 轮：引用表跟正文对不上（2026-09-13）

- 写了个一致性检查：每篇正文里的 `[user-n-hex]` vs `note_citations` 表。terrence 23 篇里 2 篇「未命名（创业一年回顾）」正文一条引用都没有、表里却各挂三条——树上那两行的 ◆6 就是这么来的（幽灵引用，反查也会把它们列出来）。改正文的入口里 `update_note_from_source`（源侧同步）没重建引用；那两篇的历史原因查不到了（可能是早期没有 sync_citations 的版本写的）。
- **改**：源侧同步也 `sync_citations`；启动时 `reindex_citations()` 按正文重建对不上的那几篇（dev 库启动打出「重建了 2 篇」，树上 ◆6 消失，只剩 harness 测试 ◆31）。顺手：第 279 轮删掉 MemoryBrowser 的 tab 之后 `memoryTimeline` / `TimelineBucket` 成了没人用的导出（契约测试抓到），删了。测试 +1，后端 895。

## [314] 巡检第 285 轮：库里其它反查表的一致性（2026-09-13）

- 照第 284 轮的思路把 dev 库其它几张关联表全查了一遍：branches → 不存在的笔记 / 父节点 0、note_remotes / writing_sections / note_revisions / harness_snapshots 指向已删笔记的 0、活跃计划的父节点都在、没有一篇笔记不在树上、没有重复 branch。只有引用表那一处对不上，已修。没改代码。

## [315] 巡检第 286 轮：知识库本身的一致性（2026-09-13）

- terrence 2 万条事实：没有指向不存在会议的、没有没日期 / 空文本的、主题 / 实体码全在词表里、取代关系没有悬空和自指；同一场会里文本一模一样的事实 6 对（抽取重复，第 158 轮的合并功能能处理，量小不动）；77 场会一条事实都没抽出来（短录音 / 单句），列表里它们没有箭头和计数。没改代码。

## [316] 巡检第 287 轮：一条事实都没抽出来的段（2026-09-13）

- `open:kb:unit:terrence-2051-12`（Interview Prep 第 13/33 段）：页面只有「0 条事实 · 没有事实。」——其实这段有一行原话。77 场这样的段都是这样一片空。
- **改**：零事实的段 `unit_page` 带上原话（最多 50 行），页面标题「这段没抽出事实 · 原话 N 行」逐行列出，有事实的段不带（原话走每条事实自己的）。实拍原话显示出来了。测试 +1，后端 896。

## [317] 巡检第 288 轮：引用索引跟着保存走（2026-09-13）

- 隔离库：建一篇带两条引用 → 反查两条都指到它；改正文删掉一条 → 反查那条立刻空、另一条还在、树上 ◆1。引用页对不存在的事实 id 不列（那是「找不到」的红提示那条路）。`notekb:5f65df10cad6 --dark`：31 条引用的事实卡在深色下对。没改代码。

## [318] 巡检第 289 轮：笔记侧几个接口计时、历史页（深色 1000 宽）（2026-09-13）

- `/notes/{id}/kb` 7ms → 1ms、`/links` 2ms、`/revisions` 3ms、`/tree/paths` 3ms。`ribbon:history --dark --win=1000x700`：13 个版本、每版字符数和增减、「恢复到这一版」一行一个。没改代码。

## [319] 第 290 轮：重打 dmg（2026-09-13）

- 含 281–289 轮：快捷键表核对、端口被占退路、引用表启动重建 + 源侧同步重建引用、库内关联表 / 知识库一致性核查、零事实段给原话。
- `npm run dist` → 183MB。杀第 280 轮那份起新的：`/api/health` ok；正式版启动时引用表按正文重建，树上的 ◆ 是真实引用数。

## [320] 巡检第 291 轮：820 宽深色首页、最近摄入（深色）（2026-09-13）

- 首页 820 深色：统计卡 4+1、主题 / 实体两栏；最近摄入深色：15 份材料各带段数、总条数，零事实的「Single-utterance Request」0 条也列着（点进去现在能看到原话）。没改代码。

## [321] 第 292 轮：实体去重——规则版（用户指定，先简单）（2026-09-13）

- 新模块 `kb/entities.py`：**只在展示 / 查询层归组，不改知识库**。三条零歧义规则：大小写不分；空格 / 下划线 / 连字符 / 点 / 撇号忽略；词表里登记的 name / aliases 算同一个。中英文对译（苹果手表 / Apple Watch）、缩写不碰。每组代表 = 事实最多的码（并列取更短的），显示名用代表的。按当前索引算一次挂在 store 上（1223 个实体几毫秒）。
- 接进去的地方：树 / 实体索引只列代表行（事实数按不同事实算、preview 列出其它写法）；首页实体 chip 按组计数；实体页任何一种写法进来都落到代表、事实合并、标题下「同一实体的写法：MemoCat」；事实表 `entity=` 筛选和树上实体节点展开都取全组；事实卡 / 浮层的 chip 显示代表名（`annotate()` 统一改）。
- 真库效果：1223 个实体 → 1213 组，归并了 10 组（memo cat + MemoCat 169 条、Apple Watch + AppleWatch 60、ChatGPT + chat gpt 56、Hui Long + Huilong、open ai + OpenAI、AppStore + App Store、MemoKit + memo_kit、T0 + T 0、T1 + T 1、SamAltman + Sam Altman）。实拍三页都对（索引 1213 个、memo cat 169、事实表按 MemoCat 筛出 169）。
- 测试：新文件 test_kb_entities.py + test_kb_pages 合并用例（实体页 / 树 / 事实表 / 展开），后端 898；前端 87。
- 追加（第 293 轮）：主题页 / 会议页的实体 chip 计数按组算（不再出现 memo cat 和 MemoCat 两个 chip）；召回的符号通道把认出的实体扩到同一组的所有写法。测试 +1，后端 899。
- 再追加：排序给「挂着查询实体组」的事实加 4 分、六个字母以上的 ASCII 词去掉空格再比一次（memocat ↔ memo cat）。真库：「AppleWatch 续航」从 3:6 变 6:2 两种写法都有；「MemoCat 的定位」仍 7:1（写成 memo cat 的那批事实正文里根本没这个词，只有标签，靠文本排不上来——先这样）。60 条自召回 57 / 46。后端 899。

## [322] 巡检第 294 轮：去重之后实拍树与首页（2026-09-13）

- `kbexpand:kb:entities`：实体层「memo cat 169」一行（不再有 MemoCat），Facebook 211 / app 190 …；首页实体 chip 走同一套组。架构文档补上 `kb/entities.py`。没改代码。

## [323] 巡检第 295 轮：去重之后各种写法进实体页（2026-09-13）

- `/api/kb/entity/{memocat | memo_cat | MemoCat | applewatch | Apple Watch}` 都落到代表（memo_cat 169 条、apple_watch 59 条），带 `variants`；实体索引页的 chip 悬停能看到其它写法（preview）。没改代码。

## [324] 巡检第 296 轮：旧写法的实体标签页、类型节点标签名（2026-09-13）

- `open:kb:entity:memocat`：页面落到代表 memo cat，标签、面包屑「知识库 / 实体 / memo cat」都用代表名。标签栏上还挂着一个「kb:etype:…」裸 id——类型节点页没发 virtual-title。补上「实体 · 其他」。前端 87。
- 跟进：补上之后探针里标签还是裸 id——effect 与 openVirtual 建标签同一帧，事件先于标签存在。改为 setTimeout(0) 下一拍再发，截图标签读「实体 · 其他」。

## [325] 巡检第 297 轮：去重之后主题页（深色）、多段材料第 7/9 段（2026-09-13）

- `open:kb:topic:work_product_design`（深色）：主题地图里实体节点用方块、主题用圆，子主题 / 常一起出现的实体两栏都在。没抓到问题。
- `open:kb:unit:terrence-1872-6`：「样机节奏与量产风险评审会议纪要（7/9）」，分页钮 1–9 当前 7 高亮，说话人 speaker a / b / 白老师，实体 PCBA 2 与 PCB 1 分开（规则去重只合大小写 / 分隔符，不会把 PCBA 并进 PCB——对）。没抓到问题。
- 探针日志：只有 first-paint 计时行，没有 error / traceback。

## [326] 巡检第 298 轮：日记 × 最近删除（2026-09-13）

- 隔离库实测：⌘⇧D 建出「日记 / 2026 / 09 月 / 09-13 周日」，整棵「日记」删掉（四篇都进最近删除），只恢复那篇日记——它落到了**树根**（父节点不在了就挂树根的老规则），再按 ⌘⇧D 又建出一套新的「日记 / 2026 / 09 月 / 09-13 周日」：同一天两篇日记，一篇孤零零在根上。
- 改：恢复一篇时，父节点也在最近删除里的（整棵子树一起删的那种）先递归把父链放回去，笔记落回原位；父节点被彻底删掉了才挂树根。同一连接里做，带 seen 集防环。Trilium 的 undelete 也是整棵连位置一起回来。
- 测试改写：父链一起回来（三篇都出回收站、位置对）+ 父节点彻底删过的仍到树根。后端 900。

## [327] 第 299 轮：重打 dmg 前的全量门（2026-09-13）

- 后端 900 通过；前端 tsc + eslint + vitest 87 + 12 条 check / smoke 脚本全过；`npm run build` ✓。最近删除面板恢复后自己重拉列表，父链跟着从列表里消失，不用改。

## [328] 第 300 轮：重打 dmg（2026-09-13）

- `npm run dist` → `out/MEMOKET NOTE-0.1.0-arm64.dmg`（183MB），Resources 里带 LICENSE 与 third-party-notices.md。
- 装好的包冒烟：health ok（LLM 通、ASR 主机不通是环境）；`POST /api/notes/today` 建出「日记 / 2026 / 09 月 / 09-13 周日」；brief 搜「日记」命中；kb/tree 14 行；最近删除空。冒烟建的四篇日记随手删了并从最近删除清掉，装好的库跟之前一样。
- 两个测法上的坑，记下免得下次再绕：① `curl -m 1` 探 health 永远失败——health 自己要去探 ASR 主机，2 秒才回，探就绪该用 `/api/notes/trash` 这种不出网的接口（冷启动到就绪 0.6 秒）；② curl 请求行里直接写中文 query 会被后端 400，浏览器会编码，不是 app 的问题。

## [329] 巡检第 301 轮：健康检查轮询、正文底部留白（2026-09-13）

- health：启动查一次，之后只在有红字时每 30 秒再查，一次要探 LLM `/models` + 语音主机，语音不通时 2 秒才回——桌面版只是状态栏那行字晚 2 秒出现，不动。
- `end:5f65df10cad6` 实拍：滚到底时最后一行紧贴状态栏，只有 `.note-scroll` 的 120px 底边距。Trilium 的 ScrollPadding 是把正文底部垫半屏，最后一行也能滚到视线中间。补：`.cm-content` 底部 padding 30vh（先试 40vh，滚到底整屏空白太多）；点正文下方空白光标落到文末是顺带的好处。
- 顺手：styles.css 里 `.col` 两条规则是老布局的死代码（现在滚动容器是 `.note-scroll`），删掉；探针 `end:` 现在 8.5 秒时把 `.note-scroll` 滚到底，能拍到留白。前端 87。

## [330] 巡检第 302 轮：底部留白只给主编辑器（2026-09-13）

- 第 301 轮的 30vh 垫在 `editorTheme` 里，等于快速查看 / 分屏 / 历史版本 / 回顾这些只读小窗也各垫一截——它们本身有高度上限，只是多滚一屏空白。改成单独的 `scrollPadding` 扩展 + `scrollPad` prop，App.tsx 主编辑器一处传 true（不能按 readOnly 判：AI 跑的时候主编辑器也是只读的）。
- 实拍：`end:` 主编辑器留白在；`quick-view` 浮层里正文顶到底没有空尾。前端 87。

## [331] 巡检第 303 轮：数字词真查、库里的重名节点、裸 except（2026-09-13）

- dev 库真查：brief「4月10日」「十个失败测试」「1000台」都命中 harness 测试篇并给出前后文 snippet；召回「4月10日拿到手板」「十个失败测试」「5月1号测APP」「PCBA 风险」首条都对（冷索引 769ms，之后 50–100ms）；「1000台」库里本来没有，0 条是对的。
- 树上两个并列的「Notes」是 9 月 7 日两次导入同一份「公司汇报」留下的（Trilium 导入也是每次新起一棵，不合并），「日记」是第 179 轮 ⌘⇧D 试出来的，都不是 bug。
- 后端 6 处 `except Exception: pass` 全部带 noqa 与理由；前端没有裸 `catch {}`。`/api/memory/facts` 没有 `q` 参数（事实表按 kind / who / topic / entity 过滤，全文走 /recall），我先前传 q 被忽略是我探错，不是问题。

## [332] 巡检第 304 轮：1000×700 下 ribbon「信息」（深色）与「格式」条（2026-09-13）

- `ribbon:info --dark --win=1000x700`：创建 / 修改 / 字数 / 引用 / 摄入 / 副本（Obsidian · 导回 · 之后改过）/ 位置 / id 八行对齐，深色下可读。`pane:outline --win=1000x700`：窄窗右栏按规则自动收起（中栏不够 520 先收右栏），格式条一行放下。没抓到问题。

## [333] 巡检第 305 轮：深色 1000×700 下历史 / 查找条 / 标签菜单 / 移动到（2026-09-13）

- `history-open`：13 个版本一列，每行「时间 · 自动 · 字符数 · 增减」+「恢复到这一版」，当前版本高亮；`find`：查找 / 替换两行 + 下一个 / 上一个 / 全选中 / 三个开关，命中处高亮；`tab-menu`：分屏 / 关闭四种 / 重新打开刚关的（灰）；`picker`：「移动「09 月」到哪儿?」列表带父路径。四张都没抓到问题。
- 记一个不动的：历史面板写「9613 字符」、状态栏写「8330 字」——前者是原文长度（含 markdown 记号），后者是正文字数，两个口径都标了单位，不合并。

## [334] 巡检第 306 轮：原生 confirm 残留 + 确认框被面板盖住（2026-09-13）

- 静态扫：前端只剩一处 `window.confirm`——写作计划面板的「换个目标」，Electron 里弹的是系统原生框，跟整套界面不是一个风格。改走 App 的 `askConfirm`（跟删子树同一个框，危险态焦点在取消），面板多一个 `confirm` prop。`console.log` 零处，后端 print 都带 `[模块]` 前缀。
- 接上之后实拍没弹出来：确认框和写作计划面板用的是同一个 `.palette-backdrop`（z-index 200），确认框在 DOM 里排在面板前面，被面板盖住了。挪到面板之后渲染，加了注释说明为什么顺序不能动。
- 新探针 `plan-panel:abandon`：开面板后 5 秒点「换个目标」（要那个文件夹上有 active 计划——dev 库现在没有，我临时插一行 `probe-r306-*` 拍完即删）。第三张实拍：「放弃「…」这份计划?」+ 取消 / 红色「放弃计划」浮在面板之上。前端 87。

## [335] 巡检第 307 轮：叠着的浮层谁吃 Esc（2026-09-13）

- 第 306 轮把确认框叠到写作计划面板上之后顺着查 Esc：面板本来**没有** Esc（只能点 × 或点外面），确认框的 Esc 在元素上处理但不拦事件，⌘K 面板的 window 监听不管开没开都吃 Esc 也不拦。
- 改：面板加 window 级 Esc（`defaultPrevented` 的跳过）；确认框 / 文本输入框 / 选择器的 Esc `preventDefault + stopPropagation`；⌘K 只在开着时拦 Esc。规则一句话：Esc 只关最上面一层。
- 探针 `plan-panel:abandon-esc`（确认框上按 Esc → 只关确认框，面板留着）、`plan-panel:esc`（面板上按 Esc → 面板关掉），都拍到了预期结果；探针日志会写 `esc → 目标 prevented= backdrops=`。注意快门要 10 秒：8.5 秒时按键还没到，拍的是按之前，白查了一轮。前端 87。

## [336] 巡检第 308 轮：深色 1000×700 下写作 Skill / 导入 / ⌘K（2026-09-13）

- `skills`：卡片一列，启用标 / 标题 / 来源 / 用途 / 上下移 / 编辑 / 删除都在；`import`：Markdown / Obsidian / Evernote / Notion 分块，「导入到」下拉一行放下；`palette:创业`：三篇同名「创业一年回顾」各自带首句区分，知识库两条带高亮。没抓到问题。

## [337] 第 309 轮：分屏第二栏可编辑（2026-09-13）

- PROGRESS 里挂了很久的债：「分屏第二栏是只读的（可编辑要把编辑器状态从 App.tsx 拆出来）」。Trilium 的分屏每一栏都能写。没有去拆 App.tsx——第二栏本来就不需要 harness / 续写 / 提案层 / 引用高亮，它的定位是「看着另一篇改几笔」，所以给它一套**自己的**正文状态 + 自动保存就够了。
- `util/autosave.ts`：防抖 800ms；存的过程中又改了就排队不并发；`flush()`（关分屏 / 换篇前）；`dispose()`；失败不吞、不自动重试、留到下一笔改动。4 条 vitest（最后一版 / 排队 / flush-dispose / 失败重试）。
- `components/SplitEditor.tsx`：`key={note.id}` 挂，存完把服务端那篇交给 App 就地替换 notes 里的那条 + `reloadTree(false)`；主栏正开着的那篇在分屏里仍只读（提示一行），两边不互相盖；探针模式不落库（跟主编辑器 save() 同一条纪律）。右下角一行「保存中… / 已保存 / 没存上」。
- 探针 `split:edit`：在分屏 CM 视图文末插一句，5.5 秒后把状态行文本写进日志。实拍：分屏里出现那句 + 右下角「已保存」（深色）。第一版状态行 `position: sticky; bottom: 0` 落在编辑器 60vh 最小高度之下、悬在半空——改成分屏栏绝对定位右下角。
- 文档：harness-framework §19 那条「分屏只读」改成现状；PROGRESS 债勾掉。前端 91。
- 跟进：分屏里刚改了几笔（还在 0.8 秒防抖里）就点「在标签里打开」或在树上点它——主栏拿着旧正文开出来，1.5 秒后自动保存把分屏那几笔盖掉。`switchTo` 现在先 `splitFlush`（SplitEditor 通过 ref 交出 flush）再重新 GET 一次这篇拿最新正文。探针模式里 flush 不落库所以这条只能靠读代码 + autosave 单测兜。

## [338] 第 310 轮：重打 dmg（2026-09-13）

- `npm run dist` → `out/MEMOKET NOTE-0.1.0-arm64.dmg`（183MB），Resources 带 LICENSE / third-party-notices.md，前端包里有 `split-save-status`（第 309 轮的分屏编辑进包了）。
- 装好的包冒烟（这次只读接口，不再建日记）：就绪 3 秒（探 `/api/notes/trash`）、health ok、kb/tree 14 行。

## [339] 巡检第 311 轮：全新空库的第一屏（2026-09-13）

- 装好的包里那个库是空的（0 篇、kb/tree 0 行），照这个状态用一个全新用户 `fresh-r311` 实拍第一屏（亮 / 深色 1000×700）：三张卡（新建 / 导入 / 知识库）+ 常用键四条，右栏一句「打开一篇笔记后…」，状态栏「0 篇笔记」。没抓到问题。
- 全新用户只要打开过一次，`data/<user>/` 目录就会建出来（空的 codebook 落盘）——探针用完删掉。

## [340] 巡检第 312 轮：分屏同一篇、空库的知识库首页（2026-09-13）

- 新探针 `split:same`：把主栏正开着的那篇放进分屏——分屏顶上一行「这篇在主栏正开着，这里只看不改。」+ 只读正文，对。
- 全新用户 `open:kb:overview`（深色）：「知识库还是空的」+ 一段说明 + 「导入」「看看事实表长什么样」两个钮，搜索框在。没抓到问题。

## [341] 巡检第 313 轮：空库的知识库树、事实表（2026-09-13）

- 全新用户展开「知识库 / 主题」：词表自带的六个根主题（finance / health / learning / personal / project / work）一排空节点，点进去什么都没有。改：闭包里一条事实都没有的主题不上树（有事实的主题的父链一定也有事实，不会留孤儿），`kb:topics` 的 child_count 跟着只数活的。实拍：空库的「主题」现在没有展开箭头。terrence 库六个根主题都有事实，树没变。
- 空库 `open:kb:facts`（深色）：筛选条 + 「0 条 / 没有事实。」，对。
- 后端 901（新增：闭包里没有事实的主题不上树）。

## [342] 巡检第 314 轮：空库的时间线 / 实体 / 最近摄入 / 主题地图 / 定期回顾（2026-09-13）

- 全新用户五张深色 1000×700：时间线「还没有带日期的事实。」对；实体「0 个。按事实数排序。」对；定期回顾三个钮（后端空区间直接回「这段时间没有记录。」不打模型）对。
- 两处不对，改了：① 最近摄入——「最近 0 场会议。点开看这场会抽出来的事实。」下面挂着一个空的「会议」小节，改成「还没有摄入过。」+ 一句导入 / 存入知识库的指引，小节不渲染；② 主题地图——词表自带的六个根主题一条事实都没有也画成六个孤零零的圆，改成一条事实都没有（主题事实数全 0 且没有实体）就不画图，给一句话；数据没回来之前不判空（loaded 标记），免得闪一下空状态。
- terrence 库 `open:kb:graph` 复核：111 簇的图照常画。前端 91。

## [343] 巡检第 315 轮：全新用户写第一篇时右栏「记忆」说什么（2026-09-13）

- 新探针 `blank:write`：新建一篇、往正文插一段（不落库）。全新用户看到的是「知识库里暂时没有找到相关内容。」——第一次用的人会以为坏了。改：知识库一条事实都没有（树上 kb 根的 fact_count = 0，且树已加载）时换成「知识库还是空的。导入会议记录，或把写好的笔记「存入知识库」，之后这里会跟着你写的内容浮现相关记忆。」
- 同一探针在 terrence 库上：右栏浮出 PCBA 样品 / EVT 15 套 PCBA 两条相关事实，对。续写在空库上是「没命中退回模型自由发挥」，设计如此，不改。
- 探针建的笔记（fresh 用户一篇、terrence 一篇空的未命名）跑完都删了。前端 91。

## [344] 巡检第 316 轮：正文首行当标题时截到句读（2026-09-13）

- `blank:write` 实拍：没起名的笔记，标题栏占位 / 标签页 / 树都拿正文首行当显示名，一整句「今天跟供应商确认了 PCBA 样品的交期，4 月 10 日拿到手板之后再定下一步的测试安…」60 字硬截，读起来像句子不像标题。
- 改 `displayTitle` 的 `clipTitle`：按出现顺序找第一个够格的句读——句号（。！？）不在开头一两个字就算，逗号 / 分号要 ≥ 8 字（「好的，那就这么定了」不截）；都没有才 60 字硬截。第一版写成「句号优先」，结果整句到句号才截、等于没截——实拍才发现，改成按顺序。实拍标题栏现在是「今天跟供应商确认了 PCBA 样品的交期」。只在显示层，不写回库。
- 顺手核了一下 `frontend/dist`：8.1MB / 178 个文件、18 个 index-*.js 看着像攒了 18 次旧包，其实是一次构建里 mermaid 的懒加载 chunk（主包 1.86MB，其余都是几十 KB），vite 每次构建都会清 outDir，不是问题。
- 新测试 displayTitle.test.ts（4 条）。前端 95。

## [345] 巡检第 317 轮：模型不可达的红字可点（2026-09-13）

- 装好的包第一次开、或换了台机器没填模型：状态栏只有一行红字「LLM 不可达 (http://…)」，用户不知道去哪修（续写失败的 toast 有「打开设置」，但没动过续写的人看不到）。改成可点的按钮「LLM 不可达 (…) · 去设置」，点开设置页；新加 `.linklike`（长得像文字的按钮）。
- 实拍法：把 dev 库 provider 临时切成 local（.env 默认的 LAN 地址不通）拍一张，拍完切回 gpt，密钥没动。前端 95。

## [346] 巡检第 318 轮：全新用户的设置页（深色）（2026-09-13）

- 外观三选一 / LLM 供应商（本地 / GPT，key 只显示尾巴）/ 语音服务 / 笔记↔知识库自动同步 / 模型用量四段 / 个人偏好，供应商配置是全局的所以新用户看到的也是已配好的 GPT。没抓到问题。

## [347] 第 319 轮：文档同步（2026-09-13）

- trilium-ui-gap.md 三行改成现状：分屏（可编辑）、ScrollPadding（已做）、delete_notes 确认框（已做，含放弃写作计划）；harness-framework.md 的 components 列表加 SplitEditor。

## [348] 第 320 轮：重打 dmg（2026-09-13）

- 后端 901 / 前端 95 全绿后 `npm run dist` → `out/MEMOKET NOTE-0.1.0-arm64.dmg`（183MB）。装好的包冒烟：就绪 3 秒、health ok、Resources 带 LICENSE / notices、前端 css 里有第 317 轮的 `.linklike`、一个后端子进程；空库的 kb/tree 从 14 行变 8 行（第 313 轮把没有事实的六个根主题滤掉了）。

## [349] 巡检第 321 轮：全新用户拖文件进树（2026-09-13）

- `filedrop` 探针在空库上找不到树上的目标行（它要往某个节点上放），什么都没发生——探针的限制不是产品的：空库里拖文件到树的空白区走的是根节点，第 31–35 轮做过；不另写探针。没抓到问题。

## [350] 第 322 轮：桌面日志落盘（2026-09-13）

- 桌面主进程只在内存里留最近 400 行日志，「导出后端日志」要应用活着才能点——应用崩了 / 起不来的时候正是最需要日志的时候，反而什么都没有。改：每行同时追加到 `app.getPath('logs')/memoket-note.log`（macOS 是 ~/Library/Logs/<应用名>/），超过 2MB 滚成 `.1` 只留一代，写不了就静默；帮助菜单加「打开日志文件夹」。dev 实例实测文件生成、末尾是「退出前保存 / Shutting down」。

## [351] 巡检第 323 轮：分屏自动保存会不会把历史版本刷爆、日志里有没有密钥（2026-09-13）

- 分屏 0.8 秒一存：历史版本在 `update_note` 里 `_snapshot_locked(force=False)`，同一篇两版之间有 REVISION_INTERVAL_S 间隔、总数 REVISION_KEEP 封顶，跟主编辑器 1.5 秒自动保存一个规则，刷不爆。
- 日志落盘之后核了一遍会写进日志的东西：后端 print 没有 key / token；client-log 的 stack 截 4000 字；探针插的正文只在探针模式出现。没抓到问题。

## [352] 巡检第 324 轮：空库的主题索引页（2026-09-13）

- 第 313 轮空主题不上树之后，空库的「主题」页成了「0 个一级主题。计数含子主题。」+ 一个空网格。改成「还没有主题。」+ 一句指引。前端 95。

## [353] 巡检第 325 轮：空库的实体索引页（2026-09-13）

- 「0 个。按事实数排序。」+ 一个搜索框——空库里搜不出东西。改成「还没有实体。」+ 一句指引，搜索框只在有实体时出现；按类型进来的 `kb:etype:*` 页保留原来的「没有按类型分组」提示。terrence 库复核 1223 个实体的页面照旧。前端 95。

## [354] 巡检第 326 轮：空库的 ⌘K 与定期回顾（2026-09-13）

- 全新用户 `palette:pcba`：「没有匹配结果」一行，对；`digest:7:all`：后端不打模型直接回「这段时间没有记录。」，卡片「0 条事实」，「存为笔记」带 disabled。没抓到问题。

## [355] 巡检第 327 轮：820 宽的状态栏、发灰的动作 chip（2026-09-13）

- 820×600 + 模型不可达：状态栏「⚠ LLM 不可达 (http://…) · 去设置 · 语音离线 · 8330 字」一行放下，不折。
- 「存为笔记」在 0 条事实时带 disabled 但看着跟能点一样：`.chip:disabled { opacity: 1 }` 是给展示用的 chip（KbBits，disabled = 不可点）留的。动作 chip 加 `.chip-action`（存为笔记 / 现在存一版 / 恢复），disabled 发灰。顺手修 styles.css 里一个 `;;`。前端 95，修订语义 check 9 条过。

## [356] 巡检第 328 轮：列表接口计时（terrence 库，2 万条事实）（2026-09-13）

- 热索引后三次取最小：kb/tree 15ms / 55KB（第 313 轮多算一遍主题闭包没变慢）、tree 1.5ms、brief 1.4ms / 9KB、memory/topics 2ms、memory/entities 3ms / 108KB、stats 0.7ms。没抓到问题。

## [357] 第 329 轮：重打 dmg 前的全量门（2026-09-13）

- 后端 901 通过；前端 tsc + eslint + vitest 95 + 12 条 check / smoke 全过。

## [358] 第 330 轮：重打 dmg（2026-09-13）

- `npm run dist` → `out/MEMOKET NOTE-0.1.0-arm64.dmg`（183MB）。装好的包冒烟：就绪 3 秒、health ok、空库 kb/tree 8 行、css 里有 `.chip-action`。
- 第 322 轮的日志落盘在正式版实测：写到 `~/Library/Logs/memoket-note-desktop/memoket-note.log`——`app.getPath('logs')` 用的是 package.json 的 name 不是 productName，正式版和 dev / 探针实例落同一个目录。dev 实例改写 `memoket-note-dev.log`，两边不互相插行（下次打包生效）。

## [359] 巡检第 331 轮：全新一篇上 ribbon 四个标签的空状态（2026-09-13）

- 新探针 `blank:ribbon:<tab>`：新建空笔记并打开 ribbon 某标签。引用「还没摄入知识库 / 这篇还没有引用知识库里的记录」、链接「正文里打 [[ … / 还没有别的笔记链到这篇」、历史「还没有历史版本——正文改动后每隔十分钟自动留一版」、路径「树根 / 未命名 + 克隆到另一个位置」，四张都有话说，没有空白。
- 顺手核了 64 份今天的探针日志：没有一条 `client:error`。terrence 库历史版本 15 条 / 23 篇（harness 测试篇 13 条），KEEP 100 / 间隔 600 秒。

## [360] 巡检第 332 轮：空笔记上「存入知识库」「现在存一版」（2026-09-13）

- 第 331 轮那四张里两个钮在空笔记上都能点：「存入知识库」点了什么都不发生（App 那边 `!content.trim()` 直接 return，没任何反馈）；「现在存一版」点了后端 409「正文是空的」只能靠 toast。都改成 disabled + 悬停说明「正文是空的，先写点东西 / 没什么可存」。实拍两个钮发灰。前端 95。

## [361] 第 333 轮：笔记图标（Trilium 的 NoteIcon）（2026-09-13）

- trilium-ui-gap §9 剩下的「中」级项。做法：`notes.icon` 一列（boxicons 类名，空 = 默认）+ `POST /api/notes/{id}/icon`（只认 `bxs?-[a-z0-9-]{1,40}`，否则 422——图标名会原样进 className）；树行 / 笔记都带 icon；标题行的图标变成按钮，点开 `IconPicker`（8×6 一组 48 个常用 boxicons + 「默认」清掉），点外面 / Esc 关；树上的 `iconOf` 用户挑的优先。
- 实拍：第一版选择器第 8 列画到框外——全局 button 的 padding 把格子撑宽了，格子加 `padding: 0; min-width: 0; width: 100%` 后放得下。`icon-picker:pick` 探针真点了「rocket」：标题行图标变火箭、后端 POST 200，拍完清回空。
- 后端 903（新增 test_note_icon 2 条）/ 前端 95；README API 表加一行，harness-framework 路由说明同步。

## [362] 第 334 轮：笔记图标跟着导出 / 导入走（2026-09-13）

- `icon-picker:pick` 再拍一张看树：树行图标也变成火箭（拍完清回空）。
- 图标是笔记的一部分，整库导出的 front-matter 加 `icon:`；导入（`/api/import/files`，我们自己的 zip 走 obsidian 洗法）认 front-matter 里的 icon——只认 boxicons 类名，别的当没有；文件夹笔记（`项目/项目.md`）的图标也回到文件夹本身。最近删除的快照是 `SELECT *`，恢复时图标自然在。
- 测试：导出再导回图标都在、front-matter 剥干净。后端 904。

## [363] 第 335 轮：⌘K 行带笔记图标（2026-09-13）

- 笔记图标做了之后 ⌘K 的笔记行还是清一色 bx-note。`/api/notes/brief` 两条 SELECT 加 icon、NoteBrief 加字段，前端行图标 `n.icon || 'bx-note'`。实拍 `palette:harness`：那篇带火箭（拍前设、拍完清）。后端 904 / 前端 95。

## [364] 第 336 轮：欢迎页最近列表、链接面板、反向链接的行也带图标（2026-09-13）

- 扫了一遍前端里写死 bx-note 的地方：欢迎页最近笔记（Note 对象本来就有 icon，直接用）、ribbon「链接」的链出 / 链入行（CitingNoteOut 加 icon；backlinks / notes_citing 两条 SELECT 带 icon；outgoing 从笔记取）。`[[链接]]` 行内小图标和知识库里「打开摄入它的那篇」的 chip 保持 bx-note——那两个是「这是一篇笔记」的类型标记，不是某篇的标识。
- 测试：链接面板链出行 / 反向链接行都带图标。后端 905 / 前端 95。

## [365] 巡检第 337 轮：链接面板实拍（2026-09-13）

- 新探针 `note:<id>[:ribbon:<tab>]`：按 id 打开某篇真笔记并切 ribbon 标签（之前 `ribbon:` 只会挑第一篇 >80 字的）。给 harness 测试篇设火箭、链接测试篇设星星：链接测试的「链到的笔记」行带火箭，harness 篇的「链到这篇的」行带星星（深色）。拍完两篇图标都清回空。前端 95。

## [366] 巡检第 338 轮：820×600 深色下的图标选择器（2026-09-13）

- `icon-picker --dark --win=820x600`：8×6 的格子全在框里，「默认」在右下，没盖住标题行。没抓到问题。

## [367] 第 339 轮：重打 dmg 前的全量门（2026-09-13）

- 后端 905 通过；前端 tsc + eslint + vitest 95 + 12 条 check / smoke 全过。

## [368] 第 340 轮：重打 dmg（2026-09-13）

- `npm run dist` → `out/MEMOKET NOTE-0.1.0-arm64.dmg`（183MB）。装好的包冒烟：就绪 3 秒、health ok、后端 rss 81MB（空库）、kb/tree 8 行、css 里有 `.icon-picker`；日志目录里正式版 `memoket-note.log` 与 dev 的 `memoket-note-dev.log` 已分开。
- 顺手核了 dev 数据目录：没有残留的 fresh-* 用户目录，git 工作区干净。

## [369] 第 341 轮：换图标的另外两个入口（2026-09-13）

- 图标只能从标题行那个小图标点开，⌘K 和树菜单都不知道有这回事。加：⌘K 命令「换个图标（当前笔记）」、树右键「换个图标…」（先切到那篇再弹，树 / 标签 / 标题行同一个），都走 `open-icon-picker` 事件；没开笔记时 toast「先打开一篇笔记再换图标」。探针 `icon-cmd`。实拍：命令在列表里、事件弹出选择器。前端 95。

## [370] 第 342 轮：标签页带笔记图标（2026-09-13）

- Trilium 的标签也画 NoteIcon。TabBar 加 `iconOf(noteId)`（App 从 notes 里查，不进 Tab 模型——那个落 localStorage，图标改了会留旧值），标题前画一个小图标。
- 实拍第一版：terrence 开着十几个标签，每个 84px，图标把标题挤成「h…」。改：标签 <120px 时（`data-size` 加一档 narrow）不画图标，标题照旧「harne…」。前端 95。

## [371] 巡检第 343 轮：宽标签上的图标（2026-09-13）

- 新探针 `blank:icon`：空库新建一篇、选择器里挑 rocket。只有一个标签、够宽：标签、树行、标题行三处都是火箭（深色）。那篇空的未命名退出时被自动收掉（第 51–55 轮的规则），库里没留东西。前端 95。

## [372] 第 344 轮：挑过图标的空笔记不再被当「没动过」收掉（2026-09-13）

- 第 343 轮实拍顺出来的：新建一篇、只挑了个图标没写字，切走 / 退出时按「空的未命名自动收掉」的规则被删了。挑图标也是「我要这篇」的表态，`dropIfStillEmpty` 加一条：有 icon 就留。实测：空库新建 + 挑火箭 + 退出，库里那篇还在（title 空、icon bx-rocket）。
- 顺手把 App.tsx 里「分屏第二栏是只读的」那段老注释改成现状。前端 95。

## [373] 第 345 轮：功能建出来的笔记带默认图标（2026-09-13）

- Trilium 的日记根 / 年 / 月 / 日各有图标，我们建出来全是默认的。`today_note` 每层建时设图标：日记根 bx-calendar、年 / 月 bx-folder、当天 bx-calendar-event；「定期回顾 → 存为笔记」的那篇 bx-history；多文件导入的父节点「导入 <日期>」bx-import。已经建过的不回填（那是用户的树，不替他改）。
- 实拍全新用户 ⌘⇧D：树上「日记」是日历图标，标签 / 标题行是当天那个。后端 906（test_today 加一条）/ 前端 95。

## [374] 第 346 轮：分屏里正在改的那篇被删了（2026-09-13）

- 分屏里刚打了几个字（还在防抖里）就把那篇从树上删掉：0.8 秒后 PUT 回 404，分屏会 toast「分屏这篇没存上：…」——对象都没了，报「没存上」只会吓人。SplitEditor 的 onError 遇 404 静默丢掉没存的（dispose）、状态行清空；别的错照旧提示。前端 95。

## [375] 第 347 轮：崩溃对话框带日志路径 + 文档（2026-09-13）

- 「后端反复崩溃」「后端没能重新启动」两个对话框只给最后 12 行，现在末尾加「完整日志：<路径>」（第 322 轮落盘的那个文件）。harness-framework 的 Electron 日志一节、README 功能表补「日志落盘」。desktop tsc 过。

## [376] 巡检第 348 轮：空库首页的搜索框（2026-09-13）

- 空库 `open:kb`：「知识库还是空的」上面还摆着一个 autoFocus 的搜索框，光标停在那里像在等你输入，搜出来只能是空。一条事实都没有就不渲染搜索框。实拍 1000×700：只剩空状态 + 两个钮。README 功能表补「笔记图标」一行。

## [377] 第 349 轮：重打 dmg 前的全量门（2026-09-13）

- 后端 906 通过；前端 tsc + eslint + vitest 95 + 12 条 check / smoke 全过。

## [378] 第 350 轮：重打 dmg（2026-09-13）

- `npm run dist` → `out/MEMOKET NOTE-0.1.0-arm64.dmg`（183MB）。装好的包冒烟：就绪 2 秒、health ok、后端 rss 81MB、空库 kb/tree 8 行、css 里有 `.note-tab-icon`、日志文件在写。

## [379] 巡检第 351 轮：智能续写真跑（150 秒截图）（2026-09-13）

- `harness:5f65df10cad6`（跑前存一份正文，跑完 PUT 回原样 9613 字）：第 2 轮同步「本地 9950 vs 服务端 9946，用服务端的」；150 秒时第 4 轮「修订 0 处，续写中…」，新写的段落绿底，右栏「改动 2 · 计划 4」，骨架被 replan 成新的核心张力 + 五条节拍，引用角标 31 → 34。这十几轮改的图标 / 分屏 / 空状态没碰主链路。没改代码。

## [380] 第 352 轮：树上一个分类超过 300 条之后的尾巴（2026-09-13）

- PROGRESS 里的债：一个分类下最多给 300 条事实。terrence 库 work 主题 6034 条，树上只见 300 条，没人告诉你还有 5700 多条在哪。`children()` 超过 MAX_CHILDREN 时（主题 / 实体）尾巴加一行 `kb:facts?topic=<code>` / `?entity=<code>`「还有 N 条 · 去事实表看」，树上画成表格图标；月份 / 会议自己的页面本来就分页，不加。
- 顺手：`knownVirtual` 的正则原来不认 `kb:facts?…`（重启时带查询串的事实表标签会被当不认识的收掉），补上 `\?`。探针 `kbexpand:<id>:end` 展开后把左栏滚到底。实拍：work 展开到底是「还有 5734 条 · 去事实…」。
- 后端 907（新增：超过 300 条给尾巴、没超不给）/ 前端 95。

## [381] 第 353 轮：带筛选的事实表标签叫什么（2026-09-13）

- 从树上尾巴行点进去的事实表，标签本来会叫树上那行的名字「还有 5734 条 · 去事实表看」（那是入口的话，不是页面的名字）。改：`kb:facts?…` 的标签叫「事实表 · work」（查询串的值拼上）；已经开着的虚拟标签再打开时按现在的规则把名字刷一遍（标签落 localStorage，老规则起的名会一直留着——实拍第一版就是这么被骗的）。前端 95。

## [382] 第 354 轮：带筛选的事实表的面包屑（2026-09-13）

- 从尾巴行进来的事实表，状态栏面包屑末尾是那句「还有 5734 条 · 去事实表看」。改成页面的名字：实拍「知识库 / 主题 / work / 事实表 · work」。前端 95。

## [383] 第 355 轮：主题页 / 实体页翻页器旁给「去事实表筛」（2026-09-13）

- `openend:kb:topic:work`（新探针：打开虚拟页并滚到底）：work 主题页的事实小节「1 / 121 · 共 6034 条」——121 页没人翻得动，也没有别的出口。翻页器（KbBits.Pager）加 `tail`，主题页 / 实体页传一条「去事实表按类型 / 说话人 / 实体（主题）筛」的链接（`kb:facts?topic=` / `?entity=`）。第一版放在小节标题右边，滚到底看翻页器的人看不见，挪到翻页器旁边。实拍 work（亮）/ Facebook（深）两页都有。前端 95。

## [384] 巡检第 356 轮：两个带筛选的事实表标签并存、树展开计时（2026-09-13）

- `open:kb:facts?kind=plan`（深色 1000×700）：标签栏同时有「事实表 · w…」和「事实…」（plan），筛选条「plan · 3996」，两个不互相盖。
- 树展开计时（热）：work 主题 301 行（300 + 尾巴）4ms、Facebook 211 行 2.6ms、2026-03 月 300 行 3ms、实体 1213 行 8ms。harness-framework 虚拟子树一节补上空主题不上树 / 300 条尾巴 / 翻页器出口。没改代码。

## [385] 第 357 轮：带筛选的事实表在分屏里叫什么（2026-09-13）

- 新探针 `splitv:<虚拟 id>`。`splitv:kb:facts?topic=work`：分屏头顶着「还有 5734 条 · 去事实表看」。把「事实表 · work」这个命名抽成模块级 `factsLabel(id)`，标签 / 分屏头 / 面包屑 / 树右键「打开」四处同一个。实拍分屏头「事实表 · work」。前端 95。

## [386] 第 359 轮：重打 dmg 前的全量门（2026-09-13）

- 后端 907 通过；前端 tsc + eslint + vitest 95 + 12 条 check / smoke 全过。

## [387] 巡检第 358 轮：1000×700 下分屏里的事实表（2026-09-13）

- `splitv:kb:facts?topic=work --win=1000x700`：右栏按规则自动收起，分屏头「事实表 · work」，筛选条折成两行、分页正常。没抓到问题。

## [388] 第 360 轮：重打 dmg（2026-09-13）

- `npm run dist` → `out/MEMOKET NOTE-0.1.0-arm64.dmg`（183MB）。装好的包冒烟：就绪 2 秒、health ok、后端 rss 81MB、空库 kb/tree 8 行、css 里有 `.kb-link-inline`。

## [389] 巡检第 361 轮：侧栏快速搜索里的三篇同名（2026-09-13）

- `search:创业 --dark --win=820x600`：三篇「创业一年回顾」各自带一行命中片段 + 日期，分得开。Agent 运行面板长在右栏「计划」标签的骨架下面，只有跑过才有内容，单测（harnessStreamRounds）盯着轮次顺序，不另拍。没改代码。

## [390] 第 362 轮：侧栏搜索：标题命中的给第一行正文（2026-09-13）

- 搜「创业」，三篇「创业一年回顾」的片段都是再印一遍 H1「创业一年回顾 时间线与里程碑…」——标题本身命中时片段没信息量。改：标题命中的行改给第一行正文（跟 ⌘K 的 first_body 一个思路），正文命中的照旧给片段。
- 实拍第二版露出「## 时间线与里程碑」：那篇是整篇缩进的（每行前 4 个空格），`previewLine` 的剥记号正则锚在行首、trim 在后面。改成先 trim 再剥，`#` 后没空格的也剥（跟后端 `_first_body_line` 一致）。第三版三篇分别是「时间线与里程碑」「现有记录只能确认部分测试计划…」「时间线与里程碑」+ 日期。前端 95。

## [391] 第 363 轮：整篇缩进的笔记的显示名（2026-09-13）

- 同一类问题再查一处：`displayTitle` 拿正文首行当名字时也是先剥 `#` 再 trim，整篇缩进的「    # 创业一年回顾」会显示成「# 创业一年回顾」。改成先 trim。测试 +1（前端 96）。

## [392] 巡检第 364 轮：图标进最近删除再回来（2026-09-13）

- 隔离库实测：设火箭 → 删 → 从最近删除恢复，icon 还是 bx-rocket（快照是 `SELECT *`，恢复按列原样插回）。没改代码。

## [393] 巡检第 365 轮：导出计时、导回也带图标、同步差异定位（2026-09-13）

- terrence 库整库导出：27 个文件（23 篇 md + 资产）5.1MB，133ms。Obsidian 目录导回和 zip 共用 `exporters.render_tree → note_body`，第 334 轮的 `icon:` 也随导回走。
- 第 351 轮那条「本地 9950 vs 服务端 9946」只有字数，看不出哪里跑偏。`harness-sync` 日志加上第一处不同的位置和前后 20 字（JSON 转义，换行看得见），下次真跑就知道是分隔符还是引用。前端 96。

## [394] 第 366 轮：导出文件名跟显示名一个规则（2026-09-13）

- 导出 zip 里没起名的笔记文件名是正文首行 80 字硬截：「我们产品当前遇到的挑战：四项核心挑战归纳为验证框架：录制信任、关联即时反馈、总结行动化、图谱自维护。本次访谈在三月 四月.md」。后端 `exporters.display_title` 加 `clip_title`，跟前端 `clipTitle` 同一条规则（句号 ≥2 字截、逗号 / 分号 / 冒号 ≥8 字截、否则 60 硬截），冒号也算进两边的句读；整篇缩进的首行同样先 strip 再剥 `#`（后端这里之前也有这个顺序问题）。
- 后端 908 / 前端 96。

## [395] 巡检第 367 轮：dev 库体检（2026-09-13）

- `pragma quick_check` ok；notes 465（terrence 23 + shot-perf 412 + 演示 / 测试用户）、branches 465（一一对应，没有孤儿）、note_revisions 16、note_trash 10、note_citations 331、writing_plans 52；数据目录 88MB（terrence 索引 36MB、资产 12MB、备份 23MB / 3 份）。桌面日志 dev 99KB / 正式版 21KB，离 2MB 滚动还远。r36x 探针日志没有 client:error。没改代码。

## [396] 巡检第 368 轮：库里的测试残留（2026-09-13）

- note_trash 那 10 条全是 `test-skeleton` 的，写作计划 done 34 / abandoned 18 都是历次真跑留下的（30 天不活跃会被启动清理扫掉）；隔离前的测试用户（harness-test-2 / cancel-test3 / cleanup-test / search-test）还在，等用户点头再清（第 187 轮起的约定）。没改代码。

## [397] 第 369 轮：重打 dmg 前的全量门（2026-09-13）

- 后端 908 通过；前端 tsc + eslint + vitest 96 + 12 条 check / smoke 全过。

## [398] 第 370 轮：重打 dmg（2026-09-13）

- `npm run dist` → `out/MEMOKET NOTE-0.1.0-arm64.dmg`（183MB）。装好的包冒烟：就绪 2 秒、health ok、后端 rss 81MB、空库 kb/tree 8 行、整库导出 200（空库 477B）。

## [399] 第 371 轮：只有图标的按钮都要有名字（2026-09-13）

- 扫 `icon-btn`：24 个里 4 个既没 title 也没 aria-label——主题地图 / 知识库首页的搜索清空 ×、翻页器的 ‹ ›。补上「清空搜索」「上一页」「下一页」。再用「只含一个 <i> 的 button」扫一遍整个 src：0 个漏网。前端 96。

## [400] 第 372 轮：弹层都标上 role（2026-09-13）

- 确认框 / 选择器 / 文本输入 / 图标选择器 / 右键菜单本来就有 role；⌘K、`/` 块生成、写作计划面板、选中文本菜单、快速查看、快捷键表六个没有。补 `role="dialog" | "menu"` + aria-label。前端 96。

## [401] 第 373 轮：输入框的名字（2026-09-13）

- 扫所有文本输入框：只有重命名 / 输入对话框（TextPrompt）那个既没 placeholder 也没 aria-label，用对话框标题当 aria-label。⌘K、标题行、知识库两个搜索框都有 placeholder。前端 96。

## [402] 第 374 轮：下拉框的名字（2026-09-13）

- 8 个原生 `<select>` 里 6 个没有 aria-label / title（主题地图「父主题」、导入页「导入到」「飞书范围」、事实表「类型」「说话人」「置信度」），补上。前端 96。

## [403] 巡检第 375 轮：智能续写真跑——同步差异终于看见了（2026-09-13）

- 第 365 轮加的差异定位这次派上用场（跑前存正文，跑完 PUT 回 9613 字）。三处不同分两类：
  - 第 2 / 5 轮：本地在段落接缝处多出空行——本地 `\n\n\n\n`（第 5 轮 `\n\n\n\n\n\n`）vs 服务端 `\n\n`。客户端拼续写段时又加了一次分隔，或者流里的段本身就带开头换行；服务端把多余空行压掉了。差 2–4 字就是这个。
  - 第 3 轮：本地「结构评和对应决策，而不是继续增加计划日期。对8」vs 服务端「结构评审、容量确认、下单与样机记录 | 对装配」，差 267 字——客户端按 anchor 重放某条修订落错了位置，把一段改烂了；轮末用服务端的对齐回来，但这一轮里用户看到的正文是错的。这就是「服务端正文是唯一权威」那条设计的由来，现在有了实锤。
- 后面两轮分别修：先修接缝空行（便宜），再看重放。

## [404] 第 376 轮：流式续写的接缝空行（2026-09-13）

- 第 375 轮那两类里便宜的一类：模型的流里一段结束多吐两个换行，服务端 `join_round_text` / `tidy_blank_lines` 压掉、客户端逐块拼进去不压。新 `editor/streamJoin.ts`：增量落进正文后只在接缝附近（插入点前两个字到增量末尾）把 `\n{3,}` 压成 `\n\n`，跨块拆开的换行也压，正文别处不碰；onDelta 改走它。5 条单测，前端 101。

## [405] 第 377 轮：修订事件不再走 updater（2026-09-13）

- 第 375 轮第 3 轮那 267 字的错位：客户端的 `locate` 跟服务端 `_locate` 逐行对过，一样（最小跨度那一对、结尾标记找不到退回只用 anchor）。差别在别处——`onRevision` 用的是 `setContent(updater)`，updater 要等 React 下一拍才跑；同一批 SSE 里紧跟着的 delta 已经按旧的 `liveContentRef` 拼过了，updater 跑起来又把 ref 盖成「有修订没增量」的版本，两边各丢一半（onDelta 那条注释早就写着「updater 的执行时机跟闭包里的游标对不上就会漂」，onRevision 漏了同样的改法）。改成跟 onDelta 一样从 ref 算、直接 setContent(next)。下次真跑看 harness-sync 还报不报大差异。前端 101。

## [406] 第 378 轮：修订事件给全量、本地重放后也压空行（2026-09-13）

- 再往下挖第 375 轮那 267 字：服务端发出去的 `revision` 事件把 `text` 截到 300 字、`anchor` 截到 120 字（当初是给面板看的），客户端拿它本地重放——长一点的 replace 只插进前 300 字，后面接着旧尾巴，正好是实拍里「结构评和对应决策，而不是继续增加计划日期。对8」那种断在半句的样子；长 anchor 则直接定位失败、整条漏掉。改：事件给全量（面板要短的自己截）。
- 服务端每应用一条修订还会 `tidy_blank_lines`，客户端没有对应动作——delete 留下的三个空行两边就差开了。`editor/streamJoin.ts` 照抄一份 `tidyBlankLines`（围栏内不动、围栏外剥行尾空白），onRevision 应用完跑一遍。
- 后端 909（新增：修订事件带全量）/ 前端 104。下次真跑看 harness-sync 还剩什么。

## [407] 第 379 轮：重打 dmg 前的全量门（2026-09-13）

- 后端 909 通过；前端 tsc + eslint + vitest 104 + 12 条 check / smoke 全过。

## [408] 第 380 轮：重打 dmg（2026-09-13）

- `npm run dist` → `out/MEMOKET NOTE-0.1.0-arm64.dmg`（183MB）。装好的包冒烟：就绪 7 秒（这次机器忙）、health ok、后端 rss 81MB、空库 kb/tree 8 行。

## [409] 巡检第 381 轮：智能续写真跑——同步差异只剩一类（2026-09-13）

- 第 376–378 轮修完再跑一次（跑前存、跑完 PUT 回 9613 字）：五轮里只有第 1 轮报了一次差异，而且不再是空行或截断——本地「…分开。现有记录提到一共做了三台样机 [terr…」比服务端多 135 字，服务端在同一位置是下一句「回填时应逐台记录硬件版本…」。也就是服务端在轮内把一整句（带引用的那句）删掉了，客户端没跟着删。查是哪一步删的、发没发事件，下一轮补上镜像。

## [410] 第 382 轮：服务端轮内删句发 scrub 事件，客户端镜像（2026-09-13）

- 第 381 轮那一类：服务端在修订之后 `scrub_meta_sentences_v` 整句删元话语，只发一条截到 60 字的 `dropped` 给面板看，客户端没法照删。新事件 `scrub`（`{sentence 全量, why}`），revise 路径每删一句发一条；客户端 `onScrub` 在本地正文里找到同一句删掉、压空行、游标前移。续写落盘那一侧的清理紧挨着轮末，轮末本来就会用服务端正文对齐，不另发。
- 后端 909（元话语测试加断言）/ 前端 104。

## [411] 巡检第 383 轮：智能续写真跑——第三次复核（2026-09-13）

- 五轮只剩一处差异，且是预期内的那类：第 3 轮轮末本地比服务端多一句「若只有收订金记录，没有订单明细、交付记录…」——续写落盘那一侧的元话语清理（紧挨着轮末，轮末服务端正文对齐回来）。从第 375 轮的三处（空行 2–4 字、截断 267 字、删句 135 字）到现在只剩这一处，客户端正文在轮内跟服务端基本同步。正文跑完 PUT 回 9613 字。没改代码。

## [412] 第 384 轮：文档——轮内同步的规则（2026-09-13）

- harness-framework 事件表加 `scrub` 一行，`revision` 全量的原因写进去；表后加一段「客户端轮内正文怎么跟服务端保持一致」（重放 / 接缝压空行 / 删句 / 都从 liveContentRef 算 / 差异定位靠 harness-sync）。

## [413] 巡检第 385 轮：文件夹 harness 与 magic tap 那侧的正文拼接（2026-09-13）

- 顺着第 377 轮查另外两条流：文件夹 harness（runHarness）的轮初分隔、delta、段末对齐全走 `setContent(updater)`，没有跟 ref 混用，轮末 / 段末又用服务端正文对齐，不会出现单篇 harness 那种「一半在 ref 一半在 state」的漂；magic tap 的增量插进编辑器走 CM dispatch（跟敲键一条路）。没改代码。

## [414] 第 386 轮：README 的接口表反查（2026-09-13）

- 契约测试只保证「README 里写的后端都有」，反过来没人查。按 openapi 扫：114 个端点，README 精确列了 31 个（还有几组是合写的）。最近删除、历史版本、链接、置顶、导入导出、事实 CRUD / 冲突收件箱、各知识库页、回顾 / 校验 / 扩写 / 重写、Skill / 偏好 / 供应商 / 用量整段没提。补 7 行合写的表 + 一句「完整清单看 /docs」。补的时候写错了一个路径（restore 少了 trash 段），契约测试当场抓住——这就是它存在的意义。

## [415] 巡检第 387 轮：/docs 在装好的包里能开、仓库没有大文件（2026-09-13）

- README 刚指了 `/docs`：装好的包 `GET /docs` 200、`openapi.json` 124KB，Swagger 没被打包配置关掉。git 里没有 >500KB 的 blob（截图都在 scratchpad，没混进仓库）。scratchpad 攒了 1189 张截图 775MB，第 350 轮之前的删掉。没改代码。

## [416] 巡检第 388 轮：60 条事实自召回复测（2026-09-13）

- 随机 60 条事实（seed 7）拿原文 / 前 40 字当查询，看自己在不在 top-5：全文 58/60、前 40 字 57/60，中位 115ms。第 230 轮那次是 57 / 46——实体归组 + 数字词 + 整词匹配这一路改下来，短查询的召回从 46 提到 57。没改代码。

## [417] 第 389 轮：重打 dmg 前的全量门（2026-09-13）

- 后端 909 通过；前端 tsc + eslint + vitest 104 + 12 条 check / smoke 全过。

## [418] 第 390 轮：重打 dmg（2026-09-13）

- `npm run dist` → `out/MEMOKET NOTE-0.1.0-arm64.dmg`（183MB）。装好的包冒烟：就绪 3 秒、health ok、`/docs` 200。
- health 里后端 rss 从之前的 81MB 变成 319MB，查了一下不是回归：`memory.users` 是 `['default', 'terrence']`——正式版的界面身份是 terrence，它的知识库索引装进内存就是这个量（dev 实例一样）；我冒烟用的 curl 不带 X-User-Id，落的是 default 这个空用户，所以之前一直以为「装好的库是空的」、rss 才 81MB。以后看正式版的真库要带 identity.json 里的用户。

## [419] 第 391 轮：「这篇周围有什么」——笔记级局部图（2026-09-13）

- trilium-ui-gap §9 最后一个没做的：NoteMap。Trilium 画笔记之间的链接；我们这儿更有用的是「这篇的知识语境」：`GET /api/notes/{id}/graph` 把正文引用的 + 它贡献的事实挂到主题（≤12）/ 实体（≤20，说话人伪实体不进）上，主题↔实体按同一条事实里共现连线；ribbon「引用」顶部画成 `LocalGraph`（复用 KnowledgeGraph），点节点进它的页面。
- harness 测试篇实拍：31 条事实 → 6 主题 + 6 实体，接口 5ms。后端 `kb/who.py` 补 `is_speaker_tag`（跟前端同一条正则）。
- 后端 911（test_note_graph 2 条）/ 前端 104；README、harness-framework、gap 表同步。

## [420] 巡检第 392 轮：局部图的深色与空态（2026-09-13）

- 深色 1000×700 下局部图节点 / 虚线 / 标签都看得清；全新空笔记的「引用」页不出这一节（不到两个节点就不画，接口 1ms）。terrence 库 23 篇里只有 harness 测试篇有引用，其它 22 篇接口都是空图 1ms。退出时那次 404 是空笔记被自动收掉之后面板又刷了一次，前端吞掉了。没改代码。

## [421] 巡检第 393 轮：摄入过的笔记的局部图（2026-09-13）

- shot-demo 五篇：摄入过的四篇各有图（3 / 5 / 8 / 15 条事实牵出 1–5 个主题、1–9 个实体），没摄入也没引用的那篇空图。`note:674aa9a1b4b7:ribbon:cites`（8 条事实 → 5 主题 + 4 实体）实拍在「这篇贡献的事实」之上。前端全套 104 + 12 条 check 过。没改代码。

## [422] 巡检第 394 轮：正式版数据目录里的 user-xxxxxx（2026-09-13）

- 正式版 userData/data 下有 6 个 `user-xxxxxx/`（各一个 4KB 的空 codebook.xml），时间都是 9 月 10–12 日——身份落到 identity.json 之前（第 4 轮修的）每次开都随机一个身份留下的壳，今天没有新增；`getUser()` 现在 `?user=` 优先并记进 localStorage，主进程每次都带。是用户的真实数据目录，不替他删，记一笔。没改代码。

## [423] 第 395 轮：局部图挪到「引用」面板最后（2026-09-13）

- 820×600 实拍：ribbon 只有 40vh 高，局部图放在最前面把「这篇引用的事实」整个顶到看不见，只露半张图。面板的正事是事实本身，图是顺带的——挪到最后、高度 240 → 200。前端 104。

## [424] 巡检第 396 轮：挪完之后的「引用」面板（2026-09-13）

- 默认宽度深色：面板从「这篇引用的事实 · 31 条」开始，局部图在 31 条之后（ribbon 体内滚到底才看到）。前端全套 104 + 12 条 check 过，r39x 探针日志没有 client:error。没改代码。

## [425] 巡检第 397 轮：滚到「引用」面板底部看图（2026-09-13）

- 探针 `note:<id>:ribbon:<tab>:end`（把 ribbon 体滚到底）。深色实拍：31 条引用之后是「这篇周围有什么」局部图，节点 / 虚线在深色下清楚。后端 911 / 前端 104。

## [426] 第 398 轮：设计文档补「这篇周围有什么」（2026-09-13）

- kb-fusion-design.md 加一节：笔记 × 知识库的第三条线（引用 / 反查之外的「知识语境」），接口、上限、说话人过滤、跟 Trilium NoteMap 的取舍。

## [427] 第 399 轮：重打 dmg 前的全量门（2026-09-13）

- 后端 911 通过；前端 tsc + eslint + vitest 104 + 12 条 check / smoke 全过。

## [428] 第 400 轮：重打 dmg（2026-09-13）

- `npm run dist` → `out/MEMOKET NOTE-0.1.0-arm64.dmg`（183MB）。装好的包冒烟这次带 `X-User-Id: terrence`（正式版的真身份）：kb/tree 180 行 / 20406 条事实、23 篇笔记、health ok、`/docs` 200——正式版的库跟 dev 一样满，前 39 次冒烟看到的「空库」都是 default 用户。

## [429] 巡检第 401 轮：正式版真库上的局部图（2026-09-13）

- 正式版 terrence 库（跟 dev 不是同一批笔记，没有 harness 测试篇）：三篇带引用的（4 / 5 / 10 条）局部图接口分别牵出 3 / 2 / 2 个主题 + 1 个实体；10 条引用那篇只解析出 7 条——3 条引用指向已经不在的事实（库重建过），面板上是「（找不到）」。没改代码。

## [430] 第 402 轮：找不到的引用可以一键清掉（2026-09-13）

- 第 401 轮正式版真库里有一篇 10 条引用 3 条找不到，面板只有一块红字，用户要自己在正文里一个个找 `[id]` 删。红卡上加「清掉这些引用」：只删正文里那几个 `[id]`（连同前面那个空格），句子留着；走 CM 的 changes（可撤销、跟敲键一条路），没编辑器时直接改 content。`util/wordCount.citationRanges` + 2 条单测。
- 探针 `note:<id>:ribbon:cites:strip`：文末塞一条指向不存在事实的引用（不落库）再点按钮。实拍：前「1 条引用在知识库里找不到：terrence-9999-FF」+ 按钮，后红卡消失、角标 32 → 31。前端 106。

## [431] 巡检第 403 轮：第 402 轮之后的全量门（2026-09-13）

- 前端 tsc + eslint + vitest 106 + 12 条 check / smoke 全过；r40x 探针日志没有 client:error。没改代码。

## [432] 第 404 轮：链到已删笔记的链接列出来、可改成纯文本（2026-09-13）

- 跟第 402 轮同一类：`[标题](note://id)` 链到的笔记删了，`/links` 之前静默丢掉，面板上什么都看不出，只有正文里悬停那条才提示「这篇笔记不存在了」。`NoteLinksOut` 加 `dangling`（id 列表），ribbon「链接」顶上红卡「N 条链接指向已经不在的笔记」+「改成纯文本」——`noteLinkRanges` 把那几条链接替换成标题文本，走 CM changes（可撤销）。
- 探针 `note:<id>:ribbon:links:unlink`。实拍前（亮）「1 条链接指向已经不在的笔记」+ 按钮，后（深）红卡消失、正文里剩「另见 早就删掉的那篇。」。后端 912（dangling 测试）/ 前端 107；README links 行补 dangling。
- 跟进：第一版红卡靠服务端 `/links` 的 `dangling`，而它读的是**落库的**正文——刚敲进去的坏链接要等自动保存 1.5 秒后的下一次重查才出现，探针模式（不落库）更是永远看不见，实拍两次都没卡。改成本地按正文算：`]\(note://id)` 里不在 `notes` 列表里的就是坏的（App 传 `knownIds`），服务端那个字段留给 API 用户。探针的插入也加了一次重试（编辑器 2.5 秒时可能还没挂上）。第三次实拍：前「1 条链接指向已经不在的笔记」+「改成纯文本」，后卡片消失。

## [433] 第 405 轮：局部图跟着落库刷（2026-09-13）

- 同一类坑再查一处：「这篇周围有什么」的图从服务端落库的正文算，之前跟着本地引用数重查——保存之前就查了一次，拿到的是上一版。改成跟着树行的 `updated_at`（自动保存落库后 `reloadTree(false)` 会刷）重查。前端 107 + 12 条 check 过；harness-framework 路由说明补 dangling / 两个清理动作。

## [434] 第 406 轮：链接面板的「链到的笔记」本地算（2026-09-13）

- 同一类的第三处：ribbon「链接」的链出列表从服务端落库的正文算，刚打完 `[[链接]]` 面板不会立刻出现它（要等 1.5 秒自动保存后的下一次重查）。链出去的现在本地按正文算（正文里的 `note://id` ∩ notes 列表，按出现顺序去重），链进来的还得问服务端、跟着这篇的 `updated_at` 重查；「引用」面板的 stale（改过没同步）也跟着 `updated_at` 重查，不然黄字要重开这篇才出现。实拍链接测试篇：链到的笔记 harness 测试（可删）。前端 107 + 12 条 check 过。

## [435] 巡检第 407 轮：剩下的三条债 + 大库量接口计时（2026-09-13）

- PROGRESS 还开着的：`GET /api/notes` 带全文（413 篇 584KB / 3ms，第 406 轮链出列表本地算又多了一处用它，先不动）、真正分库、实体没有类型。shot-perf 用户：tree 208KB / 6ms（413 行每行带 80 字 preview）、brief 24KB / 2ms、kb/tree 2KB。后端 912。没改代码。

## [436] 巡检第 408 轮：整表重拉的次数（2026-09-13）

- App.tsx 里 `reload()`（整表重拉 notes）23 处：新建 / 删除 / 恢复 / 导入 / 回顾存为笔记 / harness 收尾这些都会拉一次；保存那条早改成就地替换（第 191 轮）。413 篇 584KB 服务端 3ms，客户端解析几毫秒，先不动（PROGRESS 那条债照旧）。没改代码。

## [437] 第 409 轮：重打 dmg 前的全量门（2026-09-13）

- 后端 912 通过；前端 tsc + eslint + vitest 107 + 12 条 check / smoke 全过。

## [438] 第 410 轮：重打 dmg（2026-09-13）

- `npm run dist` → `out/MEMOKET NOTE-0.1.0-arm64.dmg`（183MB）。装好的包冒烟（terrence 身份）：就绪 3 秒、health ok、kb/tree 180 行、`/links` 带 `dangling` 字段（真库两篇带引用的笔记都没坏链接）。

## [439] 第 411 轮：历史版本列表跟着落库刷（2026-09-14）

- 同一类的第四处：ribbon「历史」只在换笔记时查一次，保存时服务端留的自动版本要重开这篇才看到。加 `updatedAt` prop（落库后 `current.updated_at` 变）重查列表，展开着的那一版不动。前端 107。

## [440] 巡检第 412 轮：三张核心屏（深色 1000×700）（2026-09-14）

- 知识库首页 / 编辑器 / 设置页在今天这一堆改动之后实拍：统计四格、近 12 个月、主题 / 实体 / 最近摄入 / 类型说话人 chip、ribbon 六个标签、设置四段都在，没有错位。后端 912 / 前端 107 + 12 条 check。没改代码。

## [441] 巡检第 413 轮：还有哪些面板是「只在换笔记时查一次」（2026-09-14）

- 扫了一遍按 noteId 拉服务端数据的面板：路径（keyed 树行，克隆 / 移动后会刷）、副本（走 note-remotes-changed 事件）、链入 / stale / 局部图 / 历史（这几轮改成跟 updated_at）都对了。规矩写进 harness-framework：本地能算的本地算，服务端从落库正文算的跟 updated_at 重查。没改代码。

## [442] 巡检第 414 轮：正式版闲置、备份翻天（2026-09-14）

- 正式版开着 20 小时：主进程 134MB、后端闲置 CPU 0%。过零点后 dev 实例启动留下了 notes-20260914.sqlite3（每天一份，留最近 7 日 + 3 个月）。没改代码。

## [443] 第 415 轮：笔记库 81% 是空页——启动时 VACUUM（2026-09-14）

- 昨天的备份 3.1MB，今天文件 10.1MB，`dbstat` 一算表里的数据只有 1.6MB：freelist 2103/2593 页（8.2MB，81%）——sqlite 删行不缩文件，历次清理 / 删测试数据攒出来的，备份是整文件拷、跟着一起胖。
- 启动清理多一步 `vacuum_if_bloated`：空页 ≥4MB 且 ≥25% 才 VACUUM（小库不动，大库只在真胖了才重写整个文件），打一行 `[startup] 笔记库 VACUUM：10.1MB → 1.9MB`。dev 库实跑就是这个数。测试 2 条（删 300 篇 6MB 后能缩、小库不动）。后端 914；README / harness-framework 补一句。

## [444] 巡检第 416 轮：VACUUM 之后再起一次、正式版的库（2026-09-14）

- dev 实例再起：没有 VACUUM 行（已经干净），启动无错。正式版的库 2.3MB、空页 366/578（1.4MB，63%）——比例够、绝对量不到 4MB 门槛不会动；2MB 的文件 VACUUM 是瞬间的事，门槛降到 1MB（比例 25% 不变）。

## [445] 巡检第 417 轮：数据目录里还有什么在胖（2026-09-14）

- `data/terrence/` 里除了 11MB 的 codebook.xml，还躺着三份 9 月 1 日重抽实验留下的 8.4MB 副本（`.pre-reextract-moved-aside` / `.bak-pre-reextract-…` / `codebook.bak-…`），25MB，不在任何保留策略里——是用户的数据目录，不替他删，记一笔等他点头。
- `data/assets/` 10 张图 12MB，6 张（6.6MB）没有任何笔记 / 历史版本 / 最近删除引用（探针拖图、删掉的笔记留下的），也没有清理机制。下一轮补一个带 7 天宽限的启动清理。

## [446] 第 418 轮：没人引用的图启动时清掉（2026-09-14）

- `sweep_orphan_assets`：资产目录里没有任何笔记 / 历史版本 / 最近删除引用、且超过 7 天的图删掉（刚贴进来还没自动保存的不能误删），启动打一行「清掉 N 张…」。dev 库那 6 张孤儿都是 1–6 天前的，这次没动，明天起会清。测试 2 条（老的孤儿删、新的 / 有引用的 / 在最近删除里的留；没目录不报错）。后端 916；harness-framework 清理列表补一句。

## [447] 第 419 轮：重打 dmg 前的全量门（2026-09-14）

- 后端 916 通过；前端 tsc + eslint + vitest 107 + 12 条 check / smoke 全过（第 412 轮后前端没再改）。

## [448] 第 420 轮：重打 dmg（2026-09-14）

- `npm run dist` → `out/MEMOKET NOTE-0.1.0-arm64.dmg`（183MB）。装好的包冒烟（terrence 身份）：就绪 3 秒、health ok、kb/tree 180 行；正式版日志里第一行就是「笔记库 VACUUM：2.3MB → 0.8MB（空页 1.4MB）」，之后才做当天备份（0.8MB 的干净文件）。

## [449] 巡检第 421 轮：知识库备份（2026-09-14）

- `data/backups/kb/` 34MB，9 个用户各一份（一周一份），里面有 writing-bench / quality-sample / editing-bench 这些评测用的 dev 用户——正式版只有真用户，不是问题。r41x 探针日志没有 client:error。没改代码。

## [450] 巡检第 422 轮：知识库备份的保留策略、台账头（2026-09-14）

- `maybe_backup_kb`：每用户每周一份 codebook.xml，留最近 2 份，用户目录没了的备份一起删——没问题。PROGRESS 的「最后更新」还停在 9 月 11 日，刷成当前轮次。没改代码。

## [451] 巡检第 423 轮：依赖体检（2026-09-14）

- `npm audit --omit=dev` 前端 / 桌面都是 0 个漏洞；`pip check` 没有坏依赖；`pip list --outdated` 只有 anyio / lxml / pydantic / pyinstaller / setuptools 的补丁级更新，不动。没改代码。

## [452] 第 424 轮：on_event → lifespan；前端依赖到 wanted 版本（2026-09-14）

- 后端唯一一处 `@app.on_event("startup")`（放闲置索引的后台循环）改成 lifespan：进去起任务，退出时 cancel 并等它收掉。每次起后端 / 跑测试刷的那条 DeprecationWarning 没了（8 → 6 条 warnings，剩下的是 httpx / swig 那两种）。后端 916。
- 前端 `npm update` 到 semver 允许的版本（@codemirror/state 6.7.4、view 6.43.11、react 19.3.0、tsx 4.23.13 等；major 的 eslint 10 / mermaid 12 / plugin-react 6 不动）：tsc + eslint + vitest 107 + 12 条 check 全过。

## [453] 巡检第 425 轮：lifespan 之后起一次 dev 实例、台账旧行（2026-09-14）

- dev 实例起停各一次：启动只有托管前端一行，退出「Shutting down」干净，没有 pending task 的告警。PROGRESS 那张 300 轮计划表里「116–125 特殊笔记 ⬜」早就做完了（第 211–235 轮），勾上。没改代码。

## [454] 巡检第 426 轮：.gitignore（2026-09-14）

- backend/data · frontend/dist · desktop/out · 两个 node_modules · .venv · backend/dist 都在忽略里，仓库 465 个文件里没有一个来自这些目录。没改代码。

## [455] 巡检第 427 轮：剩下的 6 条测试 warning、依赖更新后的编辑器（2026-09-14）

- 6 条 warning：starlette testclient 的 httpx 弃用提示（环境层面）、swig（pdf 库）、test_batch_ingest 读 pdf 的 4 条——都不在我们代码里。CodeMirror 6.43.11 之后 `find`（查找 / 替换条、高亮）实拍照旧；主包 1.86 → 1.82MB。没改代码。

## [456] 第 428–429 轮：重打 dmg 前的全量门（2026-09-14）

- 后端 916 通过（6 warnings）；前端 tsc + eslint + vitest 107 + 12 条 check / smoke 全过（依赖更新之后跑的）。

## [457] 第 430 轮：重打 dmg（2026-09-14）

- `npm run dist` → `out/MEMOKET NOTE-0.1.0-arm64.dmg`（183MB，前端依赖更新后的包）。装好的包冒烟（terrence 身份）：就绪 3 秒、health ok、kb/tree 180 行；正式版日志：上一实例「Shutting down」干净、新实例启动 VACUUM 行照旧。

## [458] 巡检第 431 轮：依赖更新后的 `/` 菜单（2026-09-14）

- `slash` 探针：行首 `/` 菜单（用 AI 写 / 智能插图 / 智能表格 / 图片转表格 / 数据可视化…）照常弹在光标下。r42x 探针日志 4 份，0 份有 client:error。没改代码。

## [459] 巡检第 432 轮：Electron / electron-builder 版本（2026-09-14）

- electron 44.3.0、electron-builder 26.15.3 都是最新。没改代码。

## [460] 巡检第 433 轮：文件夹 harness 真跑（110 秒截图）（2026-09-14）

- `plan-run:2f73294746f0`（Notes 文件夹）：起计划拆三段（硬件 / APP / 种子用户），建「📋 写作追踪」+ 三篇分段笔记；110 秒时两篇 done、第三篇 in_progress——跟随模式已切到第三篇，正文 801 字、两级小标题，右下角「🚀 Notes · 正在写：种子用户招募与市场反馈验证」浮条，面包屑「Notes / 种子用户…」，右栏记忆跟着浮现。lifespan / streamJoin / 依赖更新都没碰到这条链路。跑完 abandon 计划、删掉四篇新建的（含追踪页）并清出最近删除，树回到只有「公司汇报：」。没改代码。

## [461] 巡检第 434 轮：lifespan 下的闲置索引回收（2026-09-14）

- 正式版日志里有「[memory] 放掉了 3 项闲置索引」——后台循环在 lifespan 里照常跑。文档里没有 on_event 的残留。没改代码。

## [462] 巡检第 435 轮：依赖更新后的两个计时（2026-09-14）

- 3 万字笔记 switchTo→paint 3ms（CodeMirror 6.43.11）；shot-perf 413 篇库上 `/links`（链入靠 LIKE 扫正文）1.7ms。没改代码。

## [463] 巡检第 436 轮：全新用户第一屏（依赖更新后）、正式版日志（2026-09-14）

- 全新用户深色第一屏照旧三张卡 + 常用键；正式版今天的日志里 0 条 client:error。没改代码。

## [464] 第 437 轮：树行的 preview 只给没起名的笔记（2026-09-14）

- `/api/tree` 每行带 80 字 preview，前端只在标题是占位词（未命名那几种）时拿它当显示名，起了名的笔记 80 字白带。SQL 里 `CASE WHEN title IN (占位词) THEN substr(...) ELSE '' END`：shot-perf 413 篇树 208KB → 120KB，terrence 23 篇（12 篇没起名）9.6 → 9KB、显示名不变。后端 916。

## [465] 第 438–439 轮：树实拍 + 重打 dmg 前的全量门（2026-09-14）

- 树上 12 篇没起名的仍按正文首行显示（「plaud的优势分析」「创业一年回顾」…），起了名的照旧。后端 916 通过；前端 tsc + eslint + vitest 107 + 12 条 check / smoke 全过。

## [466] 第 440 轮：重打 dmg（2026-09-14）

- `npm run dist` → `out/MEMOKET NOTE-0.1.0-arm64.dmg`（183MB）。装好的包冒烟（terrence 身份）：就绪 3 秒、health ok、tree 23 行（14 篇没起名的带 preview，起了名的不带）。

## [467] 第 441 轮：图标选择器的方向键（2026-09-14）

- 选择器只能 Tab 一格格走。加：打开时焦点落在当前图标（没有就第一格），方向键在 8 列的格子里上下左右走，回车 / 空格选（原生 button）。探针 `icon-picker:keys`：右、右、下之后焦点在第 11 格「task」，日志实证。r43x / r44x 探针日志 0 份有 client:error。前端 107。

## [468] 第 442 轮：选择器关掉后焦点回到图标钮（2026-09-14）

- 之前 Esc / 选完之后焦点掉到 body，键盘用户得重新 Tab 到位。App 给标题行图标钮一个 ref，选择器 onClose / onPick 之后 focus 回去。探针 `icon-picker:esc`：Esc 之后 picker=closed、activeElement=title-icon-btn。前端 107。

## [469] 第 443 轮：弹层关掉后焦点回原位（2026-09-14）

- 确认框 / 文本输入 / 笔记选择器 / 快速查看 / 快捷键表关掉后焦点都掉到 body。新 `util/restoreFocus.useRestoreFocus`：挂载时记下 activeElement，卸载时它还在页面上就 focus 回去；五个弹层各一行。
- 探针 `plan-panel:abandon-esc` 的 esc 日志加 `active=`：第一版看到焦点回到了面板容器——程序 `click()` 不像真人鼠标那样把焦点给按钮，探针先 `focus()` 再 `click()` 才测得对；之后 Esc 关掉确认框，焦点回到「换个目标」。前端 107。

## [470] 第 444 轮：⌘K 关掉后焦点回编辑器（2026-09-14）

- ⌘K 面板一直挂着（open 状态切换），不能用挂载 / 卸载那套：open 变 true 时记下 activeElement，变 false 时 focus 回去。探针 `keypalette:esc`：从编辑器 ⌘K 打开、Esc 关掉，activeElement=cm-content。前端 107。

## [471] 第 445 轮：右键菜单 / 选中文本菜单 / `/` 输入框也还焦点（2026-09-14）

- 三个按打开 / 关闭挂载卸载的弹层补 `useRestoreFocus()`。脚本往组件里插那一行时两次插错位置（一次插进 props 类型块、一次插进旁边的纯函数 tidyMenu），tsc 和 rules-of-hooks 都当场报了——插到签名 `}) {` 之后才对。前端 107。

## [472] 巡检第 446 轮：加了还焦点之后的三个菜单（2026-09-14）

- `selection`（选中文本菜单「校验」…）、`tree-menu`（树右键：打开 / 新标签 / 快速查看 / 分屏 / 新建…）照常弹出；r44x 探针日志没有 client:error。后端 916 / 前端 107 + 12 条 check。没改代码。

## [473] 第 447 轮：图标钮的 aria（2026-09-14）

- 标题行图标钮补 `aria-label` / `aria-haspopup="dialog"` / `aria-expanded`（读屏能知道它会弹东西、现在开没开）。前端 107。

## [474] 巡检第 448 轮：标签条 / ribbon / 右栏的 role（2026-09-14）

- 三处标签条早就是 `tablist` / `tab` + `aria-selected` / `tabpanel`，不用补。没改代码。

## [475] 第 449 轮：重打 dmg 前的全量门（2026-09-14）

- 后端 916 通过；前端 tsc + eslint + vitest 107 + 12 条 check / smoke 全过。

## [476] 第 450 轮：重打 dmg（2026-09-14）

- `npm run dist` → `out/MEMOKET NOTE-0.1.0-arm64.dmg`（183MB）。装好的包冒烟（terrence 身份）：就绪 3 秒、health ok、kb/tree 180 行。

## [477] 巡检第 451 轮：正式版这次启动的清理行（2026-09-14）

- 第 450 轮的包起来：备份两行 + 托管前端，没有 VACUUM（上次已经缩过）也没有清图（正式版资产没有超过 7 天的孤儿）——清理只在该动的时候动。没改代码。

## [478] 第 452 轮：观测日志降到 info（2026-09-14）

- 前端 `clientLog` 只有 error / warn 两档，首屏耗时、大树计时、harness 开跑这些纯观测的都是 warn——扫 warn 找问题时全是它们。加 info 档：后端 `[client:info]` 走 log.info；四处观测改 info（harness-sync / 骨架失败仍是 warn）。探针实拍日志里首屏那行已是 `[client:info]`。后端 916 / 前端 107。

## [479] 第 453 轮：client-log 的测试与文档（2026-09-14）

- 之前 client-log 一条测试都没有。补 2 条：三档都打成 `[client:<level>]` 一行、stack 截 4000 字。harness-framework 的 client_log 一行补三档说明。后端 918 / 前端 107 + 12 条 check。

## [480] 第 454 轮：按 openapi 反查没被测试碰过的路径（2026-09-14）

- 104 个 `/api` 路径里 35 个没有任何测试碰过（页面逻辑在 pages 层测过，路由 + 响应模型这一层没有；另一些要模型 / 录音 / 第三方账号）。补 `test_kb_routers_smoke`：知识库树 / 展开 / 主题页 / 实体页 / 时间线 / 某一天 / 会议页 / 记忆主题 / 实体 / 主题实体连线 / 记忆时间线 11 个只读接口 200 + JSON、树里的节点、闭包展开、三个 404、树的展开 / 重排两个 PATCH（写错 payload 形状 422 当场抓住——`expanded` 要 note_id + parent_note_id，`reorder` 要 `order`）。前端唯一真的 `<img>`（图片嵌入）带 alt。后端 932。

## [481] 第 455 轮：再补五个只读接口的冒烟（2026-09-14）

- `/api/kb/clusters` `/api/kb/quality` `/api/kb/rebuild/pending` `/api/skills/scopes` `/api/import/apple/available` 进同一组参数化冒烟，都 200 + JSON。

## [482] 巡检第 456 轮：没被测试碰过的路径还剩 15 个（2026-09-14）

- 35 → 15：剩下的都是要模型（harness run / 块生成 / 重排版 / 图片转表格 / 来龙去脉 / 技能生成）、要录音（ingest audio / transcribe）、要第三方账号（飞书）或要真抽取（ingest text / batch / 同步）的；`skills/reorder` 是纯库操作，下一轮补。后端 937。

## [483] 第 457 轮：技能重排的测试（2026-09-14）

- `POST /api/skills/reorder` 之前没测试：建两个 skill、反过来 reorder、GET 列表按新顺序回来（顺序是配置——两个 skill 矛盾时以模型最后读到的为准）。没被测试碰过的路径剩 14 个，全是要模型 / 录音 / 第三方账号的。

## [484] 第 458 轮：前端 util 哪些没测试（2026-09-14）

- 13 个 util 里 4 个没测试：kbNoise（说话人伪实体正则）、ingestActive、notify、restoreFocus。kbNoise 是纯正则、跟后端 `is_speaker_tag` 同一条，补 2 条（正 / 反各一组）；另外三个是浏览器 API 包装，单测意义不大。前端 109。

## [485] 第 459 轮：重打 dmg 前的全量门（2026-09-14）

- 后端 938 通过；前端 tsc + eslint + vitest 109 + 12 条 check / smoke 全过。

## [486] 第 460 轮：重打 dmg（2026-09-14）

- `npm run dist` → `out/MEMOKET NOTE-0.1.0-arm64.dmg`（183MB）。装好的包冒烟（terrence 身份）：就绪 3 秒、health ok、kb/tree 180 行；往 `/api/client-log` 发一条 info，正式版日志里是 `[client:info] user=terrence smoke smoke`。
- 跟进（第 460 轮）：kbNoise 那两条测试第一次跑就红了——「speakers」「speaker phone」被当成说话人标签（`[a-z0-9]{1,2}` 允许两个字母、字母前也不要求分隔）。前后端同一条正则改成：字母标签必须带分隔（Speaker A / speaker_c），数字标签可以贴着（Speaker1 / 说话人2）。terrence 库 1223 个实体里前后都是 9 个标签、没有原来藏着现在露出来的。后端 950（who 正则 12 条参数化）/ 前端 109。提交时应该先看单测再提交——这次是提交之后才看到红，下不为例。

## [487] 巡检第 461 轮：其它「噪声」正则有没有测试（2026-09-14）

- 顺着第 460 轮那条查：元话语（scrub_meta_sentences_v / audit_voice_lines）在 test_grounding_check、粗体标点在 test_bold_punct、引用正则 / 链接正则 / 片段 / 截句读前端都有测试——说话人标签是唯一漏的，已补。r45x / r46x 探针日志没有 client:error；scratchpad 第 440 轮前的截图清掉。没改代码。

## [488] 巡检第 462 轮：对着北极星三条判据再过一遍（2026-09-14）

- 判据 1（一个按钮一个结果、禁用要说清为什么）：扫到 21 个带 disabled 却没 title 的按钮，busy 类（转圈可见）和当前页类不算，给 10 个「输入还空着 / 这台机器不可用 / 网页版不能选主题 / 已经是默认图标」补 title。判据 2（不离开页面）：右栏常驻、行内出处、⌘K Esc 回原位（第 444 轮实证）都在。判据 3（计划 / 判据 / 丢弃都看得见、能逐轮停）：ribbon 骨架、计划标签的轮次卡（dropped / scrub）、逐轮暂停都在。前端 109。

## [489] 巡检第 463 轮：北极星 13 个痛点逐条核对（2026-09-14）

- 1 按钮化 / 2 取材料 / 3 智能插图 / 4 数据可视化 / 5 智能排版 / 7 校验 / 8 改动分层 / 9 音频入库 / 10 飞书 Notion 导入 / 11–12 右栏记忆 + 行内出处 / 13 定期回顾 + harness 都有落点；**6「做成 PPT」没有**——只有 sandbox 能跑第三方 skill 脚本生成 .pptx，没有内置按钮。记进 PROGRESS 的债，不擅自开工。r46x 探针日志 0 份有 client:error。没改代码。

## [490] 巡检第 464 轮：正文右上角四个浮动按钮的说明（2026-09-14）

- 续写 / 智能续写 / 更多动作 / 录音四个按钮都带 title（禁用时也说清）。没改代码。

## [491] 巡检第 465 轮：几个大小（2026-09-14）

- App.tsx 3272 行、probes.ts 599 行、api.ts 1339 行；后端 `import app.main`（含启动清理）0.2–0.3 秒。App.tsx 还在长，下一轮把几个纯函数（虚拟页命名 / 预览行 / 认不认识的虚拟 id）挪出去带上测试。没改代码。

## [492] 第 466 轮：虚拟页的几条纯规则搬出 App.tsx（2026-09-14）

- `VIRTUAL_LABELS` / 认不认识的虚拟 id（原来两处各写一遍正则）/ `factsLabel` / `previewLine` 搬到 `util/virtual.ts`，App.tsx 3272 → 3249 行；新测试 virtual.test.ts 3 条（带查询串的事实表命名、认识 / 不认识的 id、预览行剥记号）。前端 112。

## [493] 巡检第 467 轮：搬完之后的实拍（2026-09-14）

- `open:kb:facts?topic=work`：标签「事实…」、面包屑「知识库 / 主题 / work / 事实表 · work」照旧；dev 实例起停无错。没改代码。

## [494] 第 468–469 轮：重打 dmg 前的全量门（2026-09-14）

- 后端 950 通过；前端 tsc + eslint + vitest 112 + 12 条 check / smoke 全过。

## [495] 第 470 轮：重打 dmg（2026-09-14）

- `npm run dist` → `out/MEMOKET NOTE-0.1.0-arm64.dmg`（183MB）。装好的包冒烟（terrence 身份）：就绪 3 秒、health ok、kb/tree 180 行、1220 个实体里按新正则 9 个说话人标签（跟 dev 一致）。

## [496] 第 471 轮：面包屑搬出 App.tsx（2026-09-14）

- 状态栏面包屑的父链推导（真笔记沿树往上 / 尾巴行进来的事实表末尾改页面名 / 懒加载虚拟页按 id 形状拼「知识库 / 实体 / 叶子」/ app:* 不冠知识库 / 知识库根不重复）搬到 `util/crumbs.ts` 的 `buildCrumbs(id, rows, tabTitle, isVirtual)`，父分类表抽成 `virtualParentOf`；App.tsx 3249 → 3228 行。新测试 crumbs.test.ts 4 条。实拍 `open:kb:facts?topic=work` 面包屑「知识库 / 主题 / work / 事实表 · work」照旧。前端 116。

## [497] 第 472 轮：引用 id 正则前端三份合一（2026-09-14）

- 之前 App.tsx 的 citedIds、factCite.ts 的高亮、wordCount.ts 的 `CITATION_RE` 各抄一份（写法还不完全一样：`[\w-]*` 对 `[A-Za-z0-9_-]*`，语义相同但改一处容易漏另两处）。现在 `util/wordCount.ts` 是唯一源：`CITE_RE_SOURCE` / `CITATION_RE` / `citedFactIds()`，factCite 从这里拿，App 只剩一行 `useMemo(() => citedFactIds(content))`；App.tsx 3228 → 3222 行。后端三处（store / checks/citations / prompts/fragments）照旧手动同步，注释改成「一处源，四处同步」。新测试 2 条（去重按首次顺序、数字 / 12 位 hex 两种 unit、普通方括号不算）。实拍 `open:5f65df10cad6`：引用角标 31、树上 ◆31、正文高亮照旧；`npm test` 全过，前端 118。

## [498] 第 473 轮：笔记链接正则前端也合成一份（2026-09-14）

- `[标题](note://id)` 的正则散在 App.tsx 角标、NoteLinksPanel 三处、wordCount.ts `noteLinkRanges` 四份。现在 `util/wordCount.ts` 是唯一源：`NOTE_LINK_RE` / `linkedNoteIds()`（去重按首次顺序），面板的「链出去」「链到的不在了」和重查 key 都用它，App 的角标改成 `linkedNoteIds(content).length`——顺手把角标口径从「链接出现次数」改成「链到几篇」，跟面板列表的数对上（同一篇链两次原来角标 2、列表 1）。新测试 1 条。实拍 `note:bb215ab441a2:ribbon:links`：角标 1、「链到的笔记 · 1」。前端 119。探针备忘：ribbon 标签用 id（cites/links/history/paths/info），不是中文名。

## [499] 第 474 轮：前后端引用 / 链接正则一致性做成 check 脚本，顺手抓到自己上一轮的回归（2026-09-14）

- 新 `scripts/check-regex-parity.mts`（挂进 `npm test`，第 13 条 check）：4 组样本同时喂前端 `citedFactIds` / `linkedNoteIds` / `^CITE_RE_SOURCE` 和后端 `store._CITE` / `checks/citations.CITE` / `store._NOTE_LINK` / `prompts/fragments._FACT_ID`，比的是行为不是字面（Python / JS 写法允许不同）。后端 venv 不在就跳过。
- 第一次跑就红了一条：第 473 轮我让 `linkedNoteIds` 用带标题的完整正则，标题里夹换行的 `[标题\n换行](note://id)` 后端（只看 `](note://id)` 尾巴）算链接、前端不算——面板列表和反链会对不上。改回跟后端同口径的 `NOTE_LINK_TAIL_RE`，`noteLinkRanges`（要拿标题替换）照旧用完整版。vitest 补这条样本。前端 119 + 13 条 check 全过。

## [500] 第 475 轮：三栏 + 分屏的宽度裁决搬出 App.tsx（2026-09-14）

- 「窗口窄时正文优先」那段算术（左栏封顶 30%、分屏封顶 45%、中栏不够 520 先收右栏再收左栏、专注模式两边都收）搬到 `util/layoutPanes.ts` 的 `layoutPanes({winW, panes, splitW, focusMode})`，常量 `LAUNCHER_W` / `MIN_EDITOR_W` 挂出来；App.tsx 3222 → 3215 行。新测试 4 条，样本就是当年实拍的两个窗口：900×600（右栏收、左栏留、800 宽时左栏压到 240）、1000×700 开分屏（分屏 450、两侧都收）。实拍 `--win=900x600` 和 `split:same --win=1000x700` 跟搬之前一样。前端 123。

## [501] 第 476 轮：没存上的草稿——读写 + 放不放回的判断搬出 App.tsx（2026-09-14）

- `util/draft.ts`：`readDraft` / `writeDraft` / `clearDraft`（localStorage 键只在这一个文件里出现，探针 `draft:` 也改用 `writeDraft`）+ 纯判断 `resolveDraft(note, draft, isProbe)` → 用哪份标题正文、要不要提示、要不要清草稿。open() 里那 15 行变 5 行；App.tsx 3215 → 3204 行。新测试 3 条（比库里新才放回、题目空用库里的、探针模式放回也清、旧草稿清掉、没草稿什么都不动）。实拍 `draft:bb215ab441a2`：切走切回后末尾多出「（这一段是上次没存上的草稿）」照旧。前端 126。

## [502] 第 477 轮：「重新打开刚关的」回原位（2026-09-14）

- 之前 ⌘⇧T 把刚关的标签追加到最右边；关错中间一个再找回，顺序就乱了。Chrome 的 ⌘⇧T 是回原位，改成一样：关闭栈里连原索引一起记（`{tab, at}`，关闭 / 关闭其他 / 关闭右侧 / 关闭全部四条路都记），找回时插到 `min(at, 当前长度)`。
- 新探针 `tabs:reopen`：连开三篇 → 中键关第 2 个 → ⌘⇧T，三个时刻把标签行标题写进 client log。实拍日志：before `harnes | 链接测试 | work | …`，closed `harnes | work | …`，reopened `harnes | 链接测试 | work | …`——回到第 2 位。截图看不出来（标签行横向滚了），靠日志。
- 顺带看到：dev 用户 terrence 攒了 50 个标签（重启都恢复），标签行早就横向滚动、看不到全貌。Trilium 也不封顶；先记着，下一轮想想要不要在标签行右键加「关闭左侧」或者标签栏溢出提示。

## [503] 第 478 轮：标签装不下时多一个 ▾ 列出全部（2026-09-14）

- 50 个标签靠 ◀ ▶ 一格 210px 地滚是找不到的（Chrome 有标签搜索、VS Code 有「打开的编辑器」）。TabBar 溢出时在 ▶ 右边多一个 ▾（title「列出全部 N 个标签」），弹 ContextMenu：表头「打开的标签 · N」、每项带笔记图标（虚拟页 #）、当前那个右侧标「当前」、点了切过去；末尾「关闭其他」。
- 连带两处修：ContextMenu 比窗口还高时原来把头顶到窗口外（第一版实拍表头看不见）——`y` 也钳到 ≥ 5px，`.context-menu` 加 `max-height: calc(100vh - 10px); overflow-y: auto`，里面滚；菜单里的标签名硬截 36 字加省略号（不用 `clipTitle`，那个按句读截，「产品计划会：APP/硬件…」会只剩前半）。
- 新探针 `tabs:list`。实拍：表头在、「当前」在、长标题带 …、列表内滚。

## [504] 第 479 轮：重打 dmg 前的全量门（2026-09-14）

- 后端 950 通过；前端 tsc + eslint + vitest 126 + 13 条 check / smoke 全过（含新加的 check-regex-parity）。

## [505] 第 480 轮：重打 dmg（2026-09-14）

- `npm run dist` → `out/MEMOKET NOTE-0.1.0-arm64.dmg`（183MB）。装好的包冒烟（terrence 身份）：就绪 2 秒、tree / kb/tree 正常。正式版 userData 是独立库（只 1 篇笔记），拿 dev 库的 id 查链接接口是 404，属正常——冒烟别混用两边的 id。PROGRESS 补 471–480 行。

## [506] 第 481 轮：翻正式版日志——后端输出按行打标（2026-09-14）

- 翻 `~/Library/Logs/memoket-note-desktop/memoket-note.log`：117 行裸的「INFO:     Waiting for application startup.」没有 `[backend]` 前缀——子进程一个 data 块常带好几行，原来只给块首加前缀。`desktop/src/backend.ts` 抽出 `lineTagger(emit)`：按行切、半行留到下一块再拼、流结束 flush；stdout / stderr 各一个。node 直接验过拼接（跨块半行、\r\n）；dev 实例实拍日志裸 INFO 0 行、带前缀 21 行。正式版下次（第 490 轮）重打时带上。

## [507] 第 482 轮：内部滚动的菜单里 ↑↓ 高亮跟着滚（2026-09-14）

- 第 478 轮让 ContextMenu 超高时内部滚，键盘 ↑↓ 走到看不见的项时不会跟着滚（scrollIntoView 只给了树和标签行）。补一个 `hi` 变化时 `.context-menu-item.hi` `scrollIntoView({block:'nearest'})`。探针 `tabs:list:keys`（按 40 下 ↓）；实拍高亮在第 40 项「实体 · 其他」、列表滚到了它。探针备忘：一个 tick 里连发 40 个 keydown 没用——菜单的 keydown 闭包里 `hi` 是同一个值，要隔 30ms 发。

## [508] 第 483 轮：横扫「键盘高亮要滚进视野」（2026-09-14）

- 有 ↑↓ 的列表六个：树 / 标签行 / ContextMenu 有 scrollIntoView，图标挑选器走 focus() 浏览器自己滚，斜杠菜单 8 项 320px 放得下；**⌘K 和「移动到…」选择器没有**。两个都补 `.palette-item.active` `scrollIntoView({block:'nearest'})`。
- 「移动到…」还有一处更糟：列表容器叫 `.palette-list`，CSS 里根本没这个类——外层 `.palette` 是 `max-height:60vh; overflow:hidden`，笔记多的人超出 60vh 的那截**根本滚不到**（200 项上限形同虚设）。改用现成的 `.palette-results`（overflow-y:auto）。
- 探针 `palette:keys`（12 下 ↓）/ `picker:keys`（25 下 ↓）。实拍：⌘K 高亮走到最底「设置」列表跟着滚；选择器高亮在第 25 项「日记」、滚动条在下半截。

## [509] 第 484 轮：「用了的类名 CSS 里得有」做成 check（2026-09-14）

- 上一轮 `.palette-list` 那种错 tsc / eslint / vitest 都不管。写 `scripts/check-css-classes.mts`（第 14 条 check）：扫 TSX / TS 的 className（字面量 / 模板 / `+ ' xxx'` 拼接 / `el.className = `），跟所有 css 里的 `.xxx` 对；`bx*` / `cm-*` / `mm-*` 前缀放过；纯锚点类（子元素有样式、自己不需要）进 HOOK_ONLY 名单，名单里的类有了定义或没人用了也报。首跑 223 个类名全有定义，名单 6 个（app-logo / export-back / export-back-result / mini-bars / tab-list / tl-month）——逐个看过都是容器或探针锚点，不是漏样式。

## [510] 第 485 轮：死样式反向扫（2026-09-14）

- check-css-classes 加反向：CSS 里定义了、代码里一个字都没提到的类算死样式（`drop-*` 这种代码里拼出来的按前缀放过）。首跑抓到 5 个早就没人用的：`.note-actions`（两处，旧的 AI 动作行）、`.sidebar-footer`（旧侧栏底部工具条）、`.toolbar-secondary`、`.right-pane-heading`、`.right-pane-ambient`，连同它们的注释一起删（37 行）。实拍 `open:5f65df10cad6` 布局不变。
- 教训：用「上一个空行」当删除起点会把前面的规则一起切掉（第一版把 `.app` 和 `button {}` 也删了，靠打印片段发现、git checkout 重来）——删 CSS 块要用块自己的注释首行做起点。

## [511] 第 486 轮：前后端死代码扫（2026-09-14）

- 前端「export 了却没别的文件 import」53 个：全是类型导出、本文件内在用、或 scripts/ 里的 check 在用（roundDiff / mermaid 的几个），没有真正的死代码，不做成 check（噪声大）。
- 后端「def 了没人调」10 个：`kb/search._norm` 真死（连 `_WS` 都另有用处）——删；`store.get_skeleton` / `store.citation_counts` 只有测试在用（树接口一趟 SQL 早就带 `cite_count`）——删，测试改走 `get_note` / `tree`；`chart_gap` / `dropped_keys` / `is_virtual` / registry 的三个 `_reset_for_tests` 类是测试用的公共小工具，留着。后端 950 照旧。

## [512] 巡检第 487 轮：标签列表的深色 / 窄窗实拍（2026-09-14）

- `tabs:list --dark`：菜单底色、表头、「当前」提示都跟深色令牌走；`--win=900x600`：菜单贴窗口右缘、高度封顶到窗口、内部滚。没改代码。

## [513] 第 488 轮：指向不存在事实的标签给个出口（2026-09-14）

- 标签列表里那个「事实 terrence-9999-FFF」是探针留下的：真笔记没了标签会在 reload 时收掉，虚拟标签只按 id 形状认，事实 404 的标签会一直恢复回来、点开是「找不到这条事实」就完了。不在启动时逐个探（50 个标签 50 个请求），改成页面自己给出口：「找不到这条事实」下面加「收掉这个标签」chip，发 `virtual-gone` 事件，App 关掉那个标签（走 tabsRef / closeTabRef 拿最新闭包）。
- 探针 `badfact:close`：打开坏事实 → 点 chip → 日志 `tabs=0 status=知识库 / 时间线`（标签没了、切到旁边那个）。实拍页面带 chip。

## [514] 第 489 轮：重打 dmg 前的全量门（2026-09-14）

- 后端 950 通过；前端 tsc + eslint + vitest 126 + 14 条 check / smoke 全过（含 check-regex-parity、check-css-classes）。

## [515] 第 490 轮：重打 dmg（2026-09-14）

- `npm run dist` → `out/MEMOKET NOTE-0.1.0-arm64.dmg`（183MB）。装好的包冒烟（terrence 身份）：就绪 3 秒、tree / kb/tree 正常；正式版日志新增 8 行里裸 `INFO:` 0 行、带 `[backend]` 7 行——第 481 轮的按行打标在正式版生效。PROGRESS 补 481–490 行。

## [516] 第 491 轮：标签列表里三个「会议记录」是怎么来的（2026-09-14）

- 查了：真实数据 2429 场会议全有标题，`part_labels` 也从不给空——那三个是旧版标签恢复时的兜底名，页面一打开就会被 `virtual-title` 刷成真标题，不是 bug。顺手把无标题材料的兜底从裸 id「note-674aa9a1b4b7-0」改成「会议记录 · 日期」（连日期都没有才退回 id 去掉块号），测试加两条。

## [517] 第 492 轮：真跑回归三次，抓到 scrub 的一个字（2026-09-14）

- 第一次真跑（`harness:5f65df10cad6`）：第 2、3 轮各差 1 字，都是文末多一个 `\n`——服务端 scrub 之后整篇 strip，本地没有。轮末对齐时只差尾部空白不再记 warn。
- 第二次真跑：第 2 轮差 1 字在正文中间——「。 [terrence-1833-10F1]」本地有空格、服务端没有。根因：服务端 `scrub_meta_sentences_v` 删元话语句子时，命中的段按「。！？」切句、**其余每句 strip 后无缝拼回**；本地 onScrub 只把那句从字符串里抠掉。照服务端规则写了 `editor/streamJoin.applyScrub`（表格 / 代码块 / 标题段不动；对不上整句就不动；发来的句子前面带引用 id 也算在句里）。`scripts/check-scrub-parity.mts`（第 15 条 check）拿 5 组样本喂 Python 的 `scrub_meta_sentences_v` 得到 (正文, 删掉的句子)，客户端逐句 `applyScrub`，两边全等。vitest 2 条。
- 第三次真跑：harness-sync 0 条 warn。三次跑完都从 `r492-before.json` 把正文 / 骨架 / updated_at 还原、删掉新增的历史版本（17 条不变）。harness-framework 同步。前端 128 + 15 check。

## [518] 第 493 轮：轮内镜像的另两条规则也对拍（2026-09-14）

- `scripts/check-stream-parity.mts`（第 16 条 check）：4 组样本喂 `tidy_blank_lines` vs `tidyBlankLines`；5 组「底稿 + 插入点 + 续写文字切成 1–5 块」——本地 `prepareInsert` 腾位置再 `insertStreamed` 逐块拼，服务端 `outline.insert_into` 一次插——尾部空白除外全等。首跑全绿。
- `prepareInsert` 从 App.tsx 的 onInsertAt 里搬出来，顺手补上服务端有、本地一直没有的一条：紧跟插入点后面的孤立标点（「…成果。**，而要按**…」那种残留）去掉。vitest 1 条。harness-framework 同步。前端 129 + 16 check。

## [519] 第 494 轮：README 的测试段跟上（2026-09-14）

- README 还写着「649 条 / 11 个检查脚本 / 十个」：改成 950 条、16 个，补一段说明四个前后端对拍脚本各盯什么、为什么两份实现是有意的。合约测试的 docstring 不再写死数字。

## [520] 第 495 轮：文件夹 harness 真跑回归（2026-09-14）

- `plan-run:2f73294746f0`（110 秒）：计划 done、写出 3 篇 + 「📋 写作追踪」，harness-sync 0 条、client 无 error、后端无 traceback。跑完按 t0 把这 4 篇（notes / branches / 引用 / 历史 / 回收站）和这份计划（writing_plans / writing_sections）删干净，文件夹下回到 1 篇。没改代码。

## [521] 第 496 轮：拿 ruff 的 F 类规则扫一遍后端（2026-09-14）

- 仓库自己有 `test_no_undefined_names`（pyflakes 未定义名 / 残留 import）。用 ruff `--select F,E9,B006,B008` 再扫 app + tests：真问题只有测试里两个没用的局部变量（F841）——删；`F811 mem` 是 pytest 夹具的 import 再当参数，误报；`B008 File(...)` 是 FastAPI 惯用法。不引入 ruff 进 CI（pyflakes 那条测试已经把 F 类里要紧的盖住了，再加一个工具链不值）。

## [522] 第 497 轮：没人接的 Promise（2026-09-14）

- 试开 typescript-eslint 带类型信息的 `no-floating-promises`（`ignoreVoid`）：27 处。多数是 fire-and-forget 的 `reload()` / `save()` / `refresh()`（自己 catch 过）——标 `void` 表示有意不接；4 处 `.then()` 没 `.catch`：写作计划面板读计划、skill 页读列表（接口一失败就永远「加载中」）、启动时 `reload().then(open…)`、mermaid widget 的渲染链（意外抛出就停在「渲染图表…」）——补 catch（toast / 错误态）。`provider-changed` 事件监听原来直接挂的 async 函数，改成同步包一层。
- 规则进 `eslint.config.mjs`（只对 src，带 `project: tsconfig.json`），eslint 全量仍 2.4 秒；探针文件验证过规则真在抓。`no-misused-promises` 没开（4 处都是事件回调惯用法）。前端 129 全绿，skills / plan-panel 实拍照旧。

## [523] 第 498 轮：桌面壳的三处没人接的 Promise（2026-09-14）

- 拿前端那套 typed eslint 临时扫 desktop/src：3 处。`shell.openExternal` 标 void；`win.loadURL(url)` 失败原来是一片白什么都不说——加 catch 记日志；`app.whenReady().then(boot)` 里 boot 没兜住的意外原来是 unhandled rejection（进程活着、窗口没有）——加 catch：记日志 + 错误框带日志路径 + 退出。desktop 没有 eslint 工程配置，不为 3 处新增一套工具链，记在这里下次重扫。dev 实例实拍启动照旧。

## [524] 第 499 轮：重打 dmg 前的全量门（2026-09-14）

- 后端 950 通过；前端 tsc + eslint（含 no-floating-promises）+ vitest 129 + 16 条 check / smoke 全过。

## [525] 第 500 轮：重打 dmg（2026-09-14）

- `npm run dist` → `out/MEMOKET NOTE-0.1.0-arm64.dmg`（183MB）。装好的包冒烟（terrence 身份）：就绪 3 秒、tree / kb/tree（180 行）正常、`/api/nope` 回 404 不回 index.html；正式版日志新增 22 行裸 `INFO:` 0 行。PROGRESS 补 491–500 行。半程（500/1000）到此。

## [526] 巡检第 501 轮 + 第 502 轮：全新用户实拍；⌘K 按名字找开着的标签（2026-09-14）

- 501：`fresh-r501 blank:write`：空库首篇、右栏空状态、标签行只有一个标签没有 ▾，无错。跑完清掉 fresh 用户。没改代码。
- 502：50 个标签靠 ▾ 列表翻也累，⌘K 打字时先列「打开的标签」（命中最多 6 个，笔记组里不再重复；知识库 / app 页用 # 图标，右侧标「标签」）；同名标签（三个「创业一年回顾」）跟笔记组一样给一截正文（`previewLine`）。App 传 `tabs` + `onOpenTab → activateTab`。实拍 `palette:创业`：三个同名标签各带不同的首行。

## [527] 第 503 轮：标签列表里同名的也带一截正文；深色实拍（2026-09-14）

- ▾ 标签列表：同名标签（「创业一年回顾」×3、「公司汇报：」×2）标签名后接「 · 正文首行」（24 字截断），跟 ⌘K 的标签组一个做法。`palette:创业 --dark` 实拍：高亮、片段、右侧「标签」在深色下都对。虚拟页的同名（三个「会议记录」旧标签）没正文可给，打开一次就会刷成真名。

## [528] 第 504 轮：应用菜单加「标签」（2026-09-14）

- 桌面菜单栏多一个「标签」：新建 ⌘T / 关闭 ⌘W / 重新打开刚关的 ⇧⌘T / 下一个 ⌃Tab / 上一个 ⌃⇧Tab / 列出全部标签 / 关闭其他（浏览器和 Trilium 都有这一栏；快捷键仍由渲染层接，菜单只显示）。链路：菜单 → `menu` 通道 `tab:*` → main.tsx 转成 window `tab-action` → App 的 `tabActionRef`（每次渲染刷最新闭包）。「列出全部标签」不再依赖标签行装不装得下——不带鼠标也有入口。
- 探针 `tabaction:<动作>`：`next` 日志 active 从 harness 变成 链接测试；`list` 日志 menu=true、实拍列表在右上角展开。快捷键一览的 ⌘K 一行补「开着的标签」。

## [529] 巡检第 505 轮：README 桌面版一行补标签相关（2026-09-14）

- README 功能表「桌面版」一行补上第 477–504 轮加的：⇧⌘T 回原位、▾ 列表、⌘K 找开着的标签、菜单栏「标签」。没改代码。

## [530] 第 506 轮：可点的 div 键盘也能按（2026-09-14）

- 横扫：只带 onClick 的 div / span 18 处，去掉遮罩层（palette-backdrop 那种）剩 8 处真交互：侧栏笔记项 + 置顶 / 删除、右下角「harness 还在跑」卡片、相关记忆的事实卡 / 事实行、冲突收件箱的事实、事实表的 `.fact-text`、时间线的月 / 日行。图标按钮 0 处漏 title。
- `util/clickable.ts`：`{...clickable(handler)}` 给 role=button + tabIndex + Enter / 空格；时间线的行再带 aria-expanded；`[role="button"]:focus-visible` 焦点环。探针 `factkeys`：事实表第一条聚焦按 Enter → 打开了那条（日志 active=那条事实的前几个字）。check-css-classes 照旧全绿。

## [531] 第 507 轮：a11y 扫描做成 check + toast 的「撤销」变真按钮（2026-09-14）

- `scripts/check-a11y.mts`（第 17 条 check）：带 onClick 的 div / span 必须有 role 或 onKeyDown（遮罩 / modal 容器、树的展开三角、toast 正文点一下关掉不算）；只有一个 `<i class="bx …">` 的按钮必须有 title / aria-label。第一版把标签在 `onClick={() =>` 的箭头处截断了，改成花括号外数 `>`。
- 抓到一处：toast 里的动作（「撤销」删除那种 5 秒窗口）是没有 href 的 `<a>`，Tab 走不到——改成 `<button class="linklike">`。上一轮加的 `[role=button]:focus-visible` 撤掉——全局 `:focus-visible` 早就有一条（`--focus-ring`）。焦点环没法用探针实拍：程序 `focus()` 不触发 `:focus-visible`。
- 查了下第 506 轮以为没人用的 `renderNoteItem`：`visibleNotes.map(renderNoteItem)` 在用（grep 带括号漏了），差点删掉，git checkout 回来。

## [532] 第 508 轮：toast「撤销」实拍补色（2026-09-14）

- 探针 `toast:action`（发一条带「撤销」的 toast 并把焦点给按钮）。第一拍「撤销」是黑字：`.linklike` 的 `color: inherit` 写在 `.link` 后面，把蓝色盖了。加 `.linklike.link { color: var(--accent) }`，第二拍蓝了。合约测试（每个 check 脚本都被 npm test 跑到）6 条过。

## [533] 第 509 轮：重打 dmg 前的全量门（2026-09-14）

- 后端 950 通过；前端 tsc + eslint + vitest 129 + 17 条 check / smoke 全过（含 check-a11y）。

## [534] 第 510 轮：重打 dmg（2026-09-14）

- `npm run dist` → `out/MEMOKET NOTE-0.1.0-arm64.dmg`（183MB）。装好的包冒烟（terrence 身份）：就绪 3 秒、tree / kb/tree（180 行）正常；正式版日志新增 8 行裸 `INFO:` 0 行。PROGRESS 补 501–510 行。
