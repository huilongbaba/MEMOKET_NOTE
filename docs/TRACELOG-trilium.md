
## [678] 第 662 轮：「随时可撤」那句话得有地方兑现（2026-09-14）

- 提交前那次全量有一条红的（`test_前端没有导出了谁都不用的东西`：`undoEntityMerge` 导出了没人用），我没拦住就提交了。查出来**不只是 lint**：收件箱上写着「合并只影响显示和检索，知识库本身不动，**随时可撤**」，而界面上根本没有撤的地方。**跟这一段一直在修的是同一类：界面在承诺一件代码没提供的事。**
- 补上：判完那一下的提示条带「撤销」（7 秒），撤了就把这一对放回列表最前面——用户刚才就在看它。连着点一屏，点错一下是常事，而判完这一对就从列表里消失了；没有这个撤销，那句话就是假的。
- 教训记一笔：**提交前那次门禁的输出要真读**，不是看一眼 exit code。这一轮是靠 `grep FAILED` 补救的。

## [679] 第 663 轮：真跑无限续写读产出——占位判据两处误伤（2026-09-14）

真跑一次文件夹级 harness（6 个分段 / 19 轮 / 9 分 51 秒，跑完按 t0 清了 58 行）。

- **第 647 轮加的那条规矩第一次在真跑里触发**：第 6 节第 2 轮 `gave_up: ["coherence"]` → 「repairing these did not move the score; writing instead」→ 第 3 轮回去写。按设计工作。
- **占位判据误伤一：围栏里的不算。** 模型画了张 mermaid，节点写着 `B[EVT样品实际时间待确认]`，判据当成占位句拦下来、**连拦两轮**。可那句话不是「我还没写」，是**这个项目的状态就是「实际时间待确认」**——而这篇笔记的目标恰恰是「计划时间、决策状态、验证证据、实际结果，缺哪一项就写待核实」。图里的节点名是内容。跟第 607 轮「手写最简流程图放行」同一个道理：**判据不能把自己看不懂的东西一律当成毛病。**
- **占位判据误伤二：引号里的占位词是在「提到」它，不是在用它。** 产出里有「缺失项明确标为「待补」，而不是用阶段名称代替事实」——那是在陈述一条规矩。而原来这件事是**碰运气**的：`“待补”` 不报（后面的 `”` 不在终止符表里），`「待补」` 就报（`」` 在表里）——同一个意思、两种引号、两种结果。短引号（≤8 字）里的先摘掉再判；引号长到装得下一句话时照旧算。
- 顺带补了个守卫：**幻灯片那一篇上不给无限续写 / 智能续写**（实拍撞到——探针在幻灯片那篇上打开了写作计划弹层）。往一份 Marp 里塞散文分段既破坏 `---` 分页、也回不到原笔记。
- 产出本身是好的：带引用编号、有 mermaid、具体到项目名和日期、缺证据的地方老实写「当前材料仍没有给出」。
- 后端 1086 / 前端 200 + 22 条 check。

## [680] 第 664 轮：容器查询的阈值要对着「真能有多窄」挑（2026-09-15）

- 批量实拍编辑器里三个高频交互（查找条 / 选区右键菜单 / `/` 菜单）——**都干净**。右边那个橙点是「边缘记忆」的关系标记，设计如此。顺带确认第 663 轮那个守卫生效了：幻灯片那篇上「智能续写」是灰的。
- **查出一件结构性的事**：桌面壳 `minWidth: 900`（`desktop/src/main.ts`），减去启动栏 80、右栏 340（左栏收起时右栏不收），**正文栏在桌面版的下限是 480px**。而我给屏幕活动页写的 `@container (max-width: 380px)` 是按「感觉」定的——**桌面版一次都不会触发**。两条改成 480。
- `.fb-label` 那条**故意留在 380**：第 620 轮它原来是 600，默认三栏下按钮那几个字从没露过面（用户实拍）。走不到正是想要的效果。加了注释说明这是有意的，不是没对过阈值。
- 阈值这件事的通用形态：**没量过可达范围就挑的断点，要么永远不触发、要么老在不该触发的时候触发**——这两种我在这个项目里都撞过了。范围的算法连同那行 `container-type` 一起写进注释。
- 顺手把测试时真截下来的屏清了（dev 的 journey 目录 2MB）：**用户没要求记录，那些图不该留着。**
- 前端 200 + 22 条 check。

## [681] 第 665 轮：查「代码里有、用户却够不着」——翻出六条接口，接出来一条最要紧的（2026-09-15）

- 上一轮那个视角继续用：枚举全部 132 条 API，拿路径去前端源码里找调用方。**六条一次都没被调过**：`kb/coverage`、`kb/quality`、`kb/quality/judged`、`kb/rebuild`、`kb/rebuild/pending`、`memory/timeline`。全是建好了、测过了、有注释的诊断能力——**只是用户够不着**。（写这个审计本身也踩了一下：FastAPI 新版的 `app.routes` 里是 `_IncludedRouter`，要走 `original_router` 才展得开，第一版只数出 2 条。）
- **`kb/quality` 那条值得当场接出来**。全库扫一遍只要 70ms（`shapes` 不看原文、只看事实本身的形状），给的是：
  - **20406 条里 3256 条（16%）形状上就用不上**——太短 1699 · 只有说话人+短语 668 · 是提问不是事实 598 · 口语填充 / ASR 噪声 291
  - 9521 条（46.7%）带说话人标签
  - 数字对账：2656 条带数字的里只有 8 条（0.3%）的数字原文里查不到——这一项很好
- 首页多一格「**84% 能用的事实**」，明细在悬停里。**为什么值得占一格**：16% 用不上直接解释了「为什么召回有时候给我一堆废话」——看得见才谈得上去修。跟着首页数据一起给，不另开请求。
- 另外五条**先不接**，理由记在这儿：`coverage` 是碎片化诊断（给我看的，不是给用户看的）；`rebuild*` 是两个 codebook 之间的迁移工具（实测 `pending: 0`）；`quality/judged` 要花模型调用；`memory/timeline` 跟树上的时间线重复。**不接也是一个决定，得写下来**，不然下次审计又翻出来一遍。
- 后端 1086 / 前端 200 + 22 条 check。
