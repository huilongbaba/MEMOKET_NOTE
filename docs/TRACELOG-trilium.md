
## [678] 第 662 轮：「随时可撤」那句话得有地方兑现（2026-09-14）

- 提交前那次全量有一条红的（`test_前端没有导出了谁都不用的东西`：`undoEntityMerge` 导出了没人用），我没拦住就提交了。查出来**不只是 lint**：收件箱上写着「合并只影响显示和检索，知识库本身不动，**随时可撤**」，而界面上根本没有撤的地方。**跟这一段一直在修的是同一类：界面在承诺一件代码没提供的事。**
- 补上：判完那一下的提示条带「撤销」（7 秒），撤了就把这一对放回列表最前面——用户刚才就在看它。连着点一屏，点错一下是常事，而判完这一对就从列表里消失了；没有这个撤销，那句话就是假的。
- 教训记一笔：**提交前那次门禁的输出要真读**，不是看一眼 exit code。这一轮是靠 `grep FAILED` 补救的。

## [679] 第 663 轮：真跑无限续写读产出——占位判据两处误伤（2026-09-14）

真跑一次文件夹级 harness（6 个分段 / 19 轮 / 9 分 51 秒，跑完按 t0 清了 58 行）。

- **第 647 轮加的那条规矩第一次在真跑里触发**：第 6 节第 2 轮 `gave_up: ["coherence"]` → 「repairing these did not move the score; writing instead」→ 第 3 轮回去写。按设计工作。
- **占位判据误伤一：围栏里的不算。** 模型画了张 mermaid，节点写着 `B[EVT样品实际时间待确认]`，判据当成占位句拦下来、**连拦两轮**。可那句话不是「我还没写」，是**这个项目的状态就是「实际时间待确认」**——而这篇笔记的目标恰恰是「计划时间、决策状态、验证证据、实际结果，缺哪一项就写待核实」。图里的节点名是内容。跟第 607 轮「手写最简流程图放行」同一个道理：**判据不能把自己看不懂的东西一律当成毛病。**
- **占位判据误伤二：引号里的占位词是在「提到」它，不是在用它。** 产出里有「缺失项明确标为「待补」，而不是用阶段名称代替事实」——那是在陈述一条规矩。而原来这件事是**碰运气**的：`“待补”` 不报（后面的 `”` 不在终止符表里），`「待补」` 就报（`」` 在表里）——同一个意思、两种引号、两种结果。短引号（≤8 字）里的先摘掉再判；引号长到装得下一句话时照旧算。
- 顺带补了个守卫：**幻灯片那一篇上不给无限续写 / 智能续写**（实拍撞到——探针在幻灯片那篇上打开了写作计划弹层）。往一份 Marp 里塞散文分段既破坏 `---` 分页、也回不到原笔记。
- 产出本身是好的：带引用编号、有 mermaid、具体到项目名和日期、缺证据的地方老实写「当前材料仍没有给出」。
- 后端 1086 / 前端 200 + 22 条 check。
