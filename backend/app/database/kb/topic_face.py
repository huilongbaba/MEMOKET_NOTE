"""**「泛词」和「主题词」分开的那条轴**（P75 造的尺，P84 搬进产品）。

P75 把它写成一把量具（`backend/scripts/topic_spread_ruler.py`），
P84 判它**能直接用在 `common_term()` 上**，于是实现挪到这里，那把尺改成 import 这一份
——**一份实现，两个用途**。尺子那头还留着两个同名的字面量当**钉死的镜子**
（口径同 `recall_ruler` 抄前端那三个参数：`test_p84` 逐个对拍，飘了当场红）。

---

## 这条轴是什么

> **「泛」= 拿它当线索捞回来的那一堆事实，话题面跟「从这个库里随便抓同样多条」一样宽。
> 命中它，等于没告诉你这两段讲的是不是同一件事。**

轴是 `FactRecord.topics`——KITE 抽取时给每条事实打的话题码。**这条轴本来就在库里**，
它不是词频，**也不是 IDF**（`topic_spread_ruler.main()` 每次都打一组同 n 的对照：
同一个 n，`模式` 0.98 / `学生` 0.39，两头相反）。

量法是稀疏化（rarefaction）：

    E[话题种数 | n] = Σ_i (1 - (1-p_i)^n)      p_i = 话题 i 在全库事实里的占比
    spread(串) = 实际话题种数 k / E(n)

`spread ≈ 1` = 话题面跟随手抓一把一样宽 = **泛**；明显 `< 1` = 它把话题收住了 = **不泛**。

## 为什么 `common_term()` 需要它（P81 → P82 → P84 三批的账）

`COMMON_DF_MIN / COMMON_DF_RATIO` 判的是「这串在这个人的库里到处都是」。
P81 读完 95 条、P82 读完 49 条，根因是同一句话：

> **近义改写句跟查询共用的正是这个库的主题词，而单主题库里主题词的 df 天然就高。
> df 高 ≠ 它不是证据。**

P82 试过「按库大小把 `common` 关掉」，**变好 16 / 变差 23 → 退回**：那个旋钮
**砍掉的是第二份工**——`common` 在小库上还兼着口水词兜底（`因此` 在 193 unit 的库里
df 23.3%，而 `_is_cn_filler` 那张固定表里一个都没有）。
**要分开这两份工，得换一条轴。这就是那条轴。**

## 它在这儿**只当放行条件，从不用来否掉任何东西**

`common_term()` 里的用法是：df 已经判 True 了，再问一句「它是泛词还是主题词」，
**只有明确判「不泛」才把它捞回来**。`None`（判不了）一律**当 `False` 用不得**
——`SPREAD_MIN_HITS` 以下那一档不是「不泛」，是**样本不够**。
这跟 `vocab_term()` 那条（P29 #1：拿「是不是实体」当唯一的闸会砍掉真沾边的）同一个做法。

## 它今天**够不着**哪一格（P84 量出来的，照实写在这儿）

1. **英文那一半没有留出集**。P77 那份 80 条盲标全是汉字（P77 ③ / P79 ④ 两批都记过这笔账）。
   P84 实测这一格是**真的会出事**：不设这条限制时 `ai`(0.73) / `agent`(0.45) /
   `memory`(0.59) / `app`(0.66) 一起被捞回来，四条查询的整屏被六句几乎一模一样的
   英文定位套话（`MemoKet / MemuKet / MemoCat is presented as a wearable AI agent…`）灌满。
   **全库对拍逐条读下来：不限汉字 变好 17 / 变差 17（白干）；限汉字 变好 14 / 变差 7。**
   所以 `common_term()` 那头**只对汉字串问这条轴**，英文那半原样走 df。
2. **留出集是在 2362 unit 的 `terrence` 上做的**，而这条判据只在 < 333 unit 的库上起作用
   ——**门槛那个 0.75 在小库上没有留出集**。P84 是靠「全库对拍 + 28 条逐条读」接的，
   **不是靠那份留出集**。
3. **留出集验的是「判泛」那一头**（42 判泛，准确率 100%），
   而这儿用的是**「判不泛」那一头**：同一份 80 条上，判不泛 38 条里人也标不泛的只有 12 条
   （12/38）。**人标问的是「这个词本身泛不泛」，尺子问的是「它在这个库里泛不泛」,
   两把尺问的不是同一个问题**（P77 自己写过这一条），所以那个 12/38 不能直接当错误率读
   ——但它足够说明**这份留出集验不了这个用法**。
4. **大库那一档实测过了，判「不接」**（P86 ①，收 P84 ⑤）。P84 只量了形状没读，
   这一批把那 175 条逐条读完了。量具进了仓库：`recall_ruler.py --cf-bigcorpus`，
   **两个旋钮分开跑**——「大库接不接」和「英文那一半问不问」是两件事，
   P84 那个 175 是两闸一起拆量出来的，拿它判大库会把两笔账混成一笔。

   | 拆哪道闸 | 全库变了 | 落在 | 大库那几条逐条读下来 |
   |---|---:|---|---|
   | 只拆**库大小**（汉字闸留着）| 53 | `terrence` 25 · `terrence-rewrite` 28 | **变好 8 / 中性 7 / 变差 10** |
   | 两道闸**全拆** | **175** | `terrence` **134** · `terrence-rewrite` 41 | **变好 14 / 中性 27 / 变差 93** |

   **小库那一档是 14:7（两倍），大库那一档是 8:10（净亏）——形状是相反的，不是变淡。**
   `both` 那一档更清楚：**进 577 / 掉 80**，7:1 的不对称本身就是判据——它不是
   「多召回一点」，是**把空屏灌满**（134 条里 93 条变差，最常见的形状是
   `0 条 → 一屏 ai 撞词`：i=118/119/176/222/223/284/440/441/494/531/532 …），
   而且**逐句出处照样会被挤掉**（i=78 / i=407 掉的正是「公司专注的领域主要是 ICT，
   即通信技术和信息技术」这条逐字出处）。标注在
   `tests/fixtures/memory_sample.jsonl` 的 `p86-bigcorpus-175`。

   **为什么小库对、大库错，一句话**：小库里 df 高的那些串（`众筹` `手环` `样机`）
   **本来就是这个库在讲的那件事**，df 判它们 common 是判错了；大库（2362 unit、
   真实用户、多主题）里 df 高的串（`ai` `产品` `用户` `数据`）**是真的到处都是**，
   df 判对了，这条轴去翻它就是翻错。**`COMMON_DF_RATIO` 那 6% 在大库上是有效门槛**
   ——这正是 `common_term()` 里那道库大小闸当初写下的理由，这一批用 175 条把它坐实了。
5. **「只问汉字」那条限制**（第 ① 格）**这一批试过换掉，没换成**（P86 ②，收 P84 ②）。
   P84 读出来的那句话是：**这条轴分不开「窄是因为指着一个具体东西」和
   「窄是因为整个库都在讲这一件事」**（`agent` 0.45 比 `众筹` 0.47 还「不泛」，
   而它是这个单主题库的主导主题词）。这一批拿两批反例量了五个候选信号
   （A = 该捞回来的 10 个汉字串 · B = 不该捞回来的 5 个英文串 ·
   C = 今天已经捞回来、P84 读下来没出事的 10 个汉字串，新信号**不许把 C 杀掉**）：

   | 候选 | 量的是什么 | A | B | 判 |
   |---|---|---|---|---|
   | S1 `own` | 它占不占得住一个话题（max_t 命中_t / 全库_t）| .300–1.000 | .250–1.000 | **分不开**（`agent`/`ai` 都是 1.000）|
   | S2 `ride` | 它的头号话题是不是库的头号话题 | 2/10 骑 | **0/5 骑** | **反向**（`手环`/`佩戴` 骑，`agent`/`ai` 不骑）|
   | S3 `dup` | 命中的事实里几乎一样的占比 | .000 | .000–.111 | **分不开**（样板句只占一成）|
   | S5 加权话题大小 | 它落在大话题还是小话题上 | .811–1.461 | 1.159–1.532 | **分不开**（`手环` 1.461 > `ai` 1.192）|
   | S6 跟**库自己的主语**同现 | 命中的事实里提到产品自己名字的占比 | ≤.016 | **≥.065** | **分得开**（C ≤.018，不误伤）|

   **S1/S2/S3/S5 全是 `FactRecord.topics` 的函数，全部分不开，而且 S2 是反着的**
   ——根因一句话：**单主题库里话题码本身就是产品专用的**（`product_design` /
   `product_strategy` 是库里最大的话题，而 `手环` / `佩戴` 落的正是它们），
   所以「串 → 话题多重集」这个映射对两种「窄」给出的是**同一个形状**。
   **要分开它们的信息不在 `topics` 里。**

   **S6 分得开（无重叠，3.6 倍间隔），但这一批没接，理由是它今天造不出来**：
   它要「这个库自己的主语是谁」，而库里拿不到——`terrence-rewrite` 里出现最多的
   实体码是 **`app`（134）**，正是要挡的那几个串之一，真正的主语 `memocat`
   只排第三（66）；`terrence` 里最多的是 `speaker_a` / `speaker_b` / `speaker_c`
   （转写说话人标签），**大库根本没有主语**。拿「出现最多的实体」当主语会让 `app`
   自己给自己背书，是循环。**结论：信号找到了，缺的那一块是「这个库是关于谁的」，
   而库里没记这件事。**

6. **第 ⑤ 格最后那半句话是错的，这一批更正**（P88 ①）。
   ⚠️ **并排写**：上面那句「**库里没记这件事**」留在原地，跟下面这段对照着读——
   删掉它就不叫更正叫改口。

   **对的那半**：「要分开它们的信息不在 `topics` 里」——**成立**，这一批又加了四条
   `topics` 以外但同样分不开的轴（见下表）。
   **错的那半**：「库里没记这件事」。**库里记了，记在 `FactRecord.who` 上**——
   每条事实一个主语，`terrence-rewrite` 上 100% 填着（`团队` 671 / `speaker a` 234 /
   `项目团队` 219 / `用户` 138 …，413 个不同值）。P86 只在 `entities` 上找过「库级的
   那一个主语」，**没有在 `who` 上找过「每条事实的主语」**。

   把 `spread` 那条量法**一个字不动**地搬到 `who` 轴上（就是下面的 `WhoFace`），
   拿 P86 那两批反例重量（A = 该捞回来的 8 个汉字串 · B = 该挡的 4 个英文串 ·
   C = 今天真被这条轴捞回来的 **63** 个串，`recall_ruler --cf-spread` 里取，不是手挑）：

   | 轴 | A | B | 判 |
   |---|---|---|---|
   | `topics`（P86 已知）| .467–.587 | .450–.730 | 分不开 |
   | **`who`** | **.436–.747** | **.819–.932** | **分得开**（无重叠）|
   | `obj`（1237 个码）| .519–.673 | .582–.777 | 分不开 |
   | `kind`（7 个码）| .488–.965 | .833–1.014 | 分不开 |
   | `event`（15 个码）| .493–.754 | .701–.969 | 分不开 |
   | `place`（106 个码）| A / B 都够不着 20 次 | — | **判不了**（太稀）|

   **S6 那两版自动取主语也实测了**（P86 只推断，这一批在库上跑过，两版都废）：

   | 主语从哪来 | A | B | 判 |
   |---|---|---|---|
   | 手写正则（P86 第一版，禁止清单上那条）| .000–.016 | .065–.314 | 分得开，但**手写的不算**|
   | **出现最多的实体** `app` | .000–.142 | **.029**–1.000 | **分不开，而且反着**——`app` 自己 1.000（自己给自己背书，P86 那句推断**坐实了**），而 `agent`/`ai`/`memory` 只有 .029–.057，**比 A 里的 `录音` .142 还低** |
   | 出现最多的 `who` `团队` | .346–.522 | .229–.343 | **反向**（A 比 B 还高）|

   **所以这一批接的是 `WhoFace`，不是 S6**——它**不需要知道「这个库是关于谁的」**，
   只需要每条事实自己的主语。「库级的那一个主语」到今天仍然拿不到，
   **而这条路绕开了它**。

7. **`who` 这条轴今天**够不着**哪一格**（P88 实测，跟第 ①②③ 格同样照实写）：

   * **转写库上它分不开**。`terrence`（2362 unit）上 A .466–.793 / B .684–1.207，
     **重叠**。根因不是巧合：那个库的 `who` 有 **93.8%** 是说话人标签
     （`speaker a` 7291 / `speaker b` 6122 / `speaker c` 3190…），
     **那一栏装的不是主语，是谁在说话**。六个库逐个数下来这个比例是
     0.0% / 0.0% / 0.0% / **14.3%** / **93.8%** / 11.9%——两头差 6.6 倍，中间是空的。
     所以 `WhoFace` 自带一道 `SPEAKER_TAG_MAX` 闸：**过半是说话人标签就一律回 `None`**。
     ⚠️ **这道闸今天一条产出都不改**（唯一过线的 `terrence` 是大库，
     `common_term()` 那道库大小闸本来就不问这一档）。它在这儿是为了**别的小库**
     ——一个 193 unit 的转写库今天不存在，但没有理由不会存在。
     闸本身由 `test_p88` 拿**假事实表**直接测，不靠真语料（同这个文件下面那句）。
   * **门槛 `WHO_GENERIC` 的留出集只有一个库**。0.75–1.00 全库扫下来，
     产出好的那一段是 **[0.80, 0.90]**（2 条变、2 进 0 掉、0 条「有→空」），
     而**两头各由一个串顶着**：下沿 `测试` .789（掉到 0.78 就把 i=26 从 3 条打成 0 条）、
     上沿 `公司` .900（抬到 0.95 就救不回 i=293）。取的是**平台中点 0.85**，
     **这是这条判据今天最弱的一格**——跟第 ②③ 格是同一个病（这个仓只有一个
     `terrence-rewrite` 够得着这一档）。
   * ⚠️ **那个 0.85 不是从 A / B 那条缝里挑的，两件事别混**。A 和 B 之间的缝是
     **(.747, .819)**，而 0.85 落在**缝的上方**——于是在产品这个门槛下
     **`ai`(.819) 会被判「不泛」**。今天不出事，因为英文那一半根本不问这条轴
     （汉字闸，第 ① 格）；**但下一批要是拿这条轴去拆汉字闸，0.85 会把 `ai` 放进来**，
     那正是 P84 实测「产出白干」的那个形状。要拆汉字闸就得**先重新挑门槛**，
     而挑门槛需要的留出集这个仓今天还没有。闸在 `test_p88::第一条g`。
   * **它只做减法**。`common_term()` 里它串在 `TopicFace` **后面**：
     `topics` 已经判「不泛」了才问它，而且只有它也说「不泛」才捞回来。
     所以它**只可能少捞回几个串，不可能多捞回**——英文那一半、大库那一档逐字不动。

8. **第 ⑦ 格第一条那句「这道闸今天一条产出都不改」是对的，但它下面那句推论错了**
   （P90 ①，更正 P88 ①）。

   ⚠️ **并排写**：P88 在 `docs/TRACELOG-product.md` 留下的原话是

   > **i=607（大库上同一张脸）没治好**——这一刀够不着大库
   > （库大小闸在前面，而且 `terrence` 的 `who` 93.8% 是说话人标签、那条轴在那儿一律闭嘴）。

   **两半，一半对一半错：**

   **对的那半**：`terrence` 的 `who` 说话人标签占比这一批重数过，**0.9380**，
   跟 P88 逐位相同；六个库也重数过：**0.0% / 0.0% / 0.0% / 14.3% / 93.8% / 11.9%**。

   **错的那半有两处：**

   * **① `i=607` 根本不是「没治好」，它在 HEAD 上是好的。** 实测 HEAD 上
     `i=607` 召回 **1 条**（`terrence-1431-12F5`）。它那个「1 → 0 整屏变空」是
     `p86-bigsize-25` **那个反事实里的伤亡**，不是 HEAD 上的缺陷。
     **`i=293` 和 `i=607` 是同一条链的两个方向**：`i=293` 是 HEAD 上坏的、
     这条轴一开口就治好；`i=607` 是 HEAD 上好的、这条轴一开口就打坏。
     **「治」这件事在 i=607 上不存在**，所以 P90 ① 判**不改**。

   * **② 「这一刀够不着大库」说反了——它够得着，而且够得着的方向正好是保护。**
     `WhoFace` 在 `terrence` 上 `usable=False` → `generic()` 回 `None` →
     `common_term()` 里那句 `who.generic(term) is not False` 为 **`True`**
     → **判「是 common」= 不捞回来**。所以主语面在大库上不是「什么都不做」，
     是**把话题面那一问整个按住**。全库实测（`recall_ruler --cf-gates`）：
     **拆掉库大小闸、主语面照问 → 765 条一条都不变**。

   **`i=607` 是不是 i=293 那条链，实测的**（`_cjk_terms` 那 16 个名额，同样三换三）：

   | | `_cjk_terms` 16 个名额 | `terrence-1431-12F5` |
   |---|---|---|
   | HEAD | 有 `要相信` / `让用户` / `录音权` | **1 条** |
   | 拆库大小闸 · 不问主语面 | 换成 `但用户` / `用户需` / `用户要` | **0 条** |
   | 拆库大小闸 · **主语面照问** | **逐字不动** | **1 条** |

   被捞回来的是 `用户`(.736) / `需要`(.741) / `产品`(.691) / `手机`(.589)
   ——`df` 在大库上判它们 common **判对了**（第 ④ 格那一课），是这条轴翻错了。

   ⚠️ **但这不等于库大小闸可以拆掉**。上面那个「0 条」是**两道闸冗余**，不是「其中一道没用」：
   把 `SPEAKER_TAG_MAX` 一摘（强行 `usable=True`），同一份语料上立刻
   **24 条变了、全在 `terrence`、i=607 从 1 条打成 0 条**。
   因为那四个串的**主语面**分数是 **.460 / .473 / .559 / .534**，
   全都远低于 `WHO_GENERIC`(0.85)——**真让这条轴在大库上开口，它会把四个一起捞回来**。
   **⇒ 大库上的安全来自「那个库的 `who` 恰好装的不是主语」这个偶然，不是来自这条轴判得准。**
   P88 留的「要治它得换一条**大库上立得住**的轴」——**主语面不是那条轴**，
   它在大库上立得住纯属侥幸。闸在 `test_p90::第一条`。

9. **`WHO_GENERIC = 0.85` 的留出集造出来了**（P90 ②，收 P88 ②）。
   P88 自己标的最弱那一格是「留出集只有一个库、平台两头各由**一个串**顶着
   （`测试` .789 / `公司` .900）」。这一批造了两份，**两个库**：

   | 栏 | 多少条 | 什么 | 盲不盲 |
   |---|---:|---|---|
   | A | **88**（`terrence-rewrite` 64 + `terrence` 24）| **产品今天真会问到主语面的那一堆**（df-common ∧ 汉字 ∧ 话题面判「不泛」）| ⚠️ **不盲**，见下 |
   | B | **116**（两库各 58）| 两库里主语面判得了的全部汉字串**减掉 A**，按分数分带抽（分带只进代码）| **真盲** |

   ⚠️ **A 栏的盲性是造它的人自己弄坏的，照实记**：造盲标单**之前**先跑了一趟
   把那 64 个串**连同 `who_spread` 排好序**打印出来的量具，**哪几个 ≥ 0.85 已经看见了**。
   所以 A 栏只能当「按预先登记的规则标的」读。**B 栏才是干净的那一份**
   （脚本知道分数、只打总条数，分带和分数都没出脚本）。

   顺序是 P77 立的那条：**先造盲标单（只有词 + 命中它的事实，一个分数都没有）
   → 标完 → 才跑揭晓**。上下文里**故意不摆 `who` 那一栏**——那正是被测这把尺自己的轴。

   ⚠️ **这两份验的是「判不泛」那一头**，跟 P77 那份（验「判泛」）**不是同一头**
   （P84 ③ 指出过这笔账）。产品里这条轴只在 `generic(t) is False` 时才捞回来，
   所以要验的错法是「**尺子说不泛、人说泛**」= 假捞回 = 挤爆 `_cjk_terms` 那 16 个名额。

   **量出来的（A 栏，= 产品那一堆）**：

   | 门槛 | 捞回 | 假捞回 | 假捞回率 | 挡住 | 挡错 | 挡错率 |
   |---:|---:|---:|---:|---:|---:|---:|
   | 0.75 | 57 | 35 | 61.4% | 19 | 1 | 5.3% |
   | 0.80 | 61 | 38 | 62.3% | 15 | **0** | 0.0% |
   | **0.85** | 64 | 41 | **64.1%** | 12 | **0** | **0.0%** |
   | 0.90 | 69 | 46 | 66.7% | 7 | 0 | 0.0% |

   **0.85 站得住，但站住的理由要改写：**
   * **站得住的是「判泛」那一头**——挡住的 **12 条人 100% 同意**（挡错 0），
     而且 [0.80, 0.90] 这一整段挡错都是 0，**P88 那个平台复现了，
     而且不再是两个串顶着，是两头各 6 个标过的串**。
   * **站不住的是「判不泛」那一头**——那正是产品用的那一头，**64% 是「人说泛」**。
     而且**换门槛救不了**：0.60 那一档也还有 48.5%，再往下挡错就开始涨。
   * ⇒ **这个门槛今天不出事，靠的不是它判得准，是它后面那条链**
     （`_cjk_terms` 16 个名额 + `qualifies` + `_strong_enough`）**只让极少数串真改到产出**
     ——全库 765 条只变 2 条。**别把「产出只变 2 条」读成「这条轴判得准」。**
   * **判：不改**。[0.80, 0.90] 产出逐条一模一样（P88 全库扫过），
     动它等于「旋钮动完产出一样」。

   **B 栏（真盲、更宽的 population）把上面那句坐实了**：假捞回率在
   0.60–1.00 **任何**一档都是 **66.7% – 88.5%**，没有一个能用的工作点。
   人标「不泛」的 13 条里 9 条落在 .317–.713（**有信号**），但人标「泛」的串
   把那一段**塞满**（`团队计` .411 / `文档` .457 / `决定` .458 / `找到` .480 …）。
   **⇒ 主语面是一条弱信号轴，它今天的价值全在「和前面那几道闸串起来」。**

10. **拆汉字闸那条路，这一批量完了：不通，卡在两格**（P90 ③，收 P88 ③）。

    **先把汉字闸今天挡住的英文串全摆出来**（不是手挑四个）——
    `terrence-rewrite` 上过 df-common 的英文串**一共 7 个**：

    | 串 | df 比 | `topics` 面 | `who` 面 | 拆了汉字闸会被捞回来吗 |
    |---|---:|---|---|---|
    | **`ai`** | .378 | .730 不泛 | **.819 不泛** | **会** ← 就是它 |
    | `2026` | .306 | .772 泛 | .795 不泛 | 不会（话题面先挡住）|
    | `app` | .290 | .663 不泛 | **.932 泛** | 不会（**主语面挡住了**）|
    | `10` / `20` / `15` | .259/.176/.124 | 泛 | — | 不会 |
    | `agent` | .119 | .450 不泛 | **.875 泛** | 不会（**主语面挡住了**）|

    **P88 ③ 那句预测实测对了**：`0.85` 在 A/B 那条缝 (.747, .819) 的**上方**，
    于是 `ai`(.819) 被判「不泛」放进来。
    全库对拍（`--cf-gates` 的 `en` 那一档）：**变了 6 条，全在 `terrence-rewrite`，
    进 25 掉 5，有→空 0**；6 条**逐条读完 变好 0 / 中性 2 / 变差 4**
    （标注 `p90-engate-6`）。最常见的形状正是 P84 记过的那个：
    **i=295 / i=296 从 1 条变成「1 条 + 7 句 `AI` 撞词」**。

    > 主语面确实**缩小**了伤口（P84 那一版不限汉字是 22 条变、有→空 1；
    > 带上主语面是 6 条变、有→空 0），**但方向没变，还是白干**。

    **⇒ 卡在两格：**
    * **卡格 ①（门槛）**：要挡住 `ai` 得把 `WHO_GENERIC` 压到 ≤ .819，
      而 .819 落在**汉字那一半的 [0.80, 0.90] 平台里面**——
      **一个门槛服两个 population，一刀就动了两处**。要拆就得给英文串**单独一个门槛**。
    * **卡格 ②（轴）**：**主语面在英文串上根本没有信号**——这一批造了
      **英文那半的第一份留出集**（`p90-holdout-en-60`，60 条 / 两个库 / **真盲**，
      收 P77 ③ / P79 ④ / P84 ③ 三批都记过的那笔账）。量出来：

      | 轴 | 最好的那一档 | 人标「不泛」 | 人标「泛」 | 有没有缝 |
      |---|---|---|---|---|
      | `who` | 0.60：假捞回 **58.3%** | .375–.875 | **.237–1.536** | **没有**（22 条重叠）|
      | `topics` | **≤0.60：假捞回 0.0%**（捞回 9 条全对）| .450–.877 | .604–1.067 | 没有，但**低段干净** |

      而且 `who` 轴在**两个最要紧的串上都判反**：`ai` 判「不泛」（人标泛）、
      `agent` 判「泛」（人标**不泛**）。**换门槛救不了没有信号的轴。**

    **⇒ 真要拆汉字闸，该走的是 `topics` 轴 + 一个英文专用的低门槛（≈0.60）,
    不是主语面。** 这一批**没接**，三条理由：**一刀只动一处**（这一批的正题是
    ① 和 ②）；这份英文留出集只有 60 条、而 `terrence-rewrite` 上主语面
    **判得了的英文串统共只有 16 个**（另有 141 个 n < 20 判不了）；**判据宁可窄**。

    ⚠️ **英文那一半这条轴大部分时候压根判不了**，这也是新量的：
    `terrence-rewrite` **16 / 157**、`terrence` **125 / 1200**。
    「判不了」不是「不泛」，所以**拆了汉字闸也不会有想象中那么多串被捞回来**。

11. **P90 ① 留的那条路（`topics` 轴 + 英文专用低门槛 ≈0.60）走完了：还是不接，
    但卡的不是 P90 猜的那两格**（P92 ①）。

    ⚠️ **并排写**：第 ⑩ 格结尾那句「真要拆汉字闸，该走的是 `topics` 轴 + 一个英文专用的
    低门槛（≈0.60），不是主语面」**原样留在上面**。这一批把它走完了，下面是结果。

    **先核 P90 那句「这个仓的语料喂不饱它」——核完是：喂得饱轴，喂不饱产品那一格。**
    六个库逐个数（**`topics` 轴重量的，P90 那两个数量的是 `who` 轴，轴不一样不能抄**）：

    | 库 | unit | 小库 | 英文串 | `topics` 判得了 | df 过 common | 其中 `topics` 判得了 |
    |---|---:|---|---:|---:|---:|---:|
    | `fresh678` / `b` / `c` | 2 / 2 / 2 | 是 | 16 / 16 / 16 | **0** | 0 | 0 |
    | `shot-demo` | 11 | 是 | 46 | **0** | 0 | 0 |
    | `terrence` | 2362 | **否** | 1200 | **121** | 20 | 20 |
    | `terrence-rewrite` | 193 | 是 | 157 | **16** | **9** | **9** |

    * **留出集做厚的上限 = 137 条**（121 + 16），而 **121 / 137 = 88% 落在 `terrence`**
      ——**一个这一刀永远够不着的大库**（库大小闸在前面）。
    * **产品那一堆的上限 = 9 个串，全在一个库里**。所以「做厚」这件事对轴成立、
      **对产品那一格不成立**：小库那边今天有多少就是多少，**16 个判得了、9 个够得着**。
    * ⚠️ **更正 P90 的一个数**：P90 说「`terrence-rewrite` 上过 df-common 的英文串**一共 7 个**」。
      **重数是 9 个**（多出 `speaker` / `memory`）。差别在候选串从哪来：P90 那张表只取了
      分词器那一路，而 `common_term()` 真实的调用方还有 `_candidate_terms` 那一路。
      **两路都取才是产品真会问到的那一堆。**

    **留出集做厚了**（`p92-holdout-en-*`，**137 条 = 上面那 137 个，不是抽样**）：

    | 栏 | 多少条 | 盲不盲 |
    |---|---:|---|
    | **EN-blind** | **108** | **真盲**（造单脚本不算分数、不排序，只打总条数）|
    | **EN-tainted** | **29** | ⚠️ **不盲**——它们就是 df 过 common 的那 29 个，
      而「先量上限」那一趟把分数打出来过。按预先登记的 `RULE.md` 标，单列 |

    **量出来的（`topics` 轴，按库分开——比率要按对的那条轴分）**：

    | 栏 / 库 | 条数 | 人标「不泛」 | 人标「泛」 | 有没有缝 | 假捞回 0.0% 的最高门槛 |
    |---|---:|---|---|---|---|
    | EN-blind · **`terrence`** | 101 | .276–.894 | .368–1.067 | **没有**（45 条重叠）| 0.365 |
    | EN-blind · **`terrence-rewrite`** | 7 | .462–.877 | .894 | **有缝** | 0.890 |
    | EN-tainted · **`terrence-rewrite`**（= **产品那一堆**）| 9 | **.450–.663** | **.708–.880** | **有缝** | **0.705** |
    | EN-tainted · 同一批的 `who` 轴 | 29 | .684–.932 | .237–1.507 | 没有 | **一档都没有** |

    ⇒ **P90 猜的两格这一批都过得去**：门槛有缝（0.60 稳稳落在 (.663, .708) 里），
    轴也不是没信号（`who` 轴才是没信号的那条——29 条上**任何**门槛假捞回都不是 0，
    P90 ② 那个判在更宽的 population 上复现了）。

    **然后产出还是变差了。** 全库对拍（`recall_ruler --cf-gates` 的 `en060` 那一档，
    一刀只动一处：汉字闸换成「英文走 `topics`@0.60」，汉字串那条链逐字不动）：
    **变了 11 条，全在 `terrence-rewrite`，进 37 掉 26，有→空 0**；
    **11 条逐条读完：变好 0 / 中性 2 / 变差 9**（标注 `p92-en060-11`）。

    **⇒ 卡在第三格，这一格 P84 / P86 / P90 都没量到过：「不泛」≠「值得当证据」。**

    这一刀真正多放行的串**只有两个**——`agent`(.450) 和 `memory`(.592)——
    **两个人标都是「不泛」，尺子判对了**。坏事坏在它们命中的是什么：
    这个库里**同一句产品定位话有六种说法**——

        terrence-1578F1  MemuKet is described as a wearable AI agent powered by the user's own memory.
        terrence-1580F5  MemuKet is presented as a wearable AI agent powered by the user's own memory.
        terrence-1579F8  MemoKet is presented as a wearable AI agent powered by the user's own memory
                         and intended to store, organize, and use memories from conversations.
        terrence-1581F2  Speaker A is developing Memocat as a memory AI agent that stores, organizes,
                         and puts users' memories into use.
        terrence-1522F14 The team describes Memo Cat as a memory OS or AI assistant with the user's
                         own memory, rather than merely a recorder or note taker.
        terrence-1589F9  MemoCat is positioned as a memory-powered AI agent that connects memories
                         across time, topics, conversations, and tools to provide consolidated answers.

    八格的屏，这一族一进就是**六格里的四到六格**。i=645 是最清楚的一条：
    **8 → 8，七进七出**，出去的七条全是 `Ask Memory` 的具体事实
    （「1.2 版本支持多轮对话」「暂不开放互联网检索」「接 MCP 要明确授权」
    「额度与录音时长绑定」「最主要的差异化能力是 ask memory」…），
    进来的除了两条真沾边的（`366F1` / `1814F9`）**全是上面那一族**。

    > **串指着一件具体的事**（= 人标「不泛」，尺子也判对了）
    > **≠ 命中它的那些事实彼此有区别**。
    > 一个库里只要有一族近乎同义的样板句，这条轴判得越准，它们灌屏灌得越狠。

    ⚠️ **字面去重看不见这一族**：按 3-gram 包含度 ≥0.6 聚族，`agent` 的 34 条命中里
    只有 **14.7%** 落在重复组里、最大一组**只有 3 条**——上面六条里**只有三条**被认出是一族，
    另外三条是**同义改写**，字面口径够不着。（P86 ③ 那格 S3 `dup` 判「分不开」，
    **那个判在这儿仍然成立，而且这一批给了它一个更具体的理由**：要分开的不是字面重复。）

    ⚠️ **门槛扫出来产出真的在动**，所以这**不是**「旋钮动完产出一模一样 → 退回」那一类：
    0.45 变 0 条 · 0.50/0.55 变 5 条 · **0.60/0.65 变 11 条** · 0.70 变 18 · 0.75 变 20 · 0.80 变 22。
    **有工作点可挑，挑哪一档只决定伤多大——没有一档方向是对的。**

    ⚠️ **两个门槛怎么算账**（P90 卡格 ① 那句「一个门槛服两个 population = 一刀动两处」）：
    这一批的反事实里英文走的是**自己的常数**（`recall_ruler.EN060_TH`），
    汉字那一半的 `SPREAD_GENERIC`(0.75) / `WHO_GENERIC`(0.85) **一个字节没动**
    ——所以「一刀动两处」这件事**是可以拆开的**，代价是**多一个旋钮**。
    真要接，顺序得是「先单独接英文那个旋钮（本批量的就是它）、绿了下一批再谈别的」。
    **这一批量完是红的，所以第二个旋钮的账根本不用开。**

12. **P90 ③ 自己标的「A 栏盲性是坏的」——重造了一份干净的，坏盲性带偏了多少量出来了**（P92 ②）。

    ⚠️ **并排写**：P90 那份 `p90-holdout-who-a`（88 条）**不删**，它在
    `tests/fixtures/memory_sample.jsonl` 里原样留着，`_meta` 上标了 `blind: false`。
    新那份是 `p92-holdout-who-a2`，**population 跟它逐字同一条**（同样的候选串来路、
    同样三道筛：df 过 common ∧ 汉字 ∧ 话题面判「不泛」），所以**重叠是 88/88**，比得了。

    **顺序**（写在 `RULE.md` 里，读第一条之前）：造单脚本一个分数都不算、不打、不排序
    （seed 92 打散）→ 标完 → **才**跑揭晓。上下文里不摆 `who` 那一栏。

    | | 判得了 | 捞回 | 假捞回 | **假捞回率** | 挡住 | 挡错 | **挡错率** |
    |---|---:|---:|---:|---:|---:|---:|---:|
    | P90（**坏盲性**）| 76 | 64 | 41 | **64.1%** | 12 | **0** | **0.0%** |
    | **A′（干净盲标）** | 88 | 75 | 54 | **72.0%** | 13 | **1** | **7.7%** |

    * **两份标法一致 68/88 = 77.3%**。不一致的 20 条里 **12 条是 P90 标了「说不好」**
      （P90 有 12 条「说不好」，A′ 一条都没有）；**两边都下了判**的分歧只有 **8 条**。
    * **没有量出方向性偏差**：不一致里「P90 那边更跟着分数走」6 条、「A′ 这边更跟着分数走」4 条
      ——**坏盲性没有把标注系统性地拖向分数**。
    * **被带偏的是结论那句话**：P90 ② 的原话是「挡住的 **12 条人 100% 同意**（挡错 **0**），
      而且 **[0.80, 0.90] 整段挡错都是 0**」。**干净盲标下这句话不成立**：
      挡错变成 **1 条**（`连接`，`who_spread` **1.043**，A′ 标「不泛」而 P90 标「泛」），
      挡错率 **0.0% → 7.7%**。
    * ⚠️ **`连接` 那一条救不回来**：1.043 比 0.90 还高，[0.80, 0.90] 里**任何**一档都挡着它，
      往上抬到 1.05 假捞回会跟着涨。**所以「不改 0.85」这个判不变，变的是它的理由**
      ——不能再说「判泛那一头挡错是 0」，只能说「**挡错很少（1/13），而且没有别的门槛更好**」。
    * ⚠️ **大库那 24 条 A′ 全标「泛」**，于是 `terrence` 上**每一档假捞回率都是 100%**。
      这把第 ⑧ 格那句「大库上的安全来自偶然、不是来自这条轴判得准」**又坐实了一遍**
      ——真让这条轴在大库上开口，它捞回来的**每一个**都是人标「泛」的。
    * **剔掉 16 个已知污染串**（P90 台账 / 本文件注释里点名引用过分数的那些）之后
      0.85 那一档是 **假捞回 67.7% / 挡错 20.0%**——**结论同向，没有靠污染串撑着**。

## `TopicFace` 要什么

`facts` 只要求三样：`.text` / `.topics` / `.unit`——拿一份假的事实表就能测，不必有真语料。
（`WhoFace` 多要一样 `.who`。）
`units_for` 是可选的预筛（`_GrepIndex.units_for`）：给了就只扫候选 unit 里的事实，
**给不给结果必须一样**（回 `None` = 预筛不了，就全表扫）。
"""

from __future__ import annotations

import re

from .who import is_speaker_tag, norm_who

# 判得了的门槛：这个串在库里落到的**话题次**至少这么多，少于它一律 `None`（判不了）。
# **它是可判定性门槛，不是分数**——`spread` 本身已经把 n 除掉了。
# n=20 时 E≈13.1，要判泛得 20 条事实落在 ≥9 个不同话题里；n=5 那一档随机抽都常常全不同。
SPREAD_MIN_HITS = 20

# 判「泛」的门槛。≥ 它 = 话题面跟随手抓一把一样宽。
# P75 先在手挑的设计集上定 0.65，看完全库分布收紧到 0.75；
# P77 拿 80 条盲标留出集核过：0.75 那一档判泛 42 条、准确率 **100%**。
# ⚠️ 那份留出集全是汉字、全在 `terrence`（2362 unit）上 —— 见文件头第 ①②③ 格。
SPREAD_GENERIC = 0.75

# `who` 那条轴判「泛」的门槛（P88 ①）。**它是另一条轴，所以是另一个数**——
# 拿 `SPREAD_GENERIC` 那个 0.75 直接套过来实测是会出事的：
# 0.78 以下 `测试`(.789) 就不再被捞回来，i=26 从 3 条打成 0 条。
# 全库 765 条扫 0.75 / 0.78 / 0.80 / 0.85 / 0.90 / 0.95 六档下来，
# **[0.80, 0.90] 这一段产出一模一样**（2 条变 · 2 进 0 掉 · 0 条「有→空」），
# 取的是这个平台的中点。两头各由一个串顶着（下沿 `测试` .789 / 上沿 `公司` .900），
# **留出集只有 `terrence-rewrite` 一个库** —— 文件头第 ⑦ 格把这一格的弱点写清楚了。
# ⚠️ **P90 ② 给它做了留出集**（收 P88 那句「留出集只有一个库、两头各一个串顶着」）：
# 两份、两个库、204 条人标（A 栏 88 = 产品今天真问到的那一堆 · B 栏 116 = 真盲的那一份）。
# 结论**不是「0.85 判得准」**，两头得分开说，全文在文件头第 ⑨ 格：
#   · **「判泛」那一头站得住**——挡住的 12 条人 100% 同意，[0.80, 0.90] 整段挡错都是 0；
#   · **「判不泛」那一头（= 产品用的这一头）64% 是「人说泛」**，而且换任何门槛都救不了。
# **所以这个门槛今天不出事靠的是它后面那条链**（`_cjk_terms` 16 个名额 + 两道证据闸）
# **只让极少数串真改到产出**——全库 765 条只变 2 条。
# **别把「产出只变 2 条」读成「这条轴判得准」。**
#
# ⚠️ **P92 ② 重造了一份干净盲标的 A 栏，上面那句「挡错是 0」不成立了**（并排写，全文在第 ⑫ 格）：
# P90 那份 A 栏的盲性是坏的（造单前先看过排好序的分数），重造之后同一 population 88 条
# **假捞回 64.1% → 72.0%、挡错 0.0% → 7.7%（0 条 → 1 条：`连接` 1.043，人标「不泛」）**。
# **判仍然是「不改 0.85」**——`连接` 那一条 [0.80,0.90] 里任何门槛都挡着它，换门槛只会更糟；
# 变的是理由：**不能再说「判泛那一头挡错是 0」**，只能说「挡错很少（1/13），且没有更好的门槛」。
WHO_GENERIC = 0.85

# `who` 那一栏里说话人标签占到这个比例，就判「这个库的 `who` 装的不是主语」，
# 整条轴回 `None`（判不了）。六个库实测 0.0% / 0.0% / 0.0% / 14.3% / **93.8%** / 11.9%
# ——中间是空的，0.5 落在 14.3% 和 93.8% 之间（6.6 倍间隔）。
# ⚠️ **它今天一条产出都不改**（唯一过线的 `terrence` 是大库，库大小闸本来就不问这一档），
# 理由和它为什么还是要在，写在文件头第 ⑦ 格。
#
# ⚠️ **P90 ① 实测：上面那句仍然成立，但它非常容易被读成「这道闸没用」——恰好是反的。**
# 把库大小闸拿掉，这道闸就**立刻**是唯一挡着大库的东西：全库对拍
# （`recall_ruler --cf-gates`）拆掉库大小闸、这道闸留着 → **765 条一条都不变**；
# 这道闸也一摘（强行 `usable=True`）→ **24 条变了、全在 `terrence`、i=607 从 1 条打成 0 条**。
# 两道闸今天是**冗余**的，但它们挡的**不是同一种库**——所以两道都留着。全文在文件头第 ⑧ 格。
SPEAKER_TAG_MAX = 0.5

# ASCII 串按**词边界**核（同 `kite_memory._GrepIndex.unit_df` 那条）：拿子串数去数
# `pr` 会在 product / approve 里命中。中文没有词边界，子串就是要的那个数。
_ASCII_TERM = re.compile(r"^[0-9A-Za-z][0-9A-Za-z .+#_-]*$")

# 「这一串里有没有汉字」。**只有汉字串才问这条轴**——理由是文件头第 ① 格，
# 不是省事：P84 实测不设这条限制时产出白干（变好 17 / 变差 17）。
#
# ⚠️ **这道闸到今天为止一共挡过三次进攻，三次都判「留着」**（要拆它先把这三条读完）：
#   · P84：不限汉字、不问主语面 —— 22 条变 / 有→空 1 / **变好 17 变差 17**（白干）。
#   · P90 ③：拆汉字闸、主语面照问 —— 6 条变 / **变好 0 中性 2 变差 4**
#     （`recall_ruler --cf-gates` 的 `en` 档，标注 `p90-engate-6`）。
#   · **P92 ①：英文改走 `topics`@0.60（P90 自己点名的那条路）—— 11 条变 /
#     变好 0 中性 2 变差 9**（`en060` 档，标注 `p92-en060-11`）。
#     ⚠️ 这一次**门槛有缝、轴判得对**（放行的 `agent`/`memory` 人标都是「不泛」），
#     **产出照样变差**——卡在「不泛 ≠ 值得当证据」那一格，全文在文件头第 ⑪ 格。
_HAS_CJK = re.compile(r"[一-鿿]")


def has_cjk(term: str) -> bool:
    """这一串里有汉字吗。`common_term()` 拿它决定问不问这条轴。"""
    return bool(_HAS_CJK.search(term or ""))


class TopicFace:
    """一个人的库的「话题面」。

    **轴是 `_keys()` 一个方法定的**（P88 把它拆出来的）：`WhoFace` 换的就是这一个方法，
    稀疏化那段数学**一行都没抄**——抄一份出去两份迟早会飘，而这两条轴是要并排读的。
    """

    AXIS = "topics"
    GENERIC = SPREAD_GENERIC

    def _keys(self, f) -> tuple:
        """这条事实落在这条轴的哪几个码上。"""
        return tuple(getattr(f, "topics", None) or ())

    def __init__(self, facts, units_for=None) -> None:
        self._units_for = units_for
        self.by_unit: dict[str, list] = {}
        self.all: list = []
        prior: dict[str, int] = {}
        for f in facts:
            self.all.append(f)
            self.by_unit.setdefault(getattr(f, "unit", "") or "", []).append(f)
            for t in self._keys(f):
                prior[t] = prior.get(t, 0) + 1
        total = sum(prior.values()) or 1
        self.ps = [v / total for v in prior.values()]
        self._memo: dict[str, tuple[int, int]] = {}
        # `False` = 这个库的这条轴根本不该开口（`WhoFace` 用得上，见那儿）。
        self.usable = True

    # -------------------------------------------------------------- 内部

    def expected(self, n: int) -> float:
        """从这个库里随手抓 n 条事实，指望看见几个不同的话题。"""
        return sum(1.0 - (1.0 - p) ** n for p in self.ps)

    @staticmethod
    def _rx(term: str):
        if _ASCII_TERM.match(term or ""):
            return re.compile(rf"(?<![0-9A-Za-z]){re.escape(term)}(?![0-9A-Za-z])", re.I)
        return re.compile(re.escape(term or ""), re.I)

    def _pool(self, term: str):
        if self._units_for is None:
            return self.all
        try:
            units = self._units_for(term)
        except Exception:      # noqa: BLE001 —— 预筛只是快路，问不出来就全表扫
            return self.all
        if units is None:
            return self.all
        return [f for u in units for f in self.by_unit.get(u, ())]

    # -------------------------------------------------------------- 对外

    def face(self, term: str) -> tuple[int, int]:
        """(话题次 n, 不同话题数 k)。"""
        hit = self._memo.get(term)
        if hit is not None:
            return hit
        rx = self._rx(term)
        seen: dict[str, int] = {}
        for f in self._pool(term):
            if rx.search(getattr(f, "text", "") or ""):
                for t in self._keys(f):
                    seen[t] = seen.get(t, 0) + 1
        out = (sum(seen.values()), len(seen))
        if len(self._memo) >= 8192:
            self._memo.clear()
        self._memo[term] = out
        return out

    def spread(self, term: str) -> float | None:
        n, k = self.face(term)
        if n < SPREAD_MIN_HITS:
            return None
        e = self.expected(n)
        return (k / e) if e > 0 else None

    def generic(self, term: str) -> bool | None:
        """`True` = 泛 · `False` = 不泛 · `None` = **判不了**（别当 `False` 用）。"""
        if not self.usable:
            return None
        s = self.spread(term)
        return None if s is None else s >= self.GENERIC


class WhoFace(TopicFace):
    """同一个库的「**主语面**」——轴换成 `FactRecord.who`（P88 ①）。

    ## 它为什么在这儿

    P86 判「`topics` 那条轴分不开两种『窄』」，并留下一句
    「**缺的那一块叫「这个库是关于谁的」，库里没记这件事**」。
    **后半句是错的**（文件头第 ⑥ 格并排写着更正）：库里记了，
    记在**每条事实的 `who`** 上。P86 找的是**库级**的那一个主语（`entities` 里取最多的），
    那个确实拿不到、而且拿了是循环（`app` 给自己背书，第 ⑥ 格里实测坐实了）；
    **而分开两种「窄」根本不需要库级那一个**，每条事实自己的主语就够。

    量法跟 `TopicFace` **逐字同一条**（稀疏化），只换 `_keys()`：
    `spread ≈ 1` = 命中它的那些事实的主语面跟随手抓一把一样宽 = **泛**。

    ## 它什么时候闭嘴

    **`who` 那一栏在转写库里装的不是主语，是谁在说话。** `terrence` 上 93.8% 是
    说话人标签，而这条轴在那个库上**实测分不开**（A .466–.793 / B .684–1.207，重叠）。
    所以过半是说话人标签就整条回 `None`（判不了）——**`None` 不是「不泛」**，
    调用方照旧不许拿它当 `False` 用（同 `TopicFace` 那条）。
    """

    AXIS = "who"
    GENERIC = WHO_GENERIC

    def _keys(self, f) -> tuple:
        w = norm_who(getattr(f, "who", "") or "")
        return (w,) if w else ()

    def __init__(self, facts, units_for=None) -> None:
        super().__init__(facts, units_for)
        filled = tagged = 0
        for f in self.all:
            for w in self._keys(f):
                filled += 1
                if is_speaker_tag(w):
                    tagged += 1
        self.filled = filled
        self.speaker_share = (tagged / filled) if filled else 0.0
        # 一条 `who` 都没有的库也一样闭嘴：没有这条轴可量。
        self.usable = filled > 0 and self.speaker_share < SPEAKER_TAG_MAX
