# Harness 升级：执行 - 测试 - 迭代计划

> 第 765 轮重写（第四版）。**这是执行计划，不是方案。**
> 台账：`docs/TRACELOG-harness.md`。架构文档 `docs/harness-framework.md` 随改随更新
> （仓里有闸钉着，数量对不上测试会红）。

---

## 0. 依据：三份重点 + 五份补充 + 一份实地勘查

### 重点三份（用户点名，**优先落地**）

| 代号 | 文档 | 它管什么 |
|---|---|---|
| **[CE]** | `harness-context-engineering.md` | 上下文怎么组织、缓存命中、**不要截断** |
| **[IND]** | `harness-evaluator-industry.md` | 行业解法：八个 harness 分属四条线，各有成熟做法 |
| **[MR]** | `harness-multiround-retrieval.md` | 多轮检索：回路没有「我已经有什么」这份状态 |

### 补充五份

| 代号 | 文档 | 它管什么 |
|---|---|---|
| [EFF] | `harness-effect-plan.md` | 39 次真跑 + 980 次调用量出来的七处 |
| [MECH] | `harness-mechanism-rethink.md` | 机制本身三处结构性错配 |
| [EVAL] | `harness-evaluators.md` | 我们自己八个 evaluator 的拆解 + 六条建立方法 |
| [LONG] | `harness-longform-deepdive.md` | 长文两个 harness 的专项 |
| [LED] | `harness-fact-ledger.md` | 材料账本：把 [MR] 和 [CE] 合成一件事 |

### 一份实地勘查（第 764 轮，**没有调研文档**）

代号 **[SITE]**。来自两件事：① 发现仓里早有一整套评测台
（`backend/scripts/` 的 suite / soak / 两个 bench / 压测 / A-B），而八份调研一次没提到；
② 把 harness 里从没审过的那一半（`agent_loop` / `tools` / `skills` / `replan` /
`revision` / `policy`）和用户这一端列出来。

**[SITE] 里最硬的一个数**（从 `harness_stress_log.jsonl` 的 200 轮里算的）：

```
第 N+1 轮相对第 N 轮，最弱维度：变好 33% · 持平 65% · 变差 2%
总分逐轮：上升 39 · 持平 64 · 下降 25
non_repetition 跨轮几乎不动：r1 1.14 → r2 1.18 → r3 1.08 → r4 1.24
spine_fidelity 1.88–1.96 封顶（对着没人验过的计划打分，太容易满足）
```

**65% 的轮次最弱维度一动不动**，而每一轮都花了检索 + 续写 + 打分三次模型调用。

---

## 1. 铁律（每一批都适用）

1. **先能看见，再动手。** 没有数的改动无法证明有效，也无法回退判断。
2. **能用代码判准的，不交给模型。**（`checks/__init__.py` 的老规矩）
3. **判据宁可窄一点**，误伤比漏报贵；阈值一律在**真实产出**上量，不拍脑袋。
4. **闸跑绿不等于闸有用**——每条新判据都要**突变验**（把修复撤掉必须变红）。
   *突变没被抓住时，先怀疑用例不够，别以为是实现对。*
5. **不改 `loop.py` 的循环结构。** 全部落在 middleware / checks / tools / prompts / 落库。
6. **每一批跑全量闸**：后端 `pytest -q`、前端 `npm test`。
7. **质量优先于省钱。** 任何一条让产出变差，退回去。

---

## 2. 执行清单

每一条都标了 **reference**：出自哪份、哪一节、**对应哪个实测证据**。
`P0` = 重点三份直接推出来的，优先做。

### 阶段 -1 · 接上已有的评测台（**所有后续验收的基础**）

| # | 做什么 | reference | 证据 |
|---|---|---|---|
| -1.1 | 跑一次基线：`suite` / `soak` / `writing_quality_bench` / `editing_quality_bench`，存成 `before` | [SITE] | 仓里 7 个脚本、4 份日志、2 个结果目录，八份调研一次没提 |
| -1.2 | bench 判据 × `checks/` 对账：**哪些缺陷只有 bench 在看**（空壳标题 / 元评论 / 数字无据 / 示例泄漏 / 破字 / 顾问腔 / 占位符） | [SITE] | `soak.py` 顾问腔基线 4~11%；`writing_quality_bench` 八项 |
| -1.3 | 每批改完跑对应 bench，前后数字进 TRACELOG；**段内查重那批要补跑** | [SITE] | `non_repetition` 跨轮 1.08–1.24 一直不动，正对着这条线 |
| -1.4 | 压测脚本补记 `stopped` / `cached_tokens` / 账本三个数 | [SITE] | 让压测和产品路径量同一组东西 |

### 阶段 0 · 先能看见 —— ✅ 5/5 完成（批 1）

| # | 做什么 | reference | 证据 |
|---|---|---|---|
| 0.1 | ✅ `llm_usage` 记 `cached_tokens` / `cache_write_tokens` | **[CE] §0** | 连命中率都不知道；`_record` 只落三个数 |
| 0.2 | ✅ 新表 `harness_rounds`（每轮分数 + 正文长度 + 工具计数） | [EFF] P3 | 每轮评分只活在 SSE 里 |
| 0.3 | ✅ `harness_runs` 加 `stopped` 列 | [EFF] P4 | 54% 的跑记成 `continue`，三种失败混在一起 |
| 0.4 | ✅ `Ledger`：`ToolTrace` 跨轮累积（只记不改） | **[MR] §3.1 / [LED] §7.1** | `prepare` 每轮新建 `ToolTrace` 再丢掉 |
| 0.5 | ✅ `ctx_feature` 按步骤细分 | [EFF] P7 | 一次跑里所有调用同名 |

### 阶段 1 · 确定性判据 —— 7/7（批 2 做了 1.1 / 1.2；批 3 做了 1.3 / 1.4 / 1.5 / 1.7；批 4 把 1.3 摘掉；批 5 做了 1.6）

| # | 做什么 | reference | 证据 |
|---|---|---|---|
| 1.1 | ✅ 打分 prompt 挪 `dup_hints` 到 `content` 之后 | **[CE] §2②** `P0` | 断点落在 1300–2000 token 处，正文从没被缓存过 |
| 1.2 | ✅ 段内句级查重 + `no_restated_paragraph` | [MECH] §1.1 / [LONG] §2 | 一篇 42.9% 的正文是段内重复；777 个句对分布双峰 |
| 1.3 | ✅ `drop_already_written` 插入前按句再剔 | [MECH] §1.1 | 同上；注意 `test_layering` 不许 `editor/` import `harness/` |
| 1.4 | ✅ `topic_fidelity` 归进 `INNER_QUALITY` | [MECH] §4 | 16 次里 7 次不达标，而两条执行器选择规则都不看它 |
| 1.5 | ✅ `steer` 分流：内在质量诊断不进检索规划 | [MECH] §1 | 「你说了两遍」这句话进了「该查什么事实」的 prompt |
| 1.6 | ✅ 植入缺陷测 25 个维度的灵敏度（批 5：852 次真跑；15 维抓住、1 维反着来、1 维没抓住、1 维条件从不出现）。**批 10 补了两条测量完整性**：`Probe.precondition`（条件没成立的格子单独成表，不进掉分均值）+ 取材器自报偏移（`numeric-block` / `chart-block` 的前后文以前是错的） | **[IND] §8④** `P0` | CriticGPT：训练数据就是「人往代码里塞 bug 再写批评」 |
| 1.7 | ✅ 打分解析失败要能认出来（现在 = 全 0 分） | [EVAL] 问题二 | `_extract_json` 失败 → 每维 level 0 → 触发 cleanup、参与 best_of |

### 阶段 2 · 材料账本（准 + 全）—— 5/7（批 10 做了 2.1 / 2.2 / 2.3，批 13 做了 2.4 / 2.5）

| # | 做什么 | reference | 证据 |
|---|---|---|---|
| 2.1 | ✅ 查询级短路（同参数直接返上次；**同轮回提示、跨轮返全文**） | **[MR] §3.4** `P0` | 去重发生在付完钱之后；`facts.py:41` 是结果级。批 10 实测：一次真跑 12 次查询里重复 4 次（**33.3%**），算上 prepare 直接调的是 15 次里省 6 次（40%），6 次全是跨轮 |
| 2.2 | ✅ `kb_conflicts` 接进取材（`superseded_by`） | **[LED] §10②**（补 [IND]） | 表就在库里，写作 harness 全仓没一处读它；DVT 6/3 → 8/5。批 10：已裁决的带回取代它的那条，**未裁决的只提醒**（库里 4 行 open 至少 3 行是误报） |
| 2.3 | ✅ 事实日期进账本（temporal validity） | **[LED] §10②** | `_fmt_facts` 已经在显示 `when`/`date`，没被用。批 10 实测：真跑取回的 75 条事实**全部带日期** |
| 2.4 | ✅ 账本摘要进 prompt，**以「缺口」形式**（`ledger.gap_summary`，开关 `params.LEDGER_IN_PROMPT` 能单独关掉） | **[MR] §3.1 / [LED] §10⑤** `P0` | 「不要再取上几轮写过的」要求写在文字里、清单没给；而"库存"形式会缩小搜索空间。批 13 真跑（`terrence` 真库，每臂 16 次跑）：**按轮归一化**后重复查询率 **32.5% → 3.5%**、`repeat_calls`/轮 **1.14 → 0.14**、「发了查询却一条新材料都没带回来」的轮次 **4 → 0**；正文里的事实 id 引用密度 **2.42 → 4.53 / 千字**、占位句 **6 → 1**（同种子同 4 轮那一对：`off` 篇 0 处引用 + 5 句占位，`both` 篇 12 处引用、9 个不同 id） |
| 2.5 | ✅ 覆盖率驱动停机（`agent_loop.BARREN_STOP = 2`，连着两次没带回新 id 就停；策略器那条脏信号换成 `tool_stopped_barren`） | **[MR] §2③ / [LED] §3** `P0` | 固定 `tool_iters`，且「上一轮没用工具」是脏信号。批 13 隔离实验（只开 2.5，3 次跑）：重复查询率 **32.5% → 17.2%**、空手轮清零；但**压不到 0**（2.4 全开那一臂是 3.5%）——**它治得了「查到头了还在发」，治不了「又规划出同一批查询」，那是 2.4 治的** |
| 2.6 | `material_use` 改成可计算 | [LED] §6 | 25 次 24 次满分，说明不了任何事 |
| 2.7 | `citations_hold` 升级成 id 级确定性比对 | **[LED] §10④（ALCE）** | 现在查的只是「id 在不在材料里」，既不是 precision 也不是 recall |

### 阶段 3 · 取消截断 —— 3/3（批 15）

| # | 做什么 | reference | 证据 |
|---|---|---|---|
| 3.1 | ✅ 小节索引 + `read_section`（`middleware/sections.py` + `tools/longform_tools.py`，开关 `params.SECTION_INDEX`）。`Compact` 从两条长文 harness 退休，`compact_context` 仍服务「智能续写」那条一次性路径（那条没有工具循环，给指针取不回来） | **[CE] §7** `P0` | 截断是最差的一档；`Verbatim Chunks Beat Extracted Artifacts`。批 15 真跑（`terrence` 真库、38 次跑、`rails_off=("save",)`）：**模型真的会去调 `read_section`**（索引真正生效的那个种子上，12 次跑里 11 次调过，共读回 25 节；对照臂 0 次），[CE] §7 那条「模型不去调那个工具」的风险实测没有兑现 |
| 3.2 | ✅ 事实索引取代 `[-40:]`（`facts_all` 全量只追加；进 prompt 的是「账本那一行索引 + 最近 40 条逐字」，开关 `params.FACT_INDEX`） | **[CE] §7 / [MR] §3.5** `P0` | `[-40:]` 从头丢 → 整块平移 → 换进换出 + 断缓存。索引那一行**取自账本**（`ledger.facts[fid]["line"]`），不另造一份 |
| 3.3 | ✅ `prompt_cache_key`（`llm.set_cache_key(note_id, mode)` + `ctx_feature` 的步骤名） | **[CE] §4.6** | 按 note + mode + 步骤分账。**它只改路由、不改命不命中**——真正卡住 judge 命中率的是 `score_context.with_material`（见批 15 计划外发现①） |

### 阶段 4 · 评判形态 —— 2/7（批 3 做了 4.7，批 8 做了 4.1）

| # | 做什么 | reference | 证据 |
|---|---|---|---|
| 4.1 | ✅ 六个 block 模式补 `score_context`（前后文 / 指令 / 选区）**+ 事实块补给所有模式**（批 8） | [EVAL] 问题一 | `fits_context` 判「跟周围合不合」，而周围没给它看；实测 4/4 满分。事实块那一半是批 7 顶出来的：`material_use` 判 2.0 是判词规定的，不是判据废了 |
| 4.2 | 判据从整篇改成按小节 | [LONG] §3 | lost-in-the-middle 掉 30%+，而中间正是累积重复所在 |
| 4.3 | `skeleton` 配确定性判据（照 `slides` 形态） | [LONG] 建议二 | 最上游那一步是唯一没有闭环的；`spine_fidelity` 封顶 |
| 4.4 | `coherence` 拆出兜底桶 | [MECH] §3 | 6 条判据把它当第二/第三落点，分数没有可操作含义 |
| 4.5 | 给 `coherence` 补位置级探测器，或写明它只能停机 | [MECH] 第 2 层 5 | 拿过 3 次 0 分，而没有一行代码能说出它不连贯在哪 |
| 4.6 | block 模式的 `stop_when`（现在 6 个模式 0 条） | [EVAL] 问题三 | 「这一轮什么都没变」在 block 模式完全不起作用 |
| 4.7 | ✅ 清 `last_scores` 死状态 + 注释钉住 | [LED] §10⑥ | 写了没人读；论文指出喂先前分数会破坏评判独立性 |

### 阶段 5 · 有 oracle 的那几个模式 —— 0/3 `P0`

**[IND] 自己排在第 1 位的一条。** 图表 / 表格 / 分析三个模式**本来有执行 oracle**。

| # | 做什么 | reference | 证据 |
|---|---|---|---|
| 5.1 | `data_grounding` 确定性化：图 / 表里的数字跟工具返回逐个 diff | **[IND] §2–3** `P0` | 判据原文是「每个数字都能追到源表」——那是比对不是判断；实测 2/2 满分 |
| 5.2 | `numbers_from_tools` 同上（eda · analysis） | **[IND] §2** `P0` | VisEval：一组异构 checker，几乎不用打分模型 |
| 5.3 | readability 档：图例有没有、类别数是否多到读不出来 | **[IND] §2** | VisEval 的三档之一，我们只有 validity/legality |

### 阶段 6 · 指令类：从用户那条指令现场生成 checklist —— 0/2 `P0`

| # | 做什么 | reference | 证据 |
|---|---|---|---|
| 6.1 | `prompt` / `custom` 跑前生成 instance-specific checklist（二元判） | **[IND] §4（TICK/RaR）** `P0` | RaR 消融：固定通用 rubric 明显更差，我们八个功能全是那一档 |
| 6.2 | 可程序验证的约束单独走代码（字数、段数、必须提到 X） | **[IND] §4（IFEval）** | 25 类可验证指令，确定性算法判 |

### 阶段 7 · 长文专项 —— 0/4

| # | 做什么 | reference | 证据 |
|---|---|---|---|
| 7.1 | `factual_grounding` 改 **decompose-then-verify** | **[IND] §8①** `P0` | FActScore/SAFE/VeriScore；我们来源是封闭本地库，条件比论文好 |
| 7.2 | 新增「这一节材料够不够」，不够就触发既有弃答形态 | **[LED] §10③（Sufficient Context）** `P0` | 只看相关性是量错了东西；弃答形态仓里早有（`grounding_rules.py:256`） |
| 7.3 | `section_coverage` 加字数预算 | [LONG] 建议四（AgentWrite） | 「只开了个头就收尾」现在全靠模型判 |
| 7.4 | 三档 → 二元 checklist（**先按维度量一致率再决定改哪几维**） | [LONG] §4（HelloEval）/ **[IND] §6②** | HelloEval 4–6 条二元问题与人相关性最高 |

### 阶段 8 · 没有 evaluator 的那几个 —— 0/2

| # | 做什么 | reference | 证据 |
|---|---|---|---|
| 8.1 | `magic-tap` 配判据，照 `slides` 形态（判了不拦） | [EVAL] 问题四 / **[IND] §5** | 15 个产出文字的功能只有 8 个有 evaluator |
| 8.2 | `journey/report` 同上 | 同上 | 它的 prompt 注释里自己写着「跟 material_used 是同一场仗」 |

### 阶段 9 · ground truth —— 0/4（长周期，**采集要尽早开始**）

| # | 做什么 | reference | 证据 |
|---|---|---|---|
| 9.1 | 采集用户编辑（跑完落一份、下次保存再落一份） | [MECH] §5 / **[IND] §8⑥** | PRELUDE / coactive learning：假设弱到「编辑后比提出的更好」 |
| 9.2 | 按维度报 judge-vs-人 一致率 | **[IND] §6③** | 门槛 75–90%；整体 80% 可能掩盖某维只有 40% |
| 9.3 | 跨「跑」的护栏：比上次差就**提示**（不自动回滚） | [EFF] P2 | 三篇笔记 9→6→4、9→7→4→3 |
| 9.4 | judge 模型槽（留空 = 同一个） | [EFF] P6 / **[IND] §6④** | 自评偏见：可程序验证的 rubric 上错判「满足」概率高出 50% |

### 阶段 10 · 把方法固化 —— 0/3

| # | 做什么 | reference | 证据 |
|---|---|---|---|
| 10.1 | 六条「维度怎么建立」写成 `harness-framework.md` 正式一节 | [EVAL] §3 | 现在只活在注释里 |
| 10.2 | check 脚本钉住两条可机械验的 | [EVAL] §5.3 | 「判据落在本模式真有的维度上」现在 0 落空，要保持 |
| 10.3 | criteria drift 的重新校准入口 | **[IND] §8③（EvalGen）** | 判据依赖当初看到的那批产出；我们这 25 条正是这么长出来的 |

### 阶段 11 · 从没审过的部件 —— 0/7

| # | 做什么 | reference | 证据 |
|---|---|---|---|
| 11.1 | `policy.py` 的脏信号（「上一轮没用工具」→ 扣检索预算到 0） | [SITE] | `policy.py` 注释里自己记着 |
| 11.2 | `best_of` 的 `rank()` 标量折叠 → 不折叠 | [EFF] / **[IND] §6⑤（GEPA Pareto）** | 第 602 轮实拍：四轮全打回、rank 一律 (0,0.0)、best 钉在第 1 轮 |
| 11.3 | `CUSTOM_DROPPED`：后端在发，**前端一处没消费** | [SITE] | 实测 grep；被丢掉的修订用户看不到、我们也没统计 |
| 11.4 | 错误路径成体系过一遍 | [SITE] | `trace.error` 回退 / `_score` 返回 None / 取消恢复的快照保真 |
| 11.5 | `agent_loop` 的工具循环（重试、并行、报错怎么进下一轮） | [SITE] | 未审 |
| 11.6 | `skills` 那 447 行 | [SITE] | 未审 |
| 11.7 | `replan_rules` | [SITE] | 未审 |

### 阶段 12 · 用户这一端 —— 0/4

| # | 做什么 | reference | 证据 |
|---|---|---|---|
| 12.1 | 「这一轮为什么这么跑」补上 `steer` 和判据命中 | [SITE] | `AgentActivity.tsx` 已显示检索预算/温度，缺这两样 |
| 12.2 | 采集修订的接受 / 拒绝率 | [SITE] / [IND] §8⑥ | 离 ground truth 最近、最便宜的信号 |
| 12.3 | 单次跑的成本上限 | [SITE] | 现在完全没有 |
| 12.4 | 跑完比上次差时的提示（9.3 的界面那一半） | [SITE] | — |

---

## 3. 顺序

**`P0`（重点三份直接推出来的）优先**，其余按依赖排：

```
-1.1 基线           ← 没有它，后面全是空口
 └→ 1.3 1.4 1.5 1.7 4.7      （代码侧小改，快）
 └→ 1.6 植入缺陷测灵敏度  P0  ← ✅ 批 5。结论把 2.6 提到了 2.1 前面
 └→ 2.1 2.2 2.3           P0  （账本，不碰 prompt）
 └→ 5.1 5.2 5.3           P0  （有 oracle 的，便宜且确定）
 └→ 2.4 2.5               P0  ← ✅ 批 13。账本接进 prompt，`LEDGER_IN_PROMPT` 单独回退
 └→ 3.1 3.2 3.3           P0  （取消截断）
 └→ 6.1 6.2               P0  （指令类 checklist）
 └→ 7.1 7.2               P0  （decompose-then-verify + 够不够）
 └→ 9.1 采集（越早越值钱）→ 4.x → 8.x → 10.x → 11.x → 12.x
```

**明确推到 200 轮之后**（依赖 9.1 攒数据）：9.2 一致率、9.4 judge 模型槽、
10.3 重新校准、11.2 的 Pareto 调参、以及 [LONG] 建议六
（Re3 的同位置多候选重排——会让写作调用翻 N 倍，等每轮数据够了再判断值不值）。

---

## 4. 不做

- 不换 memory engine（**就用 kite**）。
- 不引入 DSPy / GEPA / Mem0 / Letta / WritingBench 任何一个当依赖——要的是判据形态，不是运行时。
- 不加新评分维度（已有 25 个；问题在执行器和上下文，不在指标）。
- 不做自动删重复、不做跨跑自动回滚（只报，不替用户决定）。
- 不上 token 级控制器（走 HTTP 接口，没有解码控制权）。
- 不为「写得更长」优化（LongWriter 那条线的目标跟我们正相反）。
- 不按 LoCoMo 那类基准选型（2026 年已饱和到塞满上下文就能拿分）。

---

## 5. 总验收（打包前要能报出来的数）

| 判据 | 现在 | 目标 | reference |
|---|---|---|---|
| 段内重复字占比（18 篇真产出） | 最差 **42.9%** | < 10% | [MECH]/[LONG] |
| 第 N+1 轮最弱维度「持平」的比例 | **65%** | 显著下降 | **[SITE] 200 轮** |
| 反复跑同一篇分数单调下滑 | **3/6** | 0/6 | [EFF] |
| 收尾仍 `continue` | **54%** | 能拆成三种 | [EFF] |
| 一次跑内重复查询占比 | 批 10 实测 **33.3%**（算上 prepare 直接调的 40%）；批 13 三臂 × 16 次跑**按轮归一化**：都关 **32.5%** → 只开 2.5 **17.2%** → 都开 **3.5%** | 0 | **[MR]** |
| judge 调用缓存命中率（第 2 轮起） | 批 15 实测 **0.0%**（38 次真跑 / 24 次 judge 调用）。根因已定位：`score_context.with_material` 把**逐轮增长**的材料块排在 `[Content]` **之前**，断点落在正文之前——**跟批 2 修掉的 `dup_hints` 一模一样的形状**。检索规划那一路是 60~62%、续写那一路 30% | > 0.7 | **[CE]** |
| 没有位置级探测器的内在质量维度 | **2** | 0 | [MECH] |
| `material_use` / `fits_context` 满分率 | 24/25、4/4 | 不再是「无从判断默认给过」 | [EVAL]/[LED] |
| `material_use` 对「材料全剔光」的灵敏度 | **2.0 → 1.92**（批 5 实测，给了事实块也一样） | 掉 ≥ 1 档 | **[IND] §8④** |
| `right_kind` 对「换成不存在的图片引用」 | **0.11 → 2.0**（反的） | 掉分 | **[IND] §8④** |
| `data_grounding` 判法 | 模型判 | **算出来的** | **[IND] §2–3** |
